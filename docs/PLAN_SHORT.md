# PLAN_SHORT.md — Current milestone tactical plan

> Updated every working session. Read at start (`/resume-session`); update at end (`/wrap-session`).

<!-- ───────────────────────────── HANDOFF ─────────────────────────────
This block is the contract between `/wrap-session` (writes it) and
`/resume-session` (reads it). It always describes where the NEXT session
should start. Keep it short and literal. Do not delete the markers.
────────────────────────────────────────────────────────────────────── -->

## ▶ Session handoff (read this first)

- **Next step — Graph-quality S1: Stop silent data loss** *(correctness; standalone, cheap — the first build slice; **failing test first**)*. The auto-chunker can drop trailing paragraphs and report success. Two fixes in `backend/src/story_forge/agents/chunking_coordinator.py`: (a) add a **completeness check after `proposal_to_outline`** — the scene ranges must cover **every** paragraph `[0, count)` with no gaps and no unassigned trailing paragraphs; raise on a hole. (b) **Fold the range-overflow check into the agent's retried validation** (`validate_with_retry`, `agents/validation.py`) so a one-off LLM off-by-one **re-prompts** instead of 500ing. Independent of the curation slices. **Write the failing test first** (`tests/unit/agents/` — mocked `LLMProvider`, per `backend/AGENTS.md` "test the agent's contract"): a proposal whose scenes skip a trailing paragraph must fail the completeness check, not pass as success. (OQ-28 hazard #1; spec/`docs/specs/graph-quality.md` §3 S1.)
- **Read before starting:** this file (handoff + the **`## Current milestone: Graph quality`** section + the live cross-cutting/Blocked lists); **`docs/specs/graph-quality.md`** §3 S1 (the slice's scope + the "kept in as a data-integrity fix" note) and §8 invariants; root `CLAUDE.md` (spec- & test-driven order, surgical changes); **`backend/AGENTS.md`** + **`backend/src/story_forge/AGENTS.md`** (layering: `api → agents → domain → adapters`; agent-contract testing; `validate_with_retry` pattern); the chunking code itself — `agents/chunking_coordinator.py` (`proposal_to_outline`, `ChunkingTooLongError`, `max_input_words`), `agents/chunking_agent.py`, `agents/validation.py`; spec **§7** (the structure/outline flow) + **§3.1** (chunking). The related cross-cutting chunker items (window-and-stitch, delete-and-replace, the overloaded 422) are *adjacent* — read them but **don't** scope-creep them in unless S1 naturally touches that code.
- **Verify on disk:** on **`main`** after the **Session-70 wrap** merge (which also landed the Session-69 S0 work). **`PLAN_SHORT.md` carries Graph-quality as the current milestone**; **S0 is `[x]` done** and the task list is the reshaped S1→S7. **`architecture/proposals/graph-curation-surface.md` EXISTS** (`status: accepted`), plus `architecture/glossary/reification.md` + `surrogate-key.md`. **`docs/specs/graph-quality.md` EXISTS** (§3–§8 amended by S0). **starlette waiver GONE** (`grep -c "starlette" infra/osv/osv-scanner.toml` → **0**; `backend/uv.lock` has `starlette 1.3.1`). **ollama CVE-2026-39832 waived** (`grep -c "39832" infra/trivy/ollama.trivyignore` → 1). **Retro changes present** (`grep -n "two unrelated reds" AGENTS.md`; `grep -n "Dropping \*on\* the floor date" .claude/skills/triage-advisory/SKILL.md`). **Security waivers still live (NEUTRAL floors — step 3b computes drop-readiness against the resume day; do NOT inherit a verdict):** pydantic-settings floor **2026-07-04** (→ 2.14.2); torch (no fix); condition-based neo4j netty/jackson/shiro + gosu/ollama Go-module waivers incl. the **new ollama x/crypto 39832** (re-scan, not date). **Vault drift (reconcile at the FIRST wired `review-architecture` sweep — next roll — writer-restricted, ADR 0002):** `open-questions.md`/`INDEX.md`/`PROJECT.md`/`learning-log.md` still frame meta-architect integration as fully deferred; the Session-59 motivation-reframe still in ~6 `proposals/*.md` + `INDEX.md`; Issue #31 slug carve-out. *(The OQ-28 "opens with a backlog triage" framing was reconciled by the S0 decompose.)*
- **Last session ended:** 2026-06-27 — **Session 70: a security-treadmill maintenance pass** (PR #161 merged; no production-logic code) **that also landed the long-uncommitted Session-69 S0 decompose**. Dropped the starlette OSV waiver (bump 1.2.0 → 1.3.1, clearing CVE-2026-54283 HIGH + 54282 LOW on the floor date) and waived a fresh ollama `x/crypto/ssh/agent` HIGH (CVE-2026-39832); the two were folded into one PR because `main` was double-red on the *required* `security` gate (single-fix PRs mutually block). Retro encoded the lesson into `/triage-advisory` + the Merge-flow split-unrelated rule. The Session-69 S0 work (proposal/spec/vault/plan reshape) rode this wrap's docs commit.
- **Open blocks/questions:** **S1 is a build slice — write the failing test first** (the test-first rule resumes here after the S0 design slice). **Standing process question (deferred S51):** whether `/code-review` becomes a *required* second gate for substantive code. **Live security waivers to watch:** pydantic-settings (floor 2026-07-04) — `/resume-session` step 3b will surface it; the condition-based image waivers re-scan, not date. **The first wired `review-architecture` sweep runs at the next milestone roll** and is where the deferred vault notes reconcile (meta-architect integration framing; motivation-reframe propagation; Issue #31). **Cross-cutting that bites Graph-quality:** the review-UX folds (`CandidateView` merge-target name, n=5 error-mappers, shared `useReviewQueue`) + the chunker/re-structure items (window-and-stitch, delete-and-replace, 422 overload) fire in **S1/S2/S3** — S1 touches the chunker, so the window-and-stitch / 422-overload items are *nearby* (fold only if naturally on the path); the rest stay "fix when the relevant code is next touched." `docs/BACKLOG.md` holds the post-PoC list.

<!-- ─────────────────────────── END HANDOFF ──────────────────────────── -->

## Current milestone: Graph quality — curate the graph you have (V1 → V2)

**Goal:** V1 is feature-complete; by design the PoC **over-extracts** — every graph write passes
the human gate (INV-1/INV-9), so the real Oakhaven graph is dense, with duplicates, near-synonym
edges, and spurious nodes *intentionally* left for curation. The owner now has that graph and wants
to **clean it in place** — fluid, context-rich tools to curate the entities and edges that already
exist, rather than re-running or improving extraction. The enabling insight: **the human gate is
only as good as the context it shows you**, and bringing the source text to the decision point is
mostly *cheap* — the data already exists (staged relations carry `paragraph_id`, entities carry
mentions; the write plumbing exists behind the gate with a full undo log). So this milestone is
**largely a UX-surfacing job, not net-new write plumbing.** Improving *extraction* is a later,
separate pass (owner steer, S66). **Source of truth for scope:**
[`docs/specs/graph-quality.md`](specs/graph-quality.md) (owner-approved, S66 prep gate).

**Slicing — RESHAPED 2026-06-26 (Session 69)** after the S0 decompose + owner resolutions (see Decided
below + `architecture/proposals/graph-curation-surface.md`): **navigation pulled early** (you can't curate
a hairball you can't see), **two human-gated "suggest, then you decide" passes added** (duplicate entities
S4, synonymous predicate-names S6), the **predicate work reframed as *naming normalisation*** (not
edge-joining), and the **§4 edge handle reserved**. Each slice is detailed (a branchy one gets a
`meta-architect:decompose-requirement` step-0) when picked up.

1. [x] **S0 — Decompose the graph-curation surface** *(architecture, no build; ✅ DONE Session 69)*.
   `decompose-requirement` → the `graph-curation-surface` proposal (register **DM-GQ-1..7 / OQ-29 —
   RESOLVED** by owner Session 69). Defining finding: the edit/merge/delete/undo plumbing already ships
   (`EntityEditService`, M4.S3a/S3b) but only on the reader's panel — so the editing slices are
   UX-surfacing onto the canvas. **§4 resolved = reserve a stable edge handle** (ADR at build).
2. [ ] **S1 — Stop silent data loss** *(correctness; standalone, cheap)*. The auto-chunker can drop
   trailing paragraphs and report success. Add a completeness check after `proposal_to_outline`
   (scene ranges cover every paragraph `[0, count)` — no gaps/unassigned trailing) + fold the
   range-overflow check into the agent's retried validation so a one-off LLM off-by-one re-prompts
   instead of 500ing. **Failing test first.** Independent — **the first build slice.** (OQ-28 hazard #1.)
3. [ ] **S2 — Navigate the graph** *(pulled early — the foundation curation sits on)*. The §3.4 filters
   (entity type / story / connection density) + node search by name + **a better layout algorithm** to
   spread a dense graph (today's `cose` is an unnavigable hairball). You can't curate a graph you can't
   see — lands before the curation slices. (Was S4 + the layout dimension, owner S69.)
4. [ ] **S3 — Edge evidence + verifiable merges** *(the enabler; data already exists)*. Click an
   edge → predicate + source sentence(s); each entity-merge option shows a context quote + type +
   aliases; cheap safeguards ride here (gate exact-name duplicate creation; don't present a score-100
   exact-name match as self-evidently correct for common-noun/group types; fix the amber
   "merge target" highlight that makes a *New* card read as a merge).
5. [ ] **S4 — Suggest duplicate clusters over the accepted graph** *(NEW — proactive entity dedup;
   human-gated)*. Re-point the §3.3 cascade matcher at the already-accepted entities → surface likely-dup
   clusters the author would otherwise hunt for; the human reviews + commits via the existing merge
   (INV-1/INV-9 hold — *suggests*, never auto-merges). Promoted from `docs/BACKLOG.md` (graph-traversal
   discovery), owner S69. Likely its own step-0 decompose.
6. [ ] **S5 — The graph as an in-place editing surface** *(the spine; sliced S5a node / S5b edge per S0)*.
   Bring the existing write paths onto the canvas — edit node name/type/aliases/properties, merge, delete;
   edit / re-target / delete an edge — with the human gate + undo intact (INV-1/INV-9/INV-3). Interaction =
   a selection-driven editable panel + a right-click shortcut, reusing the reader's edit panel (DM-GQ-3).
   Reserves the **§4 edge handle**.
7. [ ] **S6 — Predicate-name normalisation + synonym suggestion** *(the reframed "consolidate"; naming
   consistency, not edge-joining)*. Rename a predicate graph-wide to cut synonym noise (e.g.
   `PASSENGER_ON` → `ON_SHIP`), with an NLP/embedding layer that *suggests* synonymous names; identical-
   triple collapses are reported, not the goal. Human-gated; predicates stay free strings (INV-4); carries
   the §4 handle. Promoted from `docs/BACKLOG.md` (predicate proliferation), owner S69.
8. [ ] **S7 — Reader as a correction surface for existing entities** *(close the loop)*. Verify/fill
   gaps so the reader's click → side panel + right-click correction (mostly built M4.S3c, spec §3.5)
   works against existing entities, so corrections surfaced while *reading* flow back to the graph.

**Ride-alongs** (cheap; fold in where natural): review-queue "X of N remaining" count; empty-queue →
onward navigation instead of a dead end.

**The fork (owner decision after the sprint):** with a hand-cleanable graph in place, decide — go
deeper into extraction improvement (the spec §5 deferred work: re-extraction, eval baseline, relation
deep-modelling, predicate auto-suggestion) **or** move on to V2 Editing (PoC spec §4). After
Graph-quality → **V2 Editing** either way.

---

> **Earlier milestones — archived.** Milestones 1–4 (M0 Setup, M1, M2 Basic extraction, M3 Cascade matching, M4 V1 polish) **and Public readiness** (V1 → public portfolio) — their full task breakdowns, Decided entries, and Done logs — live in **[`docs/PLAN_ARCHIVE.md`](PLAN_ARCHIVE.md)** (dated, append-only). This file carries **only the current milestone**; the milestone-roll ritual (`/wrap-session`, `docs/AGENTS.md` §7) moves the just-closed milestone there.

---

### Cross-cutting (carried into Graph quality — curated at the Public-readiness→Graph-quality roll 2026-06-26, Session 68; see `docs/AGENTS.md` §3)

> **Public-readiness→Graph-quality curation pass (2026-06-26, Session 68).** Every item walked by hand per `docs/AGENTS.md` §3. **Resolved this milestone → moved to `docs/PLAN_ARCHIVE.md` (§7):** the PreNER spec-drift item (S57 stop-and-amend), the branch-protection-tighten item (S65, ADR 0009), and the "make the next trim a learning loop" item (S67). **Partially resolved this roll:** the meta-architect ritual-wiring item — `review-architecture` is now wired into the milestone roll (ADR 0002 §4); residual open parts carry forward in that item below. **Carried forward live — and several now bite directly in Graph quality:** the review-UX folds (`CandidateView` merge-target name, the n=5 error-mappers, the shared `useReviewQueue`) and the chunker/re-structure items (window-and-stitch, delete-and-replace, the 422 overload) fire in **S1/S2/S3** (chunker fix; review + graph-edit surfaces); the rest (§6.5 model-override dropdown, `last_call()` tiebreaker, the envelope-unwrap dup, repo-root `scripts/` ruff scope, the `CandidateSpan` validator, orphaned-sandbox cleanup, the vault motivation-reframe propagation) stay "fix when the relevant code is next touched." The **security-waiver drop** stays live (starlette 1.3.1 actionable on/after 2026-06-27).

- [ ] **Propagate the Session-59 motivation reframe to the vault's remaining homes (at the next `review-architecture` sweep).** The honesty amend reconciled the vault *source* notes by hand (`PROJECT.md` business driver, `overview.md` Layer 2 — "genuine personal tool / the author writes Wody Święte" → designed-for / aspirational / PoC content LLM-generated), but two homes were deliberately left: the `proposals/*.md` decompositions still use **"personal tool"** as a design-driver *lens* (~6 files — `m2s2`, `m2s3`, `m3-cascade-matching`, `backend-dependency-advisory-scan`, …), and `INDEX.md` is a *derived* note. Both should be re-synced when `meta-architect:review-architecture` next runs (it's the authorized vault writer — ADR 0002; this is the Session-57 "route to the sweep" pattern, applied to the residual after the source-note hand-fix). **Low urgency** — the proposals use the phrase as an analysis lens, not an as-built overclaim — but tracked so the next sweep closes it. `architecture/CHANGELOG.md` is append-only history and stays as-is. (Session 59, 2026-06-25.)
- [ ] **§6.5 model-override dropdown** (user picks "which provider/model for this task" + per-task-type preferences). Deferred from M2.S6 as a *feature*, not thin polish (see Decided 2026-06-11). The §8.5 panel already does the *surfacing*; this is the *override* control, which touches **INV-7** (system-derived routing) and needs an INV-7/spec reconciliation + router plumbing + a preferences store. Do as a `meta-architect:decompose-requirement` step-0, alongside or after M3. (M2→M3 roll note, 2026-06-11.)
- [ ] **meta-architect workflow integration** — the three skills (`initialize`/`decompose`/`review`) are shipped and **two live runs are done** (Session 9 `initialize` → vault; Session 10 `review`+`decompose` → ADR 0003), but the skills were — until Session 68 — **not wired into the rituals** (now `review-architecture` is; see the dated resolution at the end of this item). Deferred by design (`docs/decisions/0002`): wire on evidence *after living with the vault*, not before. Hypothesis to test: bootstrap-detect a missing `architecture/` vault in `/resume-session`; run `decompose-requirement` at feature "step 0" before the failing test; run `review-architecture` in `/wrap-session` alongside `/review-pr`. **Evidence so far (2 runs):** both produced real value (vault + ADR 0003) *and* a real maintenance cost (the accepted-proposal/decision-reconciliation drift — now mitigated by decompose §5b + `/review-pr` §2). Carry-ins: ~~(a) add a vault/docs-PR review lens to `/review-pr`~~ ✅ **done Session 10** (broadened to the decision-reconciliation lens — `/review-pr` §2 + root `AGENTS.md` + decompose §5b); (b) **[Issue #31](https://github.com/yaaisiu/story-forge/issues/31)** — meta-architect slug/filename carve-out for `PROJECT`/`INDEX`/`CHANGELOG` (still open). (Session 8 note; updated Sessions 9 + 10, 2026-06-02.) **Session 28 (retro):** fixed a smaller framework wart — the `decompose-requirement` + `review-architecture` skills said "read the matching template from `templates/`" but no `proposal.md`/`review.md` template exists (the section structure lives in each skill body); wording corrected to point at `vault-note.md` for frontmatter + name the skill-body as the single home (no duplicate template). Also **fresh evidence toward the OQ-3 ritual-wiring refinement** (run `review-architecture` at *resume* when a session opens with a decompose): Session 28 opened with a decompose on a stale-INDEX vault and had to correct the "Next steps" by hand — wiring itself stays deferred per ADR 0002, evidence accumulates. **Session 58 (2026-06-25):** the **doc-freshness-as-a-state-machine** drift recurred a **4th time** (`overview.md` stale at the V1→Public-readiness roll, plus a stale INDEX "Next") — each occurrence caught only by a *manual* sweep because the sweep isn't wired into the roll ritual. The owner's call (retro): the 4th recurrence is **strong evidence the wiring should land**, but it's an architectural change (amends ADR 0002) deserving **its own focused session**, not a retro fold — so revisit the wiring deliberately later, not now. (The interim mitigation — a §6b self-check in the `review-architecture` skill so the sweep stops *leaking* freshness debt — landed Session 58.) **Session 68 (2026-06-26, this roll): `review-architecture` is now wired into the milestone roll** (ADR 0002 §4 evidence-based integration — the 4th-recurrence evidence + the milestone-roll retro finding); `/wrap-session §5c` runs it, `/resume-session §3c` triages its report; `decompose`/`initialize` stay event-triggered. **Residual open (carried):** the decompose-at-step-0 + resume-bootstrap-detect hypotheses (untested) and Issue #31. The vault *notes* (`open-questions.md`/`INDEX.md`/`PROJECT.md`/`learning-log.md`) that still frame integration as fully deferred reconcile at the first wired sweep (writer-restricted).
- [ ] **Drop the Session-27 time-boxed security waivers as their fixes soak (`/triage-advisory`).** Clearing the security-job reds (PRs #72/#73) needed soaked bumps + **fix-first time-boxed waivers** for residuals whose fixes were still inside the 14-day soak. Drop each (bump to the now-soaked fix, delete the `[[IgnoredVulns]]` block + `WAIVERS.md` row) when it bakes. **Status:** ~~python-multipart 0.0.31~~ ✅ dropped 2026-06-18. ~~**starlette** — both CVEs (GHSA-jp82-jpqv-5vv3 LOW + GHSA-82w8-qh3p-5jfq HIGH DoS)~~ ✅ **dropped 2026-06-27 (Session 70, PR #161)** — single `1.2.0 → 1.3.1` bump on the floor date, both `[[IgnoredVulns]]` blocks + the `WAIVERS.md` section removed. **Still live:** the **condition-based** neo4j Trivy waiver **CVE-2026-50010** (netty TLS hostname-verification bypass) — drop when a Neo4j 5.26.x image ships netty ≥4.1.135.Final. `/resume-session` step 3b flags the dated ones; the `ignoreUntil` CI re-red is the backstop. (Session 27, 2026-06-16; updated S63; starlette dropped S70.)
- [ ] **Drop the ollama `x/crypto/ssh/agent` waiver CVE-2026-39832 when a patched ollama image soaks (`/triage-advisory`).** Added 2026-06-27 (Session 70, PR #161) — a fresh HIGH against the unchanged `ollama/ollama:0.24.0` pin, waived on the same unreachability as its 8 wave-6 `x/crypto/ssh` siblings (ollama opens no SSH agent client; 127.0.0.1 JSON inference). **Condition-based** drop: when an `ollama/ollama` tag rebuilt on `golang.org/x/crypto ≥0.52.0` soaks ≥7 days, bump via `/pin-image` and drop this + the sibling wave-6 ssh/net waivers together. Re-scan to confirm (not date-checkable). (Session 70, 2026-06-27.)
- [ ] Update spec §10 if any open question hardens (extraction granularity, etc.)
- [ ] **`CandidateView` carries no merge-*target* name — Stage-2/3 merges show a generic label (Session 25).** The review card resolves the proposed merge target's name by matching `target_entity_id` against the staged top-3 `alternatives` — but those are ranked by **RapidFuzz** while a Stage-2/3 merge target is chosen by **embedding cosine / judge** (`candidate_staging._cascade` + `matching_agent.stage2`), a different signal, so the target need not be in the list. `CandidateView` (`api/stories.py`) exposes only `target_entity_id`, no name, so on those paths the badge falls back to `Merge → an existing entity` (the `/code-review` should-fix; the raw-UUID was folded out in S4b). Proper fix: add `target_canonical_name` to `CandidateView` (resolve it at stage time from the in-scope `existing`/`snapshot` entities, or at read time) so the reviewer sees *what* they're merging into on every path. Backend slice + openapi regen — track here, do when the §3.3 review UX is tightened. (Session 25 `/code-review` finding.)
- [ ] **Per-screen `reviewMessage`/`extractMessage`/`decideMessage`/`messageForError` error-to-string mappers duplicate a pattern (now 5 screens — Session 25, updated S30).** `ReviewQueue`, `GraphViewer`, `UploadScreen`, `OutlineEditor`, and now `RelationQueue` (`decideMessage`, M3.S4f) each carry a near-identical `ApiError → string` switch (differing only in which statuses they special-case). At n=5 this is well past the rule-of-three; a shared `errorToMessage(error, overrides?)` / `apiErrorFallback(error, default)` in `lib/api/` with per-call status overrides would fold the *skeleton* (non-ApiError fallback + the final `error.detail || HTTP status` line) while each screen keeps its own status→string switch. Pre-existing convention — fold when one of these screens next needs an error-message change (don't pre-emptively refactor all five). (Session 25 `/code-review` reuse finding; n bumped S30.)
- [ ] **The two review queues (`extraction-review` / `relation-review`) share copied machinery with two latent nits (Session 30).** `RelationQueue` mirrors `ReviewQueue` by copy (not a shared `useReviewQueue` hook): `isEditableTarget` (n=2), the no-deps window-keydown effect, the render-time `selectedIndex` clamp, the per-card `pending` guard. Two verified-real behaviours ride along in **both**: (a) the **keyboard A/R path is not gated by an in-flight mutation** while the buttons are — a fast double-key re-fires the decision for the selected item (harmless: deterministic edge/entity ids + the backend's `already_decided` no-op); (b) a **stale decide/review error banner persists across J/K navigation** until the next mutation. Left unfolded in S4f because fixing them *here only* would diverge the two queues — the right fix is a **shared** one (extract a `useReviewQueue` hook, or gate the keyboard handler in both) at the n=3 trigger or when a queue next needs a behaviour change. (M3.S4f `/code-review`, 2026-06-17.)
- [ ] **`PostgresCostStore.last_call()` ordering has no tiebreaker.** `ORDER BY created_at DESC LIMIT 1` decides the §8.5 panel's "most recent call"; `created_at` defaults to `now()` (transaction-start time), and there's no monotonic column (the PK is a random uuid). Unreachable on the as-built **sequential single-user** extraction path (rows land seconds apart), but two rows sharing a `created_at` would make "most recent" nondeterministic. Lands with the same concurrency work spec §6.6 already defers ("a true reserve-then-reconcile lands only if batched concurrent calls do") — add a monotonic tiebreaker (a `bigserial`, or order by a real insertion sequence) **if/when** concurrent LLM calls are introduced. (M2.S5 `/code-review` finding, 2026-06-11.)
- [ ] **Orphaned sandbox files on upload failure** — `api/stories.py` writes the original file to the sandbox *before* the `Project`/`Story` inserts; if the DB write fails the transaction rolls back but the file is left on disk, pointing at a story that never persisted. Harmless on a single-user local app, so deferred. Fix when it bites or when a delete-story feature lands (clean up the file on insert failure, or write-after-commit). (Own-review note, 2026-05-21.)
- [ ] **Window-and-stitch for `auto` on long text** — `agents/chunking_coordinator.py` currently raises `ChunkingTooLongError` above `max_input_words` (a 422). The proper fix is a deterministic pre-split on paragraph boundaries, per-window agent calls, offsetting paragraph indices, and stitching the chapters/scenes back together — accepting some seam imperfection that the user corrects in the UI confirm. Deferred until the auto path is actually exercised on a real 50k-word input. (Session 4 windowing-guard decision, 2026-05-25.)
- [ ] **Re-structure semantics: delete-and-replace** — `POST /stories/{id}/structure` returns 409 if the story already has a tree. A re-run with a different mode ("I didn't like auto, let me redo manual") needs delete-and-replace, which the Session 1 repo doesn't have (Update was deferred). Session 6 added a *narrow* update — `update_story_raw_text` in the repo + an override-and-persist branch on the structure endpoint — covering the manual editor's "user accepts/edits" use case (spec §7 step 2). The full delete-and-replace UX (redo a different mode after the first persist) is still deferred. Add when the frontend wants it. (Session 4 endpoint-shape decision, 2026-05-25; narrowed Session 6, 2026-05-26.)
- [ ] **`/stories/{id}/structure` 422 is overloaded.** Two distinct failure modes return 422 with different body shapes: FastAPI's auto-generated request-validation errors (shape: `HTTPValidationError`) and the explicit `ChunkingTooLongError` raised when the input exceeds `max_input_words` (shape: `{detail: str}`). The Session 5 PR documented the unambiguous error codes (400/404/409/413/415/502) in OpenAPI but left 422 alone because declaring it would overwrite FastAPI's validation shape. Pick one: either remap `ChunkingTooLongError` to 413 (payload too large — semantically truer; the body *is* too long for the agent) or to a free status code in the 4xx range, so 422 stays purely validation-shaped and the frontend can discriminate the two cases by type rather than body inspection. Lands cleanly with the "window-and-stitch for auto on long text" cross-cutting fix above, which removes the error path entirely for the auto mode. (Session 5 Codex-review fold-in note, 2026-05-25.)
- **Agent retry loop + adapter envelope-unwrap are duplicated (revisit at rule-of-three).** Two altitude dups surfaced in the M2.S3 `/code-review`: (a) the `render → call → validate → retry-on-ValidationError → give-up` loop is near-verbatim in `chunking_agent.py` and `extraction_agent.py` (only the schema/weight/grounding differ); (b) the `response.json()` + envelope-unwrap-or-raise-`ProviderResponseError` block is near-identical in `ollama.py` and `openrouter.py` (only the dict path differs). **(a) ✅ Resolved 2026-06-15 (M3.S3, PR #60):** JudgeAgent was the n=3 trigger — the loop folded into `agents/validation.py` `validate_with_retry(...)`; chunking + extraction refactored onto it (each keeps its own schema/call/give-up error); `TaskWeight` + a `Router` Protocol promoted into `base.py` at the same time. **(b) still open** — deliberately per-copy at n=2; **fold at the first direct vendor adapter** (Anthropic/Grok/…, "as needed"), since each new copy re-implements the INV-6/OQ-4 no-body-leak rule by hand, and a `_unwrap_content(...)` in `base.py` would touch that security-sensitive guarantee. (M2.S3 `/code-review` altitude findings, 2026-06-08.)
- **Repo-root `scripts/` is outside CI's ruff scope.** The backend ruff config is `src = ["src", "tests"]` and CI runs `ruff check .` from `backend/`, so `scripts/*.py` (e.g. `check_dependency_age.py`) are never linted — a pre-existing `UP017` (`datetime.UTC`) sits there uncaught. Either add repo-root `scripts/` to a lint step or fix the one nit. Left untouched in PR #25 to stay surgical. (M2.S1 own-review nit, 2026-06-01.)
- **`CandidateSpan` has no `char_start <= char_end` validator** (`domain/extraction.py`). Internal deterministic data from spaCy, so optional — a model validator would just self-document the invariant. Add if a future producer of `CandidateSpan` is less trustworthy than spaCy. (M2.S1 own-review nit, 2026-06-01.)

### Blocked / questions

- **Review-gate policy after Codex retirement (deferred 2026-06-24).** The GitHub-wired Codex PR review is gone (owner has no subscription), so there is **no automated second reviewer**. `/review-pr` (self) stays the primary gate and `/code-review` (multi-agent) stays *recommended* for substantive code. **Open:** should `/code-review` become a **required** second gate for substantive (non-docs) code, or stay optional? Settle in a later session (see root `AGENTS.md` Merge-flow "Review model").
- _**Earlier resolved registers / questions (Milestones 1–4)** — see **[`docs/PLAN_ARCHIVE.md`](PLAN_ARCHIVE.md)**._

### Decided

- **2026-06-27 — Session 70: a security-treadmill maintenance pass + a process lesson (PR #161 merged; no production-logic code).** **Decisions:** (a) **Drop the starlette waiver by bumping `1.2.0 → 1.3.1`** on its floor date rather than re-dating — both CVEs (54283 HIGH DoS + 54282 LOW) fixed at the source, 1.3.1 soaked (15 days), fastapi has no upper bound. (b) **Waive the fresh ollama `CVE-2026-39832`** (`x/crypto/ssh/agent`, HIGH) on the same unreachability as its 8 wave-6 ssh siblings — owner-approved HIGH waiver (§6.7); *considered & rejected:* chasing an image bump (no soaked ollama tag carries x/crypto ≥0.52.0 — the established waive-on-unreachability pattern). (c) **Combine both fixes into one PR (#161), closing #162** — forced, not preferred: `main` was double-red on the *required* `security` gate (starlette waiver expired on its `ignoreUntil` floor + the fresh ollama CVE), so each single-fix PR left the other red and neither could merge; *considered & rejected:* admin-bypass of branch protection (just tightened in ADR 0009). (d) **Encode the lesson** — `/triage-advisory` drop step now scans `main` for another `security` red before dropping on a floor date, and root `AGENTS.md` Merge-flow gains the "two unrelated reds on the same required gate → combine" exception to the split-unrelated rule.
- **2026-06-26 — Session 69: Graph-quality S0 decompose + register RESOLVED + milestone reshaped (architecture/planning; no production code).** Ran `meta-architect:decompose-requirement` → `architecture/proposals/graph-curation-surface.md` (register DM-GQ-1..7 / OQ-29); the owner resolved it the same session. **Defining finding:** the edit/merge/delete/undo write plumbing already ships (`EntityEditService`, M4.S3a/S3b) but only on the *reader's* panel, so the editing slices are largely UX-surfacing onto the canvas. **Decisions (owner):** (a) **DM-GQ-1 §4 — reserve a stable edge handle now** (an opaque `uuid4` alongside the content-addressed `uuid5` id; content id stays the MERGE/dedup key per ADR 0005, the surrogate is the addressable hook a future temporal/modality qualifier hangs on, preserved through curation; *build no feature* — ADR-worthy, drafted at build); *considered & rejected:* keep content-addressing only (re-keys the future), document-the-obligation-only (the reminder rots). (b) **DM-GQ-2 — predicate work reframed: *naming normalisation*, not edge-joining** (owner steer) — rename a predicate graph-wide to cut synonym noise + an **NLP/embedding *suggest* layer** to find synonymous names; identical-triple collapses are reported, not the goal. (c) **Promote a proactive *dedup-suggest* pass** (re-point the cascade matcher over the *already-accepted* graph to surface duplicate clusters — owner ask) — the entity twin of (b)'s predicate-suggest; both *suggest*, the human commits (INV-1/9 hold). (d) **Pull navigation early + add a layout algorithm** (owner: the graph is an unnavigable hairball *now*) — you can't curate what you can't see, so filters+search+layout land before the curation slices. (e) **DM-GQ-3 canvas UI = editable panel + right-click**, reuse the reader's edit panel. (f) **DM-GQ-7 bulk multi-select → backlog** (S4/S6 suggest passes cover the top bulk need). **Milestone reshaped** (task list above): S1 chunker → S2 navigate → S3 edge evidence → S4 entity dedup-suggest (NEW) → S5 in-place editing → S6 predicate-name normalise+suggest (reframed) → S7 reader loop. Promoted two `docs/BACKLOG.md` items into scope (graph-traversal discovery → S4; predicate proliferation → S6). Reconciled across `docs/specs/graph-quality.md` (§3/§4/§5/§6/§8), this plan, the vault proposal (→ accepted), OQ-29, BACKLOG. **DM-GQ-4/5/6 confirmed as proposed** (reuse endpoints; project-scoped undo-last with preview; the S5a/S5b split).

- **2026-06-26 — Session 68: the Public-readiness → Graph-quality milestone ROLL (governance/planning only — no production code).** Closed Public readiness, opened Graph quality. **Decisions (owner, this session, surfaced by the learning loop's first run):** (a) **wire `review-architecture` into the milestone-roll ritual now** rather than in its own session — the milestone-roll retro showed ADR 0002 §4's "integrate on evidence after living with the vault" condition is met (one useful S58 sweep + the 4th doc-freshness recurrence + real vault drift reaching a roll uncaught); *considered & rejected:* a dedicated session (the handoff's prior lean — folding it in is tighter and the evidence is in), and keep-deferred (one run isn't enough — but the rot evidence settles it). (b) **encode the wiring now, run the first sweep at the next roll** — keeps this roll unit bounded (one-unit-per-conversation); *considered & rejected:* also running the full sweep this conversation (most coherent but spans several units). (c) **add a CI-skip one-liner** to root `AGENTS.md` Merge flow (verify CI gating/skip changes on a scratch PR, never reason about the skipped path) — earned twice (S56, S65). `decompose`/`initialize` stay **event-triggered**, not ritual-wired (no evidence yet).

- _**Earlier decisions (Milestones 1–4 + Public readiness)** — see **[`docs/PLAN_ARCHIVE.md`](PLAN_ARCHIVE.md)**._

### Done in previous sessions

- **2026-06-27 — Session 70: a security-treadmill maintenance pass — dropped the starlette waiver and waived a fresh ollama CVE, folded into one PR by a required-gate constraint (PR #161 merged; no production-logic code).** `/resume-session` step 3b flagged the starlette OSV waiver as due on its 2026-06-27 floor; `/triage-advisory` drove the drop — bumped `starlette` 1.2.0 → 1.3.1 (`/add-dependency`; 15-day soak, OSV-clean, age-gate green, 363 unit tests green, app imports), clearing CVE-2026-54283 (HIGH DoS) + CVE-2026-54282 (LOW) at the source and removing both `[[IgnoredVulns]]` blocks + the `WAIVERS.md` section. Opening the PR surfaced an **unrelated** fresh red: `ollama/ollama:0.24.0` carried a new `golang.org/x/crypto/ssh/agent` HIGH (**CVE-2026-39832**), waived (owner-approved §6.7) on the same unreachability as its 8 wave-6 ssh siblings (ollama opens no SSH agent client). The discovery that **`main` was double-red on the *required* `security` gate** — the starlette waiver having *expired* on its `ignoreUntil` floor that very day, plus the ollama CVE — meant neither single-fix PR could go green (each left the other red), so #162 (ollama) was folded into #161 (starlette) and the two landed as one combined PR. `/review-pr` clean on the starlette change; CI green after a Docker-Hub-flake re-run. **Process lesson (retro):** dropping a waiver on its floor date can collide with another treadmill red on the same required gate → `/triage-advisory` now scans for a second `security` red before dropping, and root `AGENTS.md` Merge-flow gains the "two unrelated reds on one required gate → combine" exception. **Also landed the long-uncommitted Session 69 S0 work in this same wrap** (it had been done in a prior conversation but never committed). **Lesson:** the resume ritual earned its keep twice — it flagged the due waiver *and* caught that a whole prior session (S0) sat uncommitted with a stale handoff. **Next: Graph-quality S1** (the chunker completeness check — first build slice).
- **2026-06-26 — Session 69: Graph-quality S0 decompose — the in-place graph-curation surface designed and the milestone reshaped (architecture/planning; no production code; landed 2026-06-27 with the Session 70 wrap).** Ran `meta-architect:decompose-requirement` on "the graph view as a direct in-place curation surface" → `architecture/proposals/graph-curation-surface.md` (data-flow, register DM-GQ-1..7, OQ-29, the "but what if" edge-case pass, affected components, S-slice boundaries) + two glossary terms (`reification`, `surrogate-key`). The owner resolved the whole register the same session (see Decided). **Defining finding:** the edit/merge/delete/undo write plumbing already ships (`EntityEditService`, M4.S3a/S3b) but only on the *reader's* panel — so the editing slices are largely UX-surfacing onto the canvas, not net-new write plumbing. Reshaped the milestone (S1 chunker → S2 navigate → S3 edge evidence → S4 entity dedup-suggest → S5 in-place editing → S6 predicate-name normalise+suggest → S7 reader loop), reserved the §4 edge handle, and reconciled across `docs/specs/graph-quality.md` (§3–§8), this plan, the vault proposal (→ accepted), OQ-29, and `docs/BACKLOG.md` (two items promoted into scope). **Lesson:** a decompose's value showed in the reshape — pulling navigation early and reframing the predicate work as naming-normalisation both came from walking the nine layers before slicing. **Next was Graph-quality S1** (the first build slice).
- **2026-06-26 — Session 68: the Public-readiness → Graph-quality milestone roll (governance/planning; no production code).** Ran the roll per `docs/AGENTS.md §7` / `/wrap-session §5c`. **(1) Mined before moving** — `/retro` in milestone-roll mode, its first-ever run: a `general-purpose` agent read the whole Public-readiness record as a corpus and surfaced 5 candidate patterns; 3 were already-absorbed wins (the survey-disproves-its-own-plan pattern → already caught by `/resume-session`'s recompute rule; reuse-before-building-tooling → already in `CLAUDE.md` and it *fired on its own offspring* in S67; the stranger-reads-cold lens → earned once), 2 cleared the bar → the two owner decisions above. **(2) Applied the outcomes** — wired `review-architecture` into the roll + added the CI-skip rule, reconciled across every non-vault home in one pass (ADR 0002 Update, root `AGENTS.md`, `architecture/AGENTS.md`, `/resume-session §3c`, `/wrap-session §5c`, `docs/AGENTS.md §7`); writer-restricted vault notes deferred to the first wired sweep. **(3) Archived** Public readiness byte-exact → `docs/PLAN_ARCHIVE.md` (25 dated bullets + 3 resolved cross-cutting items + the M4→Public-readiness curation note; `PLAN_SHORT` 109 KB → 32 KB, verified 0 dated bullets left + archive bullet count 145 → 170). **(4) Re-curated** cross-cutting/Blocked by hand (16 live carried with Graph-quality ties, 3 resolved struck-and-moved) and **opened the Graph-quality skeleton** (S0 decompose → S5) around `docs/specs/graph-quality.md`, correcting the "opens with a backlog triage" framing to "opens with S0"; updated `PLAN_LONG` milestone status. **Lesson:** the learning loop's first run did exactly its job — a pattern invisible to any single session (an architecture sweep with no forcing function, recurring as the 4th doc-freshness miss) only became actionable when a whole milestone was read at once, and it immediately hardened into a ritual. **Next: Graph-quality S0** — `decompose-requirement` on the in-place curation surface.
- _**Earlier sessions (Milestones 1–4 + Public readiness)** — see **[`docs/PLAN_ARCHIVE.md`](PLAN_ARCHIVE.md)**._

---

## How the two skills use this file

- **`/resume-session`** (session start) reads the **Session handoff** block, verifies the on-disk anchors it names, runs `git log`/`git status`, opens the spec sections listed, and confirms reality matches before work begins.
- **`/wrap-session`** (session end) runs the green-state checks (report-only), checks off completed tasks, strikes (does not delete) obsolete ones, appends a dated line to **Done in previous sessions**, refreshes **Blocked / questions** and **Decided**, and rewrites the **Session handoff** block to point at the next session. It then reminds you to commit on a feature branch (squash-merge hygiene).
- **When a spec change happened:** both skills stop and require `story-forge-poc-spec.md` + `PLAN_LONG.md` to be reconciled with this file before continuing.
