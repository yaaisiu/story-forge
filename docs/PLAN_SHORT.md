# PLAN_SHORT.md — Current milestone tactical plan

> Updated every working session. Read at start (`/resume-session`); update at end (`/wrap-session`).

<!-- ───────────────────────────── HANDOFF ─────────────────────────────
This block is the contract between `/wrap-session` (writes it) and
`/resume-session` (reads it). It always describes where the NEXT session
should start. Keep it short and literal. Do not delete the markers.
────────────────────────────────────────────────────────────────────── -->

## ▶ Session handoff (read this first)

- **Next session — two slices left before the fork closes; the owner picks.** S3 (story hub) done Session 104 (PR #229), S4 (stack scripts) Session 105 (PR #231), **S5 (security image re-scan) Session 106 (PR #233)**. **Session 107 was an out-of-band security session (PRs #235 + #236) that consumed no slice** — it started from a nightly CI failure, so the choice below is exactly the one Session 106 left open. Remaining, **owner's choice at resume:** **(A) S1 — the Grzymalin end-to-end run** (the fork-critical one): ingest `docs/samples/grzymalin/general-research.md` and run chunk→extract→match→judge→accept **unaided**, then write a findings note against the seven-point question set (in the S1 bullet). This needs the owner to actually run the pipeline (an *observation* run, not a code change) and the model/embedding groups installed; `scripts/stack.sh up` + the story hub make standing up and observing a run easy. **(B) S2 — normalise-names evidence** (fork-independent, buildable now; backend+frontend, substantive → `/review-pr` **and** `/code-review` **and** a browser walk). *S1 feeds the fork decision; do NOT lock the fork — that is S6 (fork-close).*
- **Browser-walk merge gate (set Session 104; fallback added Session 106) — a user-facing change gets a browser walk before merge, proactively (root `AGENTS.md` Merge flow + `frontend/AGENTS.md`).** **S2 is the next slice it touches.** Run the walk yourself, unprompted, and fold what it finds, *before* surfacing the PR as ready. **If you have no browser in your environment, the gate does not weaken — only the walker changes:** derive a targeted checklist from the actual routes/components the change touches, hand it to the owner *before* calling the PR ready, wait for the result, and say plainly that the walk was not agent-run. (Session 106 is the worked example: an 8-item checklist built from `routes.tsx` + `storyScreens.ts` found three real — pre-existing — issues.)
- **Read before starting:** always this file (handoff + the **"Grzymalin reality check"** milestone section + **Decided Session 107 & 106 & 105 & 104 & 102** + the live cross-cutting list + the **Blocked** entry on the false spec §6.7 rationale) and root `CLAUDE.md` — §1 gained a rule in Session 106 (*don't document around a hole you could close*) and the CI-gating bullet a tooling-config clause. **If you touch a security gate, read `/triage-advisory` first — Session 107 changed it:** step 1 now runs a Dependabot cross-check, and step 3 now says a recorded "no fix exists" is a snapshot to re-verify, not a standing fact. Then **per the slice chosen:** *S1* — `docs/PLAN_LONG.md` **"◀ THE FORK"** (incl. *Corpus & the History-Forge direction* + the *Operational logging* S102 owner note); spec **§3.1** (chunking), **§3.2/§3.3** (extraction + cascade), **§3.5** (reader/surface fidelity), **§6.6** (`llm_calls` ledger / flywheel); **`backend/AGENTS.md`** (run prereqs — `uv sync --group models --group embeddings`, `alembic upgrade head`; the disposable-naming + clean-up-by-id rule for any live write). *S2* — the S2 bullet + spec §3.3/§3.5, `architecture/proposals/graph-name-normalisation.md`, `backend/src/story_forge/AGENTS.md` (layering), `frontend/AGENTS.md` (the browser-walk gate + its no-browser fallback).
- **Verify on disk:** on **`main`** after this wrap's PR merges. **All anchors below were verified as this block was written (2026-08-13), not asserted from memory.** **Session 107 changed no milestone slice** — it was an out-of-band security session, so **S1 and S2 are still `[ ]`** and the milestone shape is unchanged. **Session-107 anchors:** `infra/npm/audit-waivers.toml` has **zero** `[[IgnoredVulns]]` blocks (as does `infra/osv/osv-scanner.toml` — two of the three gates now carry no waivers at all); `infra/trivy/ollama.trivyignore` contains **`CVE-2026-46600`** (wave 9) *in addition to* `CVE-2026-56852` (wave 8); `frontend/package.json` pins **`react-router-dom` `7.18.2`**; `docker-compose.yml` still pins **`storyforge/ollama:0.24.0-nonroot`** (deliberately unbumped — see Decided); and `.claude/skills/triage-advisory/SKILL.md` now contains **both** a `dependabot/alerts` call in step 1 and the "package-side twin of the image trap" rule in step 3. `docs/BACKLOG.md` has **two** new `surfaced Session 107 browser walk` sections. **Milestone shape unchanged:** **`docs/PLAN_SHORT.md`** carries the milestone as **six** sessions — **S1/S2 + fork-close S6 open**; **S3 `[x]`** (104), **S4 `[x]`** (105), **S5 `[x]`** (106), the S6-vault line `[x]` (103); **Decided Session 107 and 106** are present. **S5 anchors exist:** **`scripts/check_npm_audit.py`**, **`infra/npm/audit-waivers.toml`** + **`infra/npm/WAIVERS.md`**, **`ruff.toml`** at the repo root, and `backend/tests/unit/test_npm_audit_gate.py` (**30** tests). `docker-compose.yml` pins **`pgvector/pgvector:0.8.5-pg17-trixie`** and still **`storyforge/ollama:0.24.0-nonroot`** (ollama deliberately unbumped). `infra/trivy/pgvector.trivyignore` has an **empty OS-package section** (gosu Go-stdlib entries only); `infra/trivy/ollama.trivyignore` contains **`CVE-2026-56852`**. In `.github/workflows/ci.yml` the npm-audit step is in the **`security`** job, **not** `frontend` — that placement is deliberate (see Decided) and moving it back would silently disable the waiver expiry. S3/S4 anchors still exist (`scripts/stack.sh`; `frontend/src/features/story-hub/`, `frontend/src/lib/api/useStory.ts`, `get_story_summary` + the `GET /stories/{story_id}` route). **`docs/samples/grzymalin/general-research.md`** exists (~36 KB, ~4.2k words — **untracked** until S1 commits it; expected, not drift). Green at close: backend **770** tests, frontend **551** + build, all security gates clean, `main` green.
- **Last session ended:** 2026-08-13 — **Session 107: an unplanned security session** (PRs #235 + #236 merged), triggered by a scheduled-CI failure, **not** a planned slice. **The milestone's remaining slices are untouched — S1 and S2 are exactly where Session 106 left them.** Cleared the nightly red by waiving a new ollama `x/net` panic HIGH (`CVE-2026-46600`), after diffing the pin against *two* candidates proved no tag fixes it. Then a Dependabot cross-check found that the **sole npm waiver had become fixable** — the advisory was amended upstream to add a 7.x backport — so `react-router-dom 7.18.1 → 7.18.2` dropped it by a fix. **Two of the three gates now carry zero waivers.** Owner ran the browser walk from a 6-item checklist: **no routing regression**; four hiccups, all pre-existing or unrelated, two newly routed to `docs/BACKLOG.md`. Retro added a Dependabot cross-check and a drop-revisit rule to `/triage-advisory`. **No reds at close.**
- **Open blocks/questions:** **(1) The fork stays OPEN — do not lock it before S6 (fork-close).** S1's *evidence* settles deeper-extraction vs V2 Editing; the history-forge framing weights but does not decide it. **(2) S1 is an observation run** — deliverable is a findings note, not a code change; only touch code on a must-fix, via the normal flow. **(3) Spec-identity watch — now with a concrete data point.** The corpus is non-fiction and the direction is historical research, but spec §2 frames Story Forge around *narrative* text; a durable pivot needs the stop-and-amend flow, so for now it only shapes *reversible* decisions (`PLAN_LONG.md` fork section). **New evidence (Session 107):** chunking **fails outright** on Obsidian-vault Markdown carrying a YAML frontmatter block, and there is **no frontmatter handling anywhere** in the backend or spec — if the pivot is taken, vault-Markdown ingestion becomes a named requirement rather than a bug. **(4) A spec §6.7 rationale is now false — owner's call, see Blocked.** Its justification for the npm waiver mechanism rests on "no soaked forward bump can clear `GHSA-qwww-vcr4-c8h2`", which PR #236 disproved. The *rule* is unaffected; only the trigger example. Needs the stop-and-amend flow — not touched unilaterally. **(5) Live cross-cutting carried:** the **ollama waiver items** — wave-6 `x/crypto`, wave-7 `x/image`, and **new wave-9 `x/net` (`CVE-2026-46600`, needs x/net ≥ 0.56.0 — a *higher* floor than the ≥ 0.55.0 the wave-6 rows record, so a rebuild satisfying wave 6 may still leave it live)** — none droppable by any currently-soaked tag; they need an upstream rebuild on patched *modules*, not a newer tag number, re-confirmed against `0.32.6` **and** `0.32.9` on 2026-08-13, so don't re-run that comparison without new evidence. Also: **fold the `CVE-2026-45447` openssl OS waiver retirement into the next ollama bump** (every `0.32.x` fixes it; not worth the eight-version jump alone); the **`architecture/` vault reconciliation** (needs a `meta-architect:*` pass — do not hand-edit); **mypy over `scripts/`**; the `docs/code` reference refresh; the `get_story`-outside-503-guard fix; and the residual inline Polish in `architecture/proposals/` (OQ-36 watch). **(6) Post-PoC follow-ups in `docs/BACKLOG.md`:** the Structure screen's empty build editor (S3 walk), the upload screen's one-way door with project context and graph filters not surviving a reload (S5 walk), plus **two new from the Session-107 walk** — chunking fails on frontmatter-bearing Markdown, and no progress feedback during an outline build.

<!-- ─────────────────────────── END HANDOFF ──────────────────────────── -->

> **Earlier milestones — archived.** Milestones 1–4 (M0 Setup, M1, M2 Basic extraction, M3 Cascade
> matching, M4 V1 polish), **Public readiness** (V1 → public portfolio) **and Graph quality** (curate
> the graph you have) — their full task breakdowns, Decided entries, and Done logs — live in
> **[`docs/PLAN_ARCHIVE.md`](PLAN_ARCHIVE.md)** (dated, append-only). This file carries **only the
> current milestone**; the milestone-roll ritual (`/wrap-session`, `docs/AGENTS.md` §7) moves the
> just-closed milestone there.

## Current milestone: Grzymalin reality check — the pipeline on real material

The **fork prerequisite made concrete** (opened Session 102, 2026-07-24). Run the pipeline **unaided**
on a *real* (half-synthetic), *English*, *non-fiction* research corpus about **Grzymalin** (a village
near Legnica) — replacing the synthetic, overextracted Oakhaven sample as the project's working/demo
material — and use what it produces to **lock the fork** (deeper extraction vs V2 Editing,
`PLAN_LONG.md` "◀ THE FORK") on real evidence rather than on the hand-curated Oakhaven graph.
Deliberately small (the anti-pattern is Graph quality's 33 sessions / 235 KB). The fork is **locked in
this milestone's last session** on the run's evidence; the *build* of the winning branch is the **next**
milestone.

**Corpus (owner, Session 102).** Grzymalin research documents — half-synthetic (Gemini Deep Research
over PL/DE/EN sources, delivered as **English** text), non-fiction/expository. First document provided
when ready; **more will follow** as the owner researches deeper, which will exercise **cross-document
entity joining** and **timeline / temporal ordering** — the *"Story Forge → History Forge"* direction
now recorded as a fork input in `PLAN_LONG.md`.

### Sessions

- [ ] **S1 — Grzymalin end-to-end run (the fork prerequisite).** *(First text now in
  `docs/samples/grzymalin/general-research.md` — unblocked.)* Ingest the first English doc; run
  chunk→extract→match→judge→accept **unaided** (no
  hand-curation); observe + measure against the question set; write a findings note in `docs/`. **Question
  set:** (1) **entity precision** — over/under-extraction vs the Oakhaven flood; (2) **surface fidelity**
  — are entity names grounded *verbatim* in the text (they must be, or the reader can't highlight them —
  DM-IH-1); (3) **relation sanity** — do predicates reflect the source; (4) **temporal capture** — are
  dates / events / ordering extracted at all (the history-forge need); (5) **chunking on non-fiction** —
  does chapter/scene detection cope with a topically-sectioned research doc or mis-structure it (spec §2 is
  narrative-framed — *observe*, don't commit to non-fiction support here); (6) **cost** per tier; (7)
  **flywheel substrate** — confirm the finetuning data is accumulating (the `candidate_decisions`
  hard-negatives + the `llm_calls` ledger persist during the run; the Grzymalin corpus is where real
  training data starts — owner S102, kept *distinct* from operational logging, which is deferred). No
  baseline comparison — Oakhaven is a bad yardstick (overextracted, hand-curated); judge Grzymalin on its
  own terms.
- [ ] **S2 — Normalise-names evidence (Graph-quality coda; promoted from `docs/BACKLOG.md` N-1).** The S6
  normalise-names card shows a label + counts + scores but **no evidence** — a human gate in name only (the
  milestone's own thesis unapplied to its own slice; the Session-101 architecture sweep supports promotion).
  Show up to ~3 **bearing edges** per label as `Subject —PREDICATE→ Object` (predicate labels) / a few
  carrying entities (type labels) — read-only derivations of data the suggest pass already loads
  (`Neo4jRepo.get_relations` + the `domain/entity_summary` name lookup; S3a evidence machinery for on-tap
  quotes). **Fork-independent — startable before the Grzymalin text lands.** Backend + frontend; substantive
  user-facing code → `/review-pr` **and** `/code-review`.
- [x] **S3 — App navigation / story hub (NEW, owner Session 102).** ✅ **Done Session 104 (2026-07-24,
  PR #229).** Today you reach a story by pasting a
  `:storyId` UUID — `/projects` has a `ProjectPicker` but nothing lists a *project's stories* or gives a
  story a landing page linking its screens (structure / reader / review / relations / duplicates /
  normalise-names). Add a homepage → project → **story list** → **story hub** so a raw UUID is never
  needed. Surfaces existing data (projects/stories) — **not a new spec feature**, low spec risk.
  Fork-independent, high daily value during the Grzymalin runs; startable before the text lands. Frontend
  slice (+ a small stories-by-project list endpoint if not already exposed — confirm at slice start).
  `/review-pr` **and** `/code-review`. **Shipped:** the story-list endpoint already existed
  (`GET /projects/{id}/stories`); added a small `GET /stories/{id}` for the hub header, a `StoryHub` page
  linking all seven screens, and a `StoryScreenLayout` "← Story hub" back-link on every subscreen. Both
  gates run; `/code-review` folded four findings (graph overflow, 404 dead-links, two dups). **Follow-up
  deferred → `docs/BACKLOG.md`:** the Structure screen shows an empty build editor for an already-structured
  story (should show its existing outline).
- [x] **S4 — Dev ergonomics: stack scripts + service log tail (NEW, owner Session 102).** ✅ **Done
  Session 105 (2026-07-24, PR #231).** No
  Makefile/scripts existed; the README quickstart was a manual 3-terminal dance. **Shipped:**
  `scripts/stack.sh <up|down|logs|status>` — `up` brings up infra (`docker compose up -d`) + backend +
  frontend (backgrounded in their own process groups, output → gitignored `.stack/*.log`, pids →
  `.stack/*.pid`); `down` TERM→KILLs the whole app tree then `docker compose down`; `logs` tails the
  **service** logs (neo4j+postgres, narrowable); `status` shows what `up` started. `up` warns-and-skips
  a service whose port is already taken by a hand-launched process. Minimal, no §6.7 impact. **Backend
  operational logging stayed OUT** — deferred to `PLAN_LONG.md` "Operational logging & observability".
  `/review-pr` clean (2 nits folded: header/usage dedup, busy-port warning); `/code-review` + browser
  walk not required (bash dev-tooling, no product code, no UI). CI green.
- [x] **S5 — Security image re-scan (standalone).** ✅ **Done Session 106 (2026-07-31, PR #233).**
  Grew well past a re-scan: it cleared **three** reds, two of which were invisible when the session
  started. **pgvector `0.8.2 → 0.8.5-pg17-trixie`** (pub 2026-07-08, 23 days soaked) retired **eleven**
  waivers — nine the rebuild genuinely fixed (gnutls ×5, krb5 ×2, openssl, libcap2) and **two already
  stale** (`CVE-2026-42011`, `CVE-2026-29111`), absent even from the *outgoing* pin's scan, so they had
  been waiving nothing. That empties the OS block and **closes the atypical "OS CVEs waived here"
  exception carried since Issue #4** — the image now scans completely clean. **ollama stayed at
  `0.24.0`**, correcting this task's own premise: a raw diff against the newest soaked candidate
  `0.32.3` showed the tags **identical but for one OS CVE** (same Go 1.26.0 stdlib, same `x/crypto`
  v0.43.0, `x/image` v0.22.0, `x/text` v0.30.0), so the bump would clear **no** bundled waiver — the
  wave-6/7 items below stay open. Its new **`CVE-2026-56852`** (`x/text` `norm.Iter` infinite loop,
  HIGH, availability-only) — the nightly red of 07-29/07-30 — is waived instead. **Two reds the plan
  didn't know about:** the `frontend` job is skipped on the nightly (`code=false`), so five React Router
  advisories published 2026-07-24 — *hours* after PR #231 merged — had sat invisible on `main`;
  `react-router-dom 7.16.0 → 7.18.1` cleared four, and the fifth (`GHSA-qwww-vcr4-c8h2`, RSC-mode CSRF)
  is fixed **only** in `react-router` 8.3.0 while `react-router-dom` has no v8 — no soaked forward bump
  clears it. Since `npm audit` was the one §6.7 gate with **no waiver mechanism**, it got one (see
  Decided). All three fixes rode one PR: `security` and `frontend` are both required checks, so any
  single-fix PR would have tripped the other.
- [x] ~~**S6 — Vault-maintenance**~~ **→ PULLED FORWARD out of the milestone (owner, Session 102) and
  DONE as pre-milestone Session 103 (2026-07-24, PR #227).** Ran *before* S1 so the milestone's decomposes
  read a current vault. Delivered: a `meta-architect:review-architecture` sweep
  (`reports/2026-07-24-architecture-review.md`, no blockers) that reconciled the S102-flagged milestone-
  framing drift on sight + a stale `INDEX` INV range; the two `architecture/components/` notes (`LLMRouter`,
  `EntityEditService` — the vault's first, closing OQ-35 T-1); and the Polish-gloss strip → English-only
  (the true set was **~34 of 36**, not "22" — a diacritics-only miscount, OQ-36). All `meta-architect:*`
  vault writes (ADR 0002). **This wrap renumbers the milestone to six sessions (S1–S5 + fork-close).**
- [ ] **S6 — Fork decision + milestone close** *(was S7 before the Session-103 renumber)*. Read S1's
  findings (with the history-forge inputs in `PLAN_LONG.md`); **lock** deeper-extraction vs V2 Editing;
  name + scope the next milestone via a `meta-architect:decompose-requirement` step-0 (the winning branch
  *is* branchy — a completeness sweep applies there, not to this milestone). Then run the roll.

**Sequencing:** S2 / S3 / S4 / S5 don't need the Grzymalin text and filled the wait — **S3, S4 and S5
are now done** (Sessions 104, 105, 106), leaving **S2** as the only fork-independent slice left.
**S1 runs when the text is ready** (it is); **S6 needs S1's evidence and closes the milestone.** (The
vault-maintenance unit that was S6 shipped early as Session 103 — see above.)

---

### Cross-cutting (do as the relevant session touches it — curated at the Graph-quality→next roll 2026-07-23, Session 101; see `docs/AGENTS.md` §3)

- [ ] **Reconcile the §6.7 npm-waiver amendment into the `architecture/` vault (needs a `meta-architect:*` pass — the vault's only writers).** The 2026-07-31 amendment gave the `npm audit` gate a scoped-waiver mechanism, overturning a recorded position: `architecture/proposals/backend-dependency-advisory-scan.md` **D5/G4** still records `npm audit` as the gate *deliberately left without* the scoped-waiver split ("as-built (PR #44): left unchanged; the asymmetry is noted and out of scope"). That as-built status is now stale, and the vault's stated purpose is orientation — a later session reading it would take waiving an npm advisory to be the **rejected** option. **Do NOT hand-edit:** root `AGENTS.md` restricts vault writes to the `meta-architect:*` skills, and `architecture/learning-log.md:58` is a *dated append-only* entry that the reconciliation rule says to leave intact as history — only the as-built/status text needs the update, ideally as a superseded note plus a new dated log entry. `review-architecture` is ritual-wired to the milestone roll (`/wrap-session` §5c), so the roll is the natural moment; do it sooner if a session touches the advisory-gate design. (Surfaced by the S5 `/code-review`, Session 106.)
- [ ] **Extend mypy to `scripts/` (small; ruff already covers it as of Session 106).** `scripts/check_npm_audit.py` decides whether frontend advisories block `main`, and `scripts/check_dependency_age.py` gates every dependency pin — both now get ruff lint + format (pre-commit and CI), but **not** type-checking: `backend/pyproject.toml` sets `files = ["src"]`, which is package-scoped by design. Closing it needs a handful of annotations (the `-> dict` returns in `check_npm_audit.py` would fail `--strict`'s missing-type-parameters check) plus a decision on how to invoke mypy over a path outside the backend package. Low urgency — the untyped surface is small and well-tested — but worth doing when either script is next substantially edited. (Surfaced by the S5 `/code-review`, Session 106.)
- [x] ~~**Drop the react-router `GHSA-qwww-vcr4-c8h2` waiver by migrating the frontend to React Router v8 (`/triage-advisory`).**~~ ✅ **Done Session 107 (2026-08-13, PR #236) — but by a one-patch bump, not a migration.** `react-router-dom 7.18.1 → 7.18.2` clears the advisory outright; `infra/npm/audit-waivers.toml` now holds **zero** waivers and the gate reports `0 waived, 0 stale`. **The premise below was correct when written and became false without anything in this repo changing:** the advisory was **amended upstream** to record a **7.x backport**, moving the vulnerable range from `>= 7.12.0, < 8.3.0` to `>= 7.12.0, < 7.18.2`. Session 106 had verified the "no forward bump clears it" claim *empirically* (installing both 7.18.1 and 7.18.2), so this is not a case of a sloppy assessment — it is an advisory whose patched-version metadata moved afterwards. **The v8 migration is therefore no longer security-driven** and can be scheduled on its own merits, if at all. **How it was caught matters:** not by the `ignoreUntil` (still 18 days out), not by the recorded drop-condition (about *our* code — "migrate to v8" — which never became true), and not by re-running the gate (which audits the *installed* version and dutifully reported "waived"). A **Dependabot** cross-check found it, because Dependabot tracks the advisory's *current* patched range. That gap is now closed in `/triage-advisory` — step 1 gained the Dependabot cross-check, step 3 the package-side twin of the Session-99 image rule. Original detail retained below. (Session 107, 2026-08-13.)
  > *Original item text, retained as history (§4 strike-don't-delete) — resolved above, not live:*
  > The only waiver in the new `infra/npm/audit-waivers.toml`, added Session 106 (PR #233): an RSC-mode CSRF bypass, HIGH, unreachable here (Vite SPA, no SSR, **zero** RSC/`unstable_*` usage — verified by grep). **Availability is not the blocker** — `react-router` 8.3.0 published 2026-07-22 and cleared its 14-day soak on **2026-08-06** (floor = pub + 15). The blocker is that **`react-router-dom` has no v8 release**, so the fix means switching the **28** `react-router-dom` import sites to `react-router` and handling the v7→v8 breaking changes: a real slice, its own session, and a **user-facing** one (so the browser-walk merge gate applies). **`ignoreUntil = 2026-08-31`** — on that date the `security` job re-reds `main` on its own, at which point `/triage-advisory` either takes the fix or extends the waiver with a fresh written rationale. Extending is a deliberate decision, not a default. (Session 106, 2026-07-31.)

> **Graph-quality→next-milestone curation pass (2026-07-23, Session 101).** Every item walked by hand
> per `docs/AGENTS.md` §3 — and for the first time under the **two-roll rule** added the same session.
> **Resolved this milestone → moved to `docs/PLAN_ARCHIVE.md` (§7):** the Session-59 vault motivation
> reframe and the DM-IH-8 tooltip homes (both closed by this roll's `review-architecture` sweep), the
> Session-27 waiver-drop task (fully closed at S99), the setuptools OSV waiver (S99), the
> `CandidateView` merge-target name (S75), and the two-review-queues shared-machinery item (S79). Also
> struck: the meta-architect workflow-integration item (its live part landed at S68; the residual
> hypotheses are owned by ADR 0002 §4 and Issue #31, not by this list) and the perennial "update spec
> §10 if an open question hardens" (owned by the spec's own stop-and-amend flow, not a task).
> **Aged out under the two-roll rule → `docs/BACKLOG.md`:** ten items that had survived two or more
> rolls untouched, every one carrying the "fix when the relevant code is next touched" escape hatch.
> Because the incoming milestone is unchosen, the rule's other branch (name it as a task there) was
> unavailable, so all ten took the BACKLOG branch — where they get reviewed and promoted at the next
> roll rather than carried silently again. **Carried forward live —** the four condition-based image /
> OSV waiver items (all raised *this* milestone, all still actionable — the pgvector + ollama re-scan
> is the one with a soaked candidate available today), the `docs/code` reference refresh, and the
> `get_story`-outside-the-503-guard fix (S100 found the **reader** route shares the shape, widening it).

- [ ] **Drop the ollama `x/crypto/ssh` wave-6 waivers (CVE-2026-39832 + CVE-2026-39831) when a patched ollama image soaks (`/triage-advisory`).** CVE-2026-39832 added 2026-06-27 (Session 70, PR #161); **CVE-2026-39831 added 2026-07-09 (Session 80, PR #184** — a fresh `x/crypto/ssh` FIDO/U2F presence-check bypass, GHSA-89gr-r52h-f8rx, *unmasked* by the pgvector krb5 waiver letting the sequential scan reach ollama). Both against the unchanged `ollama/ollama:0.24.0` pin, waived on the same unreachability as their wave-6 `x/crypto/ssh` siblings (ollama opens no SSH client / uses no FIDO keys; 127.0.0.1 JSON inference). **Condition-based** drop: when an `ollama/ollama` tag rebuilt on `golang.org/x/crypto ≥0.52.0` soaks ≥7 days, bump via `/pin-image` and drop these + the sibling wave-6 ssh/net waivers together. Re-scan to confirm (not date-checkable). (Session 70, 2026-06-27; extended S80.)
- [x] ~~**Drop the pgvector krb5 waivers (CVE-2026-40356 + CVE-2026-40355) when a rebuilt pgvector image ships the `deb13u1` krb5 (`/triage-advisory`).**~~ ✅ **Done Session 106 (2026-07-31, PR #233)** — the `0.8.5-pg17-trixie` rebuild ships krb5 `1.21.3-5+deb13u1`, so both dropped, along with **nine** further OS waivers (gnutls ×5, openssl, libcap2, and two already-stale entries). The pgvector OS block is now empty and the Issue-#4 "waive OS CVEs" exception is closed. Original detail retained below. CVE-2026-40355 added 2026-07-09 (Session 80, PR #184) — a fresh krb5 NULL-ptr-deref DoS (GHSA-8qgv-wm66-hrmc), sibling of the already-waived CVE-2026-40356, **same** Debian fix `1.21.3-5+deb13u1` (not yet baked into a pgvector rebuild). Both waived on the same unreachability (no Kerberos service in the container; Postgres uses OpenSSL for TLS; linked libs only; 127.0.0.1 single user). **Condition-based** drop: when a rebuilt `pgvector/pgvector:0.8.2-pg17-trixie` (or a newer soaked tag) ships krb5 ≥`1.21.3-5+deb13u1`, drop both. Re-scan to confirm (part of the standing "pgvector CVE-rot" set in `infra/trivy/WAIVERS.md`). (Session 80, 2026-07-09.)
- [x] ~~**Re-scan the pgvector + ollama pins against their much-newer soaked tags — the new §3b tag-freshness check flagged both (`/triage-advisory`).**~~ ✅ **Done Session 106 (2026-07-31, PR #233)** — pgvector bumped `0.8.2 → 0.8.5` (11 waivers retired). **ollama deliberately NOT bumped:** the raw pinned-vs-candidate diff the method prescribes showed `0.32.3` is identical to `0.24.0` but for one OS CVE — same Go 1.26.0 stdlib, `x/crypto` v0.43.0, `x/image` v0.22.0, `x/text` v0.30.0 — so an eight-minor-version jump needing full stack re-verification would have retired **zero** bundled waivers. **Recorded so it is not re-litigated:** the ollama wave-6/7 items below are *not* droppable by any currently-soaked tag; they need an upstream rebuild on patched modules, not a newer tag number. Original detail retained below. The Session-99 retro added a session-start check for *condition-based* image waivers (a newer tag is the only way their drop-condition can become true), and its **first run immediately flagged two stale pins**: **pgvector** is pinned at `0.8.2-pg17-trixie` while **`0.8.5-pg17-trixie` has been out since 2026-07-08** (soaked), and **ollama** is pinned at `0.24.0` while **`0.30.2` has been out since 2026-06-03** (long soaked). Both images carry condition-based waivers whose drop-when is literally *"when a rebuilt image ships the fix"* — pgvector krb5 + gosu Go-stdlib, and the ~20 ollama `x/crypto/ssh` / `x/net` / `x/image` entries — so a rebuild that old may well clear a large batch of them. **Do this as its own security session:** for each image, diff raw Trivy findings pinned-vs-candidate (does the bump clear waivers, and does it introduce anything new?), then bump via `/pin-image` and delete every waiver it retires. Treat the version jumps as real changes, not pure rebuilds — `0.8.2 → 0.8.5` and `0.24.0 → 0.30.2` may move Postgres/ollama behaviour, so verify the stack still boots and the integration tier passes before merging. Note this is exactly the rot the retro identified, found on two *more* images. (Session 99, 2026-07-23.)
- [ ] **Drop the ollama `x/image` wave-7 waivers (CVE-2026-46602 + CVE-2026-46604) when a patched ollama image soaks (`/triage-advisory`).** Two `golang.org/x/image` TIFF-decoder DoS HIGHs compiled into `ollama/ollama:0.24.0` (x/image v0.22.0), waived Session 98 (PR #218) — *unmasked* once the setuptools OSV red cleared and the sequential `security` job reached the ollama scan (OSV runs before Trivy, so its failure had masked the ollama scan for days). Same unreachability as the wave-6 Go-module siblings (reachable only by decoding a crafted TIFF fed to a multimodal model; 127.0.0.1 single trusted user, self-inflicted). **Condition-based** drop: when an `ollama/ollama` tag rebuilt on `golang.org/x/image ≥0.43.0` soaks ≥7 days, bump via `/pin-image` and drop these together with the sibling wave-6 ssh/net waivers. Re-scan to confirm (not date-checkable). (Session 98, 2026-07-21.)
- [ ] **Drop the ollama `x/net` wave-9 waiver (CVE-2026-46600) when a patched ollama image soaks (`/triage-advisory`).** A `golang.org/x/net/dns/dnsmessage` **panic** HIGH (GO-2026-5942 / GHSA-gg3m-vvp2-p2c5) compiled into `ollama/ollama:0.24.0` (x/net v0.46.0), waived Session 107 (2026-08-13) after it reddened the *scheduled* `main` run — pure treadmill, nothing in the repo changed. Availability-only, and unreachable from the inference API: `dnsmessage` parses DNS **responses**, so it needs a hostile DNS responder / MITM on ollama's *outbound* lookups, not an inference caller. **Note the threshold moved:** this needs `x/net` **≥0.56.0**, higher than the ≥0.55.0 the wave-6 x/net entries record — so a rebuild that satisfies wave 6 may still leave this one live. Drop it together with the wave-6/7 siblings when a rebuilt tag soaks. Re-scan to confirm (condition-based, not date-checkable). (Session 107, 2026-08-13.)
- [ ] **When ollama is next bumped for a real reason, retire the `CVE-2026-45447` (openssl) OS waiver in the same move.** The pin `0.24.0` waives one **OS-package** CVE that every `0.32.x` tag already fixes — and project doctrine is to fix an OS CVE with a fresher tag, never waive it (the pgvector exception is closed). It has **not** been taken on its own because the bump buys *only* that: raw diffs of `0.24.0` vs the soaked `0.32.6` **and** the newest `0.32.9` show identical `x/net` v0.46.0 / `x/crypto` v0.43.0 / `x/image` v0.22.0 / `x/text` v0.30.0, so an eight-minor-version jump needing full stack re-verification would retire **zero** bundled waivers — and the openssl CVE is itself unreachable here (ollama does no PKCS#7/S-MIME verification). **Recorded so it is neither re-litigated nor forgotten:** don't bump for this alone; do fold it in the moment a rebuild ships patched Go modules and the pin moves anyway. (Session 107, 2026-08-13; re-verifies the Session-106 decision against fresher candidates.)
- [ ] **Refresh the `docs/code` reference notes for the graph-quality features — frontend AND now the backend edge-write surface (`/document-code changed`).** The Graph-quality slices added structure the reference layer doesn't yet describe — **frontend:** S2 navigation (`graphFilters.ts`, `useDebouncedValue`), S3b (`EdgeEvidencePanel`, edge-tap), S4b (`hooks/useReviewQueue.ts`, `features/duplicate-review/`, `useDuplicateSuggestions`/`useDismissDuplicate`, `MergeConflictFields`, the `/duplicates` route), **S5a** (the new `features/entity-panel/` — `EntityEditPanel`, `entityEditForm`, `useEntityEditForm`, and the six relocated helpers; `features/graph-viewer/selectionGuard.ts`; the deleted `NodeDetailsPanel`; `ReaderEntityPanel` recomposed as a thin wrapper); **backend (S5b-be):** the new pure `domain/relation_rekey.py`, `EntityEditService.retarget_relation`, the `PATCH …/relations/{edge_id}` route + `RetargetRelationRequest`, and the `GraphRelation.edge_uid` field; **S5b-fe:** the new `features/graph-viewer/edgeEditForm.ts` + `useEdgeEditForm.ts`, `lib/api/useRetargetRelation.ts`, the `EdgeEvidencePanel` extended read-only → editable, and the `selectionGuard.ts` generalization (`selectedNodeId` → `selectedId`, now shared by node + edge). **backend (S6a-1):** the new pure `domain/label_synonyms.py` (the label self-join + `label_dismissal_id`), `adapters/label_vocabulary_reader.py` (`LabelVocabularyReader` + the `LabelEncoder` Protocol), `adapters/postgres_label_dismissal_store.py`, `Neo4jRepo.list_predicate_vocabulary`/`list_type_vocabulary`, the `label_pair_dismissals` table, and the `GET/POST/DELETE …/label-vocabulary` routes + `name_normalise_suggest_floor` knob. **backend (S6a-2):** the new pure `domain/predicate_rename.py` (`plan_predicate_rename`), `EntityEditService.rename_predicate` + `relabel_entity_type` (+ the `PredicateRenameSummary`/`TypeRelabelSummary` results), `Neo4jRepo.relabel_entity_type` (the one net-new graph writer) + the `EntityGraphEditor` Protocol gaining `get_relations`/`relabel_entity_type`, and the `POST …/label-vocabulary/rename` route + `RenameLabelRequest`/`RenameSummaryResponse`. **frontend (S6b):** the new `features/normalise-names/` (`normaliseNames.ts`, `LabelPairCard`, `NormaliseNamesQueue`), the three `lib/api/` hooks (`useLabelVocabulary`, `useRenameLabel`, `useDismissLabel`), and the `/stories/:id/normalise-names` route + graph-viewer link. **backend (S6c):** the new pure `domain/name_similarity.label_match_score` (the subset-intolerant label scorer) alongside `name_match_score`. **backend (S7):** the new pure `domain/entity_summary.py` (`summarise_relations` + `RelationSummaryLine`/`EntityRelationSummary` — the §3.5 graph-derived tooltip summary) and the `ReaderEntity` response model gaining `relations`/`relation_overflow` (the reader route now also reads `get_relations`). **frontend (S7):** the **new `src/components/ui/` directory** — the first occupant of the folder `frontend/src/AGENTS.md` documents — holding `QueueProgress` and `EmptyQueueNext`, both consumed by all four review queues; and `decorations.tooltipText` extended to render the summary. **backend (S7 follow-up):** `LabelVocabularyReader` gained an app-lifetime **embedding cache** keyed by bare label (shared across projects) plus an `asyncio.Lock` serialising the cold-cache encode — a behavioural change the reference note's description of that adapter should mention, since it is why a vocabulary load costs ~1.7 s rather than ~14 s. **Grzymalin S3 (Session 104):** the new `GET /stories/{id}` route + `get_story_summary` repo read (backend); the new `features/story-hub/` (`StoryHub`, `StoryScreenLayout`, `storyScreens.ts`), `lib/api/useStory`, the now data-driven per-story sub-routes in `app/routes.tsx` (generated from `STORY_SCREENS`), and `isoDate` relocated to `lib/utils` (frontend). Nothing compiles `docs/code/*.md` against the code, so they've drifted silently across S2/S3/S4/S5/S6. Refresh the relevant notes (+ `CODE_GUIDE`) via `/document-code changed` — do as a focused pass, or fold at the milestone roll. (Session 79 step-3a backstop; S5a added Session 81; backend edge-write surface added Session 82; S5b-fe added Session 83; S6a-1 added Session 92; S6a-2 apply surface added Session 95; S6b frontend added Session 96; S6c label scorer added Session 97; S7 tooltip summary + the new `components/ui/` added Session 100.)
- [ ] **Guard `get_story` / `get_project` inside the declared-503 `try` across the entity-edit routes (`api/stories.py`).** Every edit route (`edit_entity_route`, `add_relation_route`, `remove_relation_route`, `retarget_relation_route`, merge, delete) calls `story = await get_story(conn, story_id)` **before** its `try/except (OperationalError, ServiceUnavailable)`, so a Postgres outage on that read raises a bare **500** while the route **declares 503** — the `/review-pr` §4 undeclared-500 lens. Pre-existing and *systemic* across the file (not introduced by S5b-be, which mirrors the pattern), so fix it in **one focused pass** over all the edit routes rather than piecemeal — move the story/project lookups inside the guard (or a shared dependency that maps the outage). Surfaced by the S5b-be `/review-pr` (Session 82); do when the edit-route error surface is next touched. (Session 82 own-review finding.) **Note:** the S6a-1 `label-vocabulary` **read** routes (Session 92) already guard `get_story` *inside* the declared-503 `try`, so they don't add to this debt — this item stays scoped to the pre-existing *edit* routes; S6a-2's apply endpoints must guard likewise.

### Blocked / questions

- **Spec §6.7 carries a now-false rationale for the npm waiver mechanism — amend or annotate? (raised Session 107, 2026-08-13; owner's call, needs the stop-and-amend flow.)** The §6.7 note that introduced the waiver mechanism justifies it with: *"`GHSA-qwww-vcr4-c8h2` … is patched **only** in `react-router` 8.3.0 while `react-router-dom` has no v8 release, so **no soaked forward bump can clear it**."* That claim is **false as of 2026-08-13** — the advisory was amended upstream and `7.18.2` cleared it (PR #236). **The mechanism itself remains fully justified** and nothing about the rule changes; only the trigger example was overtaken. Two options: (i) annotate the sentence as a dated, superseded trigger (smallest honest edit, preserves the decision's history); (ii) rewrite the rationale around a hypothetical rather than this specific advisory. Not touched unilaterally because the spec is the source of truth. **Related, smaller:** `.claude/skills/review-pr/SKILL.md` §8 cites the same case as its example of a partial bump; the lens stays valid, but this episode arguably gives it a stronger ending ("…and re-check it, because advisories get amended") — `/retro` territory, not a spec change.

- _**Earlier resolved registers / questions (Milestones 1–4, Public readiness, Graph quality)** — see **[`docs/PLAN_ARCHIVE.md`](PLAN_ARCHIVE.md)**._

### Decided

- **2026-08-13 — Session 107: an advisory's "no fix exists" is a snapshot, not a standing fact — `/triage-advisory` gains a Dependabot cross-check and a package-side drop-revisit rule (PRs #235, #236).** **(a) ollama stays at `0.24.0`, re-verified rather than inherited.** The nightly `main` run reddened on a fresh `x/net/dns/dnsmessage` panic HIGH (`CVE-2026-46600`, fixed in x/net 0.56.0). Fix-first was exhausted *empirically*: raw Trivy findings for the pin were diffed against **both** the newest soaked candidate `0.32.6` and the newest stable `0.32.9`, and all three ship identical `x/net` v0.46.0 / `x/crypto` v0.43.0 / `x/image` v0.22.0 / `x/text` v0.30.0 — across eight minor versions the only difference is one OS CVE. So the CVE was waived (availability-only; `dnsmessage` parses DNS *responses*, needing a hostile responder or MITM on ollama's **outbound** lookups — unreachable from the 127.0.0.1 inference API). **Considered and rejected:** bumping to `0.32.6` anyway to retire the `CVE-2026-45447` openssl **OS** waiver — doctrine says fix an OS CVE with a fresher tag, but the jump buys *only* that one unreachable CVE (ollama does no PKCS#7/S-MIME verification) at the cost of an eight-version jump plus full stack re-verification. Recorded as a cross-cutting item to fold in whenever ollama next moves for a reason that carries the jump on its own, so it is neither re-litigated nor forgotten. **(b) The npm waiver was dropped by a *fix*, and the way it was found is the lesson.** `GHSA-qwww-vcr4-c8h2` was waived Session 106 on a rationale that **foreclosed** a fix — patched only in `react-router` 8.3.0, and `react-router-dom` has no v8 — which was **correct and empirically verified at the time**. The advisory was then **amended upstream** to record a 7.x backport (range → `< 7.18.2`), so a one-patch bump `7.18.1 → 7.18.2` cleared it. **Nothing in this repo changed.** None of our three gates could surface that: the `ignoreUntil` was 18 days out, the recorded drop-condition was about *our* code ("migrate to v8"), and re-running the gate audits the **installed** version so it reported "waived" faithfully. A **Dependabot** cross-check found it, because Dependabot tracks each advisory's *current* patched range. **(c) Both halves of the gap were closed in `/triage-advisory` (retro, owner-approved):** step 1 gains the Dependabot enumeration as a routine second opinion; step 3 gains the **package-side twin of the Session-99 image rule** — re-auditing the frozen *lockfile* can no more reveal an available fix than re-scanning the frozen *pin* could, so every revisit must check the advisory's live patched version and treat a recorded "no fix exists" as re-verifiable. **Considered and rejected:** taking only the doctrine (relies on remembering to look) or only the tooling call (leaves the next agent without the *why*). **(d) The v8 migration is no longer security-driven** and can be scheduled on its own merits, if at all.

- **2026-07-31 — Session 106: the `npm audit` gate gains a scoped, dated waiver mechanism (spec §6.7 amended); pgvector bumped, ollama deliberately not (PR #233).** **(a) The §6.7 amendment (stop-and-amend flow, owner-approved).** `npm audit` was the **one security gate with no waiver mechanism** — fine until an advisory is both unreachable here *and* unfixable, which `GHSA-qwww-vcr4-c8h2` is: patched only in `react-router` 8.3.0 while `react-router-dom` has **no v8**, so no soaked forward bump clears it (verified empirically by installing 7.18.1 *and* 7.18.2 in a scratch project and re-auditing; npm's own `audit fix --force` suggests a *downgrade* that reintroduces a HIGH DoS). The only alternative was relaxing the whole gate to `--audit-level=critical`, which stops gating **every** future HIGH — strictly worse than a scoped, dated, justified exception. **Considered and rejected:** (i) that blanket relaxation; (ii) waiting for the v8 migration, which would have left `main` red for every code-bearing PR for a week+ and blocked this session's image work. **(b) `ignoreUntil` is mandatory here** (stricter than the OSV toml, where it's an optional backstop) — an npm advisory is always cleared by a bump we control, so every waiver has a real calendar horizon; and a mandatory expiry is what stops an ignore rotting behind a green board. A waiver matching nothing is reported **STALE** rather than failing, since a fixed dependency is good news. **(c) The gate runs in the `security` job, not `frontend` — and that placement is load-bearing.** `security` force-runs on the daily schedule; `frontend` is path-scoped, so a docs-only stretch past an expiry date would have left a lapsed HIGH suppressed — the exact rot the date exists to prevent. A dated waiver mechanism and a path-scoped job are incompatible; **any future advisory gate belongs in `security` for the same reason.** (This was got *wrong* first — see the Done line and the new §1 rule.) **(d) ollama stays at `0.24.0`.** Facts before the decision: a raw Trivy diff showed the newest soaked candidate `0.32.3` is identical to the pin but for one OS CVE, so the bump retires **zero** bundled waivers — an eight-minor-version jump needing full stack re-verification, for nothing. The plan had assumed the opposite; measuring is what corrected it. **(e) `scripts/*.py` now gets ruff** (lint + format, pre-commit and CI) — 262 lines deciding whether advisories block `main` had less checking than the tests exercising them. Needed a repo-root `ruff.toml` so the root-run hook and the `backend/`-run CI check agree on line length.

- **2026-07-24 — Session 105: S4 dev-ergonomics stack scripts shipped (PR #231); a fork-independent slice the owner picked from the open S1/S2/S4/S5 set.** A small dev-tooling session (bash + docs + gitignore; no product code). **(a) Design calls (agent's, stated up front).** One dispatcher `scripts/stack.sh <up|down|logs|status>` over several tiny scripts (smallest discoverable surface); backend/frontend **backgrounded in their own process groups** (`set -m`, pgid==pid) so `down` can TERM→KILL the whole tree (uvicorn `--reload` workers, vite children), not just the parent; output → gitignored `.stack/*.log`, pids → `.stack/*.pid`; `down` uses `docker compose down` (named volumes persist, never `-v`); `logs` defaults to the neo4j+postgres **service** logs per the S4 ask. **(b) Verification without touching the live stack.** The full stack was already running (infra 4 h, backend :8000 + frontend :5173 listening), so `up`/`down` were **not** run end-to-end (port collision / would tear down the owner's instance); instead the risky process-group `start_bg`/`stop_bg` were unit-tested against a throwaway process (leader is its own group leader, child shares the group, `stop_bg` kills **both** — proving group-kill not parent-only, idempotent re-start, stale-pidfile handling), and `help`/`status`/`logs`/unknown-cmd exercised read-only against the live stack. This harness caught a real `set -u` unbound-`$name` bug (self-referencing `$name` in one `local`), fixed before review. **(c) Gates.** `/review-pr` was the gate; `/code-review` + browser walk **not required** (dev-tooling, no correctness-bearing product code, no UI) — stated up front, not skipped silently. Two `/review-pr` nits folded per the fix-nits-up-front rule: header/`usage()` command-list dedup, and `up` now warns-and-skips on a busy port (portable `/dev/tcp` check) instead of spawning a duplicate that dies into the log. **(d) No spec amendment** — dev-tooling, no §-referenced surface; backend operational logging correctly left to `PLAN_LONG.md`.

- **2026-07-24 — Session 104: S3 app-navigation / story hub shipped (PR #229), plus a new merge-flow rule from the retro — a browser walk is required for user-facing changes.** A build session (owner picked S3 from the open fork-independent slices as a run-enabler for the coming Grzymalin runs). **(a) Endpoint choice.** The stories-by-project *list* endpoint already existed (`GET /projects/{id}/stories`), so the list is frontend-only; the fact-finding surfaced that no *single-story* detail endpoint existed, so the hub header needed one. Owner chose the **bookmark/reload-safe** variant: a small `GET /stories/{id}` → light `StorySummary` (title + ingest time, **no `raw_text`** body) over threading the title through router `location.state` (which the repo was bitten by in Session 53). **(b) What shipped.** `StoryHub` page linking all seven per-story screens (single `STORY_SCREENS` source of truth in `storyScreens.ts`, also driving the router so a screen can't be routable-but-unlinked); a `StoryScreenLayout` route-level "← Story hub" back-link on every subscreen (fixing a one-way-door gap the owner's browser walk found); a 404 shows a not-found state with no dead links while a transient 503 stays navigable. **(c) `/code-review` folded four findings** — two correctness (the back-link header overflowed GraphViewer's `h-screen` root → layout now owns viewport height + GraphViewer root `h-screen`→`h-full`; the hub showed seven dead links on a 404 → branch on `error.status`) and two dedups (screen list, `isoDate`). Both gates run (`/review-pr` + `/code-review`), CI green, browser-verified. **(d) Structure screen deferred → `docs/BACKLOG.md`:** reached cold it shows an empty build editor (it expects upload-flow text), and for an already-structured story rebuilding is 409-blocked — the proper fix (show the *existing* outline) is a small read feature of its own, not a nav fix (owner decision at the browser walk). **(e) Retro outcome — new rule (owner-approved):** *a user-facing change gets a browser walk before merge, proactively, not on request* — added to root `AGENTS.md` Merge flow + elevated in `frontend/AGENTS.md` from how-to to merge gate. This **supersedes the Session-101 roll's rejection** of a mandated browser walk (rejected as "already universal"; S104 skipped it until the owner prompted, and the walk caught the two most impactful defects — evidence it isn't universal without the rule). The S101 rejection stays in `docs/PLAN_ARCHIVE.md` as history. No spec amendment (S3 surfaces existing data; low spec risk, as scoped Session 102).

- **2026-07-24 — Session 103: the pre-milestone vault-maintenance unit (pulled forward from S6) — sweep, two component notes, English-only glossary (PR #227, `meta-architect:*` vault-only; no production code).** Ran the `meta-architect:review-architecture` sweep first (`architecture/reports/2026-07-24-architecture-review.md`, **no blockers**; 3 `risk`, all reconciled on sight): the milestone-framing drift (`PROJECT.md`/`overview.md` still "unchosen" → named the Grzymalin reality check, fork stays open), a stale `INDEX.md` "INV-1…INV-9" → "INV-1…INV-10" (INV-10 shipped S5b-be; AGENTS.md was fixed S101, INDEX missed), and the glossary-strip scope. **(a) Populated `components/` for the first time** (closes OQ-35 T-1): `LLMRouter` (the machine↔provider trust boundary + fail-closed budget + failover + the `llm_calls` ledger) and `EntityEditService` (the human-reached committed-graph writer — INV-9 instances **3–8**, i.e. *six* paths, enumerated from `invariants.md` not guessed). **(b) Glossary → English-only** (Decided 101(d)): the strip scope had been recorded as "22" from a diacritics-only grep, but the true set was **~34 of 36** term-notes — the fact was re-established before acting (the "keyword-grep completeness illusion" the resume/review rules warn about, caught at plan time). Preserved the non-Polish parentheticals (`agent (…)`, `(BFF)`, `(SCA)`) and the *content-domain* fixture name "Wody Święte"; stripped the index + two inline body glosses + `overview.md`'s three architectural glosses (the two headline notes were already being edited). **(c) Left proposals'/append-only-history inline Polish out of scope** — a possible later pass, flagged as an OQ-36 watch, not bundled into a maintenance unit. **(d) Left glossary term-note `updated:` dates unchanged** — a Polish-gloss removal is cosmetic (meaning unchanged) per the `architecture/AGENTS.md` content-vs-commit rule; the change is dated in `CHANGELOG`. `/review-pr` clean (docs-only; `/code-review` not required); CI green (secret-scan + CodeQL). **This wrap renumbers the milestone to six sessions (S1–S5 + fork-close S6).**

- **2026-07-24 — Session 102: opened the "Grzymalin reality check" milestone — the fork prerequisite made concrete, deliberately small, fork stays open until its last session.** A planning/thinking unit (no production code). **(a) Corpus chosen — Grzymalin over Oakhaven.** The fork prerequisite ("run fresh sample end-to-end before deciding") is realised on a *real* (half-synthetic), *English*, *non-fiction* research corpus about **Grzymalin** (village near Legnica): Gemini Deep Research over PL/DE/EN sources, delivered as English text. The owner explicitly **rejected Oakhaven as a baseline** — "chaotic, first pass, overextracted, oversaturated" — so S1 judges Grzymalin *on its own terms*, no comparison. This retires the synthetic-fiction sample as the working/demo material. *Note:* my first framing over-weighted a "Polish language" axis; the text is **English** (about a Polish subject), so that axis is off — what stays live is *real-research vs synthetic-fiction* and *non-fiction vs narrative*. **(b) Milestone shape — small reality-check, fork open.** Opened at five sessions, **expanded to seven** the same session (see (f)): S1 run · S2 normalise-names evidence · S3 app-navigation · S4 dev-ergonomics scripts · S5 security re-scan · S6 vault-maintenance · S7 fork-decision + close; the fork is locked only in the **last** session (S7), on S1's evidence, and the winning branch's *build* is the next milestone. *Considered + rejected:* folding the winning branch's first slices into this milestone (contradicts "don't lock the fork this session" and grows the plan footprint the roll just shrank). Sized against the Session-101 note that Graph quality ran 33 sessions / 235 KB — seven sessions is still deliberately small. **Amended the same session (owner):** S6 vault-maintenance is **pulled forward out of the milestone** to a pre-milestone unit (Session 103), expanded with a `review-architecture` sweep, so the vault is current *before* the milestone's decomposes read it rather than deferred to a late slice — the milestone drops to **six** sessions. (Prompted by the S102 `/review-pr`, which found three vault homes still framing the milestone as unchosen / N-1 as a bare backlog candidate; cleaning them now beats deferring.) **(c) Coda = normalise-names evidence only.** Promoted N-1 out of `docs/BACKLOG.md` → S2 (fork-independent, finishes S6's own thesis, reuses existing machinery). The isolated-node bulk-triage candidate stays deferred — it *interacts with the fork* (better extraction may stop producing strays), so building it before the fork decision risks wasted work. **(d) Both standalone maintenance units scheduled** as S3 (security) + S4 (vault), rather than left as opportunistic cross-cutting. **(e) "Story Forge → History Forge" recorded as a fork input** (owner: multiple Grzymalin documents will follow as he researches deeper; **timeline / temporal ordering** is "very important in historical research", and cross-document entity joining will be exercised). Captured in `PLAN_LONG.md`'s fork section — it weights the fork toward the deeper-extraction / relation-modelling branch **without locking it**, and flags the spec-identity question (§2 is narrative-framed) as a *watch*, not a stop-and-amend now (same class as Decided 101(c): a directional signal that should shape *reversible* decisions, not trigger an immediate spec rewrite). **(f) UX/tooling scope folded in (owner ask).** The owner asked whether to add UI/UX navigation, stack start/stop scripts, and easy log viewing. Grounded in a quick survey (a `ProjectPicker` exists but no story-level navigation — hence the UUID-pasting; no Makefile/scripts; the backend emits *no* operational logs today): folded in **S3 app-navigation** (homepage → project → story list → story hub) and **S4 dev-ergonomics** (stack scripts + *service* log tail). **Backend operational logging → backlogged** (owner: "backlog it") in `PLAN_LONG.md`'s observability section, not built here — it needs real logging plumbing, not a script. The owner tied it to *"start gathering data to finetune smaller models"*; recorded that this is the **data flywheel** — a concern *distinct* from operational logging (the substrate is the persisted `candidate_decisions` + `llm_calls` records, already captured, not scraped from log lines) — and added it as S1 question (7) so the Grzymalin runs are where that data starts accumulating in earnest. **No spec amendment, no ADR, no production code.**

- **2026-07-23 — Session 101 (b): four owner answers to the architecture sweep's open questions — one confirmation, one sharpening, and a real recalibration of who the vault is written for.** Raised at the wrap as findings needing the owner; answered the same session, so none carried forward. **(a) The vault's `updated:` dates stay a real freshness signal** — *"there are no people to edit those notes, only you are taking care of them, so you need to do it right."* All **12** mismatched dates corrected in the same pass (each set to its last content-bearing commit, verified by diff), and `architecture/AGENTS.md` gained the standing rule: bump `updated:` in the same edit that changes a note's body, always — with the content-vs-commit distinction spelled out so a wikilink repoint doesn't cause a false bump. *Rejected:* demoting the date to coarse with git as the source (a wrong date is worse than none, and there is no human to blame for the drift — the agent is the only maintainer). **(b) The vault's empty `components/` altitude gets populated** — *"we should keep it updated and usable"* — so per-component notes are wanted, not deliberately skipped; the two that pay (`LLMRouter`, `EntityEditService`) are a **named task**, not a write-it-when-something-touches-it carry. **(c) The persona is confirmed but now has an expiry.** Still one person, own machine, fully trusted — *and* the owner wants it *"usable one day, and not only for me."* No spec amendment (§2.3 scopes the **PoC**), nothing to build now; what changes is that "single-user forever" is no longer safe to build **irreversibly** against — a shortcut that would be *wrong* under many users and expensive to undo now deserves a note when taken. The live example is the S7 label-embedding cache, keyed on the bare label and shared across projects: harmless under one trusted user, a shared mutable structure below the tenancy key if the aspiration lands. **(d) The biggest change — the vault is not written for the owner.** He states plainly he does not read these notes, retiring the bootstrap assumption that it partly exists to teach *him* architecture vocabulary. The two real readers are an outside visitor reading cold and the agents that orient from the vault. The accessibility bar is **unchanged** (*"informative, usable for the beginner"*) but the **Polish glosses go** — they existed to teach the operator in his first language. English only going forward; the **22** existing glossary notes carrying Polish are a named stripping task. **This surfaced a leak in the now-shared `meta-architect` plugin:** `templates/project.md` **hardcoded** "glossary always carries the Polish term too", and three more files defaulted to "EN + PL" — a Story-Forge assumption that rode along into generic tooling now installed in repos with no connection to Polish. All four neutralised to defer to each project's own calibration, and the template now also asks *who actually reads these notes* rather than assuming the operator does.

- **2026-07-23 — Session 101: the Graph-quality → next-milestone ROLL — three parts, plus three process rules and a spec-free architecture re-sync (branch `chore/milestone-roll-graph-quality`).** Not a build session; the milestone's closing ritual (`/wrap-session` §5c). **(a) Mined before moving.** `/retro` in milestone-roll mode read the whole departing batch (Sessions 68–100) as a corpus and found three cross-session patterns no single session's retro could see, all owner-approved: the **two-roll rule** (`docs/AGENTS.md` §3 — an item that has survived two rolls untouched gets a binary call, never a third free carry; earned because nine items were 2+ rolls old, and Session 96 hit the error-mapper item's own fold-trigger and *added the seventh copy*); **establish the constraining facts before putting a decision to the owner** (root `AGENTS.md` — Sessions 97 and 98 each cost two decision rounds for the same reason, and the record itself noticed they shared a form while only the two instances got written down); and the **Review-model evidence updated from three sessions to 13-of-14 production-code sessions**, with the reason it doesn't converge recorded (the five new `/review-pr` lenses prevent *recurrence* of known classes — S92/S95 wrote routes correctly because of the S75 lens — but nearly every PR brings a *novel* class, so the second gate never becomes skippable as `/review-pr` grows). *Considered + rejected:* mandating a browser walk before merging a user-facing slice — the milestone's highest-yield step, but already universal, documented in `frontend/AGENTS.md`, and the one case it missed wouldn't have been caught by mandating it. **(b) The architecture sweep found a whole stale milestone.** `overview.md` and `PROJECT.md` both still read "the project is now in a Public-readiness milestone" — closed at the Session-68 roll — so the entire Graph-quality record (S0–S7, ADRs 0010/0011, INV-10) was missing from the vault's as-built snapshot: the fifth recurrence of doc-freshness drift and the second to reach a roll uncaught, which is exactly the evidence that wired the sweep into the roll (ADR 0002 §4). Both re-synced. Both queued inputs closed — **DM-IH-8 recorded as SUPERSEDED, not wrong**, across five homes (option (a) was correct for a read-only slice; what S7 found was that the *spec line* had never been updated to match it), and the Session-59 motivation residue cleared — where the item's own "~6 files" estimate was **wrong in both directions**: one live "personal tool" lens, plus two *as-built overclaims* ("the real Wody Święte draft") its keyword grep couldn't see. **(c) The archive move** took `PLAN_SHORT.md` **235 KB → 21.5 KB**, verified byte-honest (98 dense bullets before; 75 archive / 10 backlog / 13 live after; 0 lost, 0 duplicated). The §3 re-curation walked all 24 cross-cutting items in the two-roll rule's first run: 6 resolved → archive, 2 struck as owned elsewhere, **10 aged out → `docs/BACKLOG.md`**, 6 carried live. **(d) Two INV-1 near-misses recorded** (both already tracked): the **normalise-names gate is nominal** — the author approves a graph-wide rename with no evidence shown, the milestone's own thesis unapplied to its own slice, and the sweep independently supports promoting it; and the **review-queue cursor is index- not identity-anchored**, a TOCTOU at the human gate live on all four queues. **(e) The wrap's own retro caught two gaps the process missed and the owner's question found** — `watch` findings had no home outside a dated report nobody re-reads (`review-architecture` now mirrors them into `open-questions.md`), and the two-roll rule's first branch is unavailable when the incoming milestone is unchosen (`docs/AGENTS.md` §3 now names BACKLOG as the default in that case). **No production code, no spec amendment, no ADR.** `PLAN_LONG.md` reconciled: Graph quality marked complete, and **the fork opened as an explicit roadmap section** with its prerequisite and the milestone-sizing note (8 slices, 33 sessions, ~40 % unplanned overhead).

- _**Earlier decisions (Milestones 1–4, Public readiness, Graph quality)** — see **[`docs/PLAN_ARCHIVE.md`](PLAN_ARCHIVE.md)**._

### Done in previous sessions

- **2026-08-13 — Session 107: an unplanned security session — one nightly red cleared, and a waiver that "could never be fixed" fixed (PRs #235 + #236 merged).** Started from a scheduled-CI failure notification, not a planned slice. **Red #1 — ollama (PR #235).** The nightly `security` job reddened on `CVE-2026-46600`, a freshly-surfaced `golang.org/x/net/dns/dnsmessage` **panic** HIGH against the *unchanged* `0.24.0` pin — pure treadmill, green the morning before with nothing in the repo changed. Fix-first was exhausted by measurement, not assumption: raw Trivy findings were diffed against **both** the newest soaked candidate `0.32.6` **and** the newest stable `0.32.9`, and all three ship identical bundled modules — across eight minor versions the only difference is one OS CVE. Waived as availability-only and unreachable (`dnsmessage` parses DNS *responses*, so it needs a hostile DNS responder or an active MITM on ollama's **outbound** lookups; the inference API is 127.0.0.1-bound with the backend as its only client). The self-`/review-pr` then caught two of its own authoring gaps — a missing per-CVE register row (breaking the enforced-file↔register mirror) and a stale `Last reviewed:` snapshot still reading wave 8 — both folded before merge. **Red #2 — the one nobody was looking for (PR #236).** On the owner's prompt to check Dependabot rather than stop at the fixed red, a **runtime HIGH** surfaced that all three of our gates called clean: `GHSA-qwww-vcr4-c8h2`, the *sole* npm waiver, had become **fixable**. Its Session-106 rationale foreclosed a fix — patched only in `react-router` 8.3.0, and `react-router-dom` has no v8 — and that was **correct and empirically verified when written**. The advisory was later **amended upstream** to record a 7.x backport, so `react-router-dom 7.18.1 → 7.18.2` (15 days soaked, OSV-clean) cleared it outright. `infra/npm/audit-waivers.toml` now holds **zero** waivers; two of the three gates carry none at all. **Browser walk** (owner, from a 6-item route-derived checklist — no browser in the agent's environment): **no routing regression**, and critically the two routes sharing `/stories/:storyId` still rank correctly, which is what a router release is most likely to break. Four hiccups surfaced, **all pre-existing or unrelated** — two already tracked (empty Structure editor, graph filters lost on reload — the latter confirmed by grep to use no `useSearchParams` at all, so it is by construction), and two newly routed to `docs/BACKLOG.md`: chunking **fails on Obsidian-vault Markdown with YAML frontmatter** (a real 502 from the agent; a repo-wide grep finds *no* frontmatter handling anywhere, and since the Grzymalin corpus is vault-kept research notes this ties to the standing spec-identity question), and no progress feedback during an outline build (same root cause as the recorded extraction gap). Green at close: frontend **551** tests + build + lint + prettier, all dependency and security gates clean, `main` green. **Lesson (hardened into `/triage-advisory`):** a waiver's "no fix exists" is a **snapshot, not a standing fact** — advisory patched-version metadata is mutable, maintainers backport, and the amendment lands afterwards. Our own machinery structurally could not see it: the expiry was 18 days out, the drop-condition described *our* code, and re-running the gate audits the **installed** version so it reported "waived" faithfully. This is the package-side twin of the Session-99 image trap (re-scanning a frozen pin can never show its drop-condition met) — the same shape, one milestone later, on the other half of the dependency surface. Step 1 gained a Dependabot cross-check; step 3 gained the rule. **Process note:** the session's most valuable finding came from the owner declining to accept "the other eight alerts are out of scope" as the whole answer.

- **2026-07-31 — Session 106: S5 security image re-scan — three reds cleared, two of them invisible at session start (PR #233 merged).** Scoped as a routine image re-scan; the scan is what found the rest. **Images:** pgvector `0.8.2 → 0.8.5-pg17-trixie` (23 days soaked; not `0.8.6`, 2 days old) retired **eleven** waivers — nine the rebuild fixed (gnutls ×5, krb5 ×2, openssl, libcap2) and **two already stale**, absent even from the outgoing pin's own scan. That empties the OS block and **closes the atypical "OS CVEs waived here" exception carried since Issue #4**; the image now scans completely clean. **ollama stayed at `0.24.0`:** the prescribed raw pinned-vs-candidate diff showed `0.32.3` identical but for one OS CVE, so the bump would retire zero bundled waivers — correcting the plan's standing assumption that a re-scan would clear the wave-6/7 batch. Its new `CVE-2026-56852` (`x/text` `norm.Iter` infinite loop, HIGH, availability-only, unreachable on a loopback single-client deployment) — the nightly red of 07-29/30 — was waived instead. **The two hidden reds:** the `frontend` job sets `code=false` on the schedule, so five React Router advisories published 2026-07-24 (*hours* after PR #231 merged) had sat invisible on `main`; `react-router-dom 7.16.0 → 7.18.1` cleared four, and since the fifth is fixed only in a major the depended-on package has never released, `npm audit` — the last §6.7 gate with no waiver mechanism — got one: `scripts/check_npm_audit.py` + `infra/npm/` (enforced toml + register), spec §6.7 amended via the stop-and-amend flow and reconciled across eight homes, 30 unit tests. All three rode one PR because `security` and `frontend` are both required checks and either alone would trip the other. **Reviews found real defects in the new gate — twice.** `/review-pr` caught a **fail-open**: `npm audit --json` exits **0** with `{"error":…}` when it cannot audit at all, and the parser read that as "no advisories", so a missing lockfile would have silently disabled the gate while the docstring claimed it failed closed. Then a high-effort `/code-review` caught **eight** more, headed by a structural one: the gate ran in the path-scoped `frontend` job, so the mandatory expiry the whole design rests on could never self-fire — and the first pass had *documented around* that gap while three other documents in the same PR promised the expiry was automatic. Also folded: a second fail-open (`"vulnerabilities": null`), npm's exit code discarded entirely, a TOML typo crashing uncaught, no test covering `main()` (negating the verdict left all tests green), `scripts/*.py` outside every lint gate, and a stale CI comment reopening the exception the PR had just closed. **Browser walk:** run by the owner from an 8-item route-derived checklist (no browser in the agent's environment); **no routing regression** — three pre-existing issues surfaced and were routed to `docs/BACKLOG.md`. Green at close: backend **770** tests + mypy + ruff (now covering `scripts/`), frontend **551** + build + lint + prettier, all security gates clean, `main` green. **Lesson (hardened into root `AGENTS.md` §1):** *documenting* a hole you could *close* is the failure mode here. The tell was available while authoring — a caveat being written that contradicted a guarantee written minutes earlier in the same change — and the fix was two lines. Documenting a limitation is usually right; documenting one that contradicts your own change's promise never is.

- **2026-07-24 — Session 105: S4 dev-ergonomics stack scripts (dev-tooling; PR #231 merged).** Shipped the milestone's S4 slice — `scripts/stack.sh`, one command for the local stack instead of the README's three-terminal dance. `up` = `docker compose up -d` + backend + frontend backgrounded in their own process groups (output → gitignored `.stack/*.log`, pids → `.stack/*.pid`); `down` = TERM→KILL the whole app tree, then `docker compose down`; `logs` = tail the neo4j+postgres **service** logs (narrowable); `status` = what `up` started. `up` warns-and-skips a service whose port is already taken by a hand-launched process. README **Development** points at the helper; `.gitignore` excludes `.stack/`. Backend *operational* logging stayed out of scope (deferred to `PLAN_LONG.md`). **Verification:** the live stack was already up, so `up`/`down` weren't run end-to-end (would collide / tear it down); the process-group `start_bg`/`stop_bg` were unit-tested against a throwaway process (whole-tree kill confirmed, not just parent; idempotent re-start; stale-pidfile handling), and `help`/`status`/`logs`/unknown-cmd exercised read-only against the live stack. That harness caught a `set -u` unbound-variable bug before review. **Reviews:** `/review-pr` clean, two nits folded (header/usage dedup; busy-port warning via a portable `/dev/tcp` check); `/code-review` + browser walk not required (bash dev-tooling, no product code, no UI). CI green (full suite: backend, frontend, security, secret-scan, CodeQL, ollama-cloud-smoke). **Lesson:** when the environment you'd normally smoke against is already in use, don't skip verification — isolate and test the *risky* logic (here the process-group kill) against a disposable stand-in, and exercise the read-only paths against the live system; the stand-in harness is what surfaced the real bug the happy path would have hidden.

- **2026-07-24 — Session 104: S3 app-navigation / story hub (frontend + a small backend read; PR #229 merged).** Shipped the milestone's S3 slice — a per-story landing **hub** so a raw `:storyId` UUID is never needed to reach a story's review / relations / duplicates / normalise-names / structure screens, plus a "← Story hub" back-link on every subscreen. **Backend:** `GET /stories/{story_id}` → light `StorySummary` (no `raw_text`), 404-unknown / declared-503-guarded, `get_story_summary` repo read, OpenAPI + typed client regenerated. **Frontend:** `StoryHub` linking all seven screens from a shared `STORY_SCREENS` list that *also* generates the router's per-story sub-routes (can't drift), `useStory` hook, `StoryScreenLayout` (owns viewport height so the back-link never overflows a screen), a 404 not-found state with no dead links, `ProjectPicker` rows open the hub, `isoDate` shared in `lib/utils`. **Reviews:** `/review-pr` clean; the required `/code-review` folded four findings — two correctness (graph-screen overflow from the back-link header over GraphViewer's `h-screen` root; seven dead links shown on a 404) and two dedups. **Browser walk** (owner) caught the subscreen one-way-door and an already-structured story's empty Structure editor. Green: backend 740 tests + all gates, frontend 551 tests + build. **Deferred → `docs/BACKLOG.md`:** the Structure screen should show an already-structured story's *existing* outline (it currently opens an empty build editor cold). **Process (retro):** added a merge-flow rule — *a user-facing change gets a browser walk before merge, proactively, not on request* (root `AGENTS.md` + `frontend/AGENTS.md`), superseding the Session-101 rejection of a mandated walk. **Lesson:** I went straight from `/review-pr` to the PR without the browser walk `frontend/AGENTS.md` documents, and the owner had to prompt it — the walk then caught the two most impactful defects (a navigation dead-end and an empty screen) that CI + both review gates all passed. A documented-but-optional step is a skipped step under time pressure; the fix was to make it a gate.

- **2026-07-24 — Session 103: pre-milestone vault-maintenance — architecture sweep, two component notes, English-only glossary (governance/vault; no production code; PR #227).** Pulled forward from the milestone's S6 (owner, Session 102) so the vault is current *before* the milestone's build slices read it. Ran `meta-architect:review-architecture` (report `2026-07-24`, no blockers, 3 risks all reconciled on sight): re-synced the milestone-framing drift (`PROJECT.md`/`overview.md` no longer call the next milestone "unchosen" — they name the Grzymalin reality check, fork open until its last session), fixed a stale `INDEX.md` invariant range (`INV-1…INV-9`→`…INV-10`), and mirrored findings to `open-questions.md` (OQ-36). Wrote the vault's **first `components/` notes** — `LLMRouter` and `EntityEditService` (the human-reached graph writer, INV-9 instances 3–8) — closing the long-standing OQ-35 T-1. Stripped the Polish glosses from the glossary → **English-only** (owner Decided 101(d)); the strip scope had been mis-recorded as "22" notes but was actually **~34 of 36** (a diacritics-only-grep undercount, re-established before acting), with the acronyms (`BFF`, `SCA`), the `agent` disambiguation, and the content-domain fixture name "Wody Święte" preserved. `/review-pr` clean (docs-only); CI green (secret-scan + CodeQL; code jobs skipped). Milestone renumbered S1–S5 + fork-close S6. **Lesson:** a count handed down in a plan/handoff ("22 glossary notes") is a keyword-grep artifact, not a fact — re-deriving it before acting turned an incomplete strip into a complete one, the same "establish the constraining facts first" discipline the resume/review rules already encode, applied to a maintenance task rather than a decision.

- **2026-07-24 — Session 102: opened the "Grzymalin reality check" milestone (planning/governance; no production code).** A resume→deliberate→plan session. Ran `/resume-session` in full — verified every on-disk anchor from the Session-101 roll, confirmed the security board clean (OSV register empty; only condition-based Trivy waivers, none date-expiring) and the architecture report's findings all resolved-or-tracked (§3c confirm-not-discover). Reviewed `docs/BACKLOG.md` (the 10 aged-out items + 2 promotion candidates) and open questions. With the owner, scoped a **deliberately small seven-session milestone** that makes the roadmap's fork-prerequisite concrete: run the pipeline **unaided** on a *real*, English, non-fiction research corpus about **Grzymalin** (a village near Legnica; half-synthetic Gemini Deep Research over PL/DE/EN sources), replacing the synthetic/overextracted Oakhaven sample as the working corpus, and **lock the fork on that evidence** (deeper extraction vs V2 Editing) only in the last session. Sessions: **S1** run · **S2** normalise-names evidence (promoted from backlog N-1) · **S3** app-navigation/story-hub · **S4** dev-ergonomics scripts · **S5** security image re-scan · **S6** vault-maintenance · **S7** fork-decision + close. Recorded the owner's **"Story Forge → History Forge"** direction (more research docs to come; timeline/temporal ordering + cross-document entity joining become first-class) as a **fork input that weights but does not lock** the decision, with a spec-identity watch (§2 is narrative-framed). Backlogged backend operational logging, keeping it distinct from the **data flywheel** (the finetuning substrate is the already-captured `candidate_decisions` + `llm_calls` records, which the Grzymalin runs start accumulating). The first corpus document landed mid-session, unblocking S1. **No code, no spec amendment, no ADR.** **Lesson:** I built a run framing on a "Polish-language" axis before confirming the corpus is delivered as *English* text — framing options around a property of not-yet-provided material is the surface-your-assumptions rule in miniature: flag the property as an assumption to confirm, don't build the frame on it.

- **2026-07-23 — Session 101: the Graph-quality → next-milestone milestone ROLL (governance/planning; no production code).** Ran the roll per `docs/AGENTS.md` §7 / `/wrap-session` §5c, in its three ordered parts. **(1) Mined before moving** — `/retro` in milestone-roll mode read Sessions 68–100 as a corpus and produced three owner-approved rules: the **two-roll rule** for cross-cutting items that have outlived two milestones, a root-`AGENTS.md` rule to **establish the facts that constrain the options before putting a decision to the owner**, and a milestone-scale evidence update to the required-`/code-review` gate (13 of 14 production-code sessions had it catch a real defect the self-review missed) together with *why* it doesn't converge. **(2) The architecture sweep** (`meta-architect:review-architecture`, the first run under the ADR-0002 §4 roll wiring) found no blockers but one severe drift: the vault's two strategic notes were a **whole milestone stale**, so all of Graph quality was missing from its as-built snapshot — re-synced, along with both queued reconciliation inputs (the DM-IH-8 tooltip supersession; the Session-59 motivation residue, where the predicted "~6 files" turned out to be one lens plus two as-built overclaims a keyword grep had missed). Two INV-1 near-misses recorded — a *nominal* human gate on the normalise-names screen and a TOCTOU-prone review-queue cursor — both already tracked in `docs/BACKLOG.md`. **(3) The archive move** took `PLAN_SHORT.md` from **235 KB to 21.5 KB** — back under the soft flag and well clear of the 256 KB single-read limit it was approaching — with the whole Graph-quality record moved verbatim into `docs/PLAN_ARCHIVE.md` and verified byte-honest (0 bullets lost, 0 duplicated). The cross-cutting re-curation walked all 24 items under the new rule; ten that had survived two-plus rolls untouched went to the backlog, where they get reviewed and promoted rather than carried silently again. **No production code was touched**, so the green-state checks are unchanged from Session 100 (738 backend + 542 frontend tests, all gates clean). **The wrap's own retro then caught two gaps the process itself had missed** — surfaced by the owner asking whether *every* sweep finding had been fixed or marked: `watch`-severity findings had no home outside a dated report nothing re-reads, and the brand-new two-roll rule had no answer for a roll where the next milestone isn't chosen yet. Both closed. **Lesson:** a report is only as good as the thing that re-reads it — the sweep had a forcing function for its serious findings and none at all for its quiet ones, and the difference was invisible until someone asked the obvious question.

- _**Earlier sessions (Milestones 1–4, Public readiness, Graph quality — through Session 100)** — see **[`docs/PLAN_ARCHIVE.md`](PLAN_ARCHIVE.md)**._

---

## How the two skills use this file

- **`/resume-session`** (session start) reads the **Session handoff** block, verifies the on-disk anchors it names, runs `git log`/`git status`, opens the spec sections listed, and confirms reality matches before work begins.
- **`/wrap-session`** (session end) runs the green-state checks (report-only), checks off completed tasks, strikes (does not delete) obsolete ones, appends a dated line to **Done in previous sessions**, refreshes **Blocked / questions** and **Decided**, and rewrites the **Session handoff** block to point at the next session. It then reminds you to commit on a feature branch (squash-merge hygiene).
- **When a spec change happened:** both skills stop and require `story-forge-poc-spec.md` + `PLAN_LONG.md` to be reconciled with this file before continuing.
