# PLAN_SHORT.md — Current milestone tactical plan

> Updated every working session. Read at start (`/resume-session`); update at end (`/wrap-session`).

<!-- ───────────────────────────── HANDOFF ─────────────────────────────
This block is the contract between `/wrap-session` (writes it) and
`/resume-session` (reads it). It always describes where the NEXT session
should start. Keep it short and literal. Do not delete the markers.
────────────────────────────────────────────────────────────────────── -->

## ▶ Session handoff (read this first)

- **Next step — Graph-quality S6a: BUILD the backend normalise-names surface (test-first).** The S6 register is **RESOLVED** (Session 91, PR #205 — `architecture/proposals/graph-name-normalisation.md`, `status: accepted`; DM-NN-1..6 each on the proposed lean; OQ-34 struck). This is a **build** unit → spec- and test-driven, **failing test first, production code second**. **Build order (from the proposal's Hand-off "When S6 is built"):** (1) the **shared pure suggest function** — given a label vocabulary + the `name_normalise_suggest_floor`, produce deterministic ranked synonym groups; pure, no store, reusing `name_match_score` + a label-string-embedding cosine (the S4 `suggest_duplicate_pairs` analogue over *labels*, not entities). (2) The **two `DISTINCT`-vocabulary reads** (predicate names + entity-type labels, with counts) + a `GET …/label-vocabulary` endpoint. (3) The **DM-NN-3 dismissal store** — reuse the S4 `duplicate_suggestion_dismissals` design (a staging-side label-pair+surface store; reuse the table with a surface/kind discriminator *or* a sibling table — a build call, **no fresh ADR** unless a genuine new boundary appears). (4) The **two apply ops:** *predicate* = loop the shipped `plan_relation_rekey` graph-wide in **one grouped reversible op**, preserve each `edge_uid` (INV-10), fold identical triples **counted-and-reported**; *type* = a **new bulk `SET n.type` relabel op** on `EntityEditService` (grows INV-9's human-reached-writer enumeration by one path — the build assigns the ordinal + widens the grep guard). OpenAPI regen after the endpoints. **S6b (frontend list) is a later slice.** **`verify-at-build` to honour:** on the real Oakhaven vocabulary, confirm `EmbeddingAgent.encode` on short symbol-y labels (`ON_SHIP`) actually separates synonyms from unrelated labels — if cosine is noisy, fall back to fuzzy + morphological normalisation (case-fold, underscore-split) rather than ship a noisy embedding rung.
- **Read before starting:** this file (handoff + the **`## Current milestone: Graph quality`** header + the **S6 task entry** (item 7, now carrying the resolved register + build cut) + **Decided S91** (the resolutions) / **Decided S90** (the decompose) + the live cross-cutting/Blocked lists); **`architecture/proposals/graph-name-normalisation.md`** — the resolved register (each `✅ Decision`) + the **Hand-off "When S6 is built"** (the build order) + the **"But what if" edge cases** + the **Data flow** Mermaid; **`docs/specs/graph-quality.md`** §3 **S6** + **§4** (the reserved `edge_uid` handle a predicate rename re-keys through) + **§6** (criteria 4/5) + **§8** (the invariants); root `CLAUDE.md` (spec-and-test-driven, one-unit-per-conversation, owner-holds-merge, required `/code-review` for substantive code); `backend/AGENTS.md` + `backend/src/story_forge/AGENTS.md` (domain-vs-adapter layering, the rule-of-three, prompt/agent patterns — though S6a is deterministic, no LLM).
- **Verify on disk:** on **`main`** after this wrap. **Graph-quality S0–S5 `[x]`; S6 `[ ]` (register RESOLVED, build not started), S7 `[ ]`.** **S6 register RESOLVED (PR #205 merged):** `architecture/proposals/graph-name-normalisation.md` has `status: accepted` + the register heading reads `✅ RESOLVED` + each DM-NN entry carries a `✅ Decision` line; `architecture/open-questions.md` OQ-34 is **struck** (`~~OQ-34 …~~ ✅ RESOLVED`); `architecture/INDEX.md` row 48 reads **✅ ACCEPTED, register RESOLVED**. **Build-reuse anchors (verified present at S91 wrap — the S6a build composes these, writes no new identity machinery):** `backend/src/story_forge/domain/name_similarity.py` (`name_match_score`/`cosine_similarity` — the shared suggest primitives); `backend/src/story_forge/domain/duplicate_clusters.py` (`suggest_duplicate_pairs` — the S4 self-join the label-suggest fn mirrors); `backend/src/story_forge/domain/relation_rekey.py` (`plan_relation_rekey`) + `agents/entity_edit.py` (`retarget_relation` — the S5b per-edge re-key the predicate apply loops graph-wide); `agents/embedding_agent.py` (`EmbeddingAgent.encode` — the label-string embedding rung). The type-relabel + graph-wide predicate re-key are **new writer paths** — expect a new endpoint each + the INV-9 grep-guard widening.
- **Last session ended:** 2026-07-13 — **Session 91: Graph-quality S6 register RESOLVED with the owner (DM-NN-1..6 / OQ-34; PR #205, squash-merged).** Architecture-decision unit, **no production code** — the resolve step between the S6 decompose (S90) and the S6 build. All six calls landed on the decompose's proposed lean (one shared suggest engine + two forked apply paths; fuzzy + label-embeddings recall-first; persist dismissals; graph-wide S5b re-key with fold-report; a new bulk `SET n.type` relabel; a dedicated backend-first list). Reconciled across every home (proposal → accepted, OQ-34 struck, INDEX + learning-log flipped, Decided S91 + S6 task annotation). No spec amendment, no ADR. `/review-pr` clean; CI green (docs-only path-skip). Owner skipped the full `/retro` (reflective pass found no friction).
- **Open blocks/questions:** **No open blocker — the S6 register is resolved; the next unit is the S6a *build*, test-first.** **Live cross-cutting (carried forward):** the `docs/code` reference refresh at the roll; the `get_story`-outside-503-guard systemic fix across the edit routes (S82) — *watch this at S6a: the new `label-vocabulary` read + the two apply endpoints must sit inside the declared-503 store-outage guard, don't inherit a sibling's unguarded read*; **FOUR condition-based Trivy waivers** (pgvector krb5 · ollama `x/crypto` · neo4j netty · pgvector+ollama `CVE-2026-39822` Go-`os.Root`), all condition-based (not date-checkable), dropped via `/triage-advisory` when a rebuilt image ships the fix. **Housekeeping observation (already tracked):** `PLAN_SHORT.md` Decided/Done entry *density* now strains the 25 k-token `Read` cap (read it in ≤20-line windows); the fix is the §7 archive at the Graph-quality→next milestone roll — deferred until that roll, correct.

<!-- ─────────────────────────── END HANDOFF ──────────────────────────── -->

## Current milestone: Graph quality — curate the graph you have (V1 → V2)

**Goal:** V1 is feature-complete; by design the PoC **over-extracts** — every graph write passes
the human gate (INV-1/INV-9), so the real Oakhaven graph is dense, with duplicates, near-synonym
edges, and spurious nodes *intentionally* left for curation. The owner now has that graph and wants
to **clean it in place** — fluid, context-rich tools to curate the entities and edges that already
exist, rather than re-running or improving extraction. The enabling insight: **the human gate is
only as good as the context it shows you**, and bringing the source text to the decision point is
mostly *cheap* — the data already exists (staged relations carry `paragraph_id`, entities carry
mentions; the write plumbing exists behind the gate with a full undo log). So this milestone is
**largely a UX-surfacing job, not net-new write plumbing.** Improving *extraction* is a later,
separate pass (owner steer, S66). **Source of truth for scope:**
[`docs/specs/graph-quality.md`](specs/graph-quality.md) (owner-approved, S66 prep gate).

**Slicing — RESHAPED 2026-06-26 (Session 69)** after the S0 decompose + owner resolutions (see Decided
below + `architecture/proposals/graph-curation-surface.md`): **navigation pulled early** (you can't curate
a hairball you can't see), **two human-gated "suggest, then you decide" passes added** (duplicate entities
S4, synonymous predicate-names S6), the **predicate work reframed as *naming normalisation*** (not
edge-joining), and the **§4 edge handle reserved**. Each slice is detailed (a branchy one gets a
`meta-architect:decompose-requirement` step-0) when picked up.

1. [x] **S0 — Decompose the graph-curation surface** *(architecture, no build; ✅ DONE Session 69)*.
   `decompose-requirement` → the `graph-curation-surface` proposal (register **DM-GQ-1..7 / OQ-29 —
   RESOLVED** by owner Session 69). Defining finding: the edit/merge/delete/undo plumbing already ships
   (`EntityEditService`, M4.S3a/S3b) but only on the reader's panel — so the editing slices are
   UX-surfacing onto the canvas. **§4 resolved = reserve a stable edge handle** (ADR at build).
2. [x] **S1 — Stop silent data loss** *(correctness; standalone, cheap; ✅ DONE Session 71, PR #164)*.
   The auto-chunker could drop trailing paragraphs and report success. One canonical range invariant
   (`domain.paragraph_range_problem`: covers every paragraph `[0, count)`, nothing past the end) folded
   into the agent's retried validation via a new `check` hook — so **both** a coverage gap *and* an
   overshoot re-prompt (symmetric recovery, owner option B after `/code-review`) — with the coordinator
   re-asserting it as a terminal `OutlineCoverageError` backstop. Failing tests first; live-verified
   (real auto-chunk + a controlled-provider guard demo). Overlap-rejection deferred to `docs/BACKLOG.md`.
   (OQ-28 hazard #1.)
3. [x] **S2 — Navigate the graph** *(pulled early — the foundation curation sits on; ✅ BUILT Session 73, PR #168)*.
   The §3.4 filters (entity type / story / connection density) + node search by name + **a better layout
   algorithm** to spread a dense graph (today's `cose` is an unnavigable hairball). You can't curate a
   graph you can't see — lands before the curation slices. (Was S4 + the layout dimension, owner S69.)
   **Step-0 decompose ✅ DONE + register RESOLVED (Session 72, PR #166)** — `architecture/proposals/graph-navigation.md`
   (DM-GN-1..4 / OQ-30): client-side filtering (no new backend), `cytoscape-fcose` layout, S2 stays
   navigation-only (edge panel = S3), AND-combine + hide-and-relayout + search-as-focus. **Built Session 73**
   — pure `graphFilters.ts` (type/degree/diacritic-search) + `useDebouncedValue` hook, test-first;
   `cytoscape-fcose` pinned via `/add-dependency`; `/review-pr` + multi-agent `/code-review` folded;
   browser-smoke-verified at Oakhaven scale (186 nodes). Two follow-ups → `docs/BACKLOG.md` (URL-persist
   view state; label crowding on an all-disconnected filtered set).
4. [x] **S3 — Edge evidence + verifiable merges** *(the enabler; decomposed + register RESOLVED Session 74; **S3a BUILT Session 75, PR #172**; **S3b BUILT Session 76, PR #174** — ✅ COMPLETE)*. Click an
   edge → predicate + source sentence(s); each entity-merge option shows a context quote + type +
   aliases; cheap safeguards ride here (gate exact-name duplicate creation; don't present a score-100
   exact-name match as self-evidently correct for common-noun/group types; fix the amber
   "merge target" highlight that makes a *New* card read as a merge). **As decomposed (S74):** a
   read/verify slice (writes nothing) needing new **read** surfaces, not storage — **DM-EE-1** focused
   per-edge BFF evidence read (fetch-on-tap) · **DM-EE-2** all `written` `staged_relations` by `edge_id`
   (+ index; zero-provenance → "added manually") · **DM-EE-3** enrich all merge surfaces
   (type+aliases+quote+`target_canonical_name` — closes the cross-cutting `CandidateView` item below at
   build) · **DM-EE-4** score-trap solved by context not type-classification (INV-4) · **DM-EE-5**
   client-side warn-and-offer dup-create gate · **DM-EE-6** be-first (**S3a**) / fe-second (**S3b**, amber
   fix rides). The §4 edge handle is **not** consumed here (S3 reads by content id; the handle is an
   edge-*write* concern, S5/S6). No spec amendment; no ADR.
   - [x] **S3a — backend read surfaces (BUILT Session 75, PR #172).** `GET …/relations/{edge_id}/evidence`
     (predicate + all `written` `staged_relations` by `edge_id`, batched; zero-provenance → empty) via a new
     `get_written_by_edge_id` + `edge_id` index + pure `domain/edge_evidence.py`; `CandidateView` enriched
     (`target_canonical_name` + typed `AlternativeView` type/aliases/quote, best-effort — a graph-DB outage
     degrades to an unenriched queue). `/review-pr` + multi-agent `/code-review` folded; all green.
   - [x] **S3b — frontend (BUILT Session 76, PR #174).** edge-tap + `EdgeEvidencePanel`; render the merge context
     (**tolerate null type/aliases/quote — the enriched fields degrade to null when the graph DB is down**);
     DM-EE-4 (honest score) + DM-EE-5 (dup-create warn) + the amber "merge target" highlight fix on *New* cards.
     Deferred limitation to note: the evidence read returns **200 + empty** for both a manually-added edge and a
     stale/nonexistent `edge_id` (distinguishing them needs a Neo4j edge-existence check — documented, deferred).
5. [x] **S4 — Suggest duplicate clusters over the accepted graph** *(NEW — proactive entity dedup;
   human-gated; ✅ COMPLETE — S4a be Session 78 / S4b fe Session 79)*. Re-point the §3.3 cascade matcher at the already-accepted entities → surface likely-dup
   clusters the author would otherwise hunt for; the human reviews + commits via the existing merge
   (INV-1/INV-9 hold — *suggests*, never auto-merges). Promoted from `docs/BACKLOG.md` (graph-traversal
   discovery), owner S69. **Step-0 decompose ✅ DONE + register RESOLVED (Session 77)** —
   `architecture/proposals/graph-cluster-dedup.md` (DM-CD-1..6 / OQ-32): **pairwise** suggestions
   (transitive deferred), **eager/recall-first deterministic** matching (name + embeddings, ambiguous
   floor, a `duplicate_suggest_floor` knob), **persist dismissals** (a Postgres pair-store — ADR at build),
   a **dedicated review-queue-like list**, split **S4a** be / **S4b** fe. No net-new graph write; no spec
   amendment at decompose time.
   - [x] **S4a — backend duplicate-suggestion surface (BUILT Session 78, PR #179).** A pure
     `domain/duplicate_clusters.py` self-join (`suggest_duplicate_pairs`, reusing the relocated
     `name_match_score`/`cosine_similarity`) → `GET …/duplicate-suggestions` (assemble snapshot → self-join
     → drop dismissed → enrich with S3 type/aliases/quote → rank), plus the DM-CD-3 dismissal store (new
     staging-side `duplicate_suggestion_dismissals` table + `POST`/`DELETE …/dismiss`, reversible) and a
     `duplicate_suggest_floor` config knob. The pure `cosine_similarity`+`name_match_score` core was
     **relocated into `domain/name_similarity.py`** (owner call) so the pure self-join reuses them without a
     `domain/↛agents/` break. Suggests only — writes no graph (INV-1/INV-9); type soft-only (INV-4);
     zero-vector → name-only. **ADR 0010** records the dismissal store. `/review-pr` + required `/code-review`
     folded (4 findings: offload the O(n²) self-join off the event loop, complete the primitive extraction,
     gather independent reads, drop a speculative column). Live-driven on the real Oakhaven graph.
   - [x] **S4b — the review-list frontend (BUILT Session 79, PR #181).** A dedicated `/stories/:id/duplicates`
     "possible duplicates" list (DM-CD-4): each row = the pair + S3 (DM-EE-3) verification context + honest
     name/embedding score labels + an in-quote **mention highlight** (owner ask) + a Keep/Merge hint; accept
     picks the survivor explicitly (owner call) and feeds the *existing* merge (conflicts resolved by hand);
     dismiss / un-dismiss persist + reverse (DM-CD-3) with a transient Undo. **Folded the cross-cutting
     shared-`useReviewQueue`** — extracted a generic `hooks/useReviewQueue.ts` and migrated *all three* review
     screens onto it (owner call). `/review-pr` + required `/code-review` folded (3 findings). Live-driven on
     the real Oakhaven graph (234 pairs). Two upstream quality findings → `docs/BACKLOG.md` (name-scorer
     over-match; embeddings absent).
6. [x] **S5 — The graph as an in-place editing surface** *(the spine; sliced S5a node / S5b edge per S0; ✅ COMPLETE — S5a Session 81 / S5b-be Session 82 / S5b-fe Session 83)*.
   Bring the existing write paths onto the canvas — edit node name/type/aliases/properties, merge, delete;
   edit / re-target / delete an edge — with the human gate + undo intact (INV-1/INV-9/INV-3). Interaction =
   a selection-driven editable panel (right-click deferred), reusing the reader's edit panel (DM-GQ-3).
   Reserves the **§4 edge handle**.
   **Step-0 decompose ✅ DONE + register RESOLVED (Session 80, PR #183)** — `architecture/proposals/graph-canvas-editing.md`
   (DM-S5-1..6 / OQ-33). **Defining finding (two code surveys):** node-editing already ships end-to-end and is
   wired on the *reader* → **S5a is pure canvas-surfacing**; the only net-new plumbing is **edge-editing**
   (`edit-predicate`/`re-target` have no atomic op — a client remove+add re-keys the content-addressed
   `relation_edge_id`), so **S5b is the first edge-*write* slice and consumes the reserved §4 handle**
   (surrogate key, DM-GQ-1 — confirmed unbuilt; **ADR + the "handle survives curation" invariant draft at
   the S5b build**). **Owner resolutions:** DM-S5-1 = **extract a shared `EntityEditPanel` core** (reader +
   canvas both compose it) · DM-S5-2 = **atomic backend edge-edit op** (preserves the handle) · DM-S5-3 =
   mint-forward/no-backfill + coalesce-on-MERGE + invariant-at-build · DM-S5-4 = **defer right-click** (panel-only)
   · DM-S5-5 = single S5a slice + fold the §4 tag into S5b-be · DM-S5-6 = panel-vanish guard, LWW-at-PoC.
   **Build cut: S5a (fe) → S5b-be (tag + edge ops + ADR + invariant) → S5b-fe.** No spec amendment; no ADR at decompose.
   - [x] **S5a — node curation on the canvas (BUILT Session 81, PR #186).** Extracted a shared
     `EntityEditPanel` core (new `features/entity-panel/`) the reader + canvas both compose (occurrences/
     ego-mini/relations stay reader-only via `renderReadExtras`); pure `entityEditForm` + `useEntityEditForm`
     keep logic out of the component; mounted on the canvas with a toolbar `UndoButton`; deleted the read-only
     `NodeDetailsPanel`. Panel-vanish guard = pure `shouldClearSelection` (holds selection while dirty,
     re-selects the just-edited node, grace ends on user filtering). **Owner "move the helpers too":** relocated
     `MergeControls`/`MergeConflictFields`/`mergeConflicts`/`propertiesEditor`/`formatPropertyValue`/`UndoButton`
     into `features/entity-panel/`. `/review-pr` + required `/code-review` folded — **both caught a wrong-target
     PATCH** (a re-targeted panel with no `key` carried the prior node's drafts → Save PATCHed the wrong
     entity); fixed by keying the panel per entity (+ two symptom fixes). 451 tests green; browser-smoke-verified
     at Oakhaven scale (edit type → recolour + undo).
   - [x] **S5b-be — edge-write backend (BUILT Session 82, PR #188).** The §4 `edge_uid` surrogate handle
     (`uuid4`, Neo4j-property-only — **no DB migration**; mint-forward on every edge writer, no backfill;
     coalesce-on-`MERGE … ON CREATE SET` — no `ON MATCH`; survivor keeps its own handle on a fold) + the atomic
     `retarget_relation` op (pure `domain/relation_rekey.py` mirroring `plan_merge`'s re-point; reuses the merge
     writer's `repoint_relation`/`fold_relation` op-strings so undo already inverts it) + `PATCH …/relations/{edge_id}`
     (OpenAPI + typed-client regen). **ADR 0011** (escalated form — data-model identity boundary) + **INV-10**
     ("an edge's `edge_uid` survives re-point/re-predicate/merge") minted with enforcer + tests; INV-9 grep set
     unchanged (7th witnessed instance). `/review-pr` + **required** `/code-review` (high) folded — the multi-agent
     pass caught a real **data-loss bug the self-review missed**: delete-old-then-create-new lost the edge on a
     mid-failure → **reordered to create-first** (recoverable duplicate, never a missing edge — the DM-S5-2 window
     closed server-side) + 4 more (honest crash-window docstring/ADR, domain blank-predicate guard, stale
     `AddRelationRequest` docstring, skip redundant endpoint reads). 630 tests green; ruff + mypy clean.
   - [x] **S5b-fe — edge affordances on the canvas panel (BUILT Session 83, PR #190).** Extended the
     S3b `EdgeEvidencePanel` read-only → editable, mirroring the S5a node pattern: pure `edgeEditForm`
     (changed-fields-only patch, ≥1-change + non-blank-predicate validation, dirty guard) → thin
     `useEdgeEditForm` → the panel (read-mode Edit/Delete + edit-mode predicate input + endpoint
     re-target via the reused `EntityPicker`). New `useRetargetRelation` drives `PATCH …/relations/
     {edge_id}` (invalidates the story-graph triad); delete reuses `useRemoveRelation`. `GraphViewer`
     resolves the tapped edge + a `nameOf` lookup, keys the panel per edge, **re-points the selection
     on a re-key** (the new content id), and guards the edge selection against the mid-edit
     panel-vanish via the generalized `shouldClearSelection` (DM-S5-6). `/review-pr` + required
     `/code-review` (high) folded — the multi-agent pass caught a fold-notice-suppressed-on-error bug
     + a test-coverage regression; its "invalidate edge-evidence cache" finding was **refuted**
     (verified `retarget_relation` never touches `staged_relations`), the real re-key-orphans-provenance
     limitation → `docs/BACKLOG.md`. Owner-found UX bug fixed (long source paragraphs scroll inside the
     edge + node panels so the curation actions stay reachable). 489 tests green; owner ran the
     Oakhaven smoke (edit an edge → undo → clean). **S5 is COMPLETE.**
7. [ ] **S6 — Predicate- AND entity-type-name normalisation + synonym suggestion** *(the reframed
   "consolidate"; naming consistency, not edge-joining)*. Rename a predicate graph-wide to cut synonym noise
   (e.g. `PASSENGER_ON` → `ON_SHIP`), with an NLP/embedding layer that *suggests* synonymous names; identical-
   triple collapses are reported, not the goal. Human-gated; predicates stay free strings (INV-4); carries
   the §4 handle. Promoted from `docs/BACKLOG.md` (predicate proliferation), owner S69. **Scope extended
   to entity *types* (owner, Session 81):** the same casing/synonym spread the owner saw in the live type
   filter (`PERSON`/`Person`, `GROUP`/`group`/`GROUP_OF_PERSONS`, `LOCATION`/`PLACE`/`Place`, `OBJECT`/
   `ARTIFACT`, …) — types are open-world (INV-4), so nothing collapses them automatically. S6 does one
   suggest-then-you-decide normalisation pass over **both** predicate names and entity-type labels (the
   step-0 decompose scopes the shared vs per-surface machinery when S6 is picked up).
   **Step-0 decompose ✅ DONE (Session 90, PR #203) + register RESOLVED (Session 91 — each as proposed)** —
   `architecture/proposals/graph-name-normalisation.md` (DM-NN-1..6 / OQ-34). **Defining finding:** a predicate
   IS the Neo4j relationship type (renaming re-keys every bearing edge — reuse the S5b `plan_relation_rekey`,
   preserve `edge_uid`/INV-10, fold identical triples reported), a type IS a node property (renaming is a bulk
   `SET n.type` — no re-key/handle/collapse) ⇒ **one shared SUGGEST engine + two forked APPLY paths.**
   **Owner resolutions:** DM-NN-1 = shared suggest / forked apply · DM-NN-2 = fuzzy + label-string embeddings,
   recall-first, `name_normalise_suggest_floor` knob · DM-NN-3 = persist dismissals (reuse S4 / ADR 0010) ·
   DM-NN-4 = graph-wide S5b re-key in one grouped op · DM-NN-5 = new bulk `SET n.type` relabel (INV-9 +1 path) ·
   DM-NN-6 = dedicated `/stories/:id/normalise-names` list, both vocabularies together, be-first. **Build cut:
   S6a (backend: reads + shared suggest + dismissal store + both apply ops) → S6b (frontend list).** No spec
   amendment; no ADR anticipated.
8. [ ] **S7 — Reader as a correction surface for existing entities** *(close the loop)*. Verify/fill
   gaps so the reader's click → side panel + right-click correction (mostly built M4.S3c, spec §3.5)
   works against existing entities, so corrections surfaced while *reading* flow back to the graph.

**Ride-alongs** (cheap; fold in where natural): review-queue "X of N remaining" count; empty-queue →
onward navigation instead of a dead end.

**The fork (owner decision after the sprint):** with a hand-cleanable graph in place, decide — go
deeper into extraction improvement (the spec §5 deferred work: re-extraction, eval baseline, relation
deep-modelling, predicate auto-suggestion) **or** move on to V2 Editing (PoC spec §4). After
Graph-quality → **V2 Editing** either way.

---

> **Earlier milestones — archived.** Milestones 1–4 (M0 Setup, M1, M2 Basic extraction, M3 Cascade matching, M4 V1 polish) **and Public readiness** (V1 → public portfolio) — their full task breakdowns, Decided entries, and Done logs — live in **[`docs/PLAN_ARCHIVE.md`](PLAN_ARCHIVE.md)** (dated, append-only). This file carries **only the current milestone**; the milestone-roll ritual (`/wrap-session`, `docs/AGENTS.md` §7) moves the just-closed milestone there.

---

### Cross-cutting (carried into Graph quality — curated at the Public-readiness→Graph-quality roll 2026-06-26, Session 68; see `docs/AGENTS.md` §3)

> **Public-readiness→Graph-quality curation pass (2026-06-26, Session 68).** Every item walked by hand per `docs/AGENTS.md` §3. **Resolved this milestone → moved to `docs/PLAN_ARCHIVE.md` (§7):** the PreNER spec-drift item (S57 stop-and-amend), the branch-protection-tighten item (S65, ADR 0009), and the "make the next trim a learning loop" item (S67). **Partially resolved this roll:** the meta-architect ritual-wiring item — `review-architecture` is now wired into the milestone roll (ADR 0002 §4); residual open parts carry forward in that item below. **Carried forward live — and several now bite directly in Graph quality:** the review-UX folds (`CandidateView` merge-target name, the n=5 error-mappers, the shared `useReviewQueue`) and the chunker/re-structure items (window-and-stitch, delete-and-replace, the 422 overload) fire in **S1/S2/S3** (chunker fix; review + graph-edit surfaces); the rest (§6.5 model-override dropdown, `last_call()` tiebreaker, the envelope-unwrap dup, repo-root `scripts/` ruff scope, the `CandidateSpan` validator, orphaned-sandbox cleanup, the vault motivation-reframe propagation) stay "fix when the relevant code is next touched." The **security-waiver drop** stays live (starlette 1.3.1 actionable on/after 2026-06-27).

- [ ] **Propagate the Session-59 motivation reframe to the vault's remaining homes (at the next `review-architecture` sweep).** The honesty amend reconciled the vault *source* notes by hand (`PROJECT.md` business driver, `overview.md` Layer 2 — "genuine personal tool / the author writes Wody Święte" → designed-for / aspirational / PoC content LLM-generated), but two homes were deliberately left: the `proposals/*.md` decompositions still use **"personal tool"** as a design-driver *lens* (~6 files — `m2s2`, `m2s3`, `m3-cascade-matching`, `backend-dependency-advisory-scan`, …), and `INDEX.md` is a *derived* note. Both should be re-synced when `meta-architect:review-architecture` next runs (it's the authorized vault writer — ADR 0002; this is the Session-57 "route to the sweep" pattern, applied to the residual after the source-note hand-fix). **Low urgency** — the proposals use the phrase as an analysis lens, not an as-built overclaim — but tracked so the next sweep closes it. `architecture/CHANGELOG.md` is append-only history and stays as-is. (Session 59, 2026-06-25.)
- [ ] **§6.5 model-override dropdown** (user picks "which provider/model for this task" + per-task-type preferences). Deferred from M2.S6 as a *feature*, not thin polish (see Decided 2026-06-11). The §8.5 panel already does the *surfacing*; this is the *override* control, which touches **INV-7** (system-derived routing) and needs an INV-7/spec reconciliation + router plumbing + a preferences store. Do as a `meta-architect:decompose-requirement` step-0, alongside or after M3. (M2→M3 roll note, 2026-06-11.)
- [ ] **meta-architect workflow integration** — the three skills (`initialize`/`decompose`/`review`) are shipped and **two live runs are done** (Session 9 `initialize` → vault; Session 10 `review`+`decompose` → ADR 0003), but the skills were — until Session 68 — **not wired into the rituals** (now `review-architecture` is; see the dated resolution at the end of this item). Deferred by design (`docs/decisions/0002`): wire on evidence *after living with the vault*, not before. Hypothesis to test: bootstrap-detect a missing `architecture/` vault in `/resume-session`; run `decompose-requirement` at feature "step 0" before the failing test; run `review-architecture` in `/wrap-session` alongside `/review-pr`. **Evidence so far (2 runs):** both produced real value (vault + ADR 0003) *and* a real maintenance cost (the accepted-proposal/decision-reconciliation drift — now mitigated by decompose §5b + `/review-pr` §2). Carry-ins: ~~(a) add a vault/docs-PR review lens to `/review-pr`~~ ✅ **done Session 10** (broadened to the decision-reconciliation lens — `/review-pr` §2 + root `AGENTS.md` + decompose §5b); (b) **[Issue #31](https://github.com/yaaisiu/story-forge/issues/31)** — meta-architect slug/filename carve-out for `PROJECT`/`INDEX`/`CHANGELOG` (still open). (Session 8 note; updated Sessions 9 + 10, 2026-06-02.) **Session 28 (retro):** fixed a smaller framework wart — the `decompose-requirement` + `review-architecture` skills said "read the matching template from `templates/`" but no `proposal.md`/`review.md` template exists (the section structure lives in each skill body); wording corrected to point at `vault-note.md` for frontmatter + name the skill-body as the single home (no duplicate template). Also **fresh evidence toward the OQ-3 ritual-wiring refinement** (run `review-architecture` at *resume* when a session opens with a decompose): Session 28 opened with a decompose on a stale-INDEX vault and had to correct the "Next steps" by hand — wiring itself stays deferred per ADR 0002, evidence accumulates. **Session 58 (2026-06-25):** the **doc-freshness-as-a-state-machine** drift recurred a **4th time** (`overview.md` stale at the V1→Public-readiness roll, plus a stale INDEX "Next") — each occurrence caught only by a *manual* sweep because the sweep isn't wired into the roll ritual. The owner's call (retro): the 4th recurrence is **strong evidence the wiring should land**, but it's an architectural change (amends ADR 0002) deserving **its own focused session**, not a retro fold — so revisit the wiring deliberately later, not now. (The interim mitigation — a §6b self-check in the `review-architecture` skill so the sweep stops *leaking* freshness debt — landed Session 58.) **Session 68 (2026-06-26, this roll): `review-architecture` is now wired into the milestone roll** (ADR 0002 §4 evidence-based integration — the 4th-recurrence evidence + the milestone-roll retro finding); `/wrap-session §5c` runs it, `/resume-session §3c` triages its report; `decompose`/`initialize` stay event-triggered. **Residual open (carried):** the decompose-at-step-0 + resume-bootstrap-detect hypotheses (untested) and Issue #31. The vault *notes* (`open-questions.md`/`INDEX.md`/`PROJECT.md`/`learning-log.md`) that still frame integration as fully deferred reconcile at the first wired sweep (writer-restricted).
- [ ] **Drop the Session-27 time-boxed security waivers as their fixes soak (`/triage-advisory`).** Clearing the security-job reds (PRs #72/#73) needed soaked bumps + **fix-first time-boxed waivers** for residuals whose fixes were still inside the 14-day soak. Drop each (bump to the now-soaked fix, delete the `[[IgnoredVulns]]` block + `WAIVERS.md` row) when it bakes. **Status:** ~~python-multipart 0.0.31~~ ✅ dropped 2026-06-18. ~~**starlette** — both CVEs (GHSA-jp82-jpqv-5vv3 LOW + GHSA-82w8-qh3p-5jfq HIGH DoS)~~ ✅ **dropped 2026-06-27 (Session 70, PR #161)** — single `1.2.0 → 1.3.1` bump on the floor date, both `[[IgnoredVulns]]` blocks + the `WAIVERS.md` section removed. **Still live:** the **condition-based** neo4j Trivy waiver **CVE-2026-50010** (netty TLS hostname-verification bypass) — drop when a Neo4j 5.26.x image ships netty ≥4.1.135.Final. `/resume-session` step 3b flags the dated ones; the `ignoreUntil` CI re-red is the backstop. (Session 27, 2026-06-16; updated S63; starlette dropped S70.)
- [ ] **Drop the ollama `x/crypto/ssh` wave-6 waivers (CVE-2026-39832 + CVE-2026-39831) when a patched ollama image soaks (`/triage-advisory`).** CVE-2026-39832 added 2026-06-27 (Session 70, PR #161); **CVE-2026-39831 added 2026-07-09 (Session 80, PR #184** — a fresh `x/crypto/ssh` FIDO/U2F presence-check bypass, GHSA-89gr-r52h-f8rx, *unmasked* by the pgvector krb5 waiver letting the sequential scan reach ollama). Both against the unchanged `ollama/ollama:0.24.0` pin, waived on the same unreachability as their wave-6 `x/crypto/ssh` siblings (ollama opens no SSH client / uses no FIDO keys; 127.0.0.1 JSON inference). **Condition-based** drop: when an `ollama/ollama` tag rebuilt on `golang.org/x/crypto ≥0.52.0` soaks ≥7 days, bump via `/pin-image` and drop these + the sibling wave-6 ssh/net waivers together. Re-scan to confirm (not date-checkable). (Session 70, 2026-06-27; extended S80.)
- [ ] **Drop the pgvector krb5 waivers (CVE-2026-40356 + CVE-2026-40355) when a rebuilt pgvector image ships the `deb13u1` krb5 (`/triage-advisory`).** CVE-2026-40355 added 2026-07-09 (Session 80, PR #184) — a fresh krb5 NULL-ptr-deref DoS (GHSA-8qgv-wm66-hrmc), sibling of the already-waived CVE-2026-40356, **same** Debian fix `1.21.3-5+deb13u1` (not yet baked into a pgvector rebuild). Both waived on the same unreachability (no Kerberos service in the container; Postgres uses OpenSSL for TLS; linked libs only; 127.0.0.1 single user). **Condition-based** drop: when a rebuilt `pgvector/pgvector:0.8.2-pg17-trixie` (or a newer soaked tag) ships krb5 ≥`1.21.3-5+deb13u1`, drop both. Re-scan to confirm (part of the standing "pgvector CVE-rot" set in `infra/trivy/WAIVERS.md`). (Session 80, 2026-07-09.)
- [ ] **Refresh the `docs/code` reference notes for the graph-quality features — frontend AND now the backend edge-write surface (`/document-code changed`).** The Graph-quality slices added structure the reference layer doesn't yet describe — **frontend:** S2 navigation (`graphFilters.ts`, `useDebouncedValue`), S3b (`EdgeEvidencePanel`, edge-tap), S4b (`hooks/useReviewQueue.ts`, `features/duplicate-review/`, `useDuplicateSuggestions`/`useDismissDuplicate`, `MergeConflictFields`, the `/duplicates` route), **S5a** (the new `features/entity-panel/` — `EntityEditPanel`, `entityEditForm`, `useEntityEditForm`, and the six relocated helpers; `features/graph-viewer/selectionGuard.ts`; the deleted `NodeDetailsPanel`; `ReaderEntityPanel` recomposed as a thin wrapper); **backend (S5b-be):** the new pure `domain/relation_rekey.py`, `EntityEditService.retarget_relation`, the `PATCH …/relations/{edge_id}` route + `RetargetRelationRequest`, and the `GraphRelation.edge_uid` field; **S5b-fe:** the new `features/graph-viewer/edgeEditForm.ts` + `useEdgeEditForm.ts`, `lib/api/useRetargetRelation.ts`, the `EdgeEvidencePanel` extended read-only → editable, and the `selectionGuard.ts` generalization (`selectedNodeId` → `selectedId`, now shared by node + edge). Nothing compiles `docs/code/*.md` against the code, so they've drifted silently across S2/S3/S4/S5. Refresh the relevant notes (+ `CODE_GUIDE`) via `/document-code changed` — do as a focused pass, or fold at the milestone roll. (Session 79 step-3a backstop; S5a added Session 81; backend edge-write surface added Session 82; S5b-fe added Session 83.)
- [ ] **Guard `get_story` / `get_project` inside the declared-503 `try` across the entity-edit routes (`api/stories.py`).** Every edit route (`edit_entity_route`, `add_relation_route`, `remove_relation_route`, `retarget_relation_route`, merge, delete) calls `story = await get_story(conn, story_id)` **before** its `try/except (OperationalError, ServiceUnavailable)`, so a Postgres outage on that read raises a bare **500** while the route **declares 503** — the `/review-pr` §4 undeclared-500 lens. Pre-existing and *systemic* across the file (not introduced by S5b-be, which mirrors the pattern), so fix it in **one focused pass** over all the edit routes rather than piecemeal — move the story/project lookups inside the guard (or a shared dependency that maps the outage). Surfaced by the S5b-be `/review-pr` (Session 82); do when the edit-route error surface is next touched. (Session 82 own-review finding.)
- [ ] Update spec §10 if any open question hardens (extraction granularity, etc.)
- [x] ~~**`CandidateView` carries no merge-*target* name — Stage-2/3 merges show a generic label (Session 25).**~~ ✅ **Resolved 2026-07-01 (Session 75, Graph-quality S3a, PR #172):** `CandidateView` now carries `target_canonical_name` (resolved at read time from the accepted graph), so a non-top-3 merge target shows its real name instead of "an existing entity"; the frontend render lands in S3b. Original context kept below. — The review card resolves the proposed merge target's name by matching `target_entity_id` against the staged top-3 `alternatives` — but those are ranked by **RapidFuzz** while a Stage-2/3 merge target is chosen by **embedding cosine / judge** (`candidate_staging._cascade` + `matching_agent.stage2`), a different signal, so the target need not be in the list. `CandidateView` (`api/stories.py`) exposes only `target_entity_id`, no name, so on those paths the badge falls back to `Merge → an existing entity` (the `/code-review` should-fix; the raw-UUID was folded out in S4b). Proper fix: add `target_canonical_name` to `CandidateView` (resolve it at stage time from the in-scope `existing`/`snapshot` entities, or at read time) so the reviewer sees *what* they're merging into on every path. Backend slice + openapi regen — track here, do when the §3.3 review UX is tightened. (Session 25 `/code-review` finding.) **Scheduled to close at the Graph-quality S3a build (Session 74 decompose, DM-EE-3): `target_canonical_name` is added to `CandidateView` there, alongside the merge-context enrichment — strike this item when S3a lands.**
- [ ] **Per-screen `reviewMessage`/`extractMessage`/`decideMessage`/`messageForError` error-to-string mappers duplicate a pattern (now 6 screens — Session 25, updated S30/S79).** `ReviewQueue`, `GraphViewer`, `UploadScreen`, `OutlineEditor`, `RelationQueue` (`decideMessage`, M3.S4f), and now `DuplicatesQueue` (`duplicatesMessage`, S4b — each mapper keeps its own status semantics read off the real route, so it was *not* unified) each carry a near-identical `ApiError → string` switch (differing only in which statuses they special-case). At n=6 this is well past the rule-of-three; a shared `errorToMessage(error, overrides?)` / `apiErrorFallback(error, default)` in `lib/api/` with per-call status overrides would fold the *skeleton* (non-ApiError fallback + the final `error.detail || HTTP status` line) while each screen keeps its own status→string switch. Pre-existing convention — fold when one of these screens next needs an error-message change (don't pre-emptively refactor all five). (Session 25 `/code-review` reuse finding; n bumped S30.)
- [x] ~~**The two review queues (`extraction-review` / `relation-review`) share copied machinery with two latent nits (Session 30).**~~ ✅ **Resolved 2026-07-07 (Session 79, Graph-quality S4b, PR #181):** the S4b duplicate-review list was the n=3 trigger — extracted a generic **`hooks/useReviewQueue.ts`** (owns the window-keydown subscription, the render-time `selectedIndex` clamp, and `isEditableTarget`; generic over item/state/intent) and migrated **all three** screens (extraction-review, relation-review, duplicate-review) onto it, leaving each feature's pure key-reducer unchanged. (The two latent behaviours below rode the copied skeleton; the shared hook is now the one home to fix them if they bite.) Original context: `RelationQueue` mirrored `ReviewQueue` by copy — `isEditableTarget` (n=2), the no-deps window-keydown effect, the render-time clamp, the per-card `pending` guard; two verified-real behaviours in **both**: (a) the keyboard A/R path is not gated by an in-flight mutation (a fast double-key re-fires — harmless: deterministic ids + backend `already_decided` no-op); (b) a stale decide/review error banner persists across J/K until the next mutation. (M3.S4f `/code-review`, 2026-06-17; folded S79.)
- [ ] **`PostgresCostStore.last_call()` ordering has no tiebreaker.** `ORDER BY created_at DESC LIMIT 1` decides the §8.5 panel's "most recent call"; `created_at` defaults to `now()` (transaction-start time), and there's no monotonic column (the PK is a random uuid). Unreachable on the as-built **sequential single-user** extraction path (rows land seconds apart), but two rows sharing a `created_at` would make "most recent" nondeterministic. Lands with the same concurrency work spec §6.6 already defers ("a true reserve-then-reconcile lands only if batched concurrent calls do") — add a monotonic tiebreaker (a `bigserial`, or order by a real insertion sequence) **if/when** concurrent LLM calls are introduced. (M2.S5 `/code-review` finding, 2026-06-11.)
- [ ] **Orphaned sandbox files on upload failure** — `api/stories.py` writes the original file to the sandbox *before* the `Project`/`Story` inserts; if the DB write fails the transaction rolls back but the file is left on disk, pointing at a story that never persisted. Harmless on a single-user local app, so deferred. Fix when it bites or when a delete-story feature lands (clean up the file on insert failure, or write-after-commit). (Own-review note, 2026-05-21.)
- [ ] **Window-and-stitch for `auto` on long text** — `agents/chunking_coordinator.py` currently raises `ChunkingTooLongError` above `max_input_words` (a 422). The proper fix is a deterministic pre-split on paragraph boundaries, per-window agent calls, offsetting paragraph indices, and stitching the chapters/scenes back together — accepting some seam imperfection that the user corrects in the UI confirm. Deferred until the auto path is actually exercised on a real 50k-word input. (Session 4 windowing-guard decision, 2026-05-25.)
- [ ] **Re-structure semantics: delete-and-replace** — `POST /stories/{id}/structure` returns 409 if the story already has a tree. A re-run with a different mode ("I didn't like auto, let me redo manual") needs delete-and-replace, which the Session 1 repo doesn't have (Update was deferred). Session 6 added a *narrow* update — `update_story_raw_text` in the repo + an override-and-persist branch on the structure endpoint — covering the manual editor's "user accepts/edits" use case (spec §7 step 2). The full delete-and-replace UX (redo a different mode after the first persist) is still deferred. Add when the frontend wants it. (Session 4 endpoint-shape decision, 2026-05-25; narrowed Session 6, 2026-05-26.)
- [ ] **`/stories/{id}/structure` 422 is overloaded.** Two distinct failure modes return 422 with different body shapes: FastAPI's auto-generated request-validation errors (shape: `HTTPValidationError`) and the explicit `ChunkingTooLongError` raised when the input exceeds `max_input_words` (shape: `{detail: str}`). The Session 5 PR documented the unambiguous error codes (400/404/409/413/415/502) in OpenAPI but left 422 alone because declaring it would overwrite FastAPI's validation shape. Pick one: either remap `ChunkingTooLongError` to 413 (payload too large — semantically truer; the body *is* too long for the agent) or to a free status code in the 4xx range, so 422 stays purely validation-shaped and the frontend can discriminate the two cases by type rather than body inspection. Lands cleanly with the "window-and-stitch for auto on long text" cross-cutting fix above, which removes the error path entirely for the auto mode. (Session 5 Codex-review fold-in note, 2026-05-25.)
- **Agent retry loop + adapter envelope-unwrap are duplicated (revisit at rule-of-three).** Two altitude dups surfaced in the M2.S3 `/code-review`: (a) the `render → call → validate → retry-on-ValidationError → give-up` loop is near-verbatim in `chunking_agent.py` and `extraction_agent.py` (only the schema/weight/grounding differ); (b) the `response.json()` + envelope-unwrap-or-raise-`ProviderResponseError` block is near-identical in `ollama.py` and `openrouter.py` (only the dict path differs). **(a) ✅ Resolved 2026-06-15 (M3.S3, PR #60):** JudgeAgent was the n=3 trigger — the loop folded into `agents/validation.py` `validate_with_retry(...)`; chunking + extraction refactored onto it (each keeps its own schema/call/give-up error); `TaskWeight` + a `Router` Protocol promoted into `base.py` at the same time. **(b) still open** — deliberately per-copy at n=2; **fold at the first direct vendor adapter** (Anthropic/Grok/…, "as needed"), since each new copy re-implements the INV-6/OQ-4 no-body-leak rule by hand, and a `_unwrap_content(...)` in `base.py` would touch that security-sensitive guarantee. (M2.S3 `/code-review` altitude findings, 2026-06-08.)
- **Repo-root `scripts/` is outside CI's ruff scope.** The backend ruff config is `src = ["src", "tests"]` and CI runs `ruff check .` from `backend/`, so `scripts/*.py` (e.g. `check_dependency_age.py`) are never linted — a pre-existing `UP017` (`datetime.UTC`) sits there uncaught. Either add repo-root `scripts/` to a lint step or fix the one nit. Left untouched in PR #25 to stay surgical. (M2.S1 own-review nit, 2026-06-01.)
- **`CandidateSpan` has no `char_start <= char_end` validator** (`domain/extraction.py`). Internal deterministic data from spaCy, so optional — a model validator would just self-document the invariant. Add if a future producer of `CandidateSpan` is less trustworthy than spaCy. (M2.S1 own-review nit, 2026-06-01.)

### Blocked / questions

- ~~**Review-gate policy after Codex retirement (deferred 2026-06-24).** Should `/code-review` (multi-agent) become a **required** second gate for substantive code, or stay recommended?~~ ✅ **Resolved 2026-07-06 (Session 76): make it REQUIRED for substantive code.** Root `AGENTS.md` Merge-flow "Review model" now requires the multi-agent `/code-review` (in addition to `/review-pr`) before merging any PR that changes non-trivial, correctness-bearing code; docs/config/small-mechanical PRs stay `/review-pr`-only. Settled on three consecutive sessions of consistent evidence — S73 (a minDegree-clamp bug the self-pass had *seen* and called benign), S75 (a Neo4j-outage availability regression 503'ing the whole review queue), **S76 (a graph-corrupting wrong-merge into the first of N same-named entities)** — each a real bug `/code-review` caught that the self-`/review-pr` missed on substantive code.
- _**Earlier resolved registers / questions (Milestones 1–4)** — see **[`docs/PLAN_ARCHIVE.md`](PLAN_ARCHIVE.md)**._

### Decided

- **2026-07-13 — Session 91: Graph-quality S6 register RESOLVED with the owner (DM-NN-1..6 / OQ-34 — each on the proposed lean; architecture-decision unit, no production code).** Resolved the `graph-name-normalisation` register one plain-language question at a time (the proposal's "Gaps for the product owner", not register shorthand — root `CLAUDE.md`). All six calls landed on the decompose's lean: **DM-NN-1 (tool shape) → (c) one shared synonym-suggest engine + one review list over both vocabularies, two forked apply paths** (predicate re-key vs type relabel); *rejected:* a leaky one-engine that hides the storage asymmetry, two duplicated engines. **DM-NN-2 (synonym detection) → fuzzy + label-string embeddings, recall-first** — both spelling and *meaning* (local `EmbeddingAgent.encode` on the label), surface borderline pairs (a false suggestion costs one dismiss click; a missed synonym stays hidden), a spec-defaulted `name_normalise_suggest_floor` knob, deterministic-only; the `verify-at-build` stands (short symbol-y labels may embed noisily → fall back to fuzzy + morphological normalisation if cosine doesn't separate synonyms on the real vocabulary); *rejected:* fuzzy-only (misses token-disjoint synonyms like `PASSENGER_ON`↔`ON_SHIP`), a tighter precision floor. **DM-NN-3 (dismissal memory) → persist dismissals**, reversible, reusing the S4 store pattern (ADR 0010); no fresh ADR expected; *rejected:* ephemeral, materialize the queue. **DM-NN-4 (relationship rename) → loop the shipped S5b `plan_relation_rekey` graph-wide in one grouped reversible op**, preserve each `edge_uid` (INV-10), fold identical triples **counted and reported** ("merged 2 edges") — never the goal; *rejected:* a bespoke single-Cypher bulk re-key. **DM-NN-5 (type rename) → a new bulk `SET n.type` relabel op** on `EntityEditService`, grouped-reversible, no re-key/handle/collapse — the one net-new writer, grows INV-9's human-reached-writer enumeration by one path (build assigns the ordinal); *rejected:* looping the per-node edit. **DM-NN-6 (surface + slice) → a dedicated `/stories/:id/normalise-names` list** (reusing `useReviewQueue`), both vocabularies together, **backend-first (S6a) then frontend list (S6b)**; *rejected:* split predicate/type into separate slices, canvas annotation (→ BACKLOG). Reconciled across every home: the proposal → `status: accepted` (banner + each register entry's `✅ Decision`; the Mermaid already depicted the chosen flow, no rejected branch to deactivate), OQ-34 struck in `open-questions.md`, the INDEX proposal row + Session-90 learning-log pointer flipped to RESOLVED. **No spec amendment, no ADR** (as anticipated at decompose). Vault/plan-only → `/review-pr`-class. **Next: build S6a test-first — the shared pure suggest function over a label vocabulary — in a fresh conversation.**
- **2026-07-13 — Session 90: Graph-quality S6 decomposed (step-0, register OPEN; PR #203, squash-merged).** Back in the Graph-quality sequence after the out-of-sequence tooling-extraction unit. Ran `meta-architect:decompose-requirement` on **S6 — predicate- AND entity-type-name normalisation + synonym suggestion** → the vault proposal `architecture/proposals/graph-name-normalisation.md` (register **DM-NN-1..6 / OQ-34**, all OPEN — owner resolves before code). Architecture/planning only; **no production code, no spec or plan change** (§3 S6 already scopes both surfaces after the owner's Session-81 type extension; the predicate re-key/handle is ADR 0011's, the dismissal store ADR 0010's — no new ADR anticipated). **Defining finding — answers the task's "one engine or two" at the *storage* layer (grounded in a reuse-first code survey, not the plan):** a relationship **predicate IS the Neo4j relationship type** (part of the content-addressed `relation_edge_id`), so renaming it **re-keys every bearing edge** — reuse the S5b `plan_relation_rekey`, preserve `edge_uid` (INV-10 — S6 is the near-term consumer S5 predicted), fold identical triples (the *reported* "2 edges merged" side-effect, never the goal); an entity **type IS a node property**, so renaming it is a **bulk `SET n.type`** (no re-key, no handle, no collapse — two nodes sharing a type don't merge). ⇒ **one shared SUGGEST engine + two forked APPLY paths.** Second finding: unlike S4's entity dedup (surface-similar names), S6's synonyms are often fuzzy-*distant* but semantically close (`PASSENGER_ON`↔`ON_SHIP`), so the **embedding rung earns its place more than in S4** — embedding the *label string itself* (`EmbeddingAgent.encode`), not mention vectors. 0b operation-surface sweep over {predicate-vocab, type-vocab} **closes** (the type-relabel is the one net-new writer; bulk/multi-select → BACKLOG; blocking/LSH not even needed at tens-of-labels scale). Register leans: DM-NN-1 shared-suggest/forked-apply · DM-NN-2 fuzzy + label-embeddings, recall-first, a `name_normalise_suggest_floor` knob · DM-NN-3 persist dismissals (the S4 DM-CD-3 / ADR-0010 pattern) · DM-NN-4 loop the shipped S5b re-key in one grouped op at N-write scale · DM-NN-5 one new bulk `SET n.type` relabel op · DM-NN-6 dedicated list, be/fe with both surfaces together. Added the `[[controlled-vocabulary]]` glossary term (kontrolowany słownik; glossary 35→36) + a learning-log line; OQ-34 mirrored; INDEX/CHANGELOG regenerated. **Vault-only → `/review-pr`-class, no `/code-review`.** Own-`/review-pr` **caught one real overspecified claim** — "the ninth witnessed path" for the type-relabel INV-9 growth (invariants.md numbers instances only through the *seventh*; the sibling S5 proposal's decompose-time "eighth" guess itself landed as the seventh) — folded to non-numeric phrasing (commit `13efb66`). *Considered + rejected:* leaving the exact ordinal (drifts; a wrong count reads as a real error in a portfolio doc). **Retro (owner ran full `/retro`):** hardened that lesson into a `architecture/AGENTS.md` convention — proposals don't assert an exact *forward* invariant-enumeration ordinal; the build assigns it. **Next: resolve the S6 register (DM-NN-1..6) with the owner, one plain-language question at a time — a fresh conversation; the build slices come after.**
- **2026-07-13 — Session 89: tooling-extraction BUILD slice 5 — SF-side reconcile; the whole extraction is COMPLETE (PR #201).** The final slice, docs-only bookkeeping against the approved design (§9.5, path A): (1) closed the `docs/BACKLOG.md` "Distribute the meta-architect skills" item — marked the extraction complete with a per-slice (1–5) summary; (2) flipped the `docs/design/tooling-extraction.md` Status banner from "build underway / slices 1–2 done" to **BUILD COMPLETE** + marked §9 sequencing shipped (items left in their original plan-of-record tense under a "all slices shipped" header, per §4 no-rewrite-history); (3) plan reconciliation — confirmed PLAN_LONG has no extraction task and the only "remaining slice" language is the handoff block (rewritten this wrap). **`/review-pr` whole-repo reconciliation sweep** found two residual status-language homes the handoff didn't name — ADR 0002's dated Session-85 block and design-note §3 line 56 — both correctly classified as append-only / design-time history (the "leave genuine history intact" carve-out); on owner call, appended a *new* dated Session-89 completion note to ADR 0002 rather than rewriting the S85 block. *Considered + rejected:* rewriting the dated ADR/§3 text (would "fix history into the present"); leaving ADR 0002 pointer-only (owner wanted completion visible on the ADR itself). Docs-only → `/review-pr`-class, no `/code-review`. **Path A held** — SF's `.claude/skills/` untouched (9 tracked files, verified at resume). **Resume caught a benign handoff overstatement** (the S88 handoff claimed the BACKLOG item "already carries slice-1/2/3-DONE markers"; only slice-1's was present — slices 2–4 recorded DONE in the plan/design note, not on the BACKLOG item) — flagged up front, wrote 2–5-DONE instead of blindly appending slice-4. **Next: back to Graph-quality S6** (predicate + entity-type name normalisation + synonym suggestion — needs a `meta-architect:decompose-requirement` step-0), in a fresh conversation.
- **2026-07-13 — Session 88: tooling-extraction BUILD slice 4 — the trial-install acceptance test (owner-driven, interactive; the whole extraction's §2 acceptance criterion).** Ran design-note §9.4: does "apply the SF treatment" work end-to-end? Target = the owner's **`yaaisiu/image-metadata-extractor`** (a Python HEIC/JPEG library encrusted with four parallel coding-agent stacks — Claude/Codex/Gemini/Cursor). **Verdict: green end-to-end** — the `review-and-integrate` flow (scan → recommend-a-fit → owner selects → wire in) produced a correct integration, and the acid test passed: the `PLAN_SHORT` handoff the skill *seeded* survived its own consumer (`/resume-session`) with **zero drift warnings**. **The trial surfaced 5 findings, all folded back into the skill (monorepo PRs #3 + #4, both squash-merged):** (#1) **establish the real trunk** — don't assume the checked-out branch/`main`; the target's real trunk was `dev` (31 commits / a whole Sprint 3 ahead of `main`), and `/resume-session` caught that the first wire-in had been built on stale `main`; (#3) **`add-dependency` three-state fit** (recommend if pinned, *offer* if ranges, skip if no manifest); (#4) **plugin enablement → checked-in `settings.json`**, not `settings.local.json`; (#5) **flag green-state checks with no CI home** (local-only gate); and — the one that taught the most — (#2) **overlap ≠ redundancy.** The initial "full cleanup" framing (mechanically `git rm` the 'multi-agent bloat') was **wrong**, and the live skill's own judgment **refused the deletion instruction and was right to**: `.agents/` was the symlink source for `.claude/skills/` + mostly non-overlapping skills (dev-rituals is ~8 rituals, not a superset of a 20-skill tree); the Codex/Cursor/Gemini configs are a *strategic agent-retirement*, not cleanup; `sync_all_repos.sh` is the *implementation* of a live backlog goal; ~124 ghost references. PR #4 rewrote §1/§3 so the skill classifies pre-existing tooling by three cases (genuine-overlap / other-agent / load-bearing) and treats any removal as a **separate owner decision** (archive-over-delete, reconcile ghost refs), never a `git rm` bolted onto the wire-in. **Target-repo outcome:** the clean, deletion-free wire-in (config + repo-level `settings.json` + `CLAUDE.md` doctrine snippet + a seeded `PLAN_SHORT` now pointing at an architecture-review + `initialize-project-architecture` scaffold phase — the owner **postponed** the aaa-python integration) landed as **PR #3 into `dev`** (owner-gated merge); the multi-agent retirement was **recorded as a deferred decision** (target BACKLOG §1.5, with §0 honestly annotated rather than falsely closed), not acted on. **Interactive/owner-driven:** the agent cannot run `/plugin`, so the owner drove `marketplace add`/`update` + restart in the target session. *Considered + rejected:* running the deletion verbatim (would dangle the `.claude/skills` symlinks, silently retire three agents, close a backlog goal by deleting its solution, and leave ~124 doc ghost refs). **Path A held — SF's `.claude/skills/` untouched;** no SF production code (SF green-state N/A). Remaining: slice 5 (shrunk SF reconcile — close the BACKLOG distribution item + design-note tidy), then back to Graph-quality S6.
- **2026-07-13 — Session 87: tooling-extraction BUILD slice 3 — the keystone `review-and-integrate` skill (monorepo PR #2, squash-merged).** Executed §9.3 of the approved design note: built `dev-rituals/skills/review-and-integrate/SKILL.md` — **the genuinely-new piece of the whole extraction** (everything before was extraction + genericizing). It surveys a target repo and threads the tooling in *deliberately, not blindly* — the analog, for the whole ritual stack, of what `meta-architect:initialize-project-architecture` does for the architecture vault. **Modelled on that skill** (reuse-before-building, §6): scan (stack/CI/docs/deps/images/scanners/tests) → map each ritual to a **recommend/offer/skip** fit → owner selects the active set → confirm → wire in (write `.claude/dev-rituals.config.json`, enable the chosen plugins in `.claude/settings.json`, drop a doctrine snippet into `CLAUDE.md`) → human-in-the-loop throughout; idempotent on re-run. **The two "surface as you build" items, now resolved:** (a) **the `CLAUDE.md` doctrine snippet** = a template that lists *only the enabled rituals* + a one-line when-to-run per ritual (step 4c), so future agents invoke exactly the active set; (b) **fit-detection signals** = a scan→fit table (step 2) — pinned dependency manifests → `add-dependency`, Docker images → `pin-image`, a CI security scanner (OSV/Trivy/npm-audit) → `triage-advisory`, a spec/plan → `review-pr`'s full lens (else it degrades, D3), plan files → the session loop, architecture near-always. **Design nuance the survey surfaced and the skill encodes:** Claude Code enables/disables a *plugin* as a whole (no per-skill toggle), so a repo's **active ritual set is scoped by the config + the `CLAUDE.md` snippet** (which *name* the live rituals), not by flipping switches; skills whose gates are absent **degrade to no-ops** (security skills with no scanner, `review-pr` with no spec). **Both README seams wired** (§8): (1) this skill **owns the config writer** (`.claude/dev-rituals.config.json`); (2) when a repo takes **both** `meta-architect` and the session loop, they enable *together* so the architecture-report **producer/consumer pair closes** — `resume-session §3c` (consumer) triages what `wrap-session §5c`-style roll sweeps (producer) generate — and the skill sets `docs.architectureReportDir`; if meta-architect is taken *without* the session loop, the skill flags that its reports would have no in-repo consumer. **Docs:** added the `review-and-integrate` row to the `dev-rituals` README table and dropped the "coming next / planned" wording from the README + the config example now that it ships. **Review:** the `/review-pr` *skill* is scoped to SF's own working tree (it git-diffs against SF `main`), so it can't target the monorepo diff — applied its **lens manually** (config keys all exist in the example schema; handoff markers match `resume-session` verbatim; plugin-enable refs match the marketplace name; `§3c`/`§5c` cross-refs resolve; JSON re-validated; 708-char frontmatter description); a Markdown skill is not correctness-bearing runtime code, so `/code-review` is not the required gate (consistent with slices 1–2). **Path A held** — SF's `.claude/skills/` untouched; no SF production code (SF green-state N/A). *Considered + rejected:* inventing the skill from scratch (design §6 mandates modelling it on `initialize-project-architecture`); per-skill plugin toggles for selectivity (Claude Code has no such mechanism — selectivity is config + snippet). Remaining: slice 4 (trial-install acceptance test into an older repo), slice 5 (shrunk SF reconcile), then back to Graph-quality S6.
- **2026-07-13 — Session 86: tooling-extraction BUILD slice 2 — the `dev-rituals` plugin (monorepo PR #1, squash-merged).** Executed §9.2 of the approved design note: wrapped SF's 8 `.claude/skills/` rituals as a second plugin (`dev-rituals`) in the `yaaisiu/claude-dev-tooling` monorepo, alongside `meta-architect`, and listed it in the root `marketplace.json` (one `/plugin marketplace add` exposes both). **The handoff's two "surface as you build" items, now resolved:** (a) **per-repo config mechanism = `.claude/dev-rituals.config.json`** (the §5 lead option over a `CLAUDE.md` block) — a structured file the slice-3 `review-and-integrate` skill will write, that each skill reads for *overrides* with **every field defaulted**, so every skill runs with no config present (the D3-degradation spirit); the schema landed lean (`docs` / `checks` / `dependencies` / `images` / `advisories` / `workflow`). Rejected: a `CLAUDE.md` block (less clean as an integration-skill write target) and extra keys for the illustrative lockfile/backlog paths (kept the schema lean). (b) **anecdote handling** — the generic versions keep each earned *pattern* and drop the `Session N`/`PR #` archaeology (no SF-only appendix, since path A means SF keeps its own copies). **Shape:** the 4 light-param skills got a small config surface; the 4 heavy ones (`resume-session`, `wrap-session`, `review-pr`, `triage-advisory`) are a *skeleton + per-repo profile* rewrite; **`review-pr` degrades gracefully (D3)** — full spec/plan-fidelity + layering lens when those docs exist, correctness + hygiene when they don't. **Path A held** — SF's `.claude/skills/` were read-only throughout; its tree is unmodified. Built by fanning out 8 genericization subagents against the §5 classification table as spec, then full self-review of all 12 files (JSON valid, frontmatter intact, residue-grep clean); docs/packaging-only → `/review-pr`-class self-review, no `/code-review`. Remaining: slice 3 (`review-and-integrate`), slice 4 (trial-install), slice 5 (shrunk SF reconcile), then back to Graph-quality S6.
- **2026-07-13 — Session 85 (post-slice-1): SF keeps its own rituals — path A (owner).** For the session-workflow rituals, **Story Forge keeps its bespoke `.claude/skills/` untouched, permanently**; the exported `dev-rituals` plugin is built **only for the owner's other repos**. SF genericizes *from* copies of its skills into the plugin but its working copies are never modified or replaced. *Considered + rejected:* path B (the design note's original §7/§9.5 — SF eventually adopts vendored copies of the generic rituals, like it now does for `meta-architect`) — the owner prefers not to touch SF's working tooling. **Consequences:** narrows design-note §7 (SF vendors **`meta-architect` only**, not the rituals — no vendored-rituals copy/provenance in SF); **shrinks slice 5** (no SF rituals migration — just closing BACKLOG/plan + design-note tidy); the slice-2 "keep the session rituals functional" risk collapses to "simply never modify `.claude/skills/`". Recorded in `docs/design/tooling-extraction.md` (banner + §7/§9 update notes) + the handoff. (Also folded the tracked design-note-banner should-fix: it now reads APPROVED / build-underway.)
- **2026-07-13 — Session 85: tooling-extraction BUILD slice 1 — `meta-architect` graduated to its own public repo (PRs #194 + #195, both squash-merged).** Executed §9.1 of the approved design note. **What shipped:** (1) a new public monorepo **[`yaaisiu/claude-dev-tooling`](https://github.com/yaaisiu/claude-dev-tooling)** with the `meta-architect` plugin moved in + a root `.claude-plugin/marketplace.json` — `/plugin marketplace add yaaisiu/claude-dev-tooling` → `/plugin install meta-architect@claude-dev-tooling` works in any repo; (2) SF-side reconciliation onto the §7 consumption model — SF **keeps its vendored copy** (`.claude/settings.json` wiring *unchanged*, still consumes `./meta-architect`), with a new `meta-architect/UPSTREAM.md` provenance pointer + graduated README/marketplace strings; (3) ADR 0002 extended with a graduation section, kept **Accepted not Superseded** (the in-repo-folder decision still holds via the vendored copy). **Decisions taken:** the **provenance mechanism** = a human-readable `UPSTREAM.md` in the vendored dir (records upstream URL + vendored version + deliberate-re-sync policy), *considered + rejected* a plugin.json custom field / a settings comment as less legible; the **monorepo layout** = root `.claude-plugin/marketplace.json` with `source:"./meta-architect"` and the plugin subdir drops its nested (dogfooding-only) marketplace.json (mechanics confirmed via the `claude-code-guide` agent, reused not invented); **public** repo (owner call). Docs+config only → `/review-pr`-only (§2 whole-repo incubation→graduation sweep clean; no `/code-review`). **Unplanned detour → PR #195:** a fresh Go-stdlib **CVE-2026-39822** (`os.Root` trailing-slash symlink escape, HIGH/CVSS 7.8, attack vector LOCAL) reddened the required `security` gate on #194 (pure treadmill, unrelated to the diff). Handled triage-first per owner: an owner-approved condition-based **waiver** on pgvector (gosu, Go 1.24.6) + ollama (Go 1.26.0), both being compile-time-toolchain CVEs no tag fixes; unreachable here (local AV; 127.0.0.1 single trusted user; gosu opens no untrusted os.Root paths). Registered in both `.trivyignore` files + `infra/trivy/WAIVERS.md`; verified locally with `aquasec/trivy:0.70.0`. **Drop when** upstream rebuilds gosu **and** ollama on Go ≥1.25.12/1.26.5. *Considered + rejected:* merging #194 on the diagnosed red (green-main-bar exception) — triage-first keeps `main` green and unblocks the wrap PR too.
- **2026-07-13 — Session 84: tooling-extraction DESIGN PASS (docs/design; PR #192, squash-merged).** An owner-directed, out-of-sequence unit (not a Graph-quality slice): design how to package the `meta-architect` plugin + the `.claude/skills/` rituals as portable Claude Code plugins for the owner's *other* repos. Ran **interview-first** (owner steer) → the design note `docs/design/tooling-extraction.md`. **Decisions (owner-resolved):** (**D1**) **one `dev-rituals` plugin, not a split** — the `review-and-integrate` skill enables skills selectively per repo; efficiency over fragmentation. (**D2**) **one tooling monorepo** (`claude-dev-tooling`) hosts both plugins + one `marketplace.json`. *Considered & rejected:* separate repos per plugin (more to wire up; split later only if independent versioning is needed). (**D3**) **`review-pr` is adaptive** — full spec/plan-fidelity + layering lens when those docs exist, degrade to correctness + hygiene when they don't. (**D4**) **stop at the approved design**; the extraction is a multi-session build (note's §9). **Consumption model resolved (closes the Session-76 open "how"):** SF **vendors its own copies with an upstream provenance pointer**, not marketplace-auto-install — generic changes could break SF, its most demanding user. **Grounded findings** (subagent survey of every SKILL.md): the BACKLOG premise is partly stale — `meta-architect/` is *already* a checked-in plugin SF dogfoods via a local `directory` marketplace, so graduating it is "move the folder"; classification is 🟢 3 already-generic (meta-architect trio) / 🟡 4 light-param / 🔴 4 heavy-rewrite. **Keystone new capability:** a `review-and-integrate` skill modeled on `initialize-project-architecture` (reuse-before-building). `/review-pr` folded 4 self-review consistency nits; docs-only so `/code-review` not required; CI green (code jobs correctly path-skipped).
- **2026-07-10 — Session 83: Graph-quality S5b-fe built + merged — S5 COMPLETE (production code, PR #190).** Brought the three edge affordances onto the canvas edge panel (re-predicate / re-target / delete), mirroring the S5a node pattern. **Decisions:** (a) **Extend `EdgeEvidencePanel` in place** (read-only → editable) rather than fork an `EdgeEditPanel` — the edge panel is canvas-only (no reader twin), so a single component with a read/edit split (mirroring `EntityEditPanel`) is simpler than the renderReadExtras indirection. (b) **Delete reuses `useRemoveRelation` unchanged**; the only net-new API hook is `useRetargetRelation` (PATCH, invalidates the story-graph triad via the `entity-detail` *prefix* since a re-target moves endpoints). (c) **Re-key re-points the selection** — a content-addressed edge changes id on save, so `onEdited(newEdgeId, merged)` re-points `selectedEdgeId` and the DM-S5-6 guard holds it through the refetch; the guard's `shouldClearSelection` was **generalized** (`selectedNodeId` → `selectedId`) to serve node *and* edge honestly. (d) **Fold note threaded via a GraphViewer flag** onto the re-pointed panel (the panel remounts on the new id, so the note can't live in the mutation result). (e) **Endpoint re-target reuses the existing `EntityPicker`** (the reader's add-relation pick/change UI). **Reviewed** with `/review-pr` + the required multi-agent `/code-review` (high) — the second gate caught a **fold-notice suppressed when the survivor's evidence read errors** (hoisted the note to render in loading/error/populated) + a **test-coverage regression** (the panel-test rewrite dropped two S3b read assertions — restored). Its **"retarget must invalidate the edge-evidence cache" finding was refuted** on verification: `retarget_relation` never touches `staged_relations` (`agents/entity_edit.py`) and `relation_rekey.py` is pure Neo4j planning, so a fold does not merge provenance into the survivor and no cache is dirtied; the real underlying limitation (a re-key **orphans** the displayed provenance because `staged_relations` isn't re-keyed) → `docs/BACKLOG.md`. **Owner-found UX bug fixed before merge:** long source paragraphs pushed the curation buttons off-screen — the edge panel's source region now scrolls internally (actions pinned visible), and the shared `EntityEditPanel` got `overflow-y-auto`+`min-h-0` (active on the canvas, a no-op inside the reader's own scroll wrapper). *Considered & rejected:* pinning the node-panel actions via a flex-1 region (risked the reader's indefinite-height layout — the whole-panel scroll is provably safe there). 489 tests green; owner ran the Oakhaven smoke (edit an edge → undo → clean). **Retro (owner-approved):** added a **`/review-pr` §6 lens** — when a PR rewrites/extends an existing test file, diff old vs new assertions so an extension can't silently drop inherited coverage (the self-review missed exactly this; `/code-review` caught it). **Next: a tooling-extraction unit** (extract the `meta-architect` plugin + the reusable rituals as portable tooling for the owner's other repos — owner directive; see the handoff + `docs/BACKLOG.md`), then back to Graph-quality **S6**.
- **2026-07-10 — Session 82: Graph-quality S5b-be built + merged (production code, PR #188).** The edge-write backend — the §4 `edge_uid` surrogate handle + the atomic `retarget_relation` (edit-predicate / re-target) op, test-first. **Defining shape:** `edge_uid` lives **only** on the Neo4j edge + the `GraphRelation` model + the `graph_edits` before-image, so — the finding that removed a whole chunk of planned work — **no Alembic migration** (`staged_relations` is content-`edge_id`-keyed, untouched). **Decisions:** (a) **coalesce via `MERGE … ON CREATE SET` with no `ON MATCH`** — the ON-CREATE-only semantics *are* the coalesce rule (a MERGE-match sets nothing → an existing handle is never overwritten, a legacy edge stays handle-less); *rejected:* an explicit `ON MATCH SET … coalesce(...)` (a creeping back-fill, redundant). (b) **mint-forward on every edge writer, no back-fill** — the human-gated relation-decide commit, `add_relation`, and the re-key all stamp a `uuid4`; legacy edges get one lazily when first curated. (c) **reuse the merge writer's `repoint_relation`/`fold_relation` op-strings** rather than mint re-key-specific ones — same delete-old+create-new shape, so `graph_undo.invert_operation` reverses it with no new arm (the audit `description` distinguishes intent); *rejected:* parallel op-kinds (duplicate invert logic for no gain). (d) **ADR 0011** (escalated form — crosses the data-model identity boundary) + **INV-10** ("an edge's `edge_uid` survives re-point/re-predicate/merge") minted with enforcer + tests; INV-9 grep set **unchanged** (7th witnessed instance — reuses `create`/`delete_relation`). **Reviewed** with `/review-pr` **and** the required multi-agent `/code-review` (high) — the multi-agent pass caught a real **data-loss bug the self-review missed** (and had *reasoned past*, mirroring merge's ordering): the re-key did **delete-old then create-new** as two Neo4j transactions, so a store failure between them lost the edge entirely → **reordered to create-new-first** (a mid-failure now leaves a recoverable duplicate, never a missing edge — the DM-S5-2 no-edge window finally closed server-side; *considered:* a single-transaction adapter method, rejected as more surface for the same end-state). Four more findings folded (honest crash-window docstring + ADR; a domain blank-predicate guard in `plan_relation_rekey` closing an `add_relation`/re-key asymmetry `model_copy` opened; the stale `AddRelationRequest` docstring; skip redundant endpoint reads on a re-predicate). One deferred: extract the shared predicate validator (finding 6 — rule-of-three, n=2; the domain guard backstops the blank case). 630 tests green (pure planner → real-Neo4j round-trip + coalesce → service-with-fakes + undo → real-store E2E → API contract); ruff + mypy clean; CI green. **Two follow-ups tracked:** the `get_story`-outside-the-503-guard systemic gap → cross-cutting; `merge_entities`' edge re-point shares the same (now-fixed-in-retarget) delete-first data-loss window → `docs/BACKLOG.md`. **Retro:** skipped (owner) — the reflection confirmed the required-`/code-review` rule earned its keep again (caught a real bug the self-pass missed); nothing cleared the bar for a rule change. **Next: S5b-fe** (the canvas edge affordances — edit-predicate / re-target / delete on the panel).
- **2026-07-10 — Session 81: Graph-quality S5a built + merged (production code, PR #186).** Brought the entity edit/merge/delete/undo write-paths onto the graph canvas — pure canvas-surfacing over already-shipped endpoints, no net-new backend (the resolved S5 register). **Decisions:** (a) **DM-S5-1(B) realised — a shared `EntityEditPanel` core** in a new `features/entity-panel/` that the reader and the canvas both compose; it owns its own `useEntityDetail` fetch (a `GraphNode` carries no `properties`) and namespaces every `data-testid` via a `testIdPrefix`, so the reader's existing test contract stayed green **unchanged** (the regression gate). Edit-form logic factored into a pure `entityEditForm.ts` + thin `useEntityEditForm` hook (no business logic in the component). (b) **Owner call — move the shared helpers too:** relocated `MergeControls`/`MergeConflictFields`/`mergeConflicts`/`propertiesEditor`/`formatPropertyValue`/`UndoButton` into `features/entity-panel/` (cleaner layering — no feature importing another feature's internals) rather than leaving them in `text-reader/`; *considered:* new-panel-only (smaller diff). (c) **Panel-vanish guard (DM-S5-6)** = pure `shouldClearSelection` — holds the selection while an edit is dirty and keeps the just-edited node selected through the post-save refetch; the grace ends on a *user* filter action (not the refetch-driven prune effects). (d) **Undo at the canvas toolbar** (reused `UndoButton`), mirroring the reader — not a per-node affordance. **Reviewed** with `/review-pr` **and** the required multi-agent `/code-review`: both independently caught a **wrong-target PATCH** — the panel was re-targeted by changing `entityId` with no React `key` and no edit-state reset, so switching nodes mid-edit on the still-clickable canvas left the prior node's drafts loaded and Save PATCHed the *wrong* entity (graph corruption, subverting the INV-9 human gate). Fixed by keying the panel per entity (also keyed the reader's mount, closing the same latent path there) + two symptom fixes (delete-confirm carryover, the stuck just-edited panel); three regression tests added. Two `/code-review` findings refuted on verification (the removed first-seen-paragraph line — a minor UX loss, not a spec violation; `normalize`/`buildEntityEditPatch` duplication — intentional). 451 tests green; tsc/lint/format/build clean; **owner ran the browser smoke at Oakhaven scale** (edit type → recolour + undo, both working). (e) **S6 scope extended to entity *types* (owner):** the duplicate entity-type labels the owner saw in the live type filter are the same problem S6 tackles for predicate names, so S6 now normalises **both** (see the S6 task). **Retro:** skipped (owner) — the reflection found nothing actionable; the review gates caught the one real bug as designed. **Next: S5b-be** (the edge-write backend — §4 handle + atomic ops + ADR + invariant, test-first).
- **2026-07-09 — Session 80: Graph-quality S5 decomposed + register RESOLVED (PR #183) + advisory triage (PR #184).** Two units in one conversation (the decompose, then an urgent `main`-red security interrupt the owner sequenced). **S5 decompose (`architecture/proposals/graph-canvas-editing.md`, DM-S5-1..6 / OQ-33), owner walked one decision at a time:** (a) **DM-S5-1 = extract a shared `EntityEditPanel` core** the reader and canvas both compose — *owner override of my "reuse-with-toggles" lean* ("expose editing to the common core"); *rejected:* mounting `ReaderEntityPanel` directly, and forking a graph-native panel. (b) **DM-S5-2 = an atomic backend edge-edit op** for rename-predicate / re-target (one grouped reversible op preserving the §4 handle); *rejected:* the client-side remove+add compose (splits one edit into two undo steps, opens a partial-failure data-loss window, can't cleanly preserve the handle). (c) **DM-S5-3 = mint-forward + no-backfill + coalesce-on-MERGE for the §4 `edge_uid` handle, AND mint the "handle survives curation" invariant at the S5b build** (the enforcer exists then, and S6 also re-keys edges — a real near-term consumer); *rejected:* a backfill migration (premature — nothing reads the handle yet). (d) **DM-S5-4 = defer right-click entirely** — *owner override of my droppable-ride-along lean* — S5 is panel-only, `cxttap` is a later follow-up. (e) **DM-S5-5 = a single S5a slice + fold the §4 tag into S5b-be** (cut: S5a → S5b-be → S5b-fe). (f) **DM-S5-6 = add the modest panel-vanish guard** (keep the selection while an edit form is dirty; still clear on delete/merge); concurrency stays last-write-wins at PoC. **The §4-handle ADR drafts at the S5b build, not the decompose** (DM-GQ-1 obligation; no ADR unasked). No `graph-quality.md` amendment (§3 S5 + §4 already scope it). `/review-pr` clean (vault-only, decision-reconciliation swept across every home). **Advisory triage (PR #184):** the scheduled `security` gate reddened on unchanged `main` — two fresh Trivy HIGHs the DB learned overnight. **CVE-2026-40355** (pgvector/krb5 NULL-ptr-deref DoS) waived as a sibling of the accepted CVE-2026-40356 (same `deb13u1` fix, not in a rebuild; no Kerberos service, loopback). Clearing it **unmasked CVE-2026-39831** (ollama/`x/crypto/ssh` FIDO/U2F presence-check bypass) — the sequential-unmask the `/triage-advisory` "scan all three up front" step caught *proactively* (local `aquasec/trivy:0.70.0` scan before pushing), so both waivers landed on **one** PR (two reds on the required gate mutually block). Fix-first confirmed non-viable (no soaked rebuilt image carries either fix). Both owner-approved HIGH waivers (§6.7), condition-based drop-whens recorded + mirrored (`*.trivyignore` + `WAIVERS.md`). All three images verified clean locally; CI green after a re-run cleared a runner-starvation cancellation. **Retro:** skipped (owner) — the reflection turned up nothing actionable; the session validated existing process (notably the triage-advisory unmask discipline paying off). **Next: build S5a test-first in a fresh conversation.**
- **2026-07-07 — Session 79: Graph-quality S4b built + merged — S4 COMPLETE (production code, PR #181).** Built the duplicate-suggestion review-list frontend (DM-CD-4). **Decisions:** (a) **Full consolidation of the review-queue skeleton (owner call)** — rather than copy the queue machinery a third time, extracted a generic `hooks/useReviewQueue.ts` (window-keydown + `selectedIndex` clamp + `isEditableTarget`, generic over item/state/intent) and migrated *all three* screens (extraction-review, relation-review, duplicate-review) onto it, pure key-reducers unchanged; this closes the long-standing cross-cutting `useReviewQueue` item. *Considered & rejected:* build S4b standalone + defer the fold (smaller diff, but leaves a 3rd copy). (b) **Explicit survivor pick on accept (owner call)** — the author clicks which of the pair survives each time (no silent default direction), the picked side goes amber; *rejected:* default-one-side-with-toggle (fewer clicks, risks a wrong-direction merge). (c) **Reuse, don't rebuild** — accept feeds the *existing* merge endpoint unchanged; the reader's inline merge-conflict resolver was extracted to a shared `MergeConflictFields` (two consumers); a new bodied-DELETE `delJsonBody` client helper backs the un-dismiss reversal. (d) **Two live-review UX asks folded before merge** — a live pair **counter** in the header, and an in-quote **mention highlight** (a pure, test-first `markMentions` — case-insensitive, longest-term-first, apostrophe/quote-normalising after the owner spotted a curly-vs-straight miss), plus a Keep/Merge **hint** answering "what do these buttons do". **Reviewed** with `/review-pr` (clean) **and** the required multi-agent `/code-review` (3 findings, all folded — a swallowed un-dismiss error, a post-merge details-error flash from the entity-detail cache invalidation racing the list refetch, and the relation reducer's `action`→`intent` field asymmetry that had forced an adapter). Live-driven on the real Oakhaven graph (234 pairs; dismiss→233→un-dismiss→234). **Two upstream quality findings surfaced by the owner's live use, routed to `docs/BACKLOG.md` (both independent of this frontend PR):** the RapidFuzz `token_set_ratio` name scorer over-matches on shared/subset tokens (`harbor` vs `harbor master` = 100; a lone shared "heavy" = 71), *and* this graph has **no mention embeddings** (extracted without the optional `models` group → `_safe_encode` fail-closed to NULL vectors per spec §3.3, so every suggestion is name-only and nothing counters the name over-match). **Process (retro, owner-approved):** a `frontend/AGENTS.md` note — run `npm run format` before committing (the pre-commit hook is `--check` and bounced three commits this session). **Next: S5** (the graph as an in-place editing surface).
- **2026-07-07 — Session 78: Graph-quality S4a built + merged (production code, PR #179).** Built the backend duplicate-suggestion surface straight to the resolved register, test-first. **Decisions:** (a) **Relocate the pure scoring primitives into `domain/`** (owner call at plan time) — `cosine_similarity` + a new `name_match_score` (the RapidFuzz core) moved from `agents/matching_agent.py` into `domain/name_similarity.py`, matcher imports them back, so the pure self-join reuses them without a `domain/↛agents/` layering break. *Considered & rejected:* putting the self-join in `agents/` instead (smaller diff, but the "pure logic" would sit a layer above where the handoff/register put it). (b) **The embedding rung reuses the existing `match_stage2_cosine_merge` (0.85) as its suggestion floor** — keeps S4a to *one* new knob (`duplicate_suggest_floor`, name floor, 60.0); tunable later if embeddings under-contribute. (c) **Ship the `DELETE …/dismiss` un-dismiss endpoint in S4a** (a small addition beyond the literal handoff's `POST`-only) — DM-CD-3 resolved "reversible," so a dangling can't-undo is worse; owner OK'd. (d) **`actor` column dropped** (a `/code-review` catch) — copied from the `candidate_decisions` analogue but never written/read in a single-user app → speculative, cut (Karpathy). (e) **ADR 0010** drafted (owner-confirmed) for the DM-CD-3 dismissal store — staging-side (INV-9), `uuid5`-PK idempotency idiom, none-at-PoC retention, reversible. **Reviewed** with `/review-pr` (folded a misleading `__all__` + the missing ADR-registry row) **and** the required multi-agent `/code-review` (4 findings, all folded — the O(n²) self-join offloaded via `asyncio.to_thread` so it can't block the event loop; `stage1` routed through the extracted `name_match_score` to *complete* the primitive extraction; the two independent reads `asyncio.gather`ed; the speculative column cut). Live-driven on the real Oakhaven graph (113 entities → 234 suggestions; caught the `Elara`/`Elira` typo-dupe; dismiss/un-dismiss round-trip clean). **Process (retro, owner-approved):** the incomplete-extraction miss (relocated a primitive to be its "single home" but left `stage1` inlined — `/code-review` caught it, `/review-pr` didn't) hardened into a new **`/review-pr` §1 "complete-the-extraction sweep" lens** (when a PR extracts a primitive to one home, grep the module for every inline copy and confirm all were routed through it). **Next: S4b** (the review-list frontend feeding the existing merge).
- **2026-07-06 — Session 77: Graph-quality S4 (Suggest duplicate clusters) decomposed + register RESOLVED (architecture/planning; no production code).** Ran `meta-architect:decompose-requirement` on the branchy S4 slice → `architecture/proposals/graph-cluster-dedup.md` (register DM-CD-1..6 / OQ-32); the owner resolved it the same session, one question at a time. **Defining finding (grounded by the reuse survey):** S4 is **not new machinery** — both the matcher (`MatchingAgent` + `AcceptedEntityReader.load_accepted`, which already assembles the accepted entities' names+aliases+mention-vectors; `_rank`/`cosine_similarity` pure & CI-tested) *and* the merge commit path (`plan_merge` → `EntityEditService.merge_entities` → `POST …/entities/{id}/merge`) already ship. So S4 **re-points the matcher inward** (a *self-join*: each accepted entity scored against the others) to *suggest* likely-duplicate entities, and feeds each into the **existing** merge — **it suggests, never auto-merges** (INV-1/INV-9 hold; no egress/LLM; INV-4 keeps type a soft ranking signal, never a hard filter). Only net-new logic: a small pure `domain/duplicate_clusters.py` (the `entity_merge.py` analogue) + a read endpoint + a dismissal store. The entity twin of S6's predicate-name suggest. **Decisions (owner):** (a) **DM-CD-1 = pairwise for V1** — flat A–B pairs, ranked; the merge is pairwise anyway and this sidesteps *cluster drift* (chaining A≈B≈C when A≉C); transitive `connected-components` (star-guarded) is a **named later refinement**, not a gap. *Rejected:* transitive-now, guarded-clusters-now. (b) **DM-CD-2 = eager / recall-first** — Stage 1 name **+** Stage 2 embeddings, floored at the **ambiguous** band (nothing auto-merges → surface the diminutive/inflection zone; a false positive costs one dismiss click, a miss stays hidden), ranked, **deterministic-only** (no live judge), a `duplicate_suggest_floor` config knob; *rejected:* high-precision auto-merge-bar floor, name-only. (c) **DM-CD-3 = persist dismissals** — record a "not a duplicate" in a small Postgres pair-store (staging-side — INV-9 holds; none-at-PoC retention per OQ-4; reversible un-dismiss), the M3 rejected-candidate-memory (DM-rej) precedent; **ADR-worthy → drafts at the S4a build**; *rejected:* ephemeral (a dismissed pair recurs every open), materialize the full suggestion queue (heavier, buys little — the compute is cheap). (d) **DM-CD-4 = a dedicated review-queue-like list** — each row = the pair + the S3 (DM-EE-3) verification context (type, aliases, quote) + accept→merge / dismiss, reusing the review-queue pattern; canvas-annotation overlay → `docs/BACKLOG.md`. (e) **DM-CD-5/6 = S4a backend (pure self-join + `GET …/duplicate-suggestions` + the dismissal store/endpoint) / S4b frontend (the list feeding the existing merge); reuse the merge unchanged; project-scoped.** **No `graph-quality.md` §3 S4 amendment at decompose time** — the dismissal store is staging-side plumbing, not a change to what §3 S4 already scopes ("suggest … the human commits via the existing merge"); confirm by reading §3 S4 at the S4a build (the M4.S3b "delete → §3.5" miscue). Added three glossary terms (`entity-resolution`, `connected-components`, `blocking`; 31→34). Reconciled across `architecture/` (proposal → accepted, OQ-32 struck, INDEX, CHANGELOG, glossary, learning-log) + this plan. **Next: build S4a test-first in a fresh conversation** (pure `domain/duplicate_clusters.py` self-join first, then the read endpoint + dismissal store + OpenAPI regen; `/code-review` is the required gate once real code lands).
- **2026-07-06 — Session 76: Graph-quality S3b built + merged, a security waiver dropped, and the review-gate question settled.** **Decisions:** (a) **`/code-review` is now a REQUIRED second gate for substantive code** (retro, owner-approved) — settles the S51 open question on three sessions of consistent evidence (S73/S75/S76 each caught a real bug the self-`/review-pr` missed); root `AGENTS.md` Merge-flow "Review model" amended; docs/config/mechanical PRs stay `/review-pr`-only. (b) **DM-EE-5 one-click "Merge instead" only fires for a *single* same-named match** — the `/code-review` caught that returning the *first* of N same-named alternatives could silently merge into the wrong entity (a legitimate INV-1 case); `exactNameDuplicates` now returns all matches and an ambiguous match defers to a manual pick. (c) **Edge-evidence panel keeps last-good data on a background-refetch error** (show error only when there's no data) + a real **Try-again** button (re-tapping the same edge is a no-op) — both `/code-review` findings, folded with tests. (d) **Dropped the pydantic-settings OSV waiver by bumping 2.14.0 → 2.14.2** (its floor 2026-07-04 reached; the `ignoreUntil` had expired and re-red the gate) rather than re-dating — fix-first per `/triage-advisory`; removed from both mirrored homes (PR #175). (e) **Prettier/eof pre-commit re-commit friction: no rule** — considered a "format before commit" note, rejected as ceremony (the hook self-corrects); adopt the habit instead. *Considered & rejected on the review-gate question:* keep it recommended + gather more evidence (three sessions is enough — the pattern is consistent).
- **2026-07-01 — Session 75: Graph-quality S3a built + a `/review-pr` §4 lens earned (production code, PR #172).** Built the two backend read surfaces the resolved register (DM-EE-1/2/3) scopes, test-first, backend-only. **(1) Edge evidence:** `GET /stories/{id}/relations/{edge_id}/evidence` returns an edge's predicate + all `written` `staged_relations` for the content-addressed `edge_id` (`{paragraph_id, paragraph_text, evidence_quote}`, one-to-many), via a new store method `get_written_by_edge_id` + an `edge_id` index (Alembic) + a pure `domain/edge_evidence.py`; a zero-provenance (manually-added) edge → **200 + empty** (not 404). **(2) Verifiable merges:** `CandidateView` gains `target_canonical_name` + a typed `AlternativeView` (type/aliases/sample quote), batched (no N+1). **Design calls made in-build:** typed `AlternativeView` over the untyped dict (visible in `schema.d.ts` for S3b — but this forced reconciling frontend test fixtures, a `/review-pr` §2 catch); enrichment is **best-effort** — a graph-DB outage (or transient enrichment-read failure) degrades to an *unenriched* queue rather than 503ing the Postgres-backed review (the `/code-review` availability-regression fix). **Reviewed** with `/review-pr` (caught the frontend-fixture break + `docs/code` drift) **and** multi-agent `/code-review` (caught the Neo4j-503 regression + two undeclared-500 contract holes + an N+1 — all folded, with tests). **Retro:** the self-review had dismissed the contract holes as "matches the existing pattern"; that reviewer-discipline miss hardened into a new **`/review-pr` §4 lens** — a route that declares 503 must route *every* store read through the guard (an undeclared 500 breaks the contract; a sibling sharing the gap isn't a clearance). No spec amendment; no ADR. All CI green.
- **2026-07-01 — Session 74: Graph-quality S3 (Edge evidence + verifiable merges) decomposed + register RESOLVED (architecture/planning; no production code).** Ran `meta-architect:decompose-requirement` on the branchy S3 slice → `architecture/proposals/graph-edge-evidence.md` (register DM-EE-1..6 / OQ-31); the owner resolved it the same session, one question at a time. **Defining finding (grounded by two code-survey agents):** S3 is a **read/verify** slice whose data is *real but not on the wire* — edge **provenance** (source paragraph(s) + `evidence_quote`) survives commit in Postgres `staged_relations` (`status='written'`, keyed by the content-addressed `edge_id`) but reaches no client and has **no by-`edge_id` read or index**; `GraphCanvas` taps **nodes only**; the merge surfaces show a bare `(100)` + a generic "an existing entity" (`CandidateView` has no `target_canonical_name`). So S3's real work is a small set of new **read** surfaces, **not** new storage and **not** the §4 write-handle. **S3 writes nothing** — INV-1/3/9 untouched; INV-4 constrains DM-EE-4. **Decisions (owner — every entry on the architect's leaned option a, a coherent "leaner reads, verify by evidence" cut):** (a) **DM-EE-1 = focused per-edge BFF read** `GET …/relations/{edge_id}/evidence` (fetch-on-tap); *considered & rejected:* enrich every `GraphEdge` in the whole-graph payload (inflates a dense-graph download with one-to-many data read on a few edges) — the M4.S2 DM-SP-1 precedent. (b) **DM-EE-2 = all `written` `staged_relations` rows by `edge_id`** (+ an `edge_id` index) — the complete one-to-many source set; a zero-provenance edge (manually-added — no staged row) renders "added manually"; *rejected:* the single Neo4j `source_paragraph_id` (lossy — only the triggering paragraph). No offset-resolved sentences (LLM offsets null). (c) **DM-EE-3 = enrich all merge surfaces** (type + aliases + a sample context quote + `target_canonical_name` on `CandidateView`) — **this closes the cross-cutting `CandidateView`-no-target-name item at the S3a build**. (d) **DM-EE-4 = solve the score-100 trap by context, not type-classification** (honest score label + the DM-EE-3 context); *rejected:* a "group/common-noun" type heuristic — needs a closed enum INV-4 forbids and under-serves the general case. (e) **DM-EE-5 = client-side warn-and-offer** against the loaded alternatives on exact-name create (never a hard block — INV-1); backend exact-name lookup deferred unless the top-3 false-negative bites. (f) **DM-EE-6 = backend-first / frontend-second** — **S3a** the two reads test-first, **S3b** the edge panel + merge-context render + the three safeguards (incl. the amber "merge target" highlight fix on *New* cards). **No spec amendment; no ADR** (S3 reads what §3 S3 already scopes; the §4 handle's ADR stays with the first edge-*write* slice, S5/S6). **Handoff correction:** the Session-73 handoff's "S3 consumes the reserved §4 edge handle for addressing" was drift — code-verified there is no surrogate handle built yet; S3 addresses edges by the existing content-addressed id for a *read* (stable because S3 writes nothing). Added the `provenance` glossary term (30→31). Reconciled across `architecture/` (proposal → accepted, OQ-31 struck, INDEX, CHANGELOG, glossary) + this plan. **Next:** build **S3a** test-first from the pure `staged_relations`-by-`edge_id` read + the enriched `CandidateView` projection.
- **2026-07-01 — Session 73: Graph-quality S2 (Navigate the graph) BUILT + merged (PR #168; the milestone's first navigation slice, frontend-only, read-only).** Built straight from the resolved `graph-navigation` proposal, test-first per `frontend/src/AGENTS.md`. **Build-time decisions:** (a) **Two UX sub-decisions below the register, owner-confirmed at build:** name search **folds diacritics** (`mlyn` matches `Młyn` — Polish-first tool; `ł` handled explicitly since NFD doesn't decompose it), and a search match hidden by an active filter shows a **"match hidden by active filters" hint** rather than overriding filters. (b) **Filter state stays in `useState`** per the accepted proposal §5 (and the existing scope-toggle precedent) — `/code-review` flagged the `frontend/AGENTS.md` URL-state convention; owner chose useState-now, **URL-persist deferred to `docs/BACKLOG.md`** (filters + scope together). (c) **Degree measured over the *scoped* graph independent of the type filter** (proposal "But what if") — kept as spec-faithful after `/code-review` flagged the isolated-node-under-type-filter artifact; a k-core-style recompute was rejected as unasked complexity. (d) **`cytoscape-fcose` uses `randomize:true`** — the browser smoke caught that `randomize:false` (my determinism choice) is fcose's *incremental* mode and left the graph clumped; the layout is human-smoke-verified, not asserted, so a non-deterministic seed is correct. **`/code-review` folded (4 findings):** minDegree clamp on refetch, a single de-dangled degree source shared by the slider bound + `filterGraph`, the search/no-match message conflict, `isNodeElement` reuse. **Process (retro, owner-approved):** root `AGENTS.md` "One unit of work per conversation" gains a carve-out — **`/wrap-session` is *not* a separate unit to defer to a new conversation; it closes its session in the *same* conversation** (needs live context for an honest Done line + handoff). *Considered & rejected:* also editing the wrap skill + merge-flow section (redundant; the misread was of the one-unit rule, so one home suffices).
- **2026-06-29 — Session 72: Graph-quality S2 (Navigate the graph) decomposed + register RESOLVED (architecture/planning; no production code).** Ran `meta-architect:decompose-requirement` on the branchy S2 navigate slice → `architecture/proposals/graph-navigation.md` (register DM-GN-1..4 / OQ-30); the owner resolved it the same session. **Defining finding (completeness sweep):** every §3.4 filter runs **client-side over the payload `useStoryGraph` already fetches** — `GraphNode` carries `type`+names+aliases, node degree derives from `edges[]`, and the "by story" axis is *already shipped* as the binary `scope` toggle (DM-MS-2) — so S2 is a clean read-only, frontend-led slice (INV-1/3/9 untouched; INV-4 constrains the type filter — derive options from data, never a closed enum). **Decisions (owner):** (a) **DM-GN-1 = client-side filtering**, no new backend/endpoint; *considered & rejected:* server-side query params (buys nothing at PoC scale, adds a round-trip per keystroke); the finer "one-of-N-stories" filter (per-node membership on `GraphNode`) **deferred-as-YAGNI** — no many-stories-per-project graph foreseen this milestone. (b) **DM-GN-2 = `cytoscape-fcose`** layout (better spread than `cose`, near drop-in, large-graph-fast), `animate:false` for a smoke-verifiable settle; lands via `/add-dependency` (`verify-at-build` the pin's age/OSV); *rejected:* `cose` param-tuning only (the algorithm is the limit, not the tuning). (c) **DM-GN-3 = spec-faithful (option A): S2 is navigation-only; the edge-tap evidence panel stays S3** (where spec §3 already puts it). This resolved a real **spec↔vault conflict** — the accepted parent `graph-curation-surface` proposal had claimed the edge panel was "S2's"; per the vault's source-wins rule that note was the drift and is **reconciled** (its §3 operation-sweep + cross-refs now show the edge panel home as S3). *Considered & rejected:* (b) pull the read-only edge panel into S2 (enlarges S2, needs a §3 stop-and-amend for internal-tidiness gain). No substantive spec change; one tiny §4 wording nit (spec §4's "S2's edge surface" forward-reference) **fixed with owner OK** → "the S3+ edge slices respect it" (S2 is navigation-only; the §4 constraint is unchanged). (d) **DM-GN-4 = AND-combine filters + hide-and-relayout the visible subset + search-as-focus/pan-to** (not a hard filter); clear the selection when a filter hides the selected node. No ADR (frontend read-only slice). Reconciled across `architecture/` (proposal → accepted, parent-proposal drift fixed, OQ-30 struck, INDEX, CHANGELOG) + this plan. **Next:** build **S2** test-first per `frontend/src/AGENTS.md` (pure filter/degree/search functions first, then UI, then the `/add-dependency` layout swap browser-smoke-verified).
- **2026-06-29 — Session 71: Graph-quality S1 built + a retro rule (PR #164 merged).** **Decisions:** (a) **Symmetric recovery (option B)** — fold the *coverage* check (gap / dropped-trailing) into the agent's retried validation alongside the range-overshoot check, so **both** re-prompt rather than only overshoot; the `/code-review` flagged the original asymmetry (overshoot retried, coverage hard-failed 502) as giving the *more important* failure mode the worse UX. *Considered & rejected:* keep coverage terminal-only (the approved-plan default; simpler but inconsistent). (b) **One canonical rule home** — `domain.paragraph_range_problem(ranges, count)` (overshoot + coverage), used by both the agent's retriable `check` and the coordinator's terminal `OutlineCoverageError` backstop; resolves the duplicate-overshoot-rule finding. `proposal_to_outline` keeps a documented slice-safety guard. (c) **`validate_with_retry` gains an optional `check` hook** (post-parse semantic invariant the schema can't express; raises `ValueError` to re-prompt) — reconciled in `validation.py` + `backend/src/story_forge/AGENTS.md` + `docs/code`. (d) **Overlap-rejection (partition check) deferred** to `docs/BACKLOG.md` — overlap is duplication, not loss; out of S1's stated scope. (e) **Retro rule (owner-approved):** live/manual testing against the dev stores must use a disposable name + **delete by exact id after inspecting, never by name** — added to `backend/AGENTS.md` after an S1 smoke named its story `oakhaven`, colliding with the owner's real graph (delete-by-name would have wiped real projects).
- **2026-06-27 — Session 70: a security-treadmill maintenance pass + a process lesson (PR #161 merged; no production-logic code).** **Decisions:** (a) **Drop the starlette waiver by bumping `1.2.0 → 1.3.1`** on its floor date rather than re-dating — both CVEs (54283 HIGH DoS + 54282 LOW) fixed at the source, 1.3.1 soaked (15 days), fastapi has no upper bound. (b) **Waive the fresh ollama `CVE-2026-39832`** (`x/crypto/ssh/agent`, HIGH) on the same unreachability as its 8 wave-6 ssh siblings — owner-approved HIGH waiver (§6.7); *considered & rejected:* chasing an image bump (no soaked ollama tag carries x/crypto ≥0.52.0 — the established waive-on-unreachability pattern). (c) **Combine both fixes into one PR (#161), closing #162** — forced, not preferred: `main` was double-red on the *required* `security` gate (starlette waiver expired on its `ignoreUntil` floor + the fresh ollama CVE), so each single-fix PR left the other red and neither could merge; *considered & rejected:* admin-bypass of branch protection (just tightened in ADR 0009). (d) **Encode the lesson** — `/triage-advisory` drop step now scans `main` for another `security` red before dropping on a floor date, and root `AGENTS.md` Merge-flow gains the "two unrelated reds on the same required gate → combine" exception to the split-unrelated rule.
- **2026-06-26 — Session 69: Graph-quality S0 decompose + register RESOLVED + milestone reshaped (architecture/planning; no production code).** Ran `meta-architect:decompose-requirement` → `architecture/proposals/graph-curation-surface.md` (register DM-GQ-1..7 / OQ-29); the owner resolved it the same session. **Defining finding:** the edit/merge/delete/undo write plumbing already ships (`EntityEditService`, M4.S3a/S3b) but only on the *reader's* panel, so the editing slices are largely UX-surfacing onto the canvas. **Decisions (owner):** (a) **DM-GQ-1 §4 — reserve a stable edge handle now** (an opaque `uuid4` alongside the content-addressed `uuid5` id; content id stays the MERGE/dedup key per ADR 0005, the surrogate is the addressable hook a future temporal/modality qualifier hangs on, preserved through curation; *build no feature* — ADR-worthy, drafted at build); *considered & rejected:* keep content-addressing only (re-keys the future), document-the-obligation-only (the reminder rots). (b) **DM-GQ-2 — predicate work reframed: *naming normalisation*, not edge-joining** (owner steer) — rename a predicate graph-wide to cut synonym noise + an **NLP/embedding *suggest* layer** to find synonymous names; identical-triple collapses are reported, not the goal. (c) **Promote a proactive *dedup-suggest* pass** (re-point the cascade matcher over the *already-accepted* graph to surface duplicate clusters — owner ask) — the entity twin of (b)'s predicate-suggest; both *suggest*, the human commits (INV-1/9 hold). (d) **Pull navigation early + add a layout algorithm** (owner: the graph is an unnavigable hairball *now*) — you can't curate what you can't see, so filters+search+layout land before the curation slices. (e) **DM-GQ-3 canvas UI = editable panel + right-click**, reuse the reader's edit panel. (f) **DM-GQ-7 bulk multi-select → backlog** (S4/S6 suggest passes cover the top bulk need). **Milestone reshaped** (task list above): S1 chunker → S2 navigate → S3 edge evidence → S4 entity dedup-suggest (NEW) → S5 in-place editing → S6 predicate-name normalise+suggest (reframed) → S7 reader loop. Promoted two `docs/BACKLOG.md` items into scope (graph-traversal discovery → S4; predicate proliferation → S6). Reconciled across `docs/specs/graph-quality.md` (§3/§4/§5/§6/§8), this plan, the vault proposal (→ accepted), OQ-29, BACKLOG. **DM-GQ-4/5/6 confirmed as proposed** (reuse endpoints; project-scoped undo-last with preview; the S5a/S5b split).

- **2026-06-26 — Session 68: the Public-readiness → Graph-quality milestone ROLL (governance/planning only — no production code).** Closed Public readiness, opened Graph quality. **Decisions (owner, this session, surfaced by the learning loop's first run):** (a) **wire `review-architecture` into the milestone-roll ritual now** rather than in its own session — the milestone-roll retro showed ADR 0002 §4's "integrate on evidence after living with the vault" condition is met (one useful S58 sweep + the 4th doc-freshness recurrence + real vault drift reaching a roll uncaught); *considered & rejected:* a dedicated session (the handoff's prior lean — folding it in is tighter and the evidence is in), and keep-deferred (one run isn't enough — but the rot evidence settles it). (b) **encode the wiring now, run the first sweep at the next roll** — keeps this roll unit bounded (one-unit-per-conversation); *considered & rejected:* also running the full sweep this conversation (most coherent but spans several units). (c) **add a CI-skip one-liner** to root `AGENTS.md` Merge flow (verify CI gating/skip changes on a scratch PR, never reason about the skipped path) — earned twice (S56, S65). `decompose`/`initialize` stay **event-triggered**, not ritual-wired (no evidence yet).

- _**Earlier decisions (Milestones 1–4 + Public readiness)** — see **[`docs/PLAN_ARCHIVE.md`](PLAN_ARCHIVE.md)**._

### Done in previous sessions

- **2026-07-13 — Session 91: Graph-quality S6 register RESOLVED with the owner (DM-NN-1..6 / OQ-34; PR #205, squash-merged).** The architecture-decision unit that sits between the S6 decompose (S90) and the S6 build — **no production code**, matching the S4/S5 resolve-then-build cadence. Surfaced the six calls in plain language (the proposal's "Gaps for the product owner", not register shorthand) and resolved them one at a time; all six landed on the decompose's proposed lean: **DM-NN-1** one shared synonym-suggest engine + one review list, two forked apply paths (predicate re-key vs type relabel) · **DM-NN-2** fuzzy spelling + local label-string embeddings, recall-first, a `name_normalise_suggest_floor` knob · **DM-NN-3** persist dismissals (reversible; reuse the S4 store / ADR 0010, no fresh ADR) · **DM-NN-4** relationship rename = graph-wide S5b `plan_relation_rekey` in one grouped reversible op, keeps `edge_uid` (INV-10), folds identical-triple duplicates **counted-and-reported** ("merged 2 edges") · **DM-NN-5** type rename = a new bulk `SET n.type` relabel op (INV-9 +1 human-reached writer path) · **DM-NN-6** a dedicated `/stories/:id/normalise-names` list, both vocabularies together, backend-first (S6a) then frontend (S6b). The owner paused on DM-NN-4/5 to clarify what "merge two edges" means; unpacked the edge-identity-is-a-triple model (a rename only folds two *parallel* edges between the **same** node pair; different endpoints never merge) before re-posing. Reconciled across every home in one pass — the proposal → `status: accepted` (banner + each register entry's `✅ Decision`; forward-design body kept for history per the S4/S5 precedent), OQ-34 struck, the INDEX proposal row + Session-90 learning-log pointer flipped, a Decided S91 entry + S6 task-item annotation. **No spec amendment, no ADR** (as anticipated at decompose). Own-`/review-pr` clean — the §2 whole-repo reconciliation sweep confirmed no stale live status homes (remaining "OPEN" strings are protected append-only history or the handoff block). Docs/vault-only → `/review-pr`-class, no `/code-review`; CI green (docs-only path-skip). Retro: reflective pass surfaced no process friction; owner opted to skip the full `/retro`. **Next: build S6a test-first — the shared pure suggest function over a label vocabulary — in a fresh conversation.**
- **2026-07-13 — Session 90: Graph-quality S6 decomposed — step-0 architecture, register OPEN (PR #203, squash-merged).** Back in the Graph-quality sequence after the tooling-extraction detour. Ran the `meta-architect:decompose-requirement` step-0 for **S6 — predicate- AND entity-type-name normalisation + synonym suggestion**: one human-gated *suggest-then-you-decide* pass that will let the author normalise the noisy vocabulary of an over-extracted graph — both the relationship names (`PASSENGER_ON`→`ON_SHIP`) and the entity-type labels (`PERSON`/`Person`, `GROUP`/`group`/…) — with an NLP/embedding layer *suggesting* which names look synonymous and the human deciding each rename (nothing auto-collapses; INV-4). The deliverable is the vault proposal `architecture/proposals/graph-name-normalisation.md` (nine-layer + nine-station pass, an operation-surface completeness sweep, a data-flow diagram, an edge-case enumeration, and the register **DM-NN-1..6** left OPEN for the owner) — **no production code, no spec or plan change.** The architectural payoff was answering the task's central "one engine or two?" at the *storage* layer via a reuse-first code survey: a predicate is stored as the Neo4j relationship type (so renaming it re-keys every edge and rides the `edge_uid` handle S5 reserved), while an entity type is a plain node property (so renaming it is a bulk relabel) — hence **one shared "find the synonyms" engine + two different "do the rename" actions**, not two whole engines nor one leaky one. Added the `controlled-vocabulary` glossary term. Vault-only, so `/review-pr` was the gate (no `/code-review`); the self-review caught and folded one overspecified invariant-count claim. **Retro (owner ran full `/retro`):** turned the caught-twice claim into a standing `architecture/AGENTS.md` convention (proposals don't assert an exact *forward* invariant-enumeration ordinal — the build assigns it). **Next: the owner resolves the S6 register (DM-NN-1..6), one plain-language question at a time, in a fresh conversation; the build slices follow.**
- **2026-07-13 — Session 89: tooling-extraction BUILD slice 5 — SF-side reconcile; the extraction is complete (out-of-sequence unit; PR #201, squash-merged).** The final, docs-only slice of the owner-directed tooling-extraction build: closed out the whole unit in Story Forge's own docs now that the toolchain ships as portable plugins in the public [`yaaisiu/claude-dev-tooling`](https://github.com/yaaisiu/claude-dev-tooling) monorepo and is trial-validated end-to-end (slices 1–4, Sessions 85–88). Three edits: closed the `docs/BACKLOG.md` "Distribute the meta-architect skills" item with a per-slice (1–5) completion summary; flipped the design-note Status banner to **BUILD COMPLETE** and marked §9 sequencing shipped; and — on owner call, after `/review-pr` surfaced it — appended a dated Session-89 completion note to **ADR 0002** (leaving its Session-85 graduation block intact as history). The `/review-pr` **whole-repo reconciliation sweep** confirmed no stale "in-progress" home survives: PLAN_LONG carries no extraction task; PLAN_SHORT's only "remaining slice" language is the handoff (rewritten this wrap); the two residuals it found (ADR 0002's dated block, design-note §3 grounding) are append-only/design-time history correctly left intact. Docs-only, so `/code-review` not required; CI green (code jobs path-skipped, secret-scan/CodeQL pass). **Path A held throughout the extraction** — SF's bespoke `.claude/skills/` rituals were never touched; only `meta-architect` is vendored back into SF. **Process note:** `/resume-session` caught a benign handoff overstatement (a claimed set of BACKLOG per-slice markers that weren't all present) before any edit — the anchor-verification discipline working as designed. **Retro:** skipped by owner call; the reflective pass turned up one mild handoff-overstatement (caught by resume, non-recurring) that didn't clear the bar for a rule change. Next: **back to Graph-quality S6** — predicate- AND entity-type-name normalisation + synonym suggestion (a `meta-architect:decompose-requirement` step-0 first), in a fresh conversation.
- **2026-07-13 — Session 88: tooling-extraction BUILD slice 4 — the trial-install acceptance test (out-of-sequence unit; owner-driven, interactive).** The whole extraction's acceptance criterion (design note §2/§9.4): does "apply the SF treatment" actually work on a real older repo? Trialled the `review-and-integrate` keystone skill against the owner's `image-metadata-extractor` (a Python library carrying four parallel coding-agent stacks). **It works end-to-end** — the skill scanned the repo, recommended a fitting ritual set, wired it in on the owner's selection, and the handoff it seeded read back through `/resume-session` with zero drift. The trial also *sharpened the skill*: five findings fed back via two monorepo PRs (both merged) — establish the real trunk (not the checked-out branch), a three-state `add-dependency` fit, write plugin-enablement to the checked-in settings file, flag local-only green-state, and — the headline — **overlap is not redundancy.** An initial "delete the multi-agent bloat" instruction was *wrong* and the skill's own judgment refused it (the stacks were symlink sources, other-agent tooling a Claude plugin can't replace, and the implementation of a live goal); the skill now classifies pre-existing tooling deliberately and treats removal as a separate, archival, owner-made decision. The target repo got a clean, deletion-free wire-in (PR into its `dev` trunk, owner-gated) with the multi-agent retirement recorded as a decision to make, not taken. **Process observation:** a strong demonstration of the tooling catching the operator's mistakes — `/resume-session` caught a stale-trunk wire-in, `review-and-integrate` refused a destructive deletion. Path A held (SF rituals untouched); no SF production code (SF green-state N/A). **Retro:** reflective pass run — nothing cleared the bar for an SF rule/skill change (the lessons landed upstream in the `dev-rituals` skill via PRs #3/#4; the applicable SF principle "look at the target before deleting" already exists in `CLAUDE.md`); skipped by owner call, as slices 1–3. Next: slice 5 — the shrunk SF-side reconcile (close the BACKLOG distribution item + tidy the design note/plan), then back to Graph-quality S6.
- **2026-07-13 — Session 87: tooling-extraction BUILD slice 3 — the keystone `review-and-integrate` skill, built + shipped (out-of-sequence unit; monorepo PR #2 squash-merged).** Built the genuinely-new piece of the extraction: a `dev-rituals` skill that surveys a target repo and threads the tooling in *deliberately* — scan the repo → recommend which rituals fit (architecture near-always; the security treadmill only where pinned deps/images exist; `review-pr` scaled to whether a spec/plan exists; the session loop if the repo wants it) → the owner selects → wire in (write `.claude/dev-rituals.config.json`, enable the chosen plugins, drop a doctrine snippet naming only the active rituals into `CLAUDE.md`). Human-in-the-loop throughout; writes nothing until confirmed; idempotent on re-run. Modelled on `meta-architect:initialize-project-architecture` (reuse-before-building) rather than invented from scratch. Wired both seams the plugin README names — this skill owns the config writer, and when a repo takes both meta-architect and the session loop it enables them together so the architecture-report producer/consumer forcing-function (`wrap §5c` writes, `resume §3c` triages) closes. Resolved the session's two open items: the `CLAUDE.md` snippet is a template listing only enabled rituals, and fit-detection is a scan→signal table. Also encoded the design nuance that Claude Code toggles a plugin whole, so a repo's active ritual set is scoped by config + the doctrine snippet, not per-skill switches; ungated skills degrade to no-ops. Updated the README table + dropped the "coming next / planned" wording now that the skill ships. **Review:** applied the `/review-pr` lens manually (the skill git-diffs SF's own tree, so it can't target the monorepo diff) — config keys, handoff markers, plugin refs, and section cross-refs all verified, JSON re-validated; a Markdown skill isn't correctness-bearing code so no `/code-review` (consistent with slices 1–2). **Path A held — SF's own rituals untouched;** no SF production code (SF green-state N/A). **Retro:** reflective pass run, nothing cleared the bar for a rule/skill change (skipped by owner call, as slices 1–2); the recurring "dense `PLAN_SHORT` vs the `Read` 25k-token cap" friction is already tracked and its fix (the Graph-quality milestone-roll archive) is correctly deferred until that milestone closes. Next: slice 4 — trial-install the tooling into one older repo (the §2 acceptance test for the whole extraction; owner picks the repo, interactive `/plugin` install).
- **2026-07-13 — Session 86: tooling-extraction BUILD slice 2 — the `dev-rituals` plugin, genericized + shipped (out-of-sequence unit; monorepo PR #1 squash-merged).** Wrapped Story Forge's 8 `.claude/skills/` rituals as a second, portable Claude Code plugin (`dev-rituals`) in the public `yaaisiu/claude-dev-tooling` monorepo, installable alongside `meta-architect` from one marketplace. Each skill separates a portable ritual *skeleton* from a per-repo *profile* supplied by an optional `.claude/dev-rituals.config.json` (every field defaulted → runs with no config); `review-pr` degrades gracefully — full project-fidelity lens when a spec/plan exists, correctness + hygiene when they don't. The 4 light skills gained a config surface; the 4 heavy ones are skeleton+profile rewrites; every earned pattern kept, every SF anecdote dropped. Built by fanning out 8 genericization subagents against the design-note §5 classification table, then self-reviewing all 12 files (JSON valid, frontmatter intact, residue-grep clean). Also noted where the slice-3 `review-and-integrate` skill's two hooks land (config writer; the meta-architect report producer/consumer pairing). **Path A held — SF's own rituals untouched;** these are copies for other repos. No SF production code touched (SF green-state N/A). Retro skipped (owner call — the session ran clean on existing rules). Next: slice 3, the keystone `review-and-integrate` skill.
- **2026-07-13 — Session 85: tooling-extraction BUILD slice 1 — `meta-architect` graduated to its own public repo (out-of-sequence unit; PRs #194 + #195 merged).** Executed slice 1 of the Session-84 design note (§9.1). Created the public monorepo **`yaaisiu/claude-dev-tooling`** (genesis commit) with the `meta-architect` plugin moved in + a root `.claude-plugin/marketplace.json`, so any repo can `/plugin marketplace add yaaisiu/claude-dev-tooling` → `/plugin install meta-architect@claude-dev-tooling`. Reconciled Story Forge onto the design's §7 **vendor-with-provenance** consumption model: SF keeps its in-repo `meta-architect/` copy (settings wiring unchanged), added `meta-architect/UPSTREAM.md` recording the upstream + a deliberate-re-sync policy, updated the README/marketplace status from "incubating" to "graduated; vendored copy", and extended `docs/decisions/0002` with a graduation section (kept **Accepted**, not Superseded — the vendored copy means the in-repo-folder decision still holds). Plugin/marketplace mechanics were confirmed by the `claude-code-guide` agent (reuse-before-building). `/review-pr` ran the whole-repo incubation→graduation reconciliation sweep clean; docs+config only, so no `/code-review`. **Detour:** a freshly-disclosed Go-stdlib CVE (`CVE-2026-39822`, `os.Root` symlink escape) reddened the required security gate on an unrelated docs PR; handled triage-first with an owner-approved condition-based Trivy waiver on pgvector + ollama (compile-time-toolchain CVE, unreachable in this loopback single-user deployment; drop when the images rebuild on Go ≥1.25.12/1.26.5), verified locally, landed as PR #195 first so `main` stayed green. **Lesson:** the design note being *authoritative for the whole unit* made slice 1 self-specifying — reading its §7 resolved an imprecise handoff phrase ("point settings at the graduated source") to the correct behaviour (settings unchanged; SF consumes its vendored copy). **Retro:** skipped (owner) — the reflective pass turned up nothing clearing the bar for a rule/skill change (the imprecise-handoff-phrase and CVE-treadmill both resolved via existing source-of-truth + `/triage-advisory`; the `Read`-on-large-`PLAN_SHORT` friction is a sizing signal to watch at the next roll). **Next:** slice 2 — genericize the `.claude/skills/` rituals into a new `dev-rituals` plugin in the same monorepo (fresh conversation).
- **2026-07-13 — Session 84: tooling-extraction DESIGN PASS — how to ship the Story-Forge toolchain as portable plugins (docs/design; PR #192, squash-merged).** An owner-directed unit *out of* the Graph-quality sequence (escalated from `docs/BACKLOG.md`): design how to package the `meta-architect` plugin + the `.claude/skills/` rituals as portable Claude Code plugins the owner can install into his *other* (mostly-private-going-public) repos — "apply the Story-Forge treatment" as a one-command setup. Ran **interview-first** at the owner's request (which surfaced the keystone requirement below), then a **grounded subagent survey** of every SKILL.md, producing the design note **`docs/design/tooling-extraction.md`**. **What it records:** the current state (the BACKLOG premise is partly stale — `meta-architect/` is *already* a checked-in, self-contained plugin that SF dogfoods via a local `directory` marketplace, so graduating it is "move the folder"); a **skill-by-skill classification** — 🟢 3 already-generic (the meta-architect trio), 🟡 4 light-param (`retro`, `add-dependency`, `pin-image`, `document-code` — soak numbers + paths), 🔴 4 heavy-rewrite (`resume-session`, `wrap-session`, `review-pr`, `triage-advisory` — encode SF's doc topology, toolchain, and layering; need a portable skeleton + per-repo profile); a new **keystone `review-and-integrate` skill** (the owner's "thread it in deliberately, not blindly" ask — modeled on `initialize-project-architecture` per reuse-before-building); and the **owner's four decisions** (D1 one `dev-rituals` plugin · D2 one tooling monorepo · D3 adaptive `review-pr` · D4 stop at design). The **consumption model** resolved the standing Session-76 question: SF vendors its own copies with an upstream provenance pointer, not auto-install. **Reviewed** with `/review-pr` (docs-only, so `/code-review` not required) — folded 4 self-review consistency nits (a light-param miscount, a §9/D2 monorepo contradiction, stray blanks, a stale date). **CI green** — the code-bearing jobs correctly path-skipped for a docs-only PR (spec §6.7 CI cadence). **Retro:** skipped (owner) — the reflection found nothing clearing the bar; the resume drift-catch, interview-first, branch-before-edit, and owner-holds-merge rituals all fired correctly. **Next: the extraction build** — slice 1 (graduate `meta-architect` into the monorepo), per the note's §9, in a fresh conversation.
- **2026-07-10 — Session 83: Graph-quality S5b-fe — the editable edge panel on the canvas, BUILT (production code; PR #190, squash-merged) — S5 is COMPLETE.** Surfaced the three edge affordances on the canvas edge panel — **re-predicate**, **re-target** either endpoint, **delete** — driving the S5b-be atomic `PATCH …/relations/{edge_id}` (preserves the §4 handle / INV-10) and the existing `DELETE`, with the human gate + undo intact (INV-1/3/9). Panel-only, no right-click (DM-S5-4). Mirrors the S5a node pattern: a pure `edgeEditForm.ts` (changed-fields-only patch, ≥1-change + non-blank-predicate validation, dirty guard) → a thin `useEdgeEditForm.ts` → the `EdgeEvidencePanel` extended read-only → editable (read-mode Edit/Delete via a co-located `EdgeDeleteControl`; edit-mode predicate input + endpoint re-target via the **reused `EntityPicker`**). New `useRetargetRelation` hook invalidates the story-graph triad; edge delete reuses `useRemoveRelation`. `GraphViewer` resolves the tapped edge + a `nameOf` lookup (reusing `graphElements.nodeLabel`), keys the panel per edge, **re-points the selection on a re-key** (the new content id), and guards the edge selection against the mid-edit panel-vanish via the **generalized `shouldClearSelection`** (`selectedNodeId` → `selectedId`, now shared by node + edge — DM-S5-6). Test-first for the pure module + hooks + api hook; the panel + 3 new GraphViewer container tests (dirty-guard hold, wrong-target reset, re-key re-point + fold) shaped-with-test. **Reviewed** with `/review-pr` **and** the required multi-agent `/code-review` (high, 4 findings): folded the fold-notice-suppressed-on-evidence-error bug + restored two S3b read assertions the panel-test rewrite had dropped; **refuted** the "invalidate the edge-evidence cache" finding (verified `retarget_relation` never writes `staged_relations`, so a fold doesn't merge provenance into the survivor) and recorded the real re-key-orphans-provenance limitation → `docs/BACKLOG.md`. **Owner-found UX bug fixed before merge:** long source paragraphs pushed the curation actions off-screen → the edge panel's source region scrolls internally (actions stay visible) and the shared `EntityEditPanel` got `overflow-y-auto`+`min-h-0` (active on the canvas, a no-op inside the reader's own scroll wrapper). **489 frontend tests green**, tsc + lint + format + build clean, all CI green; the **owner ran the Oakhaven-scale interactive smoke** (edit an edge → undo → clean, buttons visible). **Process (retro → applied):** a new **`/review-pr` §6 lens** — when a PR rewrites/extends an existing test file, diff old vs new assertions so the extension can't silently drop inherited coverage (the self-`/review-pr` missed the dropped S3b assertions; `/code-review` caught them). **Next: a tooling-extraction unit** (portable `meta-architect` plugin + rituals — owner directive), then Graph-quality **S6**.
- **2026-07-10 — Session 82: Graph-quality S5b-be — the edge-write backend, BUILT (production code; PR #188, squash-merged).** The first edge-*write* slice: a stable `edge_uid` surrogate handle + an atomic server-side edge re-key (edit-predicate / re-target), consuming spec §4's reserved handle so a future relation qualifier / reification can outlive ordinary curation. Story Forge edges are content-addressed (`relation_edge_id = uuid5(subject, predicate, object)`), so changing a predicate or endpoint **re-keys** the edge — until now faked client-side as remove-old + add-new, which mints a new edge each time and drops anything attached to it. **What shipped:** (1) `GraphRelation.edge_uid` (a `uuid4`, default `None`) stored **only** as a Neo4j edge property (via `create_relation`'s `MERGE … ON CREATE SET`, whose no-`ON MATCH` semantics *are* the coalesce rule) — so **no Alembic migration** (`staged_relations` is content-`edge_id`-keyed, untouched); minted forward on every edge writer (relation-decide commit, `add_relation`, the re-key), never back-filled. (2) A pure `domain/relation_rekey.py` (`plan_relation_rekey`, mirroring `plan_merge`'s re-point classification) + `EntityEditService.retarget_relation` doing create-new-then-delete-old as one grouped reversible op preserving the handle (fold onto a collision keeps the survivor's own handle); it **reuses the merge writer's `repoint_relation`/`fold_relation` op-strings**, so `graph_undo` already inverts it (undo restores predicate **and** handle). (3) `PATCH /stories/{id}/relations/{edge_id}` (`RetargetRelationRequest`, ≥1 field; empty body → 422; 404 stale edge / missing endpoint) + OpenAPI & typed-client regen. (4) **ADR 0011** (escalated form — data-model identity boundary) + **INV-10** ("an edge's `edge_uid` survives re-point/re-predicate/merge") minted with enforcer + tests; INV-9 grep set **unchanged** (7th witnessed instance — reuses `create`/`delete_relation`, so only the reachable path grows). Test-first throughout: pure planner → real-Neo4j round-trip + the ON-CREATE coalesce → service-with-fakes + undo round-trips → **real-store end-to-end** (re-key preserves the handle, undo restores it) → API contract; the producer↔inverter contract driven from the re-key's *real* rows. **Reviewed** with `/review-pr` **and** the required multi-agent `/code-review` (high, 6 verified findings, 0 refuted) — the second gate caught a real **data-loss bug the self-review missed** (and had reasoned past): the re-key deleted the old edge *before* creating the new one as two separate Neo4j transactions, so a store failure between them lost the edge entirely → **reordered to create-first** (a mid-failure now leaves a recoverable duplicate, never a missing edge — closing the very no-edge window DM-S5-2 chose the atomic op to avoid). Four more findings folded (an honest crash-window docstring + ADR; a domain blank-predicate guard closing an asymmetry `model_copy` opened; the stale `AddRelationRequest` docstring; skip redundant endpoint reads); one deferred per rule-of-three. **630 tests green** (both tiers), ruff + mypy `--strict` clean, all CI green. **Two follow-ups tracked** (both pre-existing, split out per the merge-flow rule): the `get_story`-outside-the-503-guard systemic gap across all edit routes → cross-cutting; `merge_entities`' edge re-point sharing the same delete-first data-loss window → `docs/BACKLOG.md`. **Retro:** skipped (owner) — the reflection confirmed the required-`/code-review` rule earned its keep again; nothing actionable for the process. **Next: Graph-quality S5b-fe** (the canvas edge affordances — edit-predicate / re-target / delete on the panel, driving the new PATCH op).
- **2026-07-10 — Session 81: Graph-quality S5a — node curation on the graph canvas, BUILT (production code; PR #186, squash-merged).** Brought the entity **edit / merge / delete / undo** write-paths onto the graph canvas (until now a read-only node panel) — pure canvas-surfacing over already-shipped endpoints, no net-new backend (DM-S5-1(B), the resolved S5 register). Extracted a **shared `EntityEditPanel` core** into a new `features/entity-panel/` that the reader *and* the canvas compose: it owns its own `useEntityDetail` fetch (a `GraphNode` carries no `properties`) and namespaces every `data-testid` via a `testIdPrefix`, so `ReaderEntityPanel.test.tsx` (17 tests) stayed green **unchanged** — the regression gate proving no reader behaviour change. Edit-form logic factored out into a pure `entityEditForm.ts` (`initialFormState`/`buildEntityEditPatch`/`canSaveForm`/`isFormDirty`) + a thin `useEntityEditForm` hook (no business logic in the component). Mounted the panel on `GraphViewer` with a story-scoped toolbar `UndoButton` (mirrors the reader); deleted the superseded read-only `NodeDetailsPanel`; recomposed `ReaderEntityPanel` as a thin wrapper keeping occurrences/ego-mini/relations reader-only via `renderReadExtras`. **Panel-vanish guard (DM-S5-6)** = a pure `shouldClearSelection` that holds the selection while an edit is dirty and keeps the just-edited node selected through the post-save refetch (grace ends on a user filter action, not the refetch-driven prune effects). **Owner call — relocated the shared plumbing** (`MergeControls`/`MergeConflictFields`/`mergeConflicts`/`propertiesEditor`/`formatPropertyValue`/`UndoButton`) into `features/entity-panel/` for cleaner layering. Test-first for the two pure modules; components shaped-with-test. **Reviewed** with `/review-pr` **and** the required multi-agent `/code-review` — **both independently caught a wrong-target PATCH**: the panel was re-targeted via the `entityId` prop with no React `key` and no edit-state reset, so switching nodes mid-edit on the still-clickable canvas left the prior node's drafts loaded and Save PATCHed the *wrong* entity (graph corruption, subverting the INV-9 human gate). Fixed by keying the panel per entity (+ keyed the reader's mount, closing the same latent path; + two symptom fixes: delete-confirm carryover and a stuck just-edited panel), with three regression tests. Two `/code-review` findings refuted on verification. **451 tests green**, tsc/lint/format/build clean, all CI green; the **owner ran the interactive browser smoke** at Oakhaven scale (edit a node's type → canvas recoloured; undo reverted — both working). Also: **S6 scope extended to entity types** on the owner's live observation that the type filter is full of casing/synonym duplicates (`PERSON`/`Person`, `GROUP`/`group`/`GROUP_OF_PERSONS`, …) — the same normalisation problem S6 already tackles for predicate names. **Retro:** skipped (owner) — nothing cleared the bar; the review gates caught the one real bug as designed. **Next: Graph-quality S5b-be** (the edge-write backend — §4 `edge_uid` handle + atomic edit-predicate/re-target ops + ADR + the handle-survives-curation invariant, test-first).
- **2026-07-09 — Session 80: Graph-quality S5 DECOMPOSED + register RESOLVED (PR #183; architecture/planning) + a security-advisory triage (PR #184) — two units, owner-sequenced.** **S5 decompose:** ran `meta-architect:decompose-requirement` on the milestone spine ("the graph as an in-place editing surface") → `architecture/proposals/graph-canvas-editing.md` (nine layers, operation-surface sweep over node+edge, data-flow Mermaid, register DM-S5-1..6 / OQ-33, "but what if", plain-language gaps), grounded by two code-level reuse surveys. **Defining finding:** node-editing already ships end-to-end and is wired on the reader (`EntityEditService.{edit,merge,delete,undo}` + endpoints + `lib/api` hooks + `ReaderEntityPanel`/`MergeControls`, all invalidating the reader/story-graph/entity-detail triad) → **S5a is pure canvas-surfacing**; the only net-new plumbing is **edge-editing** (`edit-predicate`/`re-target` have no atomic op — a client remove+add re-keys the content-addressed `relation_edge_id`), so **S5b is the first edge-*write* slice and consumes the reserved §4 handle** (confirmed unbuilt). Owner resolved the register one decision at a time (two overrides of my leans: extract-a-shared-panel over reuse-with-toggles; defer right-click over a droppable ride-along) → build cut **S5a → S5b-be → S5b-fe**; the §4-handle ADR + a "handle survives curation" invariant draft at the S5b build. Added the `direct-manipulation` glossary term (34→35). `/review-pr` clean (vault-only; the §2 decision-reconciliation sweep verified resolved-in-every-home). Reconciled across the vault (proposal→accepted, OQ-33 struck, INDEX, CHANGELOG, glossary, learning-log) + this plan. **Advisory triage:** the scheduled `security` gate reddened on unchanged `main` — two fresh Trivy HIGHs. **CVE-2026-40355** (pgvector/krb5 NULL-ptr-deref DoS) waived as a sibling of the accepted CVE-2026-40356 (same `deb13u1` fix, not yet in a rebuild; no Kerberos service, loopback). Clearing it **unmasked CVE-2026-39831** (ollama/`x/crypto/ssh` FIDO/U2F presence-check bypass) — the sequential-unmask the `/triage-advisory` "scan all three up front" step caught *proactively* (local `aquasec/trivy:0.70.0` scan before pushing), so both waivers rode **one** PR (two reds on the required gate mutually block). Fix-first confirmed non-viable; both owner-approved HIGH waivers (§6.7) with condition-based drop-whens, mirrored in `*.trivyignore` + `WAIVERS.md`. All three images verified clean locally; CI green after a re-run cleared a GitHub runner-starvation cancellation (jobs cancelled at 15m, no logs). `main` back to green. **Retro:** skipped (owner) — reflection turned up nothing actionable; the session validated existing process. **Next: build S5a test-first in a fresh conversation.**
- **2026-07-07 — Session 79: Graph-quality S4b — the duplicate-suggestion review list, BUILT (production code; PR #181, squash-merged) — S4 is COMPLETE.** The author-facing surface for S4a (spec/graph-quality §3 S4, DM-CD-4): a dedicated `/stories/:id/duplicates` list the author works down — each row shows the pair with S3 (DM-EE-3) verification context (name, type, aliases, an in-quote **highlighted** mention), honest name/embedding score labels (DM-EE-4), a pair **counter**, and a Keep/Merge hint. Accept **explicitly picks the survivor** (owner call) and feeds the *existing* merge endpoint unchanged (property conflicts resolved by hand via the extracted-and-shared `MergeConflictFields`); dismiss / un-dismiss persist and reverse a "not a duplicate" (DM-CD-3) with a transient Undo. Suggests only — the human commits every merge (INV-1/INV-9). **Consolidation (owner call):** the identical list/keyboard skeleton the extraction- and relation-review queues each inlined was extracted into a generic **`hooks/useReviewQueue.ts`** and all three review screens migrated onto it (closes the cross-cutting `useReviewQueue` item), pure reducers unchanged. New API hooks (`useDuplicateSuggestions`, `useDismissDuplicate`/`useUndismissDuplicate` + a `delJsonBody` client helper); the mention highlighter is a pure, test-first `markMentions` (apostrophe/quote-normalising). Test-first for the logic, components shaped-with-test; **418 frontend tests pass**, lint + tsc build clean, all CI green. **Reviewed** with `/review-pr` (clean) **and** the required multi-agent `/code-review` (3 real defects folded with tests — a swallowed un-dismiss error, a post-merge details-error flash, the relation reducer field-name asymmetry). **Live-driven** on the real Oakhaven graph (234 suggestions; dismiss→233→un-dismiss→234, state restored). **Two upstream quality findings surfaced by live use → `docs/BACKLOG.md`** (both outside this frontend PR): the `token_set_ratio` name scorer over-matches shared/subset tokens, and this graph has no mention embeddings (extracted without the optional `models` group; NULL vectors per spec §3.3's fail-closed degrade). **Process (retro → applied):** a `frontend/AGENTS.md` "run `npm run format` before committing" note (the `--check` pre-commit hook bounced three commits this session). **Next: Graph-quality S5** (the graph as an in-place editing surface — the milestone spine; sliced S5a node / S5b edge).
- **2026-07-07 — Session 78: Graph-quality S4a — the backend duplicate-suggestion surface, BUILT (production code; PR #179, squash-merged).** Re-points the §3.3 cascade matcher **inward**: a pure `domain/duplicate_clusters.py` self-join scores each already-accepted entity against the others (name via the relocated `name_match_score` **+** embeddings via `cosine_similarity`, floored at a new `duplicate_suggest_floor` knob / the Stage-2 cosine bar) and surfaces ranked likely-duplicate pairs — **suggests only, writes no graph** (INV-1/INV-9); type is a soft ranking signal, never a filter (INV-4); a zero-mention-vector entity falls back to name-only. Shipped test-first: the pure self-join + deterministic pair-id; `GET …/duplicate-suggestions` (assemble the `AcceptedSnapshot` → self-join → drop dismissed → enrich with S3 type/aliases/quote → rank, the O(n²) pass off-loaded via `asyncio.to_thread`); the DM-CD-3 dismissal store (a new staging-side `duplicate_suggestion_dismissals` table via Alembic + `POST`/`DELETE …/dismiss`, reversible, `uuid5`-PK idempotency); the `duplicate_suggest_floor` config knob; OpenAPI + typed client regenerated. To honour the layering rule, the pure `cosine_similarity` + a `name_match_score` core were **relocated out of the matcher into `domain/name_similarity.py`** (one home for the math; the matcher imports them back). **ADR 0010** records the dismissal store; `architecture/invariants.md` + the ADR registry reconciled. **Reviewed** with `/review-pr` (folded a misleading `__all__` + the missing ADR-registry row) **and** the required multi-agent `/code-review` at high (4 verified findings, **all folded** — the event-loop-blocking O(n²) offloaded; `stage1` routed through the extracted helper to complete the "one home" refactor; independent reads `gather`ed; a speculative `actor` column cut). **Live-driven** on the real Oakhaven graph (113 entities → 234 suggestions; caught a genuine `Elara Vance`/`Elira Vance` typo-duplicate; enrichment populated; the zero-vector fallback engaged; dismiss→re-GET dropped the pair, un-dismiss restored it, dismissal table left clean). **Green:** full backend suite (602 passed), `ruff` + `mypy --strict` clean, frontend `tsc` clean, all CI green. **Process (retro → applied):** a new **`/review-pr` §1 "complete-the-extraction sweep" lens** — when a PR extracts a primitive to be its single home, grep the module for every inline copy and confirm all were routed through it (the self-review missed `stage1` left inlined; `/code-review` caught it). **Next: Graph-quality S4b** (the review-list frontend feeding the existing merge).
- **2026-07-06 — Session 77: Graph-quality S4 (Suggest duplicate clusters) DECOMPOSED + register RESOLVED + merged (PR #177; architecture/planning, no production code).** Ran `meta-architect:decompose-requirement` on the branchy S4 slice → `architecture/proposals/graph-cluster-dedup.md` (nine-layer pass, operation-surface completeness sweep, data-flow Mermaid, register DM-CD-1..6 / OQ-32, "but what if", plain-language owner gaps); the owner resolved the register the same conversation, one question at a time. **Defining finding (grounded by a direct reuse survey):** S4 is **not new machinery** — both the cascade matcher (`MatchingAgent` + `AcceptedEntityReader.load_accepted`, which already assembles the accepted entities' names+aliases+mention-vectors; `_rank`/`cosine_similarity` pure & CI-tested) *and* the merge commit path (`plan_merge` → `EntityEditService.merge_entities` → `POST …/entities/{id}/merge`) already ship. So S4 **re-points the matcher inward** — a *self-join*, each accepted entity scored against the others — to *suggest* likely-duplicate entities, feeding each into the **existing** merge: it **suggests, never auto-merges** (INV-1/INV-9 hold; no egress/LLM; INV-4 keeps type a soft ranking signal, never a hard filter). Only net-new logic: a small pure `domain/duplicate_clusters.py` + a read endpoint + a dismissal store. **Resolutions (owner):** DM-CD-1 **pairwise** for V1 (transitive `connected-components` a named later refinement — sidesteps *cluster drift*); DM-CD-2 **eager/recall-first, deterministic** (Stage 1 name + Stage 2 embeddings, ambiguous floor, ranked, a `duplicate_suggest_floor` knob, no live judge); DM-CD-3 **persist dismissals** (a small Postgres pair-store, staging-side, none-at-PoC retention, reversible — the M3 DM-rej precedent; **ADR-worthy → drafts at the S4a build**); DM-CD-4 **a dedicated review-queue-like list** (reuse the S3 DM-EE-3 merge-context; canvas overlay → `docs/BACKLOG.md`); DM-CD-5/6 **S4a backend / S4b frontend split, reuse the merge unchanged, project-scoped**. **No spec amendment at decompose time** (the dismissal store is staging-side plumbing, not a §3 S4 scope change). Added three glossary terms (`entity-resolution`, `connected-components`, `blocking`; 31→34). **Reviewed** with `/review-pr` (clean — the §2 reconciliation sweep verified the decision resolved in every home, catching three stale ones on the resolve-pass: proposal frontmatter `status`, the INDEX parenthetical, the Evidence-station row, all fixed); CI green (docs-PR shape: code jobs path-skipped, secret-scan + CodeQL green). Reconciled across `architecture/` (proposal → accepted, OQ-32 struck, INDEX, CHANGELOG, glossary, learning-log) + this plan (Decided S77). **Also this session (pre-decompose housekeeping):** deleted the stale merged-superseded `security/waive-ollama-cve-39832` branch (local + remote — its waiver already lives on `main` via PR #161), and cross-checked Dependabot against the OSV/Trivy waivers (0 open alerts; the one non-fixed undici advisory is dev-only/`--omit=dev`, matching the CI gate — clean). **Next: build Graph-quality S4a** (backend-first, test-first — the pure `domain/duplicate_clusters.py` self-join first, then `GET …/duplicate-suggestions` + the dismissal store/endpoint + OpenAPI regen; draft the DM-CD-3 ADR at build; `/code-review` is the required gate once real code lands).
- **2026-07-06 — Session 76: Graph-quality S3b — the edge-evidence + verifiable-merge frontend (production code; PR #174, squash-merged) — S3 is COMPLETE.** Brought the S3a backend reads to the human at the decision point (spec §3.4/§3.3), read-only (INV-1/3/9 untouched), no backend/schema change. **Edge evidence:** a `useEdgeEvidence` fetch-on-tap hook, a `cy.on("tap","edge")` handler in `GraphCanvas`, and a new `EdgeEvidencePanel` showing the predicate + every source paragraph/quote (a zero-provenance edge → "no recorded source (added manually)"); `GraphViewer` wires `selectedEdgeId` mutually exclusive with `selectedNodeId` (one details slot), clearing on scope change + filter-hide. **Verifiable merges:** the review card now renders the resolved `target_canonical_name` (closes the standing cross-cutting `CandidateView`-no-target-name item), each alternative's type/aliases/sample quote, and an honest "name match" score (DM-EE-4, no type-classification); DM-EE-5 warns-and-offers before an exact-name duplicate create (never a hard block); the amber affordance is decoupled so a *New* card no longer reads as "will merge". Logic test-first (hook, `alternativesOf`, `exactNameDuplicates`); components shaped-with-test; the canvas edge tap is browser-smoke-verified at Oakhaven scale (186-node real graph — seeded two disposable review candidates against real entities to exercise the queue states, deleted by exact id after). **Reviewed** with `/review-pr` (clean) **and** the multi-agent `/code-review` (high), which caught **four real defects the self-review missed** — a graph-corrupting wrong-merge into the first of N same-named entities, a stuck "select the edge again" error state (a no-op refetch), background-refetch blanking of visible evidence, and invalid `dl` markup — all folded with tests. Frontend 373 tests green; lint + tsc build clean; all CI green. **Also this session:** dropped the expired pydantic-settings OSV waiver by bumping to the now-soaked 2.14.2 (PR #175, fix-first via `/triage-advisory`), taking `main`'s scheduled `security` gate back to green. **Retro → applied:** settled the long-standing S51 question — the multi-agent `/code-review` is now a **required** gate for substantive code (root `AGENTS.md` Merge-flow), on three sessions of consistent evidence.
- **2026-07-01 — Session 75: Graph-quality S3a — backend edge-evidence + verifiable-merge reads (production code; PR #172, squash-merged).** Built the two read surfaces the resolved S3 register scopes (DM-EE-1/2/3), test-first, backend-only. A new `PostgresRelationStore.get_written_by_edge_id` (+ an `edge_id` index migration) + a pure `domain/edge_evidence.py` assembly back `GET /stories/{id}/relations/{edge_id}/evidence`, which returns an edge's predicate + every `written` source paragraph/quote (one-to-many; a manually-added edge → 200 + empty "added manually", not 404). `CandidateView` gained `target_canonical_name` + a typed `AlternativeView` (type/aliases/sample context quote), batched to avoid an N+1; enrichment is best-effort so a graph-DB outage degrades to an unenriched queue rather than blocking the human review. Pure boundaries unit-tested, store + routes integration-tested; OpenAPI regenerated (`openapi.json` + `schema.d.ts`). **Reviewed** with `/review-pr` (caught a blocking frontend-CI break — the typed `alternatives` contract broke two test fixtures, the §2 "changed shape has consumers" hazard — plus `docs/code` reference drift; both folded) **and** multi-agent `/code-review` at high (caught a real availability regression — a Neo4j outage 503'ing the whole Postgres-backed queue — two undeclared-500 error-contract holes, and an N+1; all folded with tests). Backend 573 + frontend 341 tests pass; mypy/ruff/build clean; all CI green. **Lesson (retro → applied):** the self-`/review-pr` dismissed the contract holes as "matches the existing pattern" and missed the availability regression the second gate caught — so a new `/review-pr` §4 lens now names the inverse contract-hole (a declared-503 route must route *every* store read through the guard; a sibling sharing the gap is not a clearance), and this is another data point toward making `/code-review` a required gate for substantive backend code (S51 question). **Next: Graph-quality S3b** (frontend edge-tap + `EdgeEvidencePanel` + merge-context render + the DM-EE-4/5 safeguards + the amber-highlight fix).
- **2026-07-01 — Session 74: Graph-quality S3 (Edge evidence + verifiable merges) DECOMPOSED + register RESOLVED + merged (PR #170; architecture/planning, no production code).** Ran `meta-architect:decompose-requirement` on the branchy S3 slice → `architecture/proposals/graph-edge-evidence.md` (nine-layer pass, operation-surface sweep, data-flow Mermaid, register DM-EE-1..6 / OQ-31, "but what if", plain-language owner gaps); the owner resolved the register the same conversation, one question at a time. **Defining finding (grounded by two parallel `Explore` code-survey agents):** S3 is a **read/verify** slice whose data is *real but not on the wire* — edge **provenance** (source paragraph(s) + `evidence_quote`) survives commit in Postgres `staged_relations` (`status='written'`, keyed by the content-addressed `edge_id`) but reaches no client and has **no by-`edge_id` read or index** (`postgres_relation_store.py:94-112`); `GraphCanvas` taps **nodes only** (`GraphCanvas.tsx:106`); the merge surfaces show a bare `(100)` + a generic "an existing entity" (`CandidateView` has no `target_canonical_name`). So S3's real work is a small set of new **read** surfaces, **not** new storage and **not** the §4 write-handle. **S3 writes nothing** — INV-1/3/9 untouched; INV-4 constrains DM-EE-4. **Resolutions (owner — every entry on the leaner option, a coherent "leaner reads, verify by evidence" cut):** DM-EE-1 focused per-edge BFF evidence read (fetch-on-tap); DM-EE-2 all `written` `staged_relations` by `edge_id` (+ index; zero-provenance → "added manually"); DM-EE-3 enrich all merge surfaces (type+aliases+quote+`target_canonical_name` — **closes the cross-cutting `CandidateView`-no-target-name item at the S3a build**); DM-EE-4 solve the score-100 trap by context, not type-classification (INV-4-safe); DM-EE-5 client-side warn-and-offer dup-create gate; DM-EE-6 backend-first (S3a) / frontend-second (S3b, amber-highlight fix rides). **No spec amendment; no ADR.** Added the `provenance` glossary term (30→31). **Handoff correction:** the S73 handoff's "S3 consumes the reserved §4 edge handle" was drift — code-verified no surrogate handle is built; S3 reads by the content id (the handle is an edge-*write* concern, S5/S6). **Reviewed** with `/review-pr` (clean — §2 reconciliation verified home-by-home; one INDEX nit folded); CI green (docs-PR shape: code jobs path-skipped, secret-scan + CodeQL green). Reconciled across `architecture/` (proposal → accepted, OQ-31 struck, INDEX, CHANGELOG, glossary) + `docs/PLAN_SHORT.md` Decided S74. **Next: build Graph-quality S3a** (backend-first, test-first — the `staged_relations`-by-`edge_id` read + `edge_id` index, and the `CandidateView` enrichment + `target_canonical_name`).
- **2026-07-01 — Session 73: Graph-quality S2 — navigate the graph, BUILT (PR #168 merged; the milestone's first navigation slice).** A dense graph (real Oakhaven: 186 nodes / 286 edges, 46 type variants) was an unnavigable `cose` hairball; S2 makes it navigable, **client-side over the payload already fetched** (no backend/schema/endpoint/LLM change; read-only, INV-1/3/9 untouched). Shipped (spec §3.4, `frontend/src/AGENTS.md` test-first): a pure `graphFilters.ts` — `nodeDegrees`/`elementDegrees`/`distinctTypes`/`filterGraph` (AND-combine type + connection-density) + diacritic-folding `matchNodes` — plus a shared `useDebouncedValue` hook, all unit-tested first; `GraphViewer` runs the pipeline and hands a filtered element set + search-match ids to a thin `GraphCanvas`, which swaps `cose` → the **`cytoscape-fcose`** layout extension. Filters hide-and-relayout the visible subset; name search highlights + pans-to (never a hard filter); "0 of N — clear filters" and "match hidden by active filters" affordances; selection clears when a filter hides the selected node; type options prune gracefully across a refetch. **Reviewed** with `/review-pr` (self) **and** multi-agent `/code-review` (high) — the latter caught **4 real findings the self-pass missed/dismissed** (minDegree clamp, single degree source, message conflict, helper reuse), all folded with new tests. **`cytoscape-fcose@2.2.0`** pinned via `/add-dependency` (1260 d old, OSV-clean). **Browser-smoke-verified at Oakhaven scale** — which caught a real layout bug (`randomize:false` clumped the graph; fixed to `randomize:true`); all filters/search/affordances confirmed live. **Green:** 341 frontend tests, tsc/eslint/prettier/dep-age all clean, CI fully green (code-bearing PR — frontend/backend/security/CodeQL all pass). Refreshed `docs/code/frontend-features.md` for the new modules. Two follow-ups filed to `docs/BACKLOG.md` (URL-persist graph view state; label crowding on an all-disconnected filtered set). **Process (retro):** root `AGENTS.md` gains a carve-out — `/wrap-session` is not a deferrable unit; it closes its session in the *same* conversation. **Next: Graph-quality S3** (edge evidence + verifiable merges).
- **2026-06-29 — Session 72: Graph-quality S2 (Navigate the graph) decomposed + register RESOLVED + merged (PR #166; architecture/planning, no production code).** Ran `meta-architect:decompose-requirement` on the branchy S2 navigate slice → `architecture/proposals/graph-navigation.md` (nine layers, stations mostly `n/a` — read-only, data-flow Mermaid, register DM-GN-1..4 / OQ-30, the "but what if" pass, a build hand-off). The owner resolved the register the same conversation (see Decided S72). **Defining finding (completeness sweep):** every §3.4 filter runs **client-side over the payload `useStoryGraph` already fetches** — `GraphNode` carries `type`+names+aliases, node degree derives from `edges[]`, the "by story" axis is already shipped as the binary `scope` toggle — so S2 is a clean read-only, frontend-led slice (no new backend; INV-1/3/9 untouched; INV-4 constrains the type filter). **Resolutions:** DM-GN-1 client-side filtering; DM-GN-2 `cytoscape-fcose` layout (pin at build); DM-GN-3 spec-faithful — S2 navigation-only, the edge-tap evidence panel stays S3; DM-GN-4 AND-combine + hide-and-relayout + search-as-focus. **DM-GN-3 caught and reconciled a real spec↔vault drift** — the accepted parent `graph-curation-surface` proposal had claimed the edge panel was "S2's"; per the source-wins rule that note was the drift and was fixed (its §3 operation-sweep + cross-refs → S3), plus a tiny spec §4 wording fix ("S2's edge surface" → "the S3+ edge slices respect it") with owner OK. **Reviewed** with `/review-pr` (clean — the §2 reconciliation sweep verified the decision resolved in every home); CI green (docs-PR shape: code jobs path-skipped, secret-scan + CodeQL green). **Process (retro):** added a root `AGENTS.md` Merge-flow rule + a `/resume-session` pointer — **branch before the first edit of a unit, and announce the plan** — after the whole S2 decompose ran as uncommitted edits on `main` until the owner asked whether it had been branched/PR'd. **Next: build Graph-quality S2** (frontend-led, test-first — pure filter/degree/search functions, then UI, then the `/add-dependency` `cytoscape-fcose` swap, browser-smoke-verified).
- **2026-06-29 — Session 71: Graph-quality S1 — stopped silent data loss in the auto-chunker (PR #164 merged; the first build slice of the milestone).** The auto-chunker (and per-span hybrid) could drop trailing paragraphs or leave a gap and report success, silently losing text — and 502'd with no retry on a trivial off-by-one overshoot. Fix (spec `docs/specs/graph-quality.md` §3 S1, TDD failing-test-first): one canonical range invariant `domain.paragraph_range_problem` (overshoot **or** a coverage hole; overlap allowed — duplication, not loss) folded into the agent's retried validation via a new optional `check` hook on `validate_with_retry`, so **both** a coverage gap and an overshoot **re-prompt** rather than fail; the coordinator re-asserts it as a terminal `OutlineCoverageError` backstop (→ the route's already-declared 502). No API/OpenAPI/migration change. **Reviewed** with `/review-pr` (clean) + multi-agent `/code-review` (high, 35 agents): the review caught the original recovery *asymmetry* (overshoot retried, coverage hard-failed) — the owner chose **option B** (symmetric retry), which also resolved the duplicate-overshoot-rule finding via the shared domain home; two doc homes reconciled, overlap-rejection deferred to `docs/BACKLOG.md`. **Live-verified** on the branch before merge: a real Ollama Cloud `auto` chunk (5 paragraphs in → 5 persisted, no loss — the auto path's first live exercise) + a controlled-provider demo through the real wired code showing re-prompt-then-recover, give-up→502, and the coordinator backstop→502. Test data cleaned up by exact id afterward. **Green:** 555 backend tests pass, ruff/mypy clean, CI fully green. **Process (retro):** added a `backend/AGENTS.md` rule for live-test data hygiene (disposable name + delete-by-id) after the test story collided by name with the owner's real `oakhaven` graph. **Next: Graph-quality S2** (navigate the graph — filters + search + a better layout).
- **2026-06-27 — Session 70: a security-treadmill maintenance pass — dropped the starlette waiver and waived a fresh ollama CVE, folded into one PR by a required-gate constraint (PR #161 merged; no production-logic code).** `/resume-session` step 3b flagged the starlette OSV waiver as due on its 2026-06-27 floor; `/triage-advisory` drove the drop — bumped `starlette` 1.2.0 → 1.3.1 (`/add-dependency`; 15-day soak, OSV-clean, age-gate green, 363 unit tests green, app imports), clearing CVE-2026-54283 (HIGH DoS) + CVE-2026-54282 (LOW) at the source and removing both `[[IgnoredVulns]]` blocks + the `WAIVERS.md` section. Opening the PR surfaced an **unrelated** fresh red: `ollama/ollama:0.24.0` carried a new `golang.org/x/crypto/ssh/agent` HIGH (**CVE-2026-39832**), waived (owner-approved §6.7) on the same unreachability as its 8 wave-6 ssh siblings (ollama opens no SSH agent client). The discovery that **`main` was double-red on the *required* `security` gate** — the starlette waiver having *expired* on its `ignoreUntil` floor that very day, plus the ollama CVE — meant neither single-fix PR could go green (each left the other red), so #162 (ollama) was folded into #161 (starlette) and the two landed as one combined PR. `/review-pr` clean on the starlette change; CI green after a Docker-Hub-flake re-run. **Process lesson (retro):** dropping a waiver on its floor date can collide with another treadmill red on the same required gate → `/triage-advisory` now scans for a second `security` red before dropping, and root `AGENTS.md` Merge-flow gains the "two unrelated reds on one required gate → combine" exception. **Also landed the long-uncommitted Session 69 S0 work in this same wrap** (it had been done in a prior conversation but never committed). **Lesson:** the resume ritual earned its keep twice — it flagged the due waiver *and* caught that a whole prior session (S0) sat uncommitted with a stale handoff. **Next: Graph-quality S1** (the chunker completeness check — first build slice).
- **2026-06-26 — Session 69: Graph-quality S0 decompose — the in-place graph-curation surface designed and the milestone reshaped (architecture/planning; no production code; landed 2026-06-27 with the Session 70 wrap).** Ran `meta-architect:decompose-requirement` on "the graph view as a direct in-place curation surface" → `architecture/proposals/graph-curation-surface.md` (data-flow, register DM-GQ-1..7, OQ-29, the "but what if" edge-case pass, affected components, S-slice boundaries) + two glossary terms (`reification`, `surrogate-key`). The owner resolved the whole register the same session (see Decided). **Defining finding:** the edit/merge/delete/undo write plumbing already ships (`EntityEditService`, M4.S3a/S3b) but only on the *reader's* panel — so the editing slices are largely UX-surfacing onto the canvas, not net-new write plumbing. Reshaped the milestone (S1 chunker → S2 navigate → S3 edge evidence → S4 entity dedup-suggest → S5 in-place editing → S6 predicate-name normalise+suggest → S7 reader loop), reserved the §4 edge handle, and reconciled across `docs/specs/graph-quality.md` (§3–§8), this plan, the vault proposal (→ accepted), OQ-29, and `docs/BACKLOG.md` (two items promoted into scope). **Lesson:** a decompose's value showed in the reshape — pulling navigation early and reframing the predicate work as naming-normalisation both came from walking the nine layers before slicing. **Next was Graph-quality S1** (the first build slice).
- **2026-06-26 — Session 68: the Public-readiness → Graph-quality milestone roll (governance/planning; no production code).** Ran the roll per `docs/AGENTS.md §7` / `/wrap-session §5c`. **(1) Mined before moving** — `/retro` in milestone-roll mode, its first-ever run: a `general-purpose` agent read the whole Public-readiness record as a corpus and surfaced 5 candidate patterns; 3 were already-absorbed wins (the survey-disproves-its-own-plan pattern → already caught by `/resume-session`'s recompute rule; reuse-before-building-tooling → already in `CLAUDE.md` and it *fired on its own offspring* in S67; the stranger-reads-cold lens → earned once), 2 cleared the bar → the two owner decisions above. **(2) Applied the outcomes** — wired `review-architecture` into the roll + added the CI-skip rule, reconciled across every non-vault home in one pass (ADR 0002 Update, root `AGENTS.md`, `architecture/AGENTS.md`, `/resume-session §3c`, `/wrap-session §5c`, `docs/AGENTS.md §7`); writer-restricted vault notes deferred to the first wired sweep. **(3) Archived** Public readiness byte-exact → `docs/PLAN_ARCHIVE.md` (25 dated bullets + 3 resolved cross-cutting items + the M4→Public-readiness curation note; `PLAN_SHORT` 109 KB → 32 KB, verified 0 dated bullets left + archive bullet count 145 → 170). **(4) Re-curated** cross-cutting/Blocked by hand (16 live carried with Graph-quality ties, 3 resolved struck-and-moved) and **opened the Graph-quality skeleton** (S0 decompose → S5) around `docs/specs/graph-quality.md`, correcting the "opens with a backlog triage" framing to "opens with S0"; updated `PLAN_LONG` milestone status. **Lesson:** the learning loop's first run did exactly its job — a pattern invisible to any single session (an architecture sweep with no forcing function, recurring as the 4th doc-freshness miss) only became actionable when a whole milestone was read at once, and it immediately hardened into a ritual. **Next: Graph-quality S0** — `decompose-requirement` on the in-place curation surface.
- _**Earlier sessions (Milestones 1–4 + Public readiness)** — see **[`docs/PLAN_ARCHIVE.md`](PLAN_ARCHIVE.md)**._

---

## How the two skills use this file

- **`/resume-session`** (session start) reads the **Session handoff** block, verifies the on-disk anchors it names, runs `git log`/`git status`, opens the spec sections listed, and confirms reality matches before work begins.
- **`/wrap-session`** (session end) runs the green-state checks (report-only), checks off completed tasks, strikes (does not delete) obsolete ones, appends a dated line to **Done in previous sessions**, refreshes **Blocked / questions** and **Decided**, and rewrites the **Session handoff** block to point at the next session. It then reminds you to commit on a feature branch (squash-merge hygiene).
- **When a spec change happened:** both skills stop and require `story-forge-poc-spec.md` + `PLAN_LONG.md` to be reconciled with this file before continuing.
