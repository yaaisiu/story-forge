# PLAN_SHORT.md — Current milestone tactical plan

> Updated every working session. Read at start (`/resume-session`); update at end (`/wrap-session`).

<!-- ───────────────────────────── HANDOFF ─────────────────────────────
This block is the contract between `/wrap-session` (writes it) and
`/resume-session` (reads it). It always describes where the NEXT session
should start. Keep it short and literal. Do not delete the markers.
────────────────────────────────────────────────────────────────────── -->

## ▶ Session handoff (read this first)

- **Next step:** **build M4.S3c-be (backend) test-first — the slice is fully scoped, the register is RESOLVED, no design left.** M4.S3c = manual tag / un-tag / change-boundaries in the reader (spec §3.5), the **final** "manual correction in the reader" slice. Resolved model (owner, S44 — see Decided): **"save only what you touch"** — keep render-time search for the auto highlights; a **manual tag persists a real stored span** that overlays + wins over search; a rejected highlight writes a **suppression** the resolver subtracts; change-boundaries **materializes** that one occurrence (no backfill). Tagging can **create a new entity** (not just attach). Tag/un-tag/boundary ride the **S3b `graph_edits` undo** (new op-kinds + inverters). **First failing test = the pure reconciling resolver:** extend `domain/highlights.py` so `(paragraph, searched targets, stored manual spans, suppressions) → final non-overlapping ranges` under **manual-wins-then-longest-match**, each carrying `source` + identity (DM-S3c-6). Then the mention/tag mutators + endpoints (tag-existing, tag-new-entity, suppress=“not an entity”, re-assign=“not this entity”, re-bound=“change boundaries”) with `graph_edits` recording + the new undo op-kinds (contract-test the inverters from the writer's *real* output — the PR-#108 producer↔consumer discipline), then OpenAPI snapshot + typed-client regen. That's **M4.S3c-be**; **M4.S3c-fe** (next) brings in **Tiptap** (owner override DM-S3c-7 — `/add-dependency` for the pin first, §6.7) + selection + right-click menu + tag picker. **ADR 0008** (the storage model + the search∪manual−suppression reconciliation contract + INV-9 enumeration growth for the new-entity-from-tag writer) drafted at build, on confirmation. A small **§6.4 data-model note** (human-authored mentions carry real offsets) goes through stop-and-amend at build *if the migration needs it*; **no §3.5 capability amendment** (S3a precedent).
- **Read before starting:** this file (the **Session-44 Decided** entry (5) = the resolved DM-S3c-1..9 register, the build spec; the **Done** top line; **M4 feature order item 5** = M4.S3c); **`architecture/proposals/m4-s3c-manual-tagging.md`** (the full decompose — resolution banner at top is the build spec; the data-flow + "but what if" + hand-off); **`m4-inline-highlights.md`** (DM-IH-1 — *why* highlights are render-time search with NULL offsets today, the thing S3c reconciles with); **spec §3.5** (manual tagging + the right-click correction menu) + **§6.4** (entity_mentions schema) + **§3.2** (offsets). Code to read: **`backend/src/story_forge/domain/highlights.py`** (the `resolve_highlights` resolver to extend — the first failing test), **`api/stories.py`** (the `/stories/{id}/reader` endpoint + `ReaderHighlight` model to extend with `source`/`mention_id`), **`adapters/postgres_repo.py`** (`insert_entity_mention` + the mention queries; spans are written NULL today), **`agents/candidate_review.py`** (where the accept path writes a mention with NULL offsets), **`agents/entity_edit.py`** + **`domain/graph_undo.py`** (the `graph_edits` writer + `invert_operation` to extend with new op-kinds). Conventions: root `CLAUDE.md` (spec-and-test-driven; **two-gate `/review-pr` + `/code-review` on substantive write-paths**; owner-holds-merge-button; the **new** plain-language register-surfacing rule); **`backend/src/story_forge/AGENTS.md`** — the producer↔consumer contract-test rule (directly governs the new undo op-kinds) + the API-route `responses=` rule (declare every non-2xx). **ADR 0008 drafted at build — never write an ADR without explicit confirmation.**
- **Verify on disk:** on **`main`** after the Session-44 close commit. **Present (S44 design landed):** `architecture/proposals/m4-s3c-manual-tagging.md` exists, `status: accepted`, register **RESOLVED** (grep `DM-S3c` / `OQ-26 — .*RESOLVED`); `architecture/glossary/materialization.md` exists; `backend/src/story_forge/domain/highlights.py` has **`def resolve_highlights`** (the pure resolver to extend); `backend/src/story_forge/domain/graph_undo.py` has **`def invert_operation`** (the undo machinery to extend); `entity_mentions.span_start`/`span_end` columns exist but are **NULL-valued** (render-time search today — verify via `adapters/postgres_repo.py` `insert_entity_mention`, *not* by assuming). **Absent (M4.S3c-be/-fe create):** `ReaderHighlight` (in `api/stories.py`) has **no** `source`/`mention_id` field yet; **no** manual-tag / suppression endpoint (no `POST …/mentions`-style route in `api/stories.py`); **no** Tiptap/ProseMirror dep in `frontend/package.json` yet (the fe slice adds it via `/add-dependency`). **Security waivers (neutral floor dates — `/resume-session` step 3b computes drop-readiness against the resume day's date, don't inherit any verdict):** starlette **GHSA-jp82-jpqv-5vv3** droppable on/after **2026-06-25** and **CVE-2026-54283** on/after **2026-06-26** — both cleared by one bump to **starlette 1.3.1** via `/triage-advisory`; **pydantic-settings** droppable on/after **2026-07-04** (→ 2.14.2); torch (no fix); `infra/trivy/neo4j.trivyignore` `CVE-2026-50010` (condition-based — re-scan, not date). None blocks M4.S3c.
- **Last session ended:** 2026-06-22 — **Session 44 (design/planning only — no production code): M4.S3c scoped + decomposed + register RESOLVED; M4 multi-story re-scoped.** Decomposed M4.S3c → `m4-s3c-manual-tagging.md` (`accepted`, DM-S3c-1..9 resolved): storage model = **"save only what you touch"** (incremental stored spans + suppressions over the existing render-time search — no migration), tagging **creates entities**, corrections are **undoable**, **Tiptap adopted now** (owner override). New glossary term `materialization`; ADR 0008 at build. **Scope decisions:** world-graph multi-story **OUT of PoC** → `docs/BACKLOG.md`; narrowed multi-story (new story + project graph + per-story entity membership) stays in PoC (concretizes §3.4 scoping; §3.6/§9 stop-and-amend pending); **code-doc-generation** → backlog. **Retro:** one rule added — root `AGENTS.md`, translate register entries to plain language before asking the owner (earned: the first storage-model question was jargon-packed). **Gap-check** (`/review-pr` §2 sweep over the docs diff) caught + fixed a stale `PLAN_SHORT` "register OPEN" + two stale world-graph forward-refs. **Green:** docs/vault only — no code, no deps; code gates are no-ops.
- **Open blocks/questions:** **Nothing blocks M4.S3c-be — the register is resolved; build test-first.** At build: **ADR 0008** (storage model + reconciliation contract + INV-9 enumeration growth) on confirmation; the **§6.4 data-model note** via stop-and-amend *only if* the migration needs it; **Tiptap via `/add-dependency`** at the fe slice (exact pin, ≥14 days old). **Deferred to the LATER multi-story slice (not S3c):** the **§3.6/§9 stop-and-amend** (world-graph out, multi-story narrowed) **must land before** that slice's code; the **§3.4 story-vs-project scoping** cross-cutting (now == the in-PoC multi-story direction: per-story entity membership drives the "this story / whole world" toggle). **Cross-cutting still live for M4:** **orphaned-sandbox cleanup** (ties multi-story / delete-story); the **review-queue UX nits** (`CandidateView` merge-target-name, per-screen error-to-string mappers n=5, shared `useReviewQueue`) — fire only when a *review queue* next changes (S3c touches the reader, not the queues). **`docs/BACKLOG.md`** holds the post-PoC list (incl. the new **code-doc-generation** + **world-graph multi-story** items; undo/delete V1 robustness; S3a/S3b edit-UX nits; homonymy; graph filters; bulk accept). **Carry-forwards:** per-mention provenance for triple-deduped edges (ADR 0005); §6.5 model-override dropdown (OQ-14); observability/logging (OQ-15); envelope-unwrap dup; 422-overload remap + window-and-stitch; re-structure delete-and-replace; **Issue #22**; **Issue #31**; M2.S1 review nits; meta-architect ritual-wiring (deferred per ADR 0002).

<!-- ─────────────────────────── END HANDOFF ──────────────────────────── -->

## Current milestone: Milestone 4 — V1 polish

**Goal (spec §9 M4 / `docs/PLAN_LONG.md`):** the "V1 polish" set — **inline highlights**, side
panel, manual annotation, properties/relations edit, **multi-story**, ~~world-graph parent~~ — that turns
the working ingest→graph pipeline into a usable V1 authoring surface. **Scope narrowed 2026-06-22 (owner,
Decided S44): the world-graph parent (cross-story world merge, §3.6) is OUT of PoC → `docs/BACKLOG.md`;
multi-story narrows to "add a new story that reuses the project graph + per-story entity membership"
(no cross-story merge). The spec §3.6/§9 still describe the world graph — reconciling them is a pending
stop-and-amend before the multi-story slice.** Two seams M3 deferred **graduate
to live M4 work**: the **§3.4 graph story-vs-project scoping** (multi-story breaks the one-story-per-
project assumption the graph route rests on) and **DM-Rel-5**'s written-edge re-point (an
accepted-entity↔entity merge, first possible in M4, re-points an already-written edge). INV-2's
consent gate stays **deferred past M3** (persona-justified).

**First slice — M4.S1 inline highlights** (owner-chosen, 2026-06-17 roll). Decomposed step-0:
`architecture/proposals/m4-inline-highlights.md` (register **OPEN — DM-IH-1..8 / OQ-21**). A
**read-only projection** of the accepted graph (spec §3.5): render the story text, highlight accepted
entities inline (colour-by-type), hover→tooltip. **Centre of gravity = DM-IH-1 span resolution** —
`entity_mentions` carry **null char offsets**, so highlighting is a *where-does-this-entity-sit* problem
(render-time string search vs persisting real spans vs hybrid; PL inflection is the headline gap).
Side-panel, manual annotation, properties/relations edit, multi-story, world graph are **later** slices.

**Feature order** (sliced at the 2026-06-17 roll; later slices sequenced as decomposed):

1. **M4.S1 — inline highlights** (read-only reader + accepted-entity highlights + hover tooltip; spec §3.5).
   ✅ **DONE — backend (Session 32, PR #81) + frontend (Session 33, PR #86), live-verified.** Register
   resolved with the owner — DM-IH-1 (render-time search over name+aliases, *verify-first*: no stored
   span exists to persist), DM-IH-2 (new `GET /stories/{id}/reader`), DM-IH-3 (plain `<mark>` renderer,
   not Tiptap), DM-IH-4 (longest-match), DM-IH-7 (accepted-only), DM-IH-8 (name+type+aliases), DM-IH-5
   (reader-local fixed palette + hash fallback + legend, owner's call S33), DM-IH-6 (whole-story render;
   measured fine on the Oakhaven draft). Frontend = `features/text-reader/` (pure splitter + palette +
   `useReader` hook + `<mark>` renderer + tooltip + legend + route + graph link), test-first; both review
   passes ran. **Verified end-to-end by the Session-33 Oakhaven smoke test** (upload → chunk → extract →
   review/dedup → relations → reader), which surfaced ~12 post-PoC items now in `docs/BACKLOG.md`.
   _Next: side panel → manual annotation → properties/relations edit (incl. the DM-Rel-5 written-edge
   re-point) → multi-story + §3.4 scoping → world graph. The smoke test argued for prioritising
   manual correction in the reader (it's where errors surface) — confirm slice order at resume._

2. **M4.S2 — entity side panel in the reader** (read-only inspection; spec §3.4 detail panel + §3.5
   "local graph around that entity"). **Owner-chosen 2026-06-18 (Session 34)** over manual-correction-
   in-reader — the read-only inspection surface the next slice's corrections build on. Decomposed
   step-0: `architecture/proposals/m4-side-panel.md` (register **DM-SP-1..8 resolved / OQ-22**). Click a
   highlighted entity → side panel with its details (canonical/aliases/type/**properties**/occurrences/
   relations/timeline) + a **1-hop [[ego-graph]]** mini-view. **Centre of gravity = DM-SP-1 data
   source → a focused per-entity endpoint** `GET /stories/{id}/entities/{eid}` (BFF: server-side
   cross-store join + the 1-hop neighbourhood + occurrences; needs a new `Neo4jRepo` 1-hop query),
   which **splits the slice** (DM-SP-7):
   - **M4.S2a — backend** ✅ **DONE (PR #89, 2026-06-18, Session 34).** `domain/neighbourhood.py`
     (pure `build_ego_graph`) + `Neo4jRepo.get_neighbourhood` (1-hop, project-scoped) +
     `GET /stories/{id}/entities/{eid}` (details + `properties` + ego-graph), test-first (8 unit + 5
     integration). Build-time refinement: occurrences are **not** in the endpoint — DM-SP-3 makes them
     the reader's rendered highlights, so S2b derives them client-side. `/code-review` folded one
     should-fix (project-scope the neighbourhood query).
   - **M4.S2b — frontend** ✅ **DONE (PR #91, 2026-06-18, Session 35).** `occurrences.ts` (pure,
     occurrences from rendered highlights) + `egoElements.ts` (pure, ego-bundle → cytoscape elements) +
     `useEntityDetail` hook + `ReaderEntityPanel` + `EgoGraphCanvas` + `ParagraphText`/`TextReader`
     wiring (click-to-open, neighbour navigation, occurrence drill → scroll + flash), test-first; 193
     frontend tests green. **DM-SP-4 resolved = cytoscape** (browser-verified live; neighbour-node
     inspection + drill-to-text both work). `/code-review` folded two findings (memoize `ParagraphText`;
     flash-timer cleanup). Read-only; editing is the next slice.
   Read-only this slice (INV-1/3/9 untouched); property/relation **editing**, manual tagging, "not this
   entity", and boundary changes are the **next** slice. _Then: manual annotation/correction →
   properties/relations edit (incl. DM-Rel-5 re-point) → multi-story + §3.4 scoping → world graph._

3. **M4.S3a — the side panel becomes editable** (the **first M4 *write* slice**; spec §3.4 "properties
   editable"/"relations editable" + §3.5 manual correction). Edit an accepted entity's
   `canonical_name`/`aliases`/`type`/`properties` + add/edit/remove relations between accepted entities.
   **Scope + decompose ✅ DONE (Session 36, 2026-06-19); register RESOLVED. Backend ✅ DONE (Session 37,
   PR #96); frontend ✅ DONE (Session 38, PR #98) — M4.S3a is COMPLETE.** "Manual
   correction in the reader" was sliced by **write-risk, lowest-first**: **S3a** (this) = edit existing
   entities + relations (no merge, no re-point, no spans); **S3b** = entity↔entity merge + DM-Rel-5/6
   re-point + whole-entity delete + undo-merge; **S3c** = manual tag/un-tag/boundaries (reopens DM-IH-1
   span storage); general split + relation temporal/source qualifiers = post-PoC (`docs/BACKLOG.md`).
   Step-0 → `architecture/proposals/m4-entity-editing.md` (`accepted`, register **DM-S3a-1..8 / OQ-23**
   resolved). **The first slice that *writes* committed graph state** — the design weight is the write
   path + reversibility, not the UI: **DM-S3a-1 = new named edit handlers + reword INV-9** ("exactly two
   writers" → "only human-reached handlers — accept, decide, edit"; ADR-0005 broaden-don't-mint
   precedent, **ADR drafted at build, test-first**); **DM-S3a-2 = a before→after edit-evidence log**
   (INV-3 undo + flywheel); **DM-S3a-3** route manual relation-adds through the existing decide path
   (sole edge-writer), re-predicate = delete+re-add, warn-on-MERGE-collision, allow self-loops;
   **DM-S3a-5** typed `properties` values (keys free, INV-4); **DM-S3a-7 = split** S3a-be / S3a-fe.
   - **M4.S3a-be — backend** ✅ **DONE (PR #96, 2026-06-19, Session 37).** `domain/entity_edits.py`
     (pure `apply_entity_edit`/`diff_entity`) + `Neo4jRepo.update_entity`/`delete_relation`/`get_relation`
     + `graph_edits` migration + `PostgresEditStore` + `EntityEditService` + `PATCH …/entities/{eid}` /
     `POST`+`DELETE …/relations`, test-first (396 backend tests green); OpenAPI snapshot + typed client
     regenerated. **INV-9 reworded + `docs/decisions/0006` (broaden-don't-mint)** landed; relation/
     candidate state-machine notes extended. **DM-S3a-3 resolved-at-build to a *direct* edge-writer**
     (option b): the decide path resolves endpoints by surface-name-within-a-paragraph, which a
     hand-picked edge lacks, so routing through it doesn't compose — owner-approved the pre-authorized
     fallback; INV-9's edge-writer set grows too. Both review gates ran (`/review-pr` caught a
     blank-predicate 500→422; `/code-review` caught a predicate whitespace-strip).
   - **M4.S3a-fe — frontend** ✅ **DONE (PR #98, 2026-06-20, Session 38).** `useEntityEdit`/`useAddRelation`/
     `useRemoveRelation` mutation hooks (+ `patchJsonBody`/204-safe `del` client helpers) with
     reader+graph+entity-detail invalidation (DM-S3a-4); a pure `propertiesEditor.ts` (typed scalar rows,
     nested/array values preserved as read-only JSON — DM-S3a-5/INV-4); `ReaderEntityPanel` gained an Edit
     mode (name/type/aliases/properties) + a relations add (reusing `EntityPicker`)/remove section with the
     `merged_into_existing` warning. **One small read-side backend add** (the lone deviation from "no backend
     change"): `language` on `EntityDetailResponse`, so the single name field writes the project-language
     `canonical_name_{pl,en}` slot — **bilingual-per-project is out of PoC scope** (spec §10 q8, owner). Both
     review gates ran; live-smoke-verified. Live smoke surfaced a UX nit (relation-direction toggle), folded.
   _Then: **S3b** merge/delete/undo (DM-Rel-5 re-point) → S3c tag/boundaries/spans → multi-story + §3.4
   scoping → world graph._

4. **M4.S3b — graph mutations that need downstream cleanup (merge · delete · undo).** The slice S3a named
   at its seam, and the **first to re-point already-written graph state**. **Scope + decompose ✅ DONE
   (Session 39, 2026-06-20); register RESOLVED.** Step-0 → `architecture/proposals/m4-s3b-graph-mutations.md`
   (`accepted`, register **DM-S3b-1..8 / OQ-25** resolved). Owner calls: **general undo** (any edit/merge/
   delete reversible newest-first via a grouped append-only `graph_edits` log — a *compensating transaction*;
   resolves spec §10 q2) **+ undo must *show what it reverses*** (each operation carries a human-readable
   description; preview + confirm); **merge = author picks survivor, resolves property conflicts by hand**
   (owner chose more control over survivor-wins); **real `DETACH DELETE` + full-snapshot undo** (not soft).
   INV-3 goes from *substrate* (S3a recorded) to *executed* (undo); INV-9 enumeration unchanged (ops live in
   `EntityEditService`); **split be/fe taken — and the be1(merge)/be2(delete+undo) cut taken too** (owner-
   chosen, Session 40); **ADR 0007** at build. **Spec amended (Session 39):** §3.4 (merge + delete in the
   detail panel) + §10 q2 (undo).
   - **M4.S3b-be1 — backend (merge)** ✅ **DONE (PR #102, 2026-06-20, Session 40).** The first operation to
     re-point already-committed identity: merge B into survivor A — pure `domain/entity_merge.py`
     (`plan_merge`/`detect_property_conflicts`: alias union, by-hand conflict resolution, content-addressed
     edge repoint/fold/self-loop) → `graph_edits` **grouping migration** (`operation_id`/`seq`/`op_kind`/
     `description`/`project_id`/`undone_at`) + `PostgresEditStore.record_operation` → `Neo4jRepo.delete_entity`
     + `repoint_entity_mentions` (RETURNING moved ids) → `EntityEditService.merge_entities` (**B deleted last
     so a crashed merge stays retryable** — the `/code-review` fold) → `POST …/entities/{eid}/merge`
     (400/404/409/503; OpenAPI + client regen). **ADR 0007** landed (merge/delete/undo contract + grouped-log-
     as-undo-substrate + write order); INV-9 grep guard widened (+`delete_entity`, fourth-instance); INV-3
     grouped-log note (substrate now, executed in be2); relation/candidate lifecycle notes extended. Both
     review gates ran; `/review-pr` folded two (incident-edge dedup, resolved-properties restriction),
     `/code-review` folded the write-order retryability fix. 427 backend tests green.
   - **M4.S3b-be2 — backend (delete + undo)** ✅ **DONE (PR #105, 2026-06-20, Session 42).** Whole-entity
     **delete** (real `DETACH DELETE` + full-snapshot before-image) + the general **undo executor**
     (`POST …/graph-edits/undo`, undo-last, preview + confirm): `PostgresEditStore.latest_live_operation`
     (COALESCE `operation_id`→`id`) / `mark_operation_undone` read path → pure `domain/graph_undo.invert_operation`
     → `EntityEditService.delete_entity` + `undo_last` (drift-check → 409, reverse-`seq` replay) → the two
     endpoints. INV-3 *executed*; `state-machines/graph-operation.md` (`applied → undone`) drawn; INV-9 fifth
     witnessed instance (undo reverses via the same human-reached writers). **Re-do-after-undo id collision
     resolved = a generation discriminator** (a `uuid5` seed suffix past any *undone* op for the same targets;
     retry-stable because a *live* op probes False) — kept the `undone_at` stamp model rather than the ADR's
     delete-the-rows alternative; applies to re-merge *and* re-delete. **DM-S3b-5 confirmed (owner, S42): allow
     the delete + let the merge's undo drift-check refuse** (no survivor-guard on delete). Both review gates ran;
     **`/code-review` caught a real data-loss bug** (`fold_relation` undo deleted A's pre-existing edge) — folded
     + two sibling undo-correctness fixes (folded-add no-op, remove-relation full-fidelity). Undo/delete
     **robustness hardening** (depth cap, single-`created_at`-per-op, neighbour-churn signal, row-factory dedup)
     → `docs/BACKLOG.md`.
   - **M4.S3b-fe — frontend** ✅ **DONE (PR #107, 2026-06-22, Session 43) — M4.S3b COMPLETE.** Reader/side-
     panel UI over the three merged routes, test-first (pure mapper → hook → component): `mergeConflicts.ts`
     (client-side conflict detection mirroring `detect_property_conflicts` — the merge 400 carries only key
     names, no values, so the by-hand resolver computes conflicts from both entities' `properties`);
     `useMergeEntities`/`useDeleteEntity`/`useUndo` (undo invalidates only on a *real* apply, not a preview);
     `MergeControls` (EntityPicker → conflict resolver → confirm → summary counts; self-merge blocked);
     delete-with-confirm in `ReaderEntityPanel`; `UndoButton` in the reader header (undo is *story*-scoped —
     owner call) with preview→confirm→apply + 404/409 handling. Both review gates clean; self-review nits
     folded (shared `formatPropertyValue`, reset-summary-on-pick, full-width merge section). **Browser smoke
     surfaced a backend bug → PR #108** (merge-undo 500); merge/delete/undo verified live after that fix.
   _Then: **S3c** tag/un-tag/change-boundaries (next — see item 5) → narrowed multi-story (new story +
   project graph + per-story entity membership; §3.4 scoping). **World-graph multi-story dropped from PoC
   2026-06-22 → `docs/BACKLOG.md`** (see Decided S44)._

5. **M4.S3c — manual tag / un-tag / change-boundaries** (the **final** "manual correction in the reader"
   slice; spec §3.5). **Scope + decompose ✅ DONE (Session 44, 2026-06-22); register RESOLVED.** Owner chose
   it as the next slice to build (over the narrowed multi-story work). Step-0 →
   `architecture/proposals/m4-s3c-manual-tagging.md` (`accepted`, register **DM-S3c-1..9 / OQ-26** resolved).
   **Centre of gravity:** a rendered highlight is a render-time *search hit with no saved identity* (DM-IH-1),
   so a manual span (inflected form / pronoun / new entity) can't be re-found by search and un-tagging has no
   row to delete. **Owner resolutions:** **DM-S3c-1 = (B) overlay / "save only what you touch"** (keep
   render-time search for auto highlights; manual tags persist stored spans that overlay+win; a rejected
   highlight writes a suppression the resolver subtracts; change-boundaries materializes one occurrence —
   incremental, no backfill, DM-IH-1's properties preserved); **DM-S3c-2 = both attach-existing +
   create-new-entity** (a new human-reached graph writer — INV-9 grows); **DM-S3c-7 = adopt Tiptap now**
   (owner override of my native-selection lean — V2 §4 editing inherits it; `/add-dependency` at fe build);
   tag/un-tag/boundary ride the S3b `graph_edits` undo (DM-S3c-5); add `source`+`mention_id` to
   `ReaderHighlight` (DM-S3c-6); **split be/fe** (DM-S3c-8); **no §3.5 capability amendment** (S3a
   precedent) — storage model + INV-9 reword in **ADR 0008** at build, a small **§6.4 data-model note** via
   stop-and-amend if the migration needs it. Completeness sweep over mention CRUD + entity-from-tag
   **closes** (no slicing gap; general split + relation qualifiers stay post-PoC). **Build M4.S3c-be next**
   (see handoff) — first failing test = the pure reconciling-resolver function.
   _Then: narrowed multi-story (new story + project graph + per-story entity membership; needs its own
   decompose + a §3.6/§9 stop-and-amend) → V1 polish wrap._

---

> **Earlier milestones — archived.** Milestone 3 (Cascade matching), Milestone 2
> (Basic extraction), and M1/M0 — their full task breakdowns, Decided entries, and Done
> log — live in **[`docs/PLAN_ARCHIVE.md`](PLAN_ARCHIVE.md)** (dated, append-only). This
> file carries **only the current milestone**; the milestone-roll ritual (`/wrap-session`,
> `docs/AGENTS.md` §7) moves the just-closed milestone there.

---

### Cross-cutting (carried into M4 — curated at the M3→M4 roll 2026-06-17; see `docs/CLAUDE.md` §3)

> **M3→M4 curation pass (2026-06-17, Session 31).** Every item walked by hand per `docs/CLAUDE.md` §3. M3-resolved items stay struck as history (§4). Open items carried into M4 ("V1 polish": inline highlights, side panel, manual annotation, properties/relations edit, **multi-story**, world graph). **The items that bite M4 directly:** **(1, TOP)** **`GET /stories/{id}/graph` story-vs-project scoping** — *graduates from "later" to live M4 work*: multi-story breaks the one-story-per-project assumption the route rests on, and §3.4's "this story / whole world" toggle needs the backend (the 2026-06-17 architecture sweep flags this as M4's biggest forward risk); **(2)** the new **DM-Rel-5 written-edge re-point** — an accepted-entity↔entity merge (first possible in M4's properties/relations edit) re-points an already-written edge, the one relation-write case S4e deferred; **(3)** the **review-UX folds** that M4's side panel / manual-annotation / properties-relations-edit screens will trigger — the **`CandidateView` merge-target-name** gap, the **error-to-string mapper** (n=5), and the **shared `useReviewQueue`** nits (all fire when a review screen next changes); **(4)** **orphaned-sandbox cleanup** (ties M4 multi-story / a delete-story feature); **(5)** the **relation-lifecycle state machine** (OQ-20, vault) — draw at the M4 first-slice `decompose-requirement` step-0. **Resolved this roll:** the **`overview.md` as-built refresh** was folded in the 2026-06-17 sweep (decision-independent freshness); the **architecture re-sync** ran (`reports/2026-06-17-architecture-review.md`). The **M2→M3 curation note + its items remain below as history.**

> **M2→M3 curation pass (2026-06-11).** Every item walked by hand per `docs/CLAUDE.md` §3. The M2-resolved items below stay struck as history (§4). Open items carried into M3; those that **bite M3 directly:** the **embedding read path** (lands *with* MatchingAgent Stage 2), the **agent-retry-loop / envelope-unwrap rule-of-three dup** (M3's matching/judge agent is the n=3 trigger for branch (a)), **store-down→503** (its old "lands with M2.S5" pointer was stale — M2.S5 shipped without it; re-pointed to M3), **graph story-vs-project scoping** (the §3.4 world-toggle needs a backend), the **`last_call()` tiebreaker** (if M3 introduces concurrent LLM calls), and the **§6.5 model-override dropdown** (its own decompose step-0). The **vault-reflection** item resolves in this session's gate 2 (the meta-architect run).

- [x] ~~**Slim `PLAN_SHORT.md` — it's grown to 346KB / 617 lines, too heavy for a read-every-session operational doc (owner-flagged, Session 39, 2026-06-20).**~~ ✅ **Done 2026-06-20 (Session 41, this session).** Owner tightened the policy to **current-milestone-only** (was "current + immediately-prior Done tail" — even one prior milestone left the file at ~365 KB, over the 256 KB single-read limit). **`docs/AGENTS.md` §7 amended** accordingly. **First archive batch moved** — M3 (Cascade matching) + M2 (Basic extraction) + M1/M0 task sections, Decided entries, and Done log → new **`docs/PLAN_ARCHIVE.md`** (dated, append-only, newest-batch-on-top; *move, not delete* — §4 holds; verified byte-for-byte, no bullet lost/duplicated). `PLAN_SHORT.md` 368 KB → ~134 KB. **Ritual wired (owner, S39):** `/wrap-session` step 5c now does/offers the roll-time archive move; `/resume-session` step 3d flags bloat as a backstop. Live cross-cutting/Blocked stay here; their struck/resolved items move at the roll of the milestone that raised them (§7 "what never moves").
- [ ] **§6.5 model-override dropdown** (user picks "which provider/model for this task" + per-task-type preferences). Deferred from M2.S6 as a *feature*, not thin polish (see Decided 2026-06-11). The §8.5 panel already does the *surfacing*; this is the *override* control, which touches **INV-7** (system-derived routing) and needs an INV-7/spec reconciliation + router plumbing + a preferences store. Do as a `meta-architect:decompose-requirement` step-0, alongside or after M3. (M2→M3 roll note, 2026-06-11.)
- [x] ~~**Reflect the M2→M3 roll decisions in the `architecture/` vault.** The **dropdown deferral** (a new open decision touching INV-7) and the **observability-logging gap** (a new open question) are recorded in the plan but **not yet** in `architecture/open-questions.md` / `INDEX.md` — by design, since the vault is written only by the `meta-architect:*` skills (`docs/decisions/0002`). Fold both at the next meta-architect run: the M3 `review-architecture` catch-up sweep + the `decompose-requirement` step-0.~~ ✅ **Resolved 2026-06-11 (Session 19, gate 2):** the meta-architect run folded both — **OQ-14** (model-override dropdown vs INV-7) + **OQ-15** (operational logging absent ⇒ INV-6 vacuous) added to `architecture/open-questions.md`, `INDEX.md` regenerated; the dropdown is also carried in cross-cutting above. (M2→M3 roll, 2026-06-11.)
- [ ] **meta-architect workflow integration** — the three skills (`initialize`/`decompose`/`review`) are shipped and **two live runs are done** (Session 9 `initialize` → vault; Session 10 `review`+`decompose` → ADR 0003), but the skills are still **not wired into the rituals**. Deferred by design (`docs/decisions/0002`): wire on evidence *after living with the vault*, not before. Hypothesis to test: bootstrap-detect a missing `architecture/` vault in `/resume-session`; run `decompose-requirement` at feature "step 0" before the failing test; run `review-architecture` in `/wrap-session` alongside `/review-pr`. **Evidence so far (2 runs):** both produced real value (vault + ADR 0003) *and* a real maintenance cost (the accepted-proposal/decision-reconciliation drift — now mitigated by decompose §5b + `/review-pr` §2). Carry-ins: ~~(a) add a vault/docs-PR review lens to `/review-pr`~~ ✅ **done Session 10** (broadened to the decision-reconciliation lens — `/review-pr` §2 + root `AGENTS.md` + decompose §5b); (b) **[Issue #31](https://github.com/yaaisiu/story-forge/issues/31)** — meta-architect slug/filename carve-out for `PROJECT`/`INDEX`/`CHANGELOG` (still open). (Session 8 note; updated Sessions 9 + 10, 2026-06-02.) **Session 28 (retro):** fixed a smaller framework wart — the `decompose-requirement` + `review-architecture` skills said "read the matching template from `templates/`" but no `proposal.md`/`review.md` template exists (the section structure lives in each skill body); wording corrected to point at `vault-note.md` for frontmatter + name the skill-body as the single home (no duplicate template). Also **fresh evidence toward the OQ-3 ritual-wiring refinement** (run `review-architecture` at *resume* when a session opens with a decompose): Session 28 opened with a decompose on a stale-INDEX vault and had to correct the "Next steps" by hand — wiring itself stays deferred per ADR 0002, evidence accumulates.
- [x] ~~**[Issue #68](https://github.com/yaaisiu/story-forge/issues/68) — frontend `npm audit` HIGH on vite 8.0.10 (GHSA-fx2h-pf6j-xcff + GHSA-v6wh-96g9-6wx3).**~~ ✅ **Resolved 2026-06-16 (PR #73, Session 27):** `vite` 8.0.10 → **8.0.16** (published 2026-06-01, 15 days old → soaked; OSV-clean), via `/add-dependency`. `npm audit --audit-level=high` green; the 2 residual react-router *low* advisories don't fail the high gate (out of scope). Issue #68 closed.
- [ ] **Drop the Session-27 time-boxed security waivers as their fixes soak (`/triage-advisory`).** Clearing the security-job reds (PRs #72/#73) needed soaked bumps + **fix-first time-boxed waivers** for residuals whose fixes were still inside the 14-day soak. Drop each (bump to the now-soaked fix, delete the `[[IgnoredVulns]]` block + `WAIVERS.md` row) when it bakes: **python-multipart 0.0.31 — soaks 2026-06-18** (GHSA-v9pg-7xvm-68hf, low); **starlette 1.3.0 — soaks 2026-06-25** (GHSA-jp82-jpqv-5vv3, low); **starlette 1.3.1 — soaks 2026-06-26** (GHSA-82w8-qh3p-5jfq, **HIGH** DoS). Plus the **condition-based** neo4j Trivy waiver **CVE-2026-50010** (netty TLS hostname-verification bypass) — drop when a Neo4j 5.26.x image ships netty ≥4.1.135.Final. `/resume-session` step 3b now flags the dated ones; the `ignoreUntil` CI re-red is the backstop. (Session 27, 2026-06-16.)
- [ ] Update spec §10 if any open question hardens (extraction granularity, etc.)
- [x] ~~**`POST /stories/{id}/extract`: store-down surfaces as 500, and the Neo4j driver has no lifespan-close.** The route maps agent/transport failure to 502, but a *store* outage (Neo4j/Postgres connectivity error mid-ingest) propagates as an unhandled 500; and `main.py` builds the `Neo4jRepo` driver at import with no FastAPI lifespan to `close()` on shutdown.~~ ✅ **Resolved 2026-06-15 (M3.S4a, PR #63):** the extract + accept routes map `(psycopg.OperationalError, neo4j ServiceUnavailable)` → **503** (fail-closed — a connectivity blip mid-cascade must never degrade to a silent NEW); list/reject map `OperationalError` → 503; `main.py` gained a FastAPI **lifespan** that `close()`s the Neo4j driver on shutdown. (M2.S4 `/code-review` + external-review note, 2026-06-10; re-pointed to M3 at the 2026-06-11 curation; landed with the write-path refactor.)
- [x] ~~**Relation decide-on-relations UI + the relation-write backend (deferred past M3.S4b — Session 25).**~~ ✅ **FULLY resolved — backend 2026-06-16 (M3.S4e, PR #76), UI 2026-06-17 (M3.S4f, PR #78).** Both human gates ship; M3's feature work is complete. S4a *stages* relation data but nothing wrote graph edges, and a relation can only be written once **both** endpoints are accepted (re-pointing on a merge). S4b was originally scoped to own the decide-relations UI + this backend slice; at Session-25 start we **deferred both** to keep S4b a clean single-conversation additive-UI slice that closes M3 on *entity* dedupe (the §9 M3 outcome). Resolve as its own slice: a backend relation-write path (edge written only when both endpoints are accepted; re-point on merge) + the §3.3 5th human action (decide-relations) in the review queue. (Session 25 scope decision, 2026-06-15.) **Priority raised 2026-06-15 (Session 25, M3.S4c decompose):** M3.S4c/S4d make merges *more frequent*, and a merge with no relation-write **orphans the candidate's staged relations** — so this slice now gates a clean graph more than before; sequence it soon after S4c/S4d (owner kept it deferred for now, eyes open). **Now live (Session 26):** S4c shipped (PR #67) — on-accept re-match makes merges routine, so this is the **top backend cross-cutting** to sequence next (after or alongside S4d). **Session 27:** S4d shipped (PR #70) — a handpick-**merge** orphans the candidate's staged relations the same way; M3 *entity* dedupe is now complete (S4a–S4d), so this relation-write slice is **the** next M3 piece, and the natural next session. **Session 28 — DECOMPOSED + DECIDED** (`architecture/proposals/m3-relation-write.md`, register mirrored in Blocked / OQ-19): owner framed it an **M3 slice**; **DM-Rel-1 = explicit human gate** (the §3.3 5th action, not auto-write); build **split backend-now / UI-next**. The decompose reframed it smaller than feared — edges write **lazily** (resolve each surface endpoint to its candidate's *committed* entity id), so "re-point on merge" mostly dissolves (only an M4 entity↔entity merge re-points a written edge); a real must-fix surfaced (`create_relation` `CREATE` doubles an edge on retry → deterministic-id `MERGE`). Remaining register (DM-Rel-2/4/5/6/7) confirm-at-build. **Next: the S4e backend slice, test-first** (the relation-write flip test).
- [x] ~~**Review-queue "merge with…" is alternatives-only — no arbitrary-entity search (deferred, Session 25).** The Stage-4 `M` (change-target) action lets the reviewer retarget a merge, but the only targets offered are the cascade's top-3 `alternatives`.~~ ✅ **Promoted into a planned slice 2026-06-15 (Session 25): M3.S4d — manual handpick** (`GET /stories/{id}/entities?q=` project-scoped + a picker; decompose `architecture/proposals/m3s4c-intra-batch-rematch.md` DM-S4c-5/6). No longer "later" — it's the false-negative safety net paired with M3.S4c's on-accept re-match.
- [ ] **`CandidateView` carries no merge-*target* name — Stage-2/3 merges show a generic label (Session 25).** The review card resolves the proposed merge target's name by matching `target_entity_id` against the staged top-3 `alternatives` — but those are ranked by **RapidFuzz** while a Stage-2/3 merge target is chosen by **embedding cosine / judge** (`candidate_staging._cascade` + `matching_agent.stage2`), a different signal, so the target need not be in the list. `CandidateView` (`api/stories.py`) exposes only `target_entity_id`, no name, so on those paths the badge falls back to `Merge → an existing entity` (the `/code-review` should-fix; the raw-UUID was folded out in S4b). Proper fix: add `target_canonical_name` to `CandidateView` (resolve it at stage time from the in-scope `existing`/`snapshot` entities, or at read time) so the reviewer sees *what* they're merging into on every path. Backend slice + openapi regen — track here, do when the §3.3 review UX is tightened. (Session 25 `/code-review` finding.)
- [ ] **Per-screen `reviewMessage`/`extractMessage`/`decideMessage`/`messageForError` error-to-string mappers duplicate a pattern (now 5 screens — Session 25, updated S30).** `ReviewQueue`, `GraphViewer`, `UploadScreen`, `OutlineEditor`, and now `RelationQueue` (`decideMessage`, M3.S4f) each carry a near-identical `ApiError → string` switch (differing only in which statuses they special-case). At n=5 this is well past the rule-of-three; a shared `errorToMessage(error, overrides?)` / `apiErrorFallback(error, default)` in `lib/api/` with per-call status overrides would fold the *skeleton* (non-ApiError fallback + the final `error.detail || HTTP status` line) while each screen keeps its own status→string switch. Pre-existing convention — fold when one of these screens next needs an error-message change (don't pre-emptively refactor all five). (Session 25 `/code-review` reuse finding; n bumped S30.)
- [ ] **The two review queues (`extraction-review` / `relation-review`) share copied machinery with two latent nits (Session 30).** `RelationQueue` mirrors `ReviewQueue` by copy (not a shared `useReviewQueue` hook): `isEditableTarget` (n=2), the no-deps window-keydown effect, the render-time `selectedIndex` clamp, the per-card `pending` guard. Two verified-real behaviours ride along in **both**: (a) the **keyboard A/R path is not gated by an in-flight mutation** while the buttons are — a fast double-key re-fires the decision for the selected item (harmless: deterministic edge/entity ids + the backend's `already_decided` no-op); (b) a **stale decide/review error banner persists across J/K navigation** until the next mutation. Left unfolded in S4f because fixing them *here only* would diverge the two queues — the right fix is a **shared** one (extract a `useReviewQueue` hook, or gate the keyboard handler in both) at the n=3 trigger or when a queue next needs a behaviour change. (M3.S4f `/code-review`, 2026-06-17.)
- [ ] **`GET /stories/{id}/graph` returns *project*-scoped data under a story URL.** The graph is keyed by `project_id` (the §6.4 multi-tenancy seam), so the route resolves the story → its project and returns that project's whole graph. Correct while it's one-story-per-project (today's as-built — `api/stories.py` "Each upload creates its own project for now"), but the moment a project holds two stories, `/stories/A/graph` and `/stories/B/graph` return identical graphs, and spec §3.4's "this story only / whole world" toggle has no backend to drive it. Resolve when multi-story projects (or project selection) land: either a genuine story filter (entities `first_seen` in the story's paragraphs) or a `/projects/{id}/graph` route matching the data. (M2.S5 `/code-review` + `/review-pr` altitude finding, 2026-06-11.) **Session 27:** M3.S4d's new `GET /stories/{id}/entities?q=` deliberately adopts the *same* project scope (consistent with this route), so the world-toggle question is unchanged — both move together when multi-story projects land. **Session 31 (M3→M4 roll) — GRADUATED to live M4 work:** M4's stated scope *is* multi-story + world graph, so "when multi-story projects land" = now. The 2026-06-17 architecture sweep calls this M4's biggest forward `risk` — resolve it **with** the multi-story slice (a genuine per-story entity filter, or a `/projects/{id}/graph` route matching the data, to drive the §3.4 toggle), not after. **Session 44 (2026-06-22) — narrowed:** the world-graph half is **OUT of PoC** (`docs/BACKLOG.md`); this item now == the in-PoC multi-story direction (new story reuses the project graph + a per-story **entity-membership** model — which entity appears in which story — that drives the §3.4 "this story / whole world" toggle *and* lets known entities seed extraction). Still M4's top forward risk; resolve it *with* the multi-story slice (its own decompose + a §3.6/§9 stop-and-amend). S3c is being built first (owner's call).
- [ ] **DM-Rel-5: re-point a *written* relation edge on an accepted-entity↔entity merge (M4).** S4e writes edges *lazily* (resolve each endpoint to its candidate's committed id), so re-point "mostly dissolved" — *except* merging two **already-accepted** entities (which doesn't exist in M3) re-points the edges already written to the absorbed entity. M4's **properties/relations edit** + multi-story is where that merge first arrives, so this deferred case becomes live work then: on an entity↔entity merge, re-point (or `MERGE`-collapse, given the deterministic `uuid5(subject,predicate,object)` edge id) every edge touching the absorbed id onto the survivor. (M3.S4e decompose / ADR 0005 DM-Rel-5, 2026-06-16; graduated to live M4 work at the 2026-06-17 roll.)
- [ ] **OQ-20 (vault): draw the relation-lifecycle state machine.** S4e/S4f shipped a real edge lifecycle in code (`staged_relations`: *held* / *committable* / *written* / *rejected*, with a re-resolve-at-commit TOCTOU guard + idempotent-by-edge-id effect) but no `architecture/state-machines/relation-lifecycle.md` — its entity twin `candidate-lifecycle` has had one since S4a (the 2026-06-17 sweep's headline structural gap). Draw it at the **M4 first-slice `decompose-requirement` step-0** (vault is writer-restricted to the `meta-architect:*` skills). Fold two sub-gaps while drawing: **held-relation visibility** (does the author ever learn *why* an extracted relation never became an edge?) and the **edge Expiry** posture (held rows never expire — accepted none-at-PoC, ADR 0005). (2026-06-17 architecture sweep, OQ-20.)
- [ ] **`PostgresCostStore.last_call()` ordering has no tiebreaker.** `ORDER BY created_at DESC LIMIT 1` decides the §8.5 panel's "most recent call"; `created_at` defaults to `now()` (transaction-start time), and there's no monotonic column (the PK is a random uuid). Unreachable on the as-built **sequential single-user** extraction path (rows land seconds apart), but two rows sharing a `created_at` would make "most recent" nondeterministic. Lands with the same concurrency work spec §6.6 already defers ("a true reserve-then-reconcile lands only if batched concurrent calls do") — add a monotonic tiebreaker (a `bigserial`, or order by a real insertion sequence) **if/when** concurrent LLM calls are introduced. (M2.S5 `/code-review` finding, 2026-06-11.)
- [x] ~~**Embedding read path** — `adapters/postgres_repo.py` reads `NULL AS embedding` (paragraphs never store embeddings in M1). When the embedding pipeline lands (**M3, alongside MatchingAgent Stage 2**…), add the `pgvector` dep + `register_vector_async`, switch `get_paragraph`/`list_paragraphs` to `SELECT ... embedding`, and start writing the column. (Codex review note, 2026-05-20.)~~ ✅ **Resolved 2026-06-12 (M3.S2, PR #58):** `pgvector==0.4.2` added; `register_vector_async` centralised in `db.connect` across all three connect sites; the 3 paragraph reads un-stubbed to `SELECT ... embedding`; the new write path is `entity_mentions.embedding` (paragraphs have no producer yet — the cascade uses per-mention vectors). See Decided.
- [ ] **Orphaned sandbox files on upload failure** — `api/stories.py` writes the original file to the sandbox *before* the `Project`/`Story` inserts; if the DB write fails the transaction rolls back but the file is left on disk, pointing at a story that never persisted. Harmless on a single-user local app, so deferred. Fix when it bites or when a delete-story feature lands (clean up the file on insert failure, or write-after-commit). (Own-review note, 2026-05-21.)
- [ ] **Window-and-stitch for `auto` on long text** — `agents/chunking_coordinator.py` currently raises `ChunkingTooLongError` above `max_input_words` (a 422). The proper fix is a deterministic pre-split on paragraph boundaries, per-window agent calls, offsetting paragraph indices, and stitching the chapters/scenes back together — accepting some seam imperfection that the user corrects in the UI confirm. Deferred until the auto path is actually exercised on a real 50k-word input. (Session 4 windowing-guard decision, 2026-05-25.)
- [ ] **Re-structure semantics: delete-and-replace** — `POST /stories/{id}/structure` returns 409 if the story already has a tree. A re-run with a different mode ("I didn't like auto, let me redo manual") needs delete-and-replace, which the Session 1 repo doesn't have (Update was deferred). Session 6 added a *narrow* update — `update_story_raw_text` in the repo + an override-and-persist branch on the structure endpoint — covering the manual editor's "user accepts/edits" use case (spec §7 step 2). The full delete-and-replace UX (redo a different mode after the first persist) is still deferred. Add when the frontend wants it. (Session 4 endpoint-shape decision, 2026-05-25; narrowed Session 6, 2026-05-26.)
- [x] ~~**Backend advisory scan in CI (PyPI parity for `npm audit`).**~~ ✅ **Built 2026-06-08 (PR #44, Session 14).** `osv-scanner` step vs `backend/uv.lock`, **fail-on-any**, scanner **digest-pinned** (the `google/osv-scanner-action` root `action.yml` is a no-`runs:` stub and its reusable workflows do diff/SARIF scans — neither a scoped fail-on-any step; a digest pin is stronger than the planned SHA-pin, D6 intent preserved). Scoped waivers in `infra/osv/` (toml + `WAIVERS.md`, mirroring the Trivy split; no active waivers). `starlette` 1.0.0→1.0.1 explicit pin was the live self-test (gate red on 1.0.0, green on 1.0.1). Spec §6.7 amended; README + `/review-pr` §5 reconciled. (Session 5 note, 2026-05-25; decomposed Session 13; built Session 14.)
- [ ] **`/stories/{id}/structure` 422 is overloaded.** Two distinct failure modes return 422 with different body shapes: FastAPI's auto-generated request-validation errors (shape: `HTTPValidationError`) and the explicit `ChunkingTooLongError` raised when the input exceeds `max_input_words` (shape: `{detail: str}`). The Session 5 PR documented the unambiguous error codes (400/404/409/413/415/502) in OpenAPI but left 422 alone because declaring it would overwrite FastAPI's validation shape. Pick one: either remap `ChunkingTooLongError` to 413 (payload too large — semantically truer; the body *is* too long for the agent) or to a free status code in the 4xx range, so 422 stays purely validation-shaped and the frontend can discriminate the two cases by type rather than body inspection. Lands cleanly with the "window-and-stitch for auto on long text" cross-cutting fix above, which removes the error path entirely for the auto mode. (Session 5 Codex-review fold-in note, 2026-05-25.)
- ~~**LLM router doesn't record/fail-over on a malformed-but-`200` provider envelope.**~~ ✅ **Resolved 2026-06-08 (M2.S3, PR #42):** typed `ProviderResponseError` added in `adapters/llm/base.py`, raised by the Ollama + OpenRouter adapters on a malformed-`200` envelope (missing fields, unparseable body, **and null `content`** — the null case caught by `/code-review`), and `router.py` records a failure row + fails over like a 5xx; the "envelope-malformed vs schema-invalid" split is proven by router tests. Closes the §6.6 "one row per call" gap (INV-5). (M2.S2 own-review should-fix → built M2.S3.)
- ~~**ChunkingAgent doesn't map `ProviderResponseError` (malformed-`200`) to its route's 502.**~~ ✅ **Resolved 2026-06-10 (M2.S4, PR #48):** `ChunkingAgent` now catches `ProviderResponseError` around the provider call → `ChunkingError` (the structure route already maps that to 502), so a malformed-200 during chunking is a 502, not an unhandled 500. + test. Original below. M2.S3 added a typed `ProviderResponseError` (adapters raise it on a malformed-`200` envelope). `ExtractionAgent` is behind the router, which records + fails over; but `ChunkingAgent` holds a *raw* `OllamaProvider` and its retry loop catches only `ValidationError`, so a malformed-`200` during chunking propagates as `ProviderResponseError` past the route's `except ChunkingError` → an unhandled **500**, though `POST /stories/{id}/structure` advertises **502** for LLM failure. **Not a regression** (pre-M2.S3 the same body raised a raw `KeyError` → also 500). Cheap fix: catch `ProviderResponseError` in `ChunkingAgent` → `ChunkingError` (the route already maps that to 502). The deeper fix is to move chunking behind the router like extraction (gets failover + the `llm_calls` row too) — lands naturally with **M2.S4**. (M2.S3 `/code-review` finding, deferred 2026-06-08.)
- ~~**`RelationCandidate` accepts blank `subject`/`predicate`/`object` as success.**~~ ✅ **Resolved 2026-06-10 (M2.S4, PR #48):** added the same non-empty `field_validator` (covering all three fields) used for `candidate_name`, so blank relation output is retried not accepted; + an agent-retry test and a direct-validation test. Original below. `agents/extraction_agent.py:92-94` types the three relation fields as plain `str` with **no non-empty validator** (only `candidate_name` has one). A model returning `{"subject":"","predicate":"","object":"",…}` validates as a successful `ExtractionProposal`, so the agent's retry loop **stops** and the future M2.S4 graph writer gets a relation with no usable endpoint or type. **Distinct from the intentionally-accepted dangling relation** — a dangling endpoint still needs a *non-empty* surface form or id; only its *resolution* (match/create) is deferred to M3. Fix: add the same non-empty `field_validator` used for `candidate_name` to `subject`/`predicate`/`object`, so blank output is retried not accepted; + a test. A real gap **both** M2.S3 review passes missed (tests cover blank `candidate_name` + empty-lists, never blank relation fields). Small — fold into **M2.S4** (where it bites) or a quick standalone PR. (External review remark on merged PR #42, 2026-06-08.)
- **Agent retry loop + adapter envelope-unwrap are duplicated (revisit at rule-of-three).** Two altitude dups surfaced in the M2.S3 `/code-review`: (a) the `render → call → validate → retry-on-ValidationError → give-up` loop is near-verbatim in `chunking_agent.py` and `extraction_agent.py` (only the schema/weight/grounding differ); (b) the `response.json()` + envelope-unwrap-or-raise-`ProviderResponseError` block is near-identical in `ollama.py` and `openrouter.py` (only the dict path differs). **(a) ✅ Resolved 2026-06-15 (M3.S3, PR #60):** JudgeAgent was the n=3 trigger — the loop folded into `agents/validation.py` `validate_with_retry(...)`; chunking + extraction refactored onto it (each keeps its own schema/call/give-up error); `TaskWeight` + a `Router` Protocol promoted into `base.py` at the same time. **(b) still open** — deliberately per-copy at n=2; **fold at the first direct vendor adapter** (Anthropic/Grok/…, "as needed"), since each new copy re-implements the INV-6/OQ-4 no-body-leak rule by hand, and a `_unwrap_content(...)` in `base.py` would touch that security-sensitive guarantee. (M2.S3 `/code-review` altitude findings, 2026-06-08.)
- **Repo-root `scripts/` is outside CI's ruff scope.** The backend ruff config is `src = ["src", "tests"]` and CI runs `ruff check .` from `backend/`, so `scripts/*.py` (e.g. `check_dependency_age.py`) are never linted — a pre-existing `UP017` (`datetime.UTC`) sits there uncaught. Either add repo-root `scripts/` to a lint step or fix the one nit. Left untouched in PR #25 to stay surgical. (M2.S1 own-review nit, 2026-06-01.)
- ~~**[sooner-than-later — bites M2.S3] Spec §6.5 router contract is stale vs as-built; reconcile before/at M2.S3.**~~ ✅ **Resolved 2026-06-08 (M2.S3 step 0, stop-and-amend flow):** spec §6.5 router block amended `route(self, task) -> LLMProvider` → `complete(messages, *, weight, task_type, json_schema=None) -> CompletionResult` (orchestrating API, ADR 0003), and the envelope-vs-schema split written into both the §6.5 pseudocode and the §6.5/§11 "Failover" paragraphs (malformed-`200` envelope → failover via `ProviderResponseError`; schema-invalid → agent prompt-retry). Reconciled: `README.md` failover line, plan line 64 now accurate. Left intact as history: ADR 0001 (superseded-in-part) + the M2.S2 Done/Decided log lines. Original framing (for history): two §6.5 drifts the first router consumer would read as contradictions — (1) the `route()→LLMProvider` signature vs as-built `complete(...)`, and (2) the "malformed response schema → retry the prompt" lump conflating envelope-malformed (→ failover) with schema-invalid (→ retry prompt), the OQ-10 distinction. (Codex no-PR-flow review + M2.S3 decompose G6.)
- ~~**[doc-hygiene, contingent on confirmation] Simplify the §0 concurrent-agents rules if Codex Desktop is fully gone.**~~ ✅ **Resolved 2026-06-10 (PR #50):** owner confirmed Codex Desktop is retired (the only residual is the GitHub-wired Codex PR review). Root `AGENTS.md` §0 rewritten to "Commit hygiene on the working tree" — dropped the two-agents / `.codex/RUNTIME_NOTES.md` / UNC-boundary framing, kept stage-explicit-paths + surface-foreign-changes as general hygiene; `.codex/` deleted; the Review-model note + the Windows/UNC symlink caveat trimmed. Original below.
- ~~**[deprioritized 2026-06-08 — contingent on Codex still reviewing] Mirror the failover/error-taxonomy review lesson into the Codex `/review-pr` skill.**~~ ✅ **Resolved 2026-06-10 (won't-do; PRs #49/#50):** moot — the GitHub Codex PR reviewer reviews the diff and runs **no** repo skill, and the Codex-side `.agents/skills/review-pr/` (the only thing that consumed a `lessons.md`) is **deleted** (PR #50). The durable lessons live in the primary `.claude/skills/review-pr/` gate (PR #49 also de-emphasised Codex there + removed the dead UNC-artifact bullet). Nothing left to mirror into. Original below.
- ~~**INV-5 "every terminal edge" over-claims ledger coverage vs OQ-10.**~~ ✅ Handled in-vault 2026-06-08: `architecture/invariants.md` INV-5 narrowed to "every terminal edge **the router currently handles**" + an explicit "known coverage gap (OQ-10)" clause, so a reader can't mistake INV-5 as total before the `ProviderResponseError` work lands. (Codex no-PR-flow review; folded during the M2.S3 decompose since it's a vault file.)
- ~~**[doc-hygiene, low priority] M2.S2 proposal depicts the superseded `route()` router API.**~~ ✅ **Resolved 2026-06-09 (Session 15 architecture sweep):** both spots in `architecture/proposals/m2s2-llm-router-budget-cap.md` now read `complete()` — the stations table (line 139, annotated "as-built / amended §6.5") and the mermaid (line 183). Original: it showed `router.route()` + `LLMRouter.route`, the pre-build API superseded by the as-built `complete(messages, *, weight, task_type, json_schema=None)` (ADR 0003; spec §6.5 amended PR #39). (PR #39 self-review should-fix → fixed during the pre-M2.S4 `review-architecture` sweep.)
- **`CandidateSpan` has no `char_start <= char_end` validator** (`domain/extraction.py`). Internal deterministic data from spaCy, so optional — a model validator would just self-document the invariant. Add if a future producer of `CandidateSpan` is less trustworthy than spaCy. (M2.S1 own-review nit, 2026-06-01.)
- ~~CORS allowlist excludes `http://127.0.0.1:5173`~~ ✅ Resolved 2026-05-26 (Session 6 wrap, PR #20): allowlist now includes both `localhost` and `127.0.0.1` for ports 5173 and 3000. §6.7 amended with rationale. See Decided.
- ~~Dev DB needs `alembic upgrade head` before first real run~~ ✅ Resolved 2026-05-26 (Session 6 wrap, PR #20): one-line note added to `backend/CLAUDE.md` "Running locally". See Decided.

### Blocked / questions

- ~~**M4.S3b merge/delete/undo register** (`architecture/proposals/m4-s3b-graph-mutations.md`, mirrored OQ-25) — raise + resolve this session.~~ ✅ **Resolved 2026-06-20 (Session 39).** Branchy slice decomposed step-0 (completeness sweep over CRUD-of-{entities,relations,mentions} closes — no slicing gap). Owner resolved **DM-S3b-1..8**: **general undo** + *see-what-I-undo* preview; **merge = author picks survivor, resolves property conflicts by hand** (owner overrode the survivor-wins lean); **real `DETACH DELETE` + full-snapshot undo**; re-point all incident edges + **report** MERGE-collisions; Neo4j→Postgres→evidence idempotent order; split be/fe (be1/be2 fallback); INV-9 enumeration unchanged, INV-3 now *executed*, **ADR 0007** at build. **Spec amended this session (owner-approved):** §3.4 (merge + delete in the detail panel), §10 q2 (undo = append-only log). §3.5 left for S3c (mention-level un-tag, not whole-entity delete — corrected from the decompose's §3.5). **Build M4.S3b-be next** (see handoff + feature order item 4). (Raised + resolved S39.)
- ~~**M4.S3a entity-&-relation editing register** (`architecture/proposals/m4-entity-editing.md`, mirrored OQ-23) — raise + resolve this session.~~ ✅ **Resolved 2026-06-19 (Session 36).** Owner sliced "manual correction in the reader" by write-risk (S3a edit-existing / S3b merge+delete+re-point / S3c tag+spans) and resolved the whole **DM-S3a-1..8** register: **DM-S3a-1 = new named edit handlers + reword INV-9** "exactly two writers" → "only human-reached handlers" (ADR-0005 broaden-don't-mint precedent; ADR drafted at build); **DM-S3a-2 = a before→after edit-evidence log** (INV-3 undo + flywheel; *rejected:* no-record/re-type-to-undo); **DM-S3a-3** route manual relation-adds through the decide path (sole edge-writer), re-predicate = delete+re-add, warn-on-collision, allow self-loops; **DM-S3a-5** typed `properties`; **DM-S3a-7 = split** S3a-be/S3a-fe; the rest (DM-S3a-4/6/8) as proposed. The first slice that *writes* the graph. **Build M4.S3a-be next** (see handoff + feature order item 3). (Raised + resolved S36.)
- ~~**Next M4 slice (M4.S2) — confirm scope at resume.**~~ ✅ **Resolved 2026-06-18 (Session 34): M4.S2 = entity side panel** (owner chose it over manual-correction-in-reader — the read-only inspection surface the next slice's corrections build on). Step-0 decomposed → `architecture/proposals/m4-side-panel.md` (`accepted`, register **DM-SP-1..8 resolved**, OQ-22). **DM-SP-1 = a focused per-entity endpoint** → **DM-SP-7 = split** into **M4.S2a backend** + **M4.S2b frontend**; strict 1-hop ego-graph; read-only; occurrences from rendered highlights; a new reader panel. See the Session-34 Decided entry + the feature order below. Manual correction is the slice *after* this one. (Raised S33; resolved S34.)
- ~~**M4.S2b confirm-at-build — DM-SP-4 mini-graph render.**~~ ✅ **Resolved 2026-06-18 (Session 35): cytoscape, browser-verified.** Reused `GraphCanvas`/cytoscape in the narrow panel (one renderer; neighbour-tap node inspection + drill-to-text both work) rather than a static view — verified live against a 115-node graph (Garret Locke, 29 neighbours / 40 edges). Dense-graph legibility for high-degree nodes is real but deferred to the §3.4 graph filters + post-PoC panel refinement (`docs/BACKLOG.md`). _(The S2a 1-hop-Cypher index `verify-at-build` was already resolved: `infra/neo4j/init.cypher` has the `Entity.id` uniqueness constraint → index-backed lookup, no new index needed.)_
- ~~**M4.S1 inline-highlights register** (`architecture/proposals/m4-inline-highlights.md`, mirrored OQ-21) — OPEN, resolve next session.~~ ✅ **Resolved 2026-06-18 (Session 32); backend built (PR #81).** Owner resolved the gating calls: **DM-IH-1 = render-time string search over name + aliases** — *verify-first* settled it: the accept path stores **no** char offset and the spaCy span is gone by accept time, so option (b) "persist the existing span" was *illusory*; aliases accumulate the prose's surface forms (each merge-accept adds one), so inflected forms that were themselves extracted become searchable (*considered & rejected:* persist-spans-now — no span to persist + a data change in a low-risk slice). **DM-IH-3 = plain `<mark>` renderer**, Tiptap deferred to the manual-annotation slice (*rejected:* adopt Tiptap now — ceremony ahead of need). **DM-IH-2** = a new story-scoped `GET /stories/{id}/reader` (the natural first home of the §3.4 per-story filter); **DM-IH-4** longest-match, **DM-IH-7** accepted-only, **DM-IH-8** name+type+aliases — all built into the backend. **Remaining: DM-IH-5** colour palette + **DM-IH-6** 50k-word perf = confirm-at-build in the **frontend slice (next)**. (Raised S31; resolved S32.) ✅ **Frontend built S33 (PR #86), live-verified:** DM-IH-5 = reader-local fixed palette + hash fallback + legend (owner's call); DM-IH-6 = whole-story render, measured fine on the Oakhaven draft. **M4.S1 fully done.**
- ~~**Address the 2026-06-17 architecture report + OQ-20, next session.**~~ ✅ **Resolved 2026-06-18 (Session 33, PR #85, `2f361ff`):** a `meta-architect:*` vault pass drew `architecture/state-machines/relation-lifecycle.md` (closes OQ-20), reflected the resolved DM-IH register into `proposals/m4-inline-highlights.md` (→ `accepted`, OQ-21 annotated), and fixed the `glossary/model-tier-routing.md` "not yet built" drift. The two `watch`-level findings (held-relation visibility; edge-id provenance collapse) are carried as named Open points in the new state-machine note + the ADR-0005 follow-up. The original framing: The roll's `review-architecture` sweep (`architecture/reports/2026-06-17-architecture-review.md`) folded the one decision-independent drift (`overview.md`) on sight; its remaining findings need *acting on*, not filing: **OQ-20** (draw the relation-lifecycle state machine — a `decompose`/drawing pass; the S4e/S4f edge lifecycle has no model note) + the `watch`-level items (held-relation visibility; edge-id provenance collapse — already an ADR-0005 follow-up). The new **`/resume-session` step 3c** (added this session) will surface these at next resume. **Plus a pre-existing vault-drift discovery the PR-#80 `/review-pr` surfaced (and the 2026-06-17 sweep itself missed):** `architecture/glossary/model-tier-routing.md` still says "the `LLMRouter` and the paid adapters are **not yet built**" — stale since **M2.S2 / PR #36** built both; fix it in the same next-session vault pass (a `meta-architect:*` write). (Owner directive S31: "read our report next conversation and spend a session resolving things — don't report just for reporting.")
- ~~**M3 relation-write register** (`architecture/proposals/m3-relation-write.md`, mirrored OQ-19)~~ ✅ **fully resolved 2026-06-16 (Session 29, M3.S4e / PR #76; proposal now `accepted`, OQ-19 struck, ADR 0005).** DM-Rel-1 = **explicit human gate** (owner; *rejected:* auto-write, hybrid bulk-confirm). DM-Rel-2/4/5/6/7 confirmed at build as proposed: normalised-exact same-paragraph resolution reading the committed id from `candidates` (create-id derivation promoted to `domain.candidates.committed_entity_id`); a `staged_relations` table; M3 writes / M4 re-points; idempotent `MERGE` on `uuid5(subject_id,predicate,object_id)`; dangling-to-known held. Invariant: **INV-1 broadened** to cover edges (not INV-10 minted); INV-9 second instance. **New follow-up (carried):** per-mention provenance for triple-deduped edges — edge-id-from-triple collapses repeated mentions to one edge (clean graph), so post-PoC we need a way to enumerate all occurrences of a relationship (the `staged_relations` rows retain per-paragraph provenance; the graph edge does not).
- ~~**M3 cascade register** (`architecture/proposals/m3-cascade-matching.md`, mirrored OQ-16).~~ ✅ **Fully resolved 2026-06-15 (S23).** ~~DM6~~ ✅ intercept-before-write (A); ~~DM1~~ ✅ thresholds → `config.py` (S20); ~~DM2~~ ✅ `paraphrase-multilingual-mpnet-base-v2` 768-dim, SHA `4328cf26…`, pinned via the §6.7 HF-channel (S20; **implemented S21, PR #58**); ~~DM3/DM4~~ ✅ per-mention vectors on `entity_mentions`, max cosine (S20; **implemented S21, PR #58**); ~~DM5~~ ✅ JudgeAgent tier = cloud_free via `weight="medium"`/`task_type="judge"` (S22, **implemented PR #60**); ~~DM7~~ ✅ review-queue UX → build §3.3 Stage-4 elements + keyboard nav in S4b, **INV-2 consent gate DEFERRED past M3** (persona-justified; keyboard scheme an S4b-time pick) (S23); ~~DM-rej~~ ✅ **remember rejections** — the `rejected` edge writes a `candidate_decisions` evidence row the matcher consults before re-queueing (S23). Also live: spec §10 q8 (multilingual `canonical_name_pl/en`, surfaces at merge — the spec's to resolve).
- ~~**M3.S4c intra-batch-dedup register** (`architecture/proposals/m3s4c-intra-batch-rematch.md`, mirrored OQ-18).~~ ✅ **Resolved 2026-06-15 (Session 25):** ~~DM-S4c-1~~ ✅ **slice S4c (live re-match, backend-only) + S4d (manual handpick)**; ~~DM-S4c-2~~ ✅ re-match runs **synchronously in the accept handler + incremental** (vs just the newly-accepted entity); ~~DM-S4c-3~~ ✅ **auto-flip on Stage 1 `>85%` OR Stage 2 `cosine >0.85`** (owner chose the +Stage-2 option over the proposed Stage-1-only), **no live Stage-3 judge**; ~~DM-S4c-4~~ ✅ **monotone** re-proposal (only `new → merge`, never the reverse) → idempotent — kept as the transition guard, **not** a new INV-10; ~~DM-S4c-5~~ ✅ handpick **project-scoped** (the §6.4 tenancy key) — **supersedes** the deferred "arbitrary-entity search" cross-cutting; ~~DM-S4c-6~~ ✅ `GET /stories/{id}/entities?q=` over Neo4j accepted entities via `accepted_entity_reader`. Spec §3.3 amended (on-accept re-match + handpick clarifications). **INV-1/INV-9 hold** (re-match writes only the `candidates` table). **Built — S4c shipped (PR #67, 2026-06-15, Session 26); ADR 0005 declined at build** (owner): a contained change (staging-only, INV-9 holds), already fully recorded in the proposal + spec §3.3, so the rationale lives in Decided + the vault INV-9 graph-vs-staging clarification rather than a separate decision file. **Relation-write stays deferred** (the merge-orphans-relations cross-cutting, now top backend priority — S4c made merges routine). **S4d (manual handpick) is the next slice.**
- ~~**M3.S4a build-detail register** (`architecture/proposals/m3s4a-intercept-write-path.md`, mirrored OQ-17).~~ ✅ **Resolved 2026-06-15 (S23):** ~~DM-S4a-1~~ ✅ new Postgres `candidates` staging table; ~~DM-S4a-2~~ ✅ add **INV-9** "no automated stage writes the graph"; ~~DM-S4a-3~~ ✅ resume checkpoint = "candidates staged" + a zero-candidate marker (mentions move to accept-time); ~~DM-S4a-4~~ ✅ a focused append-only **`candidate_decisions`** evidence table now, **defer** the full §4.2 `edit_history` (text-edit dataset) to the editing milestone; ~~DM-S4a-5~~ ✅ no retention at PoC (rejected-memory is a feature; unreviewed backlog the only growth risk). **ADR 0004** (DM6 intercept-before-write, fuller MADR) to author **test-first with the M3.S4a code**.
- ~~**[Issue #2](https://github.com/yaaisiu/story-forge/issues/2) — invalid neo4j container image tag.**~~ ✅ Resolved 2026-05-21 (PR #3, squash `6694e23`): neo4j → `5.26.25-community-ubi10`, 3 bundled-netty CVEs waived. See Decided + Done.
- ~~**[Issue #4](https://github.com/yaaisiu/story-forge/issues/4) — pgvector (+ ollama) images CVE-stale.**~~ ✅ Resolved 2026-05-21 (PR #6, squash `4cb92a2`): pgvector → `0.8.2-pg17-trixie` (7-day bend), ollama → `0.24.0`, both scoped-waivered; `WAIVERS.md` register added. `main` fully green. See Decided + Done.
- ~~**Decide in Session 2 — language-detection library:** `langdetect` vs `fasttext`.~~ ✅ Resolved 2026-05-21: chose **`langdetect`** (no out-of-band model artifact, ample accuracy for PL/EN prose). See Decided + Done.
- ~~**Decide in Session 3 — auto-chunking trigger heuristic:** which threshold decides "long enough → use Ollama Cloud instead of local"?~~ ✅ Resolved 2026-05-22: config knob `CHUNKING_LOCAL_MAX_WORDS=4000` (conservative); but moot on this GPU-less host where cloud_free is the default tier anyway (see Decided). See Decided.

### Decided

- **2026-06-22 — Session 44 (scope decisions + M4.S3c step-0 decompose).** Mid-session owner scope calls, before any S3c code. **(1) WORLD-LEVEL multi-story is OUT of PoC → `docs/BACKLOG.md`.** The "world graph" — a story belongs to a shared *world X* whose entities become cross-story merge candidates (spec §3.6; the world-graph-parent M4 slice) — is **dropped from the PoC**. This reverses the prior "basic world-graph is in-PoC" framing (the backlog's *World graph from multiple sources* section, updated this session). **(2) The narrowed multi-story capability STAYS in PoC: add a new story that *reuses the existing project graph*, with a graph migration tracking *which entity appears in which story* (per-story entity membership — a property/label or a per-story container).** Purpose: scope the reader/graph to "this story vs the whole project", **and** let known entities **leverage extraction** in a new story (seed the matcher with the project's accepted entities). This **concretizes** the long-deferred **§3.4 story-vs-project scoping** cross-cutting item (the data-model mechanism behind its "this story / whole world" toggle) — it is the in-PoC direction, *not* the dropped world-graph. Owner framed it as "first thing we'll add" for the multi-story work. **(3) New post-PoC backlog item: code documentation generation** — first stone toward *living* project-code documentation (docs derived from source, kept in sync; design wide-open — recorded as a direction). **(4) Pending stop-and-amend (flagged, not yet done):** the spec still *describes* the world graph as in-scope (§3.6 + §9 M4 feature order); reconciling §3.6/§9 to decisions (1)+(2) is a **propose → owner-approve → amend** task before the multi-story slice is built (not a quiet edit). *Considered:* keeping world-graph in PoC "at basic level" (the prior note) — owner reversed it; the narrowed membership capability gives the useful half (per-story scoping + extraction leverage) without the cross-story-merge complexity. **(5) M4.S3c step-0 decompose ran AND its register was RESOLVED this session** (the session's main deliverable) → `architecture/proposals/m4-s3c-manual-tagging.md` (`accepted`, register **RESOLVED** DM-S3c-1..9 / OQ-26). The slice = the **final** "manual correction in the reader" slice (S3a · S3b · **S3c** tag/un-tag/change-boundaries; spec §3.5). **Centre of gravity:** today a rendered highlight is a render-time *search hit with no saved identity* (DM-IH-1; `entity_mentions` offsets are NULL/unused), so a manual span (an inflected form, a pronoun, a new entity) can't be re-found by search and un-tagging acts on a highlight with no row to delete. **Owner resolutions:** **DM-S3c-1 = (B) overlay / "save only what you touch"** — keep render-time search for auto highlights; a manual tag persists a **stored span** (real offsets) that overlays + wins; a rejected highlight writes a **suppression** the resolver subtracts; change-boundaries **materializes** that one occurrence — incremental, **no backfill**, DM-IH-1's rename-free/edit-robust properties preserved (*rejected:* (A) materialize-all = a migration + discards those properties; (C) alias-only = can't do change-boundaries or single-occurrence un-tag). **DM-S3c-2 = both attach-existing AND create-new-entity** (a new human-reached graph writer — INV-9 enumeration grows; needed to catch an entity extraction missed). **DM-S3c-3** occurrence-level (not-an-entity suppresses; not-this-entity suppresses + optional atomic re-tag); **DM-S3c-4** materialize-then-edit; **DM-S3c-5** tag/un-tag/boundary **ride the S3b `graph_edits` undo** (new op-kinds + inverters, contract-tested from writer output per the PR-#108 lesson); **DM-S3c-6** add `source`+`mention_id` to `ReaderHighlight`; **DM-S3c-7 = ADOPT TIPTAP NOW — owner override** of my native-selection lean (pay the editor setup so V2 §4 editing inherits a robust selection/decoration engine; Tiptap = a new fe dep → `/add-dependency` at the fe build; the `<mark>` renderer becomes Tiptap read-only + ProseMirror decorations); **DM-S3c-8** split be/fe; **DM-S3c-9 = no §3.5 *capability* amendment** (the S3a precedent — §3.5 already specifies manual tagging + the three corrections), storage model + INV-9 reword land in **ADR 0008** at build, with a small **§6.4 data-model note** (human-authored mentions carry real offsets) via stop-and-amend if the migration needs it. **Checks:** docs/vault only — no code, no deps.
- **2026-06-22 — Session 43 (M4.S3b-fe built + merged, PR #107; backend merge-undo bug fixed, PR #108) — M4.S3b COMPLETE.** The frontend for graph mutations, plus a bug its smoke surfaced. **(1) Undo affordance lives in the reader *header*, not the entity panel (owner call).** Undo is *story*-scoped (`POST …/graph-edits/undo` reverses the last edit *anywhere* in the story), so a per-entity placement would be semantically wrong. **(2) Property-conflict resolution is computed *client-side* from both entities' `properties`, not from the merge 400.** The merge route's 409/400 conflict body is a plain `{detail: string}` naming only the conflicting *keys* — no values — so a by-hand "pick which value to keep" resolver can't be driven from it; `mergeConflicts.buildConflictRows` mirrors the backend's pure `detect_property_conflicts` over both entities' fetched properties, and the 400 stays a safety net. *Considered & rejected:* round-trip the 400 then fetch values anyway (a wasted round-trip + a fragile string-parse). **(3) `useUndo` invalidates caches only on a *real* apply, never on a preview** (a preview touches nothing). **(4) The merge-undo 500 was op-name drift, fixed in PR #108 (split out, not scope-crept into the FE PR):** the merge writer records `op="discard_self_loop_relation"` (`_merge_rows`, `f"{kind}_relation"`) but `graph_undo.invert_operation` matched the bare `"discard_self_loop"`, so a self-loop-dropping merge's undo raised `UndoNotInvertible` → unhandled → 500 (delete-undo and self-loop-free merges were fine). CI stayed green because the inverter's unit test *hand-built* a fixture with the op name the writer never emits. **Fix:** match the real op + a **writer↔inverter contract test driven from real `_merge_rows` output** (closes the gap); the misleading fixture corrected to the real name (now a true regression). **No spec amendment** (ADR 0007 already covers the contract). **(5) Review-queue cross-cutting nits NOT discharged** — the S42 handoff optimistically said S3b-fe would discharge the "review-UX folds" note, but the reader-editing slices (S3a/S3b-fe) never touched the extraction/relation *review queues*, so the `CandidateView` merge-target-name / per-screen error-mapper / shared-`useReviewQueue` nits remain open (they fire only when a *review queue* next changes). **Retro:** two rules added — (a) `backend/src/story_forge/AGENTS.md`: test a string-coupled producer↔consumer contract from the producer's *real* output, not a hand-built fixture (the general form of the §2 fabricated-fixture lesson); (b) `frontend/AGENTS.md`: during a live smoke, use a `git worktree` for a parallel hotfix rather than `git checkout`-ing the working tree off the branch under test. **Checks:** 460 backend pass + ruff/`mypy --strict` clean; frontend build/lint/test green (247); CI green on both PRs; both review gates ran on #107; owner gave the per-merge OK on #108 and #107; merge/delete/undo verified live in the browser.
- **2026-06-20 — Session 42 (M4.S3b-be2 built + merged, PR #105): whole-entity delete + the general undo executor — INV-3 *executed*.** The first slice that *reverses* recorded graph operations. **(1) DM-S3b-5 survivor-delete confirmed (owner): allow + drift-refuse.** Deleting an entity that is an earlier merge's survivor is allowed unconditionally; if that merge's undo is later attempted, its drift-check refuses (409). No "is this a survivor?" guard on delete — the consistency check is pushed to undo, which already has a drift mechanism. *Considered & rejected:* block-the-delete (adds a survivor-lookup to every delete + a new failure mode for a window drift-refuse already covers). **(2) Re-do-after-undo id collision resolved = a generation discriminator** (the ADR 0007 design point). The deterministic `uuid5` operation id (keyed on the operation's targets, for crash-retry idempotency) collides if the *same* targets are re-operated after an undo recreates them. The build **kept the `undone_at` stamp model** and suffixes the seed with a generation = count of *undone* ops for those targets (`_op_seed`/`_next_generation`); a *live* op probes False so a retry re-derives the same id (idempotent), only an undone prior op bumps it. Applies to re-merge **and** re-delete. *Considered & rejected:* the ADR's delete-the-undone-rows alternative (discards the audit trail, contradicts the stamp model the be1 schema was built for). **(3) Layering:** pure `domain/graph_undo.invert_operation` (rows → ordered inverse plan + declarative drift check, reverse-`seq`) ; I/O in `EntityEditService.delete_entity`/`undo_last`; read path `PostgresEditStore.latest_live_operation` (COALESCE `operation_id`→`id` so S3a singletons are undoable — they now also carry `project_id`) / `mark_operation_undone`. INV-3 *executed* in `invariants.md`; INV-9 fifth witnessed instance (the undo executor *reverses* via the same human-reached writers — adds no new writer); `state-machines/graph-operation.md` (`applied → undone`) drawn. **(4) `/code-review` (multi-agent) caught a real data-loss bug `/review-pr` missed** — `fold_relation` undo issued `RemoveRelation(new_edge.id)` where the folded id is A's *pre-existing* edge, so undoing a merge that folded an edge deleted A's own edge (reachable when two merged entities share a relation to a common neighbour). **Folded** + two sibling undo-correctness fixes surfaced the same pass: a folded `add_relation` undo no longer deletes the pre-existing edge (the add now records `merged_into_existing`), and `remove_relation` undo restores the *full* edge (its before-image is now a full `model_dump`, not subject/predicate/object only — exact confidence/properties; the inverter still tolerates legacy thin rows). **(5) Undo/delete V1 robustness deferred → `docs/BACKLOG.md`** (owner ask, "note what should be done to make it more robust"): undo depth cap; one `created_at` per operation (clock-independent stack head); undo should *report* what open-world churn left unrestored; harden the delete snapshot against a mid-delete crash; collapse `_merge_rows`/`_delete_rows`; O(1) generation lookup. **No spec amendment** — §3.4 (delete) + §10 q2 (undo) were amended in S39; ADR 0007 covers the contract (re-merge resolution recorded into its Consequences). **Checks:** 459 backend pass + ruff/`mypy --strict` clean; frontend build/lint/test green; CI green; both review gates ran; owner gave the per-merge OK.
- **2026-06-20 — Session 41 (governance only — PLAN_SHORT slimming; no feature code).** **(1) `PLAN_SHORT.md` policy tightened to *current-milestone-only*** (owner). The Session-39 rule kept "current + immediately-prior milestone's Done tail", but even one prior milestone left the file at ~365 KB — over the **256 KB single-read limit** (it could no longer be read in one pass). Owner directed: PLAN_SHORT carries the **current milestone only**; all older milestones live in `docs/PLAN_ARCHIVE.md`, archived-and-referenced, **as a standing milestone-roll ritual step** (not a one-off). *Considered & rejected:* keeping M3 as "prior context" (still over the limit); a harder mid-milestone prune of struck cross-cutting/Blocked items (error-prone — deferred to the roll's §3 re-curation, where the whole list is walked anyway). **(2) `docs/AGENTS.md` §7 amended** to match (current-only; archive convention = newest-batch-on-top, verbatim reverse-chron within a batch; what-never-moves clarified). **(3) Ritual wired** (owner, raised S39): `/wrap-session` **step 5c** does/offers the roll-time archive move; `/resume-session` **step 3d** flags bloat (>~200 KB) as a backstop. **(4) First archive batch executed:** M3 + M2 + M1/M0 task sections, Decided, and Done → `docs/PLAN_ARCHIVE.md` (move-not-delete, §4; verified byte-for-byte — dated-bullet count 116 = 18 kept + 98 archived, no duplicates). `PLAN_SHORT.md` **368 KB → ~134 KB**. **Checks:** docs/governance only — no code, no deps; code gates n/a.
- **2026-06-20 — Session 40 (M4.S3b-be1 built + merged, PR #102).** The first M4.S3b backend slice — entity↔entity **merge** — shipped. **(1) Scope: be1 (merge) only** (owner-chosen — took the pre-authorized be1/be2 cut up front; the by-hand property-conflict resolution DM-S3b-2 enlarged merge, and undo can't be tested until merge first writes grouped rows). Delete + the undo executor are **be2**. **(2) Two build-time refinements beyond the resolved register, recorded in ADR 0007:** (a) `graph_edits` gained a **`project_id` column** so the undo stack is project-scoped (the resolved register didn't name it); (b) `repoint_entity_mentions` **returns the moved mention ids** (`RETURNING id`) so the before-image re-points *exactly* those rows back on undo (INV-3 completeness). **(3) Write order refined at build (the `/code-review` fold): delete B *last*** — fold A + re-point edges → re-point mentions → delete B → evidence. The resolved DM-S3b-4 said "Neo4j-then-Postgres"; deleting B first opened a window where its mentions were orphaned and the retry hit `EntityNotFound` (unrecoverable), so the build keeps B alive until the final graph write (still serving DM-S3b-4's retryable-not-half-merge intent); evidence rows reordered to match so undo recreates B before re-pointing mentions. **(4) Known be2 design point (ADR 0007):** the deterministic `uuid5(merge:B:A)` operation id collides if the same pair is re-merged after a be2 undo recreates B — undo must delete an undone op's rows (or add a nonce). **(5) `/review-pr` folds:** dedupe incident edges (a self-loop is returned twice by the undirected neighbourhood query); restrict `resolved_properties` to detected conflict keys (no silent property injection). **ADR 0007** landed (owner-confirmed); INV-9 grep guard widened (+`delete_entity`); INV-3 grouped-log note; relation/candidate lifecycle notes extended. *Considered & not done:* generalizing `_merge_rows` for be2 (YAGNI until be2 lands), parallelizing the neighbourhood reads (PoC scale). **Checks:** 427 backend pass + ruff/mypy clean; frontend lint/build/test clean; CI green; both review gates ran.
- **2026-06-20 — Session 39 (M4.S3b scope + decompose + register RESOLVED; architecture re-sync; slim-plan rule).** Pre-build session — no feature code. **(1) Architecture re-sync ran** (`meta-architect:review-architecture` → `architecture/reports/2026-06-20-architecture-review.md`) before the decompose, as the handoff asked: **no blockers**; the one `risk` was `overview.md`'s as-built stopping at M3 (every M4 slice listed as "Next" though S1/S2/S3a shipped) — **fixed on sight**; three notes' frontmatter dates lagged current bodies (bumped); the invariant/lifecycle layer (INV-9 rewording, ADR-0006, the lifecycle edit-path extensions, OQ-23) was already honest. **(2) M4.S3b decomposed** (`architecture/proposals/m4-s3b-graph-mutations.md`, register **DM-S3b-1..8 / OQ-25 RESOLVED**). Completeness sweep over CRUD-of-{entities,relations,mentions} **closes** — S3b owns entity↔entity **merge** (DM-Rel-5/6 edge re-point + `entity_mentions` re-point), whole-entity **delete**, and **undo execution** (the first *execution* of INV-3, not just S3a's substrate); tag/boundaries stay S3c, split/qualifiers post-PoC. **Owner resolutions:** **DM-S3b-1 = general undo** (any edit/merge/delete reversible newest-first via a grouped append-only `graph_edits` log — a *compensating transaction*; resolves spec §10 q2 as "append-only log of changes, executed"; full versioning = post-PoC) **+ a new requirement: undo must *show what it will reverse*** (each operation carries a human-readable description; the affordance previews + confirms, not a blind pop); **DM-S3b-2 = author picks the survivor then resolves property conflicts BY HAND** (owner chose more control over the agent's survivor-wins lean — enlarges the merge surface, makes the be1/be2 split likely); **DM-S3b-5 = real `DETACH DELETE` + full-snapshot undo** (not soft/tombstone — avoids a hidden-entity read-filter through every screen); DM-S3b-3 = re-point all incident edges, **report** MERGE-collisions, drop post-merge self-loops; DM-S3b-4 = Neo4j→Postgres→evidence, idempotent re-run; DM-S3b-6 = split be/fe (be1 merge / be2 delete+undo fallback pre-authorised); DM-S3b-7 = INV-9 enumeration unchanged (ops live in `EntityEditService`), INV-3 now *executed*, none-at-PoC Expiry + a noted undo depth-cap, **ADR 0007** at build; DM-S3b-8 = `POST …/entities/{eid}/merge` / `DELETE …/entities/{eid}` / `POST …/graph-edits/undo`. **Spec amended this session (owner-approved wording, 2026-06-20): §3.4 gained merge + delete in the entity detail panel; §10 q2 resolved (undo = append-only log of graph changes, deterministic, preview-before-reverse).** §3.5 left for S3c — its right-click is *mention*-level un-tagging, not whole-entity delete (the decompose's "delete → §3.5" was corrected to §3.4 at sign-off). *Considered & rejected:* undo-merge-only (inconsistent INV-3); soft/tombstone delete (read-filter sprawl); survivor-wins property merge (less control). **(3) New process rule — `docs/AGENTS.md` §7: keep `PLAN_SHORT.md` slim; archive completed milestones into `docs/PLAN_ARCHIVE.md` (move, not delete — §4 holds).** Owner flagged this file at 346 KB / 617 lines as too heavy for a read-every-session operational doc; the archive move is queued (cross-cutting) for a `/wrap-session` close. **Checks:** docs/vault only — code gates are no-ops; no deps changed.
- **2026-06-20 — Session 38 (M4.S3a-fe built + merged, PR #98; pydantic-settings waiver, PR #99).** **(1) Bilingual-per-project is out of PoC scope (owner — resolves spec §10 q8 for the PoC).** The merged `EntityEditPatch` takes the canonical name as two fields (`canonical_name_pl`/`_en`), but `GET …/entities/{eid}` returned only a single language-resolved `canonical_name` and never the project language — so the editable panel couldn't tell which slot a name edit writes. Owner's call: **one working language per project** (mostly English); the panel shows a **single** name field and writes the project-language slot. *Considered & rejected:* surfacing both pl/en fields (right for true bilingual, out of scope), and deferring name editing (guts the headline correction use case). **This required the slice's one backend change** (the lone deviation from the handoff's "no backend change needed"): expose `language` on `EntityDetailResponse` — the route already computed it, so a one-field read-side add. **(2) Mid-merge OSV advisory handled in its own PR (owner-chosen sequencing).** A new advisory (**GHSA-4xgf-cpjx-pc3j**, MEDIUM, pydantic-settings `NestedSecretsSettingsSource` symlink traversal) surfaced same-day and red the security gate; it's unrelated to #98. Fix-first was blocked (2.14.2 published 2026-06-19, inside the 14-day soak), so a **time-boxed scoped waiver** (drop-when floor **2026-07-04**) with an **unreachable** rationale (we read `.env` only — the `secrets_dir`/`NestedSecretsSettingsSource` code path is never instantiated). Sequenced as a **separate `fix(security)` PR (#99) → merge → rebase #98** (keeps the feature PR clean; "split unrelated out"), rather than folding the waiver in. **(3) `/code-review` "prefix-invalidation needs `exact:false`" finding verified REFUTED** — TanStack Query v5 fuzzy-matches query keys by default; our `["entity-detail", storyId]` prefix correctly invalidates the 3-element keys. Two genuine cleanup nits folded (a `entityDetailStoryKey` factory for the bare prefix literal; `neighbourNames` reuses `egoNeighbourLabel`). **(4) UX wrinkles deferred to post-PoC (owner) → `docs/BACKLOG.md`:** the relation add/edit UX (richer than a direction toggle), blank-property-key silently dropped on save, and undo *execution* (the `graph_edits` before-image is recorded but no undo button yet — lands with S3b). **(5) Retro → one skill tightening (owner-prompted).** The wrap's `/retro` step had been treated as an *optional aside* — the agent self-assessed "routine" and offered it rather than asking. Owner corrected: the retro prompt is part of the routine. Amended **`/wrap-session` step 2** to make the prompt **mandatory + explicit** — always do the reflective pass, then *ask* the user whether to run `/retro` (or run it); skipping is the user's call in answer to that ask, never the agent's. (The deeper machinery — plan-mode contract-wrinkle catch, separate-waiver-PR pattern — worked as-is.) **Checks:** backend 396 pass + ruff/mypy clean; frontend 218 pass + lint/format/build clean; both PRs CI-green; waiver verified locally (osv-scanner v2.3.8 → exit 0).

- **2026-06-19 — Session 37 (M4.S3a-be built + merged, PR #96).** **(1) DM-S3a-3 resolved-at-build to a *direct* edge-writer (option b) — the one decision that changed from the register's recorded lean.** The register's first choice routed a manual relation-add through `RelationReviewService.decide` to keep it the sole edge-writer; at build the `verify-at-build` falsified that — `decide` resolves an edge's endpoints by **surface-name-within-a-paragraph** (`_resolve` keys on `(paragraph_id, normalized_name)`), which a hand-picked edge between two arbitrary accepted entities has neither of, so a synthetic `staged_relations` row would mean fabricating a paragraph + names that round-trip to the chosen ids. Owner approved the register's **pre-authorized fallback (b)**: `EntityEditService` writes edges **directly** (`create_relation` reused for add, a new `delete_relation`). Consequence: **INV-9's edge-writer set grows too**, under the same "only human-reached handlers" rewording (not just nodes). Recorded in **ADR 0006**, the register DM-S3a-3, OQ-23, and `invariants.md`. *Considered & rejected:* forcing the synthetic-staged-row path (fragile, dishonest). **(2) DM-S3a-2 verify-at-build → one `graph_edits` table** for both node-field and edge edits (`target_kind` discriminates), columns `(target_id, target_kind, op, before, after, actor, created_at)`. **(3) Evidence-loss-on-crash window accepted as the disclosed PoC posture (DM-S3a-6 LWW):** graph-write-first/evidence-last means a crash between them leaves the edit applied but unlogged; documented in the `EntityEditService` docstring + ADR 0006 (one local author; undo-execution is a later slice). **(4) Review-gate fixes folded:** `/review-pr` — a blank `predicate` reached `GraphRelation`'s validator as an uncaught `ValidationError` → **500**, fixed to a boundary **422**; `/code-review` — the predicate validator returned the *unstripped* value (`"  LOVES  "` → a forked `uuid5` edge id + padded Neo4j type), now **stripped** to match how `apply_entity_edit` strips entity `type`. *Considered & left as-is:* the 3-copy `_now()` helper + a Postgres-store base class (out-of-scope drive-by refactors); the idempotent duplicate-add no-op and undirected delete-by-unique-id (by-design). **(5) Retro: no skill/`CLAUDE.md` change.** Observed that the vault's *derived* notes (`INDEX.md`/`CHANGELOG.md`/`overview.md`) read stale post-ADR-0006 (they describe the S36 pre-build state) — but they are architect-regenerated (not hand-edited, per `architecture/AGENTS.md`), so the fix is a near-term `meta-architect:review-architecture` re-sync, **not** a new rule (auto-running the architect from a ritual is the wiring ADR 0002 defers). Owner's call: rely on the resume-ritual triage + remember to verify the re-sync lands. **Checks:** backend 396 pass + ruff/mypy clean; frontend 193 pass + lint/build clean; CI green; no dep changes.

- **2026-06-19 — Session 36 (M4.S3a scope + step-0 decompose + register resolved; backlog additions; vault-only, no production code).** **(1) Backlog additions (owner).** Recorded two forward-looking post-PoC directions in `docs/BACKLOG.md`: **world graph from multiple sources / attributable knowledge** (the non-fiction/research use case — multiple sources that corroborate/disagree, no single ground truth → every claim attributed to its source; grounded in the existing per-mention provenance ADR 0005, the context-dependent cross-source-identity hazard, and spec §10 q3) and **timeline / temporal qualification of relations & properties** (the "when does Garret wear the cloak" need; anchored to the spec's latent `Event` node type + the M4.S2b occurrence-order list, with a *forward-compatibility* note tying it to "get the baseline graph right" — model relations now so a validity interval / event anchor / source attribution can be hung on them later). *Note:* basic world-graph/multi-story is already in the M4 plan; whether *multi-source attribution* is pulled into PoC scope is a future owner scope-call (stop-and-amend flow), recorded as a direction, not scheduled. **(2) "Manual correction in the reader" sliced by write-risk, lowest-first (owner).** **S3a** = *the side panel becomes editable* — edit an accepted entity's `canonical_name`/`aliases`/`type`/`properties` + add/edit/remove relations between accepted entities (no merge, no re-point, no new spans). **S3b** = entity↔entity **merge** (+ the long-flagged DM-Rel-5 written-edge & `entity_mentions.entity_id` re-point + DM-Rel-6 idempotency) + whole-entity **delete** + **undo-merge**. **S3c** = manual **tag** from selection / **un-tag** / **change boundaries** (reopens the DM-IH-1 span-storage decision). General **split** + relation temporal/source qualifiers = post-PoC. *Completeness-checked* the cut against the full create/read/update/delete surface over {entities, relations, mentions} and **folded two gaps into the plan**: entity scalar-field editing (name/aliases/type — needed to clean a baseline graph and to fix highlights that won't render) → **S3a**; whole-entity delete + undo-merge (shares the re-point/cleanup machinery with merge) → **S3b**. *Considered & rejected:* drawing the first slice line at properties-only (too small — can't clean names/types) and pulling merge/tagging into the first write slice (smuggles the two hard forks — re-point, span storage — into the lowest-risk slice). **(3) `meta-architect:decompose-requirement` step-0** → `architecture/proposals/m4-entity-editing.md` (register **DM-S3a-1..8 / OQ-23**), + glossary `backend-for-frontend` + `lost-update` (and the stale glossary index regenerated 22→25, folding in the `ego-graph` note M4.S2 added but didn't index), learning-log ×2, INDEX/changelog. **Centre of gravity = this is the first M4 slice that *writes* the graph**, so the station fingerprint flips from the read views' all-`n/a` (Intent/Decision/Evidence/Review go live) and the weight is the write path + reversibility, not the UI. **(4) DM-S3a register resolved (owner — all four central calls = the recommended option; the rest = my leans, no objection):** **DM-S3a-1 = new, explicitly-named edit handlers** (`PATCH …/entities/{eid}` + relation edit ops, a BFF *write* surface) **+ reword INV-9** "exactly two writers" → "only human-reached handlers — accept, decide, edit" (property unchanged, enumeration grows — the ADR-0005 broaden-don't-mint precedent; the rewording is **ADR-worthy → draft the ADR at build, test-first**). *Rejected:* mint INV-10; reuse the review services. **DM-S3a-2 = a minimal append-only before→after edit-evidence log** (the graph-edit twin of `candidate_decisions`) — real INV-3 undo + data-flywheel substrate; *rejected:* no-record / re-type-to-undo (an INV-3 narrowing the owner declined). **DM-S3a-3 = route a manual relation-add through the existing decide path** (`RelationReviewService` stays the sole edge-writer + the manual edge gets an audit row); re-predicate = **delete + re-add** (edge id = `uuid5(subject_id, predicate, object_id)`), **warn** on a MERGE-collision dedup, **allow** manual self-loops. **DM-S3a-4 = invalidate the reader/entity-detail/graph queries on edit** — payoff: a corrected name **re-highlights for free** (render-time search, no span migration). **DM-S3a-5 = typed `properties` values** (number/bool/string/nested preserved), backend-validated, keys free (INV-4). **DM-S3a-6 = last-write-wins at PoC** (the `lost-update` anomaly named + accepted for one local author). **DM-S3a-7 = split** M4.S3a-be (mutators + edit service + evidence + endpoints, test-first) / M4.S3a-fe (panel edit UI + entity-picker + hooks), the S2a/S2b rhythm. **DM-S3a-8 = reuse `search_entities_route`** as the relation-target picker. **No spec amendment** — §3.4 ("properties editable", "relations editable") + §3.5 (manual correction) already specify the editable panel; the INV-9 *rewording* is the architecture change, landed test-first at build via the ADR. **(5) Next: build M4.S3a-be test-first** — first failing test = the pure boundary-validation/field-merge function (blank name/type rejected, `properties` stays an open JSON object, INV-4 free keys), then the new `Neo4jRepo` committed-object mutators → the edit service emitting the before→after evidence row → the `PATCH …/entities/{eid}` + relation edit endpoints. **Checks:** vault/docs only — no code, no dep changes.

- **2026-06-18 — Session 35 (M4.S2b frontend reader entity side panel built & merged, PR #91).** **(1) DM-SP-4 resolved = cytoscape (owner, browser-verified).** Reused `graph-viewer/GraphCanvas`'s cytoscape pattern (a new `EgoGraphCanvas` mount over the ego subset) rather than a lightweight static view — one graph renderer, and it delivers the two things the owner wanted: **neighbour-node inspection** (tapping a neighbour re-targets the panel to that entity) and **drill-to-source-text** (clicking an occurrence scrolls the reader to that paragraph + flashes the highlight). Verified live against the `c09ff519…` 115-node graph (Garret Locke: 29 neighbours / 40 edges — the hairball stress case). *Considered & deferred:* the static fallback — unnecessary once cytoscape laid out acceptably; dense-graph legibility for high-degree nodes is the §3.4 graph-filters job (post-PoC, `docs/BACKLOG.md`). **(2) Built test-first, mirroring M4.S1's pure-logic → hook → component shape:** pure `occurrences.ts` (occurrences from the reader's rendered highlights, DM-SP-3) + pure `egoElements.ts` (`EntityDetailResponse` → cytoscape elements, focal-flagged, direction-oriented, dangling-edge-dropped) → `useEntityDetail` (`useQuery` over the S2a BFF endpoint) → `ReaderEntityPanel` + `EgoGraphCanvas` + the `ParagraphText`/`TextReader` wiring (click-to-open, neighbour nav, occurrence drill-flash). Read-only (INV-1/3/9 untouched, no LLM). 193 frontend tests green. **(3) Owner browser-check → three folds:** a real scroll bug (panel had no bounded height → occurrences below the fold unreachable; fixed with `max-h`+`overflow-y-auto`), a modest widen (`w-72→w-80`), and longer occurrence snippets (±30→±60 chars). Deeper refinement (resizable/styled panel, dense-graph legibility) is post-PoC. **(4) Reviews:** `/review-pr` (self) + `/code-review` (high, 7-angle) **folded two findings** — memoize `ParagraphText` (the whole-story flash re-render re-ran `splitParagraph` over every paragraph twice per drill) + flash-timer unmount/close cleanup; other candidates verified and left with reasons (cytoscape rebuild mirrors `GraphCanvas`; the UUID `querySelector` is injection-safe; dead re-exports follow the codebase's type-group convention). Codex PR review hit its usage limit (best-effort, not blocking). **(5) No spec amendment** — §3.4/§3.5 already specify the panel + local graph. **(6) Retro (1 change, owner approved):** a `frontend/AGENTS.md` *Manual browser verification* note — when verifying a feature that calls a *recently-merged* backend route, confirm the running backend actually loaded it (it may predate the route, or run without `--reload`; check `/openapi.json` or restart) before blaming the frontend; earned this session when the long-running uvicorn predated the S2a `/entities/{eid}` route and the panel's fetch 404'd. *Considered & rejected as a rule:* a "don't theorize on an unverified absence" discipline (I scanned an incomplete story-id list, got all-0/0, and wrongly theorized the graph was wiped — owner caught it) — too broad to encode without ceremony; logged as a lesson, not a convention. **(7) Two deferred code-level review nits recorded in `docs/BACKLOG.md`** (so they don't evaporate): cytoscape mounts rebuild-on-data-change (shared by `EgoGraphCanvas` + `GraphCanvas`); the panel `max-h-[calc(100vh-3rem)]` magic-constant coupling (folds into the resizable-panel refinement). **Next: the next M4 slice — manual correction / property+relation editing in the reader** (the edit surface this read-only inspection was built to support).

- **2026-06-18 — Session 34 (M4.S2 slice choice + step-0 decompose + M4.S2a backend built & merged, PR #89).** **(1) M4.S2 = entity side panel in the reader (owner).** Picked the **side panel** (spec §3.4 detail panel — canonical/aliases/type/properties/occurrences/relations/timeline — reachable from a highlight click per §3.5, *plus* a §3.5 "local graph around that entity") over the smoke-test-suggested *manual-correction-in-reader*. Owner's reasoning: the read-only inspection surface shows the context (an entity's neighbourhood + occurrences + properties) you need to *judge* a correction, so it should land **before** the edit tools that build on it — manual correction is the **next** M4 slice. Still a **read-only projection** (most stations n/a; INV-1/3/9 untouched; no LLM/INV-5; the read-side echo of INV-1 = show only the accepted graph). *Considered & rejected:* manual-correction-first (you'd be correcting without the context to judge by). **(2) `meta-architect:decompose-requirement` step-0** → `architecture/proposals/m4-side-panel.md` (register **DM-SP-1..8 / OQ-22**), + glossary `ego-graph`, learning-log ×2, INDEX/changelog. **Key finding:** the slice is mostly an *assembly* problem — of the §3.4 panel's data, only **`properties`** is surfaced by no endpoint today, **occurrences** derive from the reader's already-rendered highlights, and **relations/neighbours** filter the project's relations; but **no per-entity *neighbourhood* query exists** (only whole-project `get_relations`). **(3) DM-SP register resolved (owner):** **DM-SP-1 = a focused per-entity endpoint** `GET /stories/{id}/entities/{eid}` (the BFF pattern: server-side cross-store join + the 1-hop neighbourhood + this story's occurrences; needs one new `Neo4jRepo` 1-hop query) — *considered & rejected:* compose client-side from the whole-project graph fetch + reader highlights + `properties`-on-`GraphNode` (less backend, but loads the entire project graph into the reader and pushes the neighbour-filter into untested-in-Python TS). **DM-SP-7 = split** (carried by 1a): **M4.S2a backend** (the endpoint + the 1-hop query, test-first) + **M4.S2b frontend** (panel + ego-graph render + occurrence drill-down), mirroring M4.S1's S32/S33 cut. **DM-SP-2 = strict 1-hop ego-graph** (entity + direct neighbours + entity-incident edges only — legible in a narrow panel; richer/deeper views are later, pair with the §3.4 graph filters). **DM-SP-5 = `properties` from the endpoint**, read-only key→value (open-world, no fixed keys; escaped). **DM-SP-6 = a new reader panel** mirroring the graph viewer's `NodeDetailsPanel` *structure* but not forced into one shared component (they diverge when editing lands — rule-of-three). **DM-SP-3 = occurrences driven off the *rendered highlights*** (the panel agrees with the prose; doubles as §3.4's timeline; click → scroll-to-paragraph + flash), **DM-SP-8 = confirm** (occurrences story-scoped, neighbourhood project-scoped — inherits the §3.4 project-vs-story debt the reader endpoint first carried), **DM-SP-4 = confirm-at-build** (reuse `GraphCanvas`/cytoscape for the mini-graph vs a lightweight static view in a ~288px panel). **No spec amendment** — §3.4/§3.5 already specify the panel + local graph; the slice is a read-only projection. **(4) Latent coupling re-surfaced (not this slice's fix):** a future M4 entity↔entity merge must re-point written **edges** (DM-Rel-5) + `entity_mentions.entity_id` (M4.S1) or the ego-graph draws an edge to a ghost / the panel drops an occurrence — this slice fail-closes (omit the dangling neighbour). **(5) M4.S2a backend built & merged this session (PR #89, `4bc7ff5`):** `domain/neighbourhood.py` (pure `build_ego_graph`) + `Neo4jRepo.get_neighbourhood` (targeted 1-hop, both directions) + `GET /stories/{id}/entities/{eid}`, test-first (8 unit + 5 integration). **Build-time refinement of DM-SP-1a's bundle:** occurrences are **not** in the endpoint — DM-SP-3 makes them the reader's rendered highlights, derived client-side in S2b, so the endpoint is exactly `properties` + ego-graph (the M4.S1 one-pure-function-+-one-endpoint shape). DM-SP-1a's 1-hop-Cypher `verify-at-build` resolved: `infra/neo4j/init.cypher` has the `Entity.id` uniqueness constraint (index-backed lookup). `/review-pr` (×2, self) + `/code-review` (high, 7 finder angles) **folded one should-fix** — the neighbourhood query is now project-scoped (`WHERE n.project_id = e.project_id`), §6.4 defense-in-depth matching `get_relations`; nits (domain models as DTOs, single-404 description) left as deliberate pattern-consistent choices. **Next: M4.S2b frontend** (the panel + ego-graph render + occurrence drill-down); DM-SP-4 (cytoscape-reuse vs static mini-graph) is its confirm-at-build.

- **2026-06-18 — Session 33 (M3→M4 vault sync + M4.S1 frontend reader + live smoke test + a backlog-file process change).** **(1) Vault-first, then build (owner).** Cleared the carried open-block B before any code: a `meta-architect:*` vault pass (PR #85) drew `state-machines/relation-lifecycle.md` (closes OQ-20), flipped `proposals/m4-inline-highlights.md` → `accepted` + annotated the DM-IH register resolved-as-built (OQ-21), fixed the `glossary/model-tier-routing.md` "not yet built" drift; the honest subtlety recorded: `held`/`committable` are *derived views* of the persisted `staged` row, not enum values. **(2) DM-IH-5 = reader-local fixed palette + hash fallback + legend (owner).** The graph viewer keeps its own pure-hash colouring, so a long-tail type may colour differently across the two linked views — *considered & rejected:* a shared colour module (would re-colour the shipped graph; beyond the surgical reader slice). Divergence flagged in `docs/BACKLOG.md`. **(3) DM-IH-6 = whole-story render, measured fine** on the Oakhaven draft (no virtualisation needed yet). **(4) M4.S1 frontend built test-first** (`features/text-reader/`: pure splitter + palette + `useReader` + `<mark>` renderer + tooltip + legend + route + graph link; PR #86); `/review-pr` (self, folded 2 coverage gaps) + `/code-review` (high, 7-angle, **no confirmed bug** — all candidates refuted by the backend contract: overlap/zero-length impossible, `Math.abs(INT_MIN)` is positive in JS). **(5) Live smoke test verified the whole pipeline end-to-end** (Oakhaven story from scratch: upload → manual chunk → extract → review/dedup incl. the Elira↔Elara reject → relations → reader). It surfaced ~12 post-PoC items + concretely **killed the auto-commit idea**: the matcher scored two *different* sisters at **0.91** (> the 0.85 flip line), so any auto-commit threshold would have silently fused them — the human gate caught what no similarity threshold could. **(6) Retro (1 change, owner approved — a post-PoC backlog file).** Created **`docs/BACKLOG.md`** for concrete post-PoC items (features/UX/bugs/refinements), so `PLAN_LONG.md` stays milestone-level strategy; migrated this session's ~12 items out of `PLAN_LONG`; **wired the routing into every "where things live" home** — root `AGENTS.md` (Reference docs + Where-knowledge-lives), `docs/AGENTS.md`, and the `/wrap-session` + `/review-pr` skills. *Considered & rejected:* keeping everything in `PLAN_LONG` (bloats the strategic plan) and routing tactical nits to GitHub issues (heavier to manage solo at PoC). The other retro signals **confirmed existing practice** (no change): `/resume-session` caught a real path drift (`features/reader/`→`features/text-reader/`); the layered review gate + owner-holds-merge-button worked; the live-test norm earned its keep spectacularly. **(7) npm-audit treadmill → §6.7 audit-scope amendment (PR #88, owner-approved during the wrap).** While wrapping, the frontend `npm audit` gate red on a newly-surfaced **HIGH undici** advisory (GHSA-vmh5-mc38-953g) reaching us *only* transitively via `jsdom` (the test DOM env, a devDependency) — never shipped, unreachable, and with no 14-day-soaked fix (undici 7.28.0 was 3 days old). Triaged via `/triage-advisory`. **Owner's §6.7 call: scope the gate to shipped deps** — `npm audit --omit=dev --audit-level=high` — so a dev-only advisory can't red `main` on un-shipped code (standard SPA posture; *adapts, not relaxes*). **Spec §6.7 amended** (the stop-and-amend flow); reconciled across `ci.yml`, `/triage-advisory` + `/add-dependency` (the §2 sweep caught both as stale homes), and an orienting `architecture/learning-log` + SCA-proposal forward-pointer ("scan what you ship"). Also **fix-first the LOW react-router CSRF** (GHSA-84g9-w2xq-vcv6) on the shipped `react-router-dom`: bump 7.15.0 → 7.16.0 (soaked 21 days). *Considered & rejected:* a waiver tool (heavier; adds an audit-ci dep) and forcing the unsoaked undici 7.28.0 (bends the 14-day soak). **Lesson:** a security gate should reflect *shipped* risk — narrowing scope to what ships is the principled escape hatch for a genuinely-unreachable dev-dep advisory with no soaked fix. **Reviews/merges:** all three PRs CI green, review passes each, squash-merged on the owner's per-merge OK (#85 `2f361ff`, #86 `2795ae1`, #88 `c335801`).

- **2026-06-18 — Session 32 (M4.S1 backend + python-multipart waiver drop): inline-highlights span resolution decided by verifying the data first; the read-only backend shipped; a soak-date-floor rule folded.** **(1) DM-IH-1 — span resolution = render-time string search over name + aliases (owner, after a verify-first detour).** The owner chose "decide after I verify" over committing to an option blind; the verification settled it: the accept path (`candidate_review.py`) writes the mention with **null** `span_start/end`, the staged candidate carries no offset, and the spaCy `CandidateSpan` lives on a separate unlinked path — so the proposal's option (b) "persist the span we already have" was **illusory** (there is no span at accept time). And aliases *accumulate* the prose's surface forms (every merge-accept calls `add_alias(target, candidate_name)`), so an inflected form that was itself extracted+merged becomes a searchable alias — render-time search over canonical + aliases is materially stronger than the decompose assumed. *Considered & rejected:* persist-spans-now (no span to persist; a backend+data change in the slice the owner picked *because* it was low-risk UI), and a verify-build-measure hybrid up front (the verify already answered it). **(2) DM-IH-3 = plain `<mark>` renderer** (owner) — Tiptap/ProseMirror arrives with the manual-annotation slice where editing begins; *rejected:* adopt Tiptap now (ceremony ahead of need for a read-only view). **(3) DM-IH-2/4/7/8 built as proposed** — story-scoped `GET /stories/{id}/reader` doing the cross-store join + a tooltip catalog of only the entities that appeared; longest-match overlap; accepted-only (the read-side echo of INV-1, since mentions are written only by the accept path); tooltip = name+type+aliases. **DM-IH-5/6 (colour, perf)** deferred to the frontend slice (confirm-at-build). **No spec amendment** — §3.5 already specifies the reader; the slice is a read-only projection. **(4) `/code-review` caught a real crash `/review-pr` missed (5th consecutive substantive PR — #36/#60/#63/#78/#81):** the resolver matched against a *casefolded copy* of the paragraph, but casefold can change length on an expanding codepoint (ß→ss, İ→i̇), pushing an offset past the original text → `IndexError`/500 on the whole reader endpoint. Fixed by matching against the **original** text via `re.IGNORECASE` (+ `re.escape` so a name's regex metachars stay literal); regression-tested. **(5) python-multipart OSV waiver dropped (PR #82)** — the S31-deferred fix-first drop: 0.0.31 cleared its 14-day soak, bumped via `/add-dependency`, `GHSA-v9pg-7xvm-68hf` removed from `osv-scanner.toml` + `WAIVERS.md`; OSV scan clean, nothing unmasked. **(6) Retro (1 change, owner approved — soak-date floor):** a dated "drop when (soaks)" must be a **floor** = fixed-version publication date **+ (soak_days + 1)** (package + **15**, image + **8**), because the age gate (`check_dependency_age.py`: `NOW − 14d`) is **time-precise to the second** while a bare `+14` date is intra-day optimistic — this session a 0.0.31 published 06-04 08:27 UTC was still ~53 min too young at the recorded 06-18 drop date. Folded into `/triage-advisory` step 3 + the `infra/osv/WAIVERS.md` header. *Considered & rejected:* a "re-check on the drop day" note (weaker — still surprises); retro-editing the existing starlette dates (the drop-revisit re-runs the real age check anyway). The two other lessons **confirmed existing rules** (no change): `/code-review`-for-substantive-code worked (caught the crash), and verify-first (the deterministic-over-LLM instinct) paid off. **Reviews:** `/review-pr` (self) + `/code-review` (high, 7-angle); 347 backend tests green; both PRs CI 4/4 green, squash-merged.

- _**Earlier decisions (Milestone 3 and before)** — see **[`docs/PLAN_ARCHIVE.md`](PLAN_ARCHIVE.md)**._

### Done in previous sessions

- **2026-06-22 — Session 44: M4.S3c scoped + decomposed + register RESOLVED; M4 multi-story scope re-cut (design/planning only — no production code).** The next slice's full design pass, plus owner scope decisions that reshaped the rest of M4. **(1) M4.S3c — manual tag / un-tag / change-boundaries decomposed** (`meta-architect:decompose-requirement` step-0 → `architecture/proposals/m4-s3c-manual-tagging.md`, `accepted`, register **DM-S3c-1..9 / OQ-26 RESOLVED**). It's the final "manual correction in the reader" slice (after S3a edit + S3b merge/delete/undo). The dominating finding: a rendered highlight today is a render-time **search hit with no saved identity** (the M4.S1 design left `entity_mentions` offsets NULL and searches the prose live), so a manually-tagged span (an inflected form, a pronoun, a brand-new entity) can't be re-found by search, and "this highlight is wrong" has no stored row to delete. **Owner resolved the storage model = "save only what you touch"** (keep the live search for auto highlights; persist real offsets only for spots tagged/corrected by hand; record a small suppression note for a rejected highlight — incremental, no data migration, the M4.S1 "rename re-highlights for free" behaviour preserved), plus: tagging can **create new entities** (not just attach to existing), corrections are **undoable** via the existing story-scoped Undo, and — overriding the architect's lean — **adopt the Tiptap editor now** so the V2 text-editing features inherit a robust selection engine. New glossary term `materialization`; the storage model + the INV-9 enumeration growth land in **ADR 0008** at build (no §3.5 capability amendment — §3.5 already specifies manual tagging; a small §6.4 data-model note if the migration needs it). **(2) M4 multi-story re-scoped (owner):** the **world-graph** layer (cross-story world merge, §3.6) is **dropped from the PoC** → `docs/BACKLOG.md` (reversing the prior "basic world-graph is in-PoC" note); what stays is the narrower **"add a new story that reuses the existing project graph + per-story entity membership"** (which entity appears in which story — drives the §3.4 "this story / whole world" toggle *and* lets known entities seed extraction in a new story). The spec §3.6/§9 still describe the world graph — a **stop-and-amend is flagged** before that slice is built. **(3) New backlog item: code-documentation generation** (first stone toward living, source-derived project docs). **(4) Retro → one rule added (owner-approved):** root `AGENTS.md` "How to communicate" — when surfacing a decision-register entry for the owner to resolve, translate it to plain language, never lift the register's architect-shorthand into the question (earned this session: the first attempt at the storage-model question was jargon-packed and unparseable; re-asked plainly, it was immediately clear). **Gap-check (owner-requested):** ran the `/review-pr` §2 reconciliation sweep over the docs/vault diff — caught + fixed a stale `PLAN_SHORT` Decided entry still calling the S3c register "OPEN" (the authoritative home of the resolutions) and two stale forward-references to the now-dropped world graph. **Checks:** docs/vault only — no code, no deps; backend/frontend gates are no-ops. **Next: build M4.S3c-be test-first** — first failing test = the pure reconciling-resolver (search hits ∪ saved manual spans − suppressions → final highlight ranges); then the tag/un-tag/boundary mutators + endpoints + the new undo op-kinds; then M4.S3c-fe brings in Tiptap (via `/add-dependency`). ADR 0008 at build.
- **2026-06-22 — Session 43: M4.S3b-fe shipped — merge/delete/undo reader UI (PR #107, merged) — and a backend merge-undo bug it surfaced, fixed (PR #108, merged). M4.S3b is COMPLETE end-to-end.** The frontend for the graph-mutation routes, test-first in the S3a-fe rhythm (pure mapper → hook → component): `mergeConflicts.ts` (by-hand property-conflict resolver computed *client-side* from both entities' properties, because the merge 400 carries only conflicting key names, not values — mirrors the backend's `detect_property_conflicts`); `useMergeEntities`/`useDeleteEntity`/`useUndo` (undo invalidates the reader/graph/detail caches only on a *real* apply, never on a preview); `MergeControls` (reuse `EntityPicker` → conflict resolver → confirm → `MergeSummaryResponse` counts; self-merge blocked client-side); delete-with-confirm in `ReaderEntityPanel` (closes the panel on success); `UndoButton` in the reader *header* (undo is **story-scoped** — owner call) with preview→confirm→apply and 404 (nothing to undo) / 409 (drift) handling. Both review gates ran clean; three self-review nits folded before merge (shared `formatPropertyValue` leaf module, reset-summary-on-new-pick, full-width open merge section). **The browser smoke caught a real backend bug** (delete+undo and self-loop-free merges worked, but undoing a merge that *dropped a self-loop* 500'd): op-name drift — the merge writer records `op="discard_self_loop_relation"` (`_merge_rows`) while `graph_undo.invert_operation` matched the bare `"discard_self_loop"`, so the row hit `raise UndoNotInvertible` (unhandled → 500). CI had stayed green because the inverter's unit test hand-built a fixture with the op name the writer never emits. Fixed in PR #108 (split out as its own backend PR, not scope-crept into the FE PR): match the real op + a **writer↔inverter contract test driven from real `_merge_rows` output**, and the misleading fixture corrected to the real name (now a true regression). Verified live after the fix: merge→undo (magistrate restored), re-merge, and delete→undo all work. **Retro → two rules added (owner-approved):** (a) `backend/src/story_forge/AGENTS.md` — test a string-coupled producer↔consumer contract from the producer's *real* output, not a hand-built fixture of the string (the producer/consumer generalisation of the `/review-pr` §2 fabricated-fixture lesson, which had only covered HTTP-status/LLM-schema fixtures); (b) `frontend/AGENTS.md` — during a live smoke, use a `git worktree` for a parallel hotfix rather than `git checkout`-ing the working tree off the branch under test (mid-smoke the checkout flipped the reader to `main` and the buttons vanished). **Note:** the S42 handoff's claim that S3b-fe would discharge the "review-UX folds" cross-cutting nits was optimistic — the reader-editing slices never touched the extraction/relation *review queues*, so those nits stay open. **Checks:** 460 backend pass + ruff/`mypy --strict` clean; frontend 247 pass + build/lint/format clean; CI green on both PRs; owner gave the per-merge OK on each.
- **2026-06-20 — Session 42: M4.S3b-be2 shipped — whole-entity delete + the general undo executor (PR #105, merged). INV-3 moves from *recorded* to *executed*.** be1 wrote the grouped, append-only `graph_edits` log; be2 consumes it. **Delete:** `EntityEditService.delete_entity` removes an accepted entity, its relations, and its text occurrences (real `DETACH DELETE` + a full before-image snapshot — node + incident edges + mentions), recorded as one grouped reversible operation; `DELETE /stories/{id}/entities/{eid}` (204/404/503). **Undo:** `undo_last` → `POST …/graph-edits/undo` (undo-last) reverses the newest live operation — reads it, inverts it in reverse `seq` via the pure `domain/graph_undo.invert_operation`, runs a drift check (refuse → 409 if the target was edited/re-created since), applies each inverse through the same human-reached writers, then stamps it `undone`; `?preview=true` reports *what would be reversed* without acting (DM-S3b-1 see-what-I-undo). Read path: `PostgresEditStore.latest_live_operation` (project-scoped; `COALESCE(operation_id, id)` so S3a singleton edits are undoable too) + `mark_operation_undone`. **Two design points from ADR 0007 settled:** DM-S3b-5 survivor-delete = allow + drift-refuse (owner-confirmed); the re-do-after-undo `uuid5` id collision = a generation discriminator that keeps the `undone_at` stamp model (recorded back into ADR 0007's Consequences). **Architecture:** INV-3 *executed* + INV-9 fifth witnessed instance in `invariants.md`; new `state-machines/graph-operation.md` (`applied → undone`); INDEX updated. **Reviews — the two-gate rule paid off again (6th+ substantive PR):** `/review-pr` (self) flagged the doc reconciliation + crash-window notes; **`/code-review` (multi-agent, high) caught a real data-loss bug** — `fold_relation` undo deleted the survivor's pre-existing edge — plus two sibling undo-fidelity issues, all folded with regression tests (incl. a fold-undo integration test). Undo/delete V1 robustness items (depth cap, clock-independent stack head, unrestored-churn signal, mid-delete-crash hardening, row-factory dedup) recorded in `docs/BACKLOG.md` at the owner's request. **Checks:** 459 backend pass + ruff/`mypy --strict` clean; frontend build/lint/test green; CI green. **Lesson:** the documented "run `/code-review` in addition to `/review-pr` on substantive destructive write-paths because a missed bug is costly and the Codex net is thin" rule earned its keep yet again — the fold data-loss bug was exactly the cross-file/edge-case class a single self-review structurally can't catch. **Next: M4.S3b-fe** (merge flow UI + delete-with-confirm + an undo affordance that previews what it reverses). **Retro → one ritual tightening (owner-approved): neutral waiver-soak framing in the handoff.** The resume brief misread the starlette waivers as "soaked / actionable" when their drop-floor dates were days in the *future* — caused by the prior handoff's pre-judged "DUE THIS WEEK / should have soaked by now" wording (the soak-equivalent of a false infra anchor — it goes stale, and acting on it bumps a dep still inside its 14-day soak, a §6.7 misstep). Fixed both halves: **`/wrap-session` step 7** now requires waiver lines to state a *neutral floor date to compute against* ("droppable on YYYY-MM-DD"), never a verdict; **`/resume-session` step 3b** now says compute drop-readiness from the floor date vs today, don't inherit the handoff's framing. *Considered & rejected:* a rule for the pre-commit `ruff-format`-aborts-a-commit micro-friction (self-correcting — the hook is its own safety net). The two-gate review catching the fold data-loss bug *confirmed* the existing rule (no change).
- **2026-06-20 — Session 41: PLAN_SHORT slimmed to current-milestone-only; `docs/PLAN_ARCHIVE.md` created (governance only, no feature code).** A focused cleanup session: `PLAN_SHORT.md` had reached 368 KB / 661 lines — past the 256 KB single-read limit, so it could no longer be read in one pass, making every session start harder (owner-flagged). **What changed:** (1) **policy tightened** — `docs/AGENTS.md` §7 now says PLAN_SHORT carries the **current milestone only** (was "current + immediately-prior Done tail"; one prior milestone still blew the limit), with older milestones archived-and-referenced as a standing milestone-roll step. (2) **First archive batch moved** — Milestone 3 (Cascade matching) + Milestone 2 (Basic extraction) + M1/M0 task sections, Decided entries, and Done log → new **`docs/PLAN_ARCHIVE.md`** (dated, append-only, newest-batch-on-top; *move, not delete* per §4). The move was scripted for exact byte fidelity and verified — dated-bullet count 116 = 18 kept + 98 archived, no bullet lost or duplicated, every `Session NN` marker preserved. `PLAN_SHORT.md` **368 KB → ~134 KB**. (3) **Ritual wired** so it stays maintained: `/wrap-session` step 5c does/offers the archive move at a milestone roll; `/resume-session` step 3d flags bloat as a backstop. Live cross-cutting/Blocked working lists stayed in PLAN_SHORT (§7 "what never moves") — their struck/resolved items move at the roll of the milestone that raised them. **Next session resumes M4.S3b-be2** (delete + undo executor) unchanged. **Checks:** docs/governance only — no backend/frontend/dep changes, so code gates are no-ops; the handoff block's stale "archive queued" clauses were refreshed.
- **2026-06-20 — Session 40: M4.S3b-be1 shipped — entity↔entity merge, the first operation to re-point already-committed graph identity (PR #102, merged).** Merging entity B into survivor A now folds B's aliases/properties into A (author-resolved property conflicts — by hand, DM-S3b-2), re-points every edge and mention incident to B onto A, and `DETACH DELETE`s B — all recorded as **one grouped, reversible `graph_edits` operation** (the compensating-transaction substrate undo will consume). **What landed (test-first, layered):** `domain/entity_merge.py` (pure `plan_merge`/`detect_property_conflicts` — alias union, by-hand conflict resolution, content-addressed edge repoint/fold/self-loop planning); the `graph_edits` **grouping migration** (`operation_id`/`seq`/`op_kind`/`description`/`project_id`/`undone_at`) + `PostgresEditStore.record_operation`; `Neo4jRepo.delete_entity` + `repoint_entity_mentions` (bulk `UPDATE … RETURNING` the moved ids); `EntityEditService.merge_entities` (Neo4j graph writes → Postgres mention re-point → **delete B last** → grouped evidence, deterministic ids for crash-retry idempotency); the `POST /stories/{sid}/entities/{eid}/merge` route (400/404/409/503 declared; OpenAPI snapshot + typed client regenerated). **Architecture:** **`docs/decisions/0007`** drafted (owner-confirmed — the merge/delete/undo contract + grouped-log-as-undo-substrate + the two-store write order + the accepted crash-without-audit window); INV-9 grep guard widened (+`delete_entity`, fourth-instance); INV-3 grouped-log note (substrate written now, *executed* in be2); relation/candidate lifecycle notes extended (edge `written → removed` + new `→ written` on re-point; committed node `→ deleted`). **Scope = be1 (merge) only** (owner took the pre-authorized be1/be2 cut up front); delete + the undo executor are **be2**. **Checks:** 427 backend tests + ruff/`mypy --strict` clean; frontend lint/build/test clean; CI green; both review gates ran. **Review catches folded:** `/review-pr` — incident-edge dedup (an undirected neighbourhood query returns a self-loop twice) + restrict `resolved_properties` to detected conflict keys (no silent property injection); **`/code-review` — the write-order retryability bug** (delete-B-before-mention-move orphaned mentions and made the retry 404; reordered so B is deleted last). **Build-time refinements recorded in ADR 0007:** a `project_id` column on `graph_edits` (project-scoped undo) and the mention re-point returning the moved ids (undo completeness) — neither was named in the resolved register. **Lessons:** (a) the two-gate review held exactly as the rules intend on a destructive write-path — `/code-review` caught a real cross-store ordering bug `/review-pr`'s own §1 "enumerate each inter-write failure window" rule should have caught but I under-applied; the documented "run `/code-review` on substantive write-path because the Codex net is thinner" rule is what backstopped it (the secondary Codex reviewer was over its usage limit and posted nothing). (b) Building the *complete* undo before-image during be1 (even though undo executes in be2) surfaced two schema needs early (`project_id`, returned mention ids) — designing the consumer's needs while building the producer paid off. **Retro: no skill/`CLAUDE.md` change** — nothing cleared the bar (the machinery worked); owner agreed to skip `/retro`.
- **2026-06-20 — Session 39: M4.S3b scoped + decomposed + register RESOLVED; architecture re-sync; spec amended; a new slim-plan rule. Pre-build session, no feature code.** A planning/governance session that leaves M4.S3b ready to build test-first. **(1) Architecture re-sync** (`meta-architect:review-architecture` → `architecture/reports/2026-06-20-architecture-review.md`), run before the decompose as the handoff asked: **no blockers**; the one `risk` was `overview.md`'s as-built stopping at M3 (every M4 slice listed as "Next" though S1/S2/S3a shipped) — **fixed on sight** (added a *Built (M4 so far)* block); three notes' frontmatter dates lagged current bodies (bumped); the invariant/lifecycle layer (INV-9 rewording, ADR-0006, the lifecycle edit-path extensions, OQ-23) was already honest from the S36 decompose + S37 build. **(2) M4.S3b decomposed** (`architecture/proposals/m4-s3b-graph-mutations.md`, `accepted`; register **DM-S3b-1..8 / OQ-25** resolved). The first slice to **re-point already-written graph state**: entity↔entity **merge** (DM-Rel-5/6 edge re-point + `entity_mentions` re-point), whole-entity **delete**, and **undo execution** — the first *execution* of INV-3 (S3a only recorded the before-image). Completeness sweep over CRUD-of-{entities,relations,mentions} **closes** — no slicing gap. **Centre of gravity:** a merge/delete is *one action = N writes* but the S3a `graph_edits` log is per-row + ungrouped, so undo needs a grouped append-only log (a **compensating transaction** — new glossary term) — which also resolves spec §10 q2 the lightest way. **Owner decisions:** general undo + *see-what-you-undo* preview; **merge = author picks survivor then resolves property conflicts by hand** (owner overrode the survivor-wins lean for more control); real `DETACH DELETE` + full-snapshot undo. **(3) Spec amended** (owner-approved wording): **§3.4** gained merge + delete in the entity detail panel; **§10 q2** resolved (undo = append-only log of graph changes, deterministic, preview-before-reverse). The decompose's "delete → §3.5" was **corrected to §3.4** at sign-off — §3.5's right-click is *mention*-level un-tagging (the later S3c slice), not whole-entity delete. **(4) New process rule** — `docs/AGENTS.md` §7: keep `PLAN_SHORT.md` slim, archive completed milestones into `docs/PLAN_ARCHIVE.md` (move, not delete — §4 holds); the owner flagged this file at 346 KB / 617 lines as too heavy for a read-every-session doc. The archive move is queued (cross-cutting) for a future wrap. **Checks:** docs/vault/spec only — no backend/frontend code, no deps; code gates n/a. **Lessons:** (a) running the architecture re-sync *before* the decompose (so the architect reads current state) is the right order at a slice boundary — the resume handoff called it and it paid off; (b) when a decompose names a spec section for an amendment, verify the section's *current content* fits before asserting it — the "delete → §3.5" inheritance was wrong (§3.5 is mention-level) and was caught at spec-sign-off, not before; (c) when the owner flags a *recurring* hygiene problem ("better than accumulation"), encode a durable rule, not just a one-off task. **Retro → one skill tightening (owner-approved):** lesson (b) was encoded into **`meta-architect:decompose-requirement`** — a register entry naming a *spec section* as a decision's home/amendment target is a hypothesis until that section is read (the spec analogue of the existing `verify-at-build` rule for un-inspected external-tool behaviour); the §3.5→§3.4 delete miss is the worked example.
- **2026-06-20 — Session 38: M4.S3a-fe shipped — the editable entity side panel (PR #98, merged); M4.S3a is complete.** The reader's entity side panel, read-only since M4.S2b, now **edits** committed graph state — the first reader→graph mutation UI. **What landed (test-first, mirroring S2a/S2b pure-mapper → hook → component):** three mutation hooks (`useEntityEdit` PATCH, `useAddRelation` POST, `useRemoveRelation` DELETE) + `patchJsonBody`/204-safe `del` client helpers, each invalidating the reader + graph + entity-detail queries on success (DM-S3a-4 — a corrected name re-highlights / a new type recolours for free via render-time search); a pure `propertiesEditor.ts` (typed string/number/bool rows, nested/array/null preserved as read-only JSON — DM-S3a-5, free keys INV-4); and `ReaderEntityPanel` gaining an Edit mode (name/type/aliases/typed properties) + a relations add (reusing the M3.S4d `EntityPicker`, DM-S3a-8)/remove section that surfaces the `merged_into_existing` collision flag as a warning. Re-predicate = client-side remove + add; self-loops allowed; LWW at PoC (DM-S3a-6). **One small read-side backend add:** `language` on `EntityDetailResponse` so the single name field writes the project-language `canonical_name_{pl,en}` slot — **bilingual-per-project is out of PoC scope** (spec §10 q8, owner); OpenAPI snapshot + typed client regenerated. **A same-day OSV advisory** (GHSA-4xgf-cpjx-pc3j on pydantic-settings) red the security gate mid-merge; handled in its **own** `fix(security)` PR (#99, time-boxed waiver — code path unused; drop-when 2026-07-04) which merged first, then #98 rebased clean. **Checks:** 396 backend + 218 frontend tests green, ruff + `mypy --strict` + frontend lint/format/build clean; both PRs CI-green; live browser smoke passed. **Review:** own `/review-pr` + `/code-review` (Codex hit its usage limit, didn't post); the one "CRITICAL" code-review finding was verified false (TanStack v5 fuzzy-matches keys by default); two cleanup nits + a smoke-test UX nit (relation-direction toggle) folded. **Lessons:** (a) plan-mode exploration caught a real contract wrinkle (bilingual name vs single resolved field) *before* coding and turned it into a clean owner decision — the resume/plan discipline working; (b) the OSV treadmill landing mid-merge was absorbed without improvisation by the existing separate-waiver-PR-then-rebase pattern; (c) UX polish was explicitly deferred to `docs/BACKLOG.md` by the owner — PoC stays focused. **Retro: tightened `/wrap-session` step 2** so the `/retro` prompt is mandatory + explicit (always ask the user; don't self-assess "routine" and skip the ask) — prompted by owner feedback that the retro is part of the wrap routine, not an optional aside.

- **2026-06-19 — Session 37: M4.S3a-be shipped — the first M4 *write* slice's backend (PR #96, merged).** An accepted entity's `canonical_name`/`aliases`/`type`/`properties` and the relations between accepted entities are now editable under a new **human-reached** graph writer, with a reversible audit trail. **What landed (test-first, layered):** `domain/entity_edits.py` (pure `apply_entity_edit`/`diff_entity` — blank name/type rejected, `properties` stays an open JSON object with free typed keys, INV-4/DM-S3a-5); `Neo4jRepo.update_entity`/`delete_relation`/`get_relation` (project-scoped); the `graph_edits` before→after evidence table (one table, `target_kind`-discriminated — DM-S3a-2) + `PostgresEditStore`; `EntityEditService` (TOCTOU/tenancy guard, graph-write-then-evidence-last, no-op skip, duplicate-add collision flag, manual self-loops allowed); and the `PATCH /stories/{id}/entities/{eid}` + `POST`/`DELETE /stories/{id}/relations` routes (every non-2xx declared; OpenAPI snapshot + typed client regenerated). **Architecture:** INV-9 reworded ("exactly two writers" → "only human-reached handlers — accept, decide, edit"), **`docs/decisions/0006`** drafted (broaden-don't-mint, the ADR-0005 precedent), the relation/candidate state-machine notes extended (manual-add origin + `written → removed`; the committed-entity edit self-transition). **The one decision that changed at build:** DM-S3a-3 resolved to a **direct edge-writer** (not the decide path — that path is surface-name/paragraph-keyed, which a hand-picked edge lacks), so INV-9's edge-writer set grows too; recorded across ADR 0006 / register / OQ-23 / invariants. **Checks:** 396 backend tests + 193 frontend tests green, ruff + `mypy --strict` + frontend lint/build clean, CI green; both review gates ran. **Review catches folded:** `/review-pr` — blank-predicate 500→422; `/code-review` — predicate whitespace-strip. **Lessons:** (a) the decompose's `verify-at-build` markers + the owner-fallback pattern paid off exactly as designed — a design assumption (route manual edges through the decide path) was *falsified at build* before it became code, and one plain-language `AskUserQuestion` resolved it cleanly with the flip recorded in the ADR; (b) `/review-pr` and `/code-review` each caught a distinct real bug the other missed — the "two reviewers, raise the bar" pattern held; (c) the vault's source-vs-derived-notes boundary (`architecture/AGENTS.md`) prevented churn — INV-9/register/OQ-23 (source) hand-edited, INDEX/CHANGELOG (derived) left for the architect to regenerate, which leaves them transiently stale post-ADR (a `meta-architect:review-architecture` re-sync is the near-term follow-up, surfaced in the handoff). **Retro: no skill/`CLAUDE.md` change** — a clean run of the existing machinery.

- **2026-06-19 — Session 36: M4.S3a scope + step-0 decompose + register resolved; two post-PoC backlog directions added (vault/docs-only, no production code).** A planning/architecture session that locked the first M4 *write* slice without writing any code. **(1) Backlog (owner):** added two post-PoC directions to `docs/BACKLOG.md` — *world graph from multiple sources / attributable knowledge* (the non-fiction/research case: multiple sources that corroborate or disagree, no single ground truth → every claim attributed to its source; grounded in ADR-0005 per-mention provenance, the context-dependent cross-source-identity hazard, and spec §10 q3) and *timeline / temporal qualification of relations & properties* (the "when does Garret wear the cloak" need; anchored to the spec's latent `Event` node type + the M4.S2b occurrence-order list, with a forward-compatibility note so the PoC baseline graph doesn't foreclose a later validity-interval/event-anchor/source-attribution model). **(2) Slice plan (owner):** "manual correction in the reader" sliced by *write-risk, lowest-first* — **S3a** = edit existing accepted entities (`canonical_name`/`aliases`/`type`/`properties`) + add/edit/remove relations between accepted entities; **S3b** = entity↔entity merge + DM-Rel-5/6 re-point + whole-entity delete + undo-merge; **S3c** = manual tag/un-tag/change-boundaries (reopens DM-IH-1 span storage); general split + relation temporal/source qualifiers = post-PoC. Completeness-checked the cut against the full create/read/update/delete surface over {entities, relations, mentions} and *folded two gaps in*: entity scalar-field editing → S3a (needed to clean names/types and fix highlights that won't render), whole-entity delete + undo-merge → S3b (shares the re-point/cleanup machinery with merge). **(3) `meta-architect:decompose-requirement` step-0** → `architecture/proposals/m4-entity-editing.md` (`accepted`, register **DM-S3a-1..8 / OQ-23** resolved with the owner the same session) + glossary `backend-for-frontend` + `lost-update` (glossary index regenerated 22→25, folding in the `ego-graph` note M4.S2 had added but not indexed), learning-log ×2, INDEX/CHANGELOG. The headline: this is **the first M4 slice that *writes* the graph**, so the station fingerprint flips from the read views' all-`n/a` and the weight is the write path + reversibility. **(4) Register resolved (owner):** **DM-S3a-1** new named edit handlers + **reword INV-9** "exactly two writers" → "only human-reached handlers" (ADR-0005 broaden-don't-mint precedent; the rewording is ADR-worthy → drafted at build, test-first); **DM-S3a-2** a before→after edit-evidence log (INV-3 undo + flywheel) over a no-record "re-type to undo" narrowing; **DM-S3a-3** route manual relation-adds through the decide path (sole edge-writer), re-predicate = delete+re-add on the `uuid5` edge id, warn-on-MERGE-collision, allow self-loops; **DM-S3a-5** typed `properties` values (keys free, INV-4); **DM-S3a-7** split S3a-be/S3a-fe; the rest (DM-S3a-4 invalidate-on-edit, DM-S3a-6 last-write-wins, DM-S3a-8 reuse the entity-search picker) as proposed. **No spec amendment** — §3.4/§3.5 already specify the editable panel; the INV-9 rewording lands test-first at build via the ADR. **Checks:** vault/docs only — no backend/frontend/dep changes, so the code gates are no-ops; the machine-managed handoff block and the spec were left untouched (verified by diff). **Lessons:** (a) the owner's "does this slicing give *all* the tools?" completeness check before decomposing was the high-value move — it caught two unplaced operations (field-editing, entity-delete) and routed them, so the decompose was scoped right the first time; (b) caught a self-inflicted vault drift — the glossary index had been stale since M4.S2 (count 22, missing the `ego-graph` note) — and regenerated it, a reminder that the glossary *index* must be regenerated whenever a decompose adds a term (the `review-architecture` sweep is the backstop). **(6) Retro (1 change, owner approved — "both homes"):** encoded the **completeness-sweep-before-slicing-a-branchy-feature** discipline the owner valued — a new **step 0b** in `meta-architect:decompose-requirement` (enumerate the feature's full operation surface, confirm each operation has a slice-home, flag unhomed ones as a slicing gap — fires automatically on a branchy decompose) **+ a pointer in root `AGENTS.md` §3** (so it applies even when a feature is sliced without a decompose). *Considered & rejected:* a skill change for the glossary-index drift (the skill already mandates regenerating the index — a one-off execution miss, caught + backstopped by `review-architecture`).

- **2026-06-18 — Session 35: M4.S2b reader entity side panel shipped (PR #91), DM-SP-4 resolved = cytoscape.** The frontend half of the entity side panel (spec §3.4/§3.5), test-first, mirroring M4.S1's pure-logic → hook → component shape. **(1) Built:** pure `occurrences.ts` (occurrences from the reader's already-rendered highlights, DM-SP-3 — the panel's timeline always agrees with the prose) + pure `egoElements.ts` (`EntityDetailResponse` → cytoscape elements: focal flagged, edges oriented out/in, dangling-neighbour edges dropped fail-closed) → `useEntityDetail` (`useQuery` over the S2a `GET /stories/{id}/entities/{eid}` BFF endpoint) → `ReaderEntityPanel` (details + `properties` key→value + the ego-graph mini-view + the occurrence timeline) + `EgoGraphCanvas` (the cytoscape mount, smoke-only) + `ParagraphText`/`TextReader` wiring (highlights are now clickable → open the panel; tapping a neighbour node re-targets it; clicking an occurrence scrolls the reader to that paragraph + flashes the highlight). Read-only — INV-1/3/9 untouched, no LLM. **(2) DM-SP-4 (the confirm-at-build) → cytoscape**, browser-verified live against the `c09ff519…` 115-node graph (Garret Locke, 29 neighbours / 40 edges): reused the graph viewer's cytoscape pattern over the ego subset, which delivers node inspection + drill-to-text; a static fallback proved unnecessary. **(3) Owner browser-check → three folds:** a real scroll bug (unbounded panel height hid occurrences below the fold → `max-h`+`overflow-y-auto`), a modest widen (`w-72→w-80`), longer occurrence snippets (±30→±60 chars). **(4) Reviews:** `/review-pr` (self) + `/code-review` (high, 7-angle) folded two findings — memoize `ParagraphText` (the whole-story flash re-render was re-running `splitParagraph` over every paragraph twice per drill) + flash-timer unmount/close cleanup; Codex PR review hit its usage limit (best-effort, not blocking). **Checks:** 193 frontend tests green; eslint/prettier/tsc(build) clean; CI 4/4 green; squash-merged on the owner's per-merge OK (`d92bf13`). **Lessons:** (a) a stale long-running backend (started before the S2a route was merged, no `--reload`) made the panel's fetch 404 and *looked* like a frontend bug — folded a `frontend/AGENTS.md` *Manual browser verification* note to check the running backend loaded a just-merged route (via `/openapi.json` or a restart) before blaming the UI; (b) don't build a causal theory on an *unverified absence* — I scanned an incomplete story-id list, got all-0/0, and wrongly concluded the graph store was wiped; the owner caught it (logged as a lesson, judged too broad for a rule); (c) `/code-review` again surfaced real findings the single `/review-pr` didn't (the memoization perf hit) — the multi-agent pass keeps paying for substantive frontend code; (d) two deferred code-level review nits were recorded in `docs/BACKLOG.md` (cytoscape rebuild-on-data-change; the panel `calc(100vh-3rem)` magic constant) so a consciously-deferred nit doesn't evaporate.

- **2026-06-18 — Session 34: M4.S2 slice chosen + decomposed, and M4.S2a backend shipped (PR #89).** The owner-chosen second M4 slice (the **entity side panel**, spec §3.4/§3.5), all in one session: choice → step-0 decompose → backend build → review → merge. **(1) Slice choice (owner):** side panel over manual-correction-in-reader — the read-only inspection surface (an entity's neighbourhood + occurrences + properties) is what you need to *judge* a correction, so it lands before the edit tools; manual correction is the next slice. **(2) `meta-architect:decompose-requirement` step-0** → `architecture/proposals/m4-side-panel.md` (`accepted`, register **DM-SP-1..8 / OQ-22** resolved with the owner same session), + glossary `ego-graph`, learning-log ×2. Key finding: the slice is mostly an *assembly* problem — only `properties` is surfaced by no endpoint, occurrences derive from the reader's rendered highlights, neighbours filter existing relations; the one new piece is a per-entity 1-hop query. **DM-SP-1 = a focused BFF endpoint** → **DM-SP-7 split** S2a backend / S2b frontend. **(3) M4.S2a backend built test-first (PR #89, `4bc7ff5`):** `domain/neighbourhood.py` (pure `build_ego_graph` — 1-hop ego-graph, self-loops dropped, neighbours de-duped, oriented out/in) + `Neo4jRepo.get_neighbourhood` (targeted `(e)-[r]-(n)` both directions, project-scoped) + `GET /stories/{id}/entities/{eid}` (details + `properties` + ego-graph), 8 unit + 5 integration. Build-time refinement: occurrences left to the frontend (DM-SP-3), so the endpoint is exactly `properties` + ego-graph (the M4.S1 one-pure-fn-+-one-endpoint shape). Read-only — INV-1/INV-9 untouched, no LLM. **(4) Reviews:** `/review-pr` (×2, self — clean) + `/code-review` (high, 7 finder angles) **folded one should-fix** — two finders independently flagged the neighbourhood query not project-scoping the *neighbour* (unlike `get_relations`); now `WHERE n.project_id = e.project_id` (§6.4 defense-in-depth) + a cross-project-exclusion test, and the defensive guard made load-bearing with a test. Nits (domain models as DTOs, single-404 description) left as deliberate pattern-consistent choices. **(5) Backlog note (owner idea):** `docs/BACKLOG.md` — the reader as the paragraph-by-paragraph working surface (extract/merge/add/remove in-prose), with the open local-vs-cross-text relationship question to think through post-PoC. **Checks:** 361 backend + 164 frontend tests green; ruff/format/mypy/eslint/build clean; CI 4/4 green; squash-merged on the owner's per-merge OK. **Lessons:** (a) decompose + build in one session worked cleanly when context stayed healthy — the step-0 decompose still earned its place (it surfaced the assembly-vs-new-data framing that shaped the slice); (b) `/code-review` again added a real should-fix the single `/review-pr` missed (the project-scoping gap) — the multi-agent pass keeps paying for substantive code; (c) a build-time *refinement* of an approved decision (occurrences → frontend) is legitimate and expected — the decompose's `verify-at-build` markers anticipated it; record it where the decision lives.

- **2026-06-18 — Session 33: M3→M4 vault sync (PR #85) + M4.S1 inline-highlights **frontend** (PR #86), live-verified end-to-end; a post-PoC backlog file split out.** **(1) Vault-first (PR #85, `2f361ff`).** Cleared the carried open-block B with a `meta-architect:*` pass: drew `architecture/state-machines/relation-lifecycle.md` (the edge-gate twin; closes OQ-20), flipped `proposals/m4-inline-highlights.md` → `accepted` with the whole DM-IH register annotated resolved-as-built (OQ-21), fixed the `glossary/model-tier-routing.md` "not yet built" drift; `/review-pr` (incl. an explicit §2 decision-state sweep) clean; faithful surprise recorded (`held`/`committable` are derived, not persisted). **(2) M4.S1 frontend (PR #86, `2795ae1`), test-first.** `frontend/src/features/text-reader/`: a pure paragraph-splitter (`segments.ts`) + colour-by-type (`palette.ts`: fixed common-type palette + deterministic hash fallback honouring open-world INV-4 + a legend) + `useReader` query hook + `ParagraphText` `<mark>` renderer with a name+type+aliases tooltip (React-escaped) + `Legend` + the `/stories/:storyId/reader` route + a graph-viewer link. Strict TDD for the pure logic (splitter/palette red→green); components shaped-then-tested (disclosed). 164 frontend tests; tsc/eslint/prettier clean. `/review-pr` folded 2 coverage gaps; `/code-review` (high, 7-angle) found **no confirmed bug** (overlap/zero-length highlights impossible per the backend resolver's guards; `Math.abs(INT_MIN)` is a positive double in JS). **(3) Live smoke test — the whole pipeline, end to end.** Wrote the Oakhaven story from scratch (`docs/samples/oakhaven.md`) and ran upload → manual chunk (3 ch/6 sc) → extract (free Ollama cloud, ~9% session quota) → review/dedup (incl. correctly **rejecting** the Elira↔Elara false-positive merge) → relations → reader. The reader rendered highlights, colour-by-type+legend, tooltips, multi-surface-form entities via aliases, longest-match — *and* the honest failure modes (missed-coref "the magistrate" as its own node; un-highlightable hallucinated "broken table"). **(4) Backlog file (retro change).** Created **`docs/BACKLOG.md`** for concrete post-PoC items, migrated ~12 out of `PLAN_LONG.md` (kept strategic), and wired its routing rule into root `AGENTS.md`, `docs/AGENTS.md`, `/wrap-session`, `/review-pr`. **Checks:** 164 frontend tests green; lint/format/build clean; both feature PRs CI green, two review passes each, squash-merged on per-merge owner OK. **Lessons:** (a) **a live end-to-end test is irreplaceable** — it surfaced ~12 real product gaps (coreference, extraction hallucination, monotone re-match ordering, near-name discrimination, the reader-as-correction-surface) that a green suite never would, and concretely killed the auto-commit idea (two different sisters scored 0.91 > the 0.85 flip line — auto-commit would have silently fused them; the human gate caught what similarity can't); (b) the post-PoC backlog outgrew `PLAN_LONG`'s "strategic, stable" remit, so it earned a dedicated, wired-in home; (c) `/resume-session` again caught a real drift (a stale `features/reader/` path in the handoff vs the `features/text-reader/` convention) before any code. **(5) Mid-wrap, the frontend `npm audit` gate red on a newly-surfaced HIGH undici advisory** (dev-only via jsdom, unreachable, no soaked fix). Triaged via `/triage-advisory`; owner's §6.7 call = scope the gate to shipped deps (`npm audit --omit=dev`) — spec §6.7 amended, reconciled across `ci.yml` + the skills + an orienting vault note; also bumped `react-router-dom` 7.15→7.16 to clear a LOW CSRF (PR #88, `c335801`). Lesson: a security gate should reflect *shipped* risk — narrowing scope is the principled escape hatch for an unreachable dev-dep advisory with no soaked fix.

- **2026-06-18 — Session 32: M4.S1 inline-highlights **backend** shipped (PR #81) + the python-multipart OSV waiver dropped (PR #82).** The first M4 ("V1 polish") feature code, the read-only side of inline highlights (spec §3.5). **(1) Resolved the DM-IH register with the owner by verifying the data first.** The owner chose "decide after I verify" on the gating call (DM-IH-1 span resolution); the verification found the accept path writes the mention with **null** char offsets and the spaCy span is gone by accept time — so the decompose's "persist the existing span" option was *illusory* — while aliases accumulate the prose's actual surface forms (each merge-accept adds one), making **render-time string search over canonical_name + aliases** both the only zero-data-change option *and* stronger on inflection than assumed. DM-IH-3 = a plain `<mark>` renderer (Tiptap deferred to manual annotation). **(2) Built the backend test-first:** a pure `domain/highlights.py` span resolver (word-boundary, case-insensitive, longest-match-wins overlap, omit-on-miss fail-closed) + a story-scoped `GET /stories/{id}/reader` that cross-joins Postgres paragraphs+mentions × Neo4j entities in app code and returns per-paragraph highlight ranges + a tooltip catalog of only the entities that appeared + a bulk `list_entity_mentions_for_story` loader. 12 unit + 4 integration tests; mypy `--strict` clean; OpenAPI + typed TS client regenerated. **(3) The layered review gate paid off a 5th consecutive substantive PR (#36/#60/#63/#78/#81):** `/review-pr` (self) read clean, but the 7-angle `/code-review` caught a real crash it missed — the resolver matched against a *casefolded copy*, which can change length on an expanding codepoint (ß→ss, İ→i̇) and push an offset past the original text → `IndexError`/500 on the whole reader endpoint; fixed by matching the original text via `re.IGNORECASE` (+ `re.escape` for literal name metachars), regression-tested. **(4) Dropped the python-multipart waiver (PR #82):** the S31-deferred fix-first drop once 0.0.31 cleared its 14-day soak — bumped via `/add-dependency`, removed `GHSA-v9pg-7xvm-68hf` from `osv-scanner.toml` + `WAIVERS.md`; OSV scan clean, nothing unmasked. **(5) Retro — a soak-date-floor rule:** record a dated "drop when (soaks)" as publication-date **+ (soak_days + 1)** (package +15, image +8), since the age gate is time-precise to the second while a bare +14 date is intra-day optimistic — this session a fix published 06-04 08:27 UTC was still ~53 min too young at the recorded 06-18 drop date, forcing a ~50-min wait. Folded into `/triage-advisory` step 3 + `infra/osv/WAIVERS.md`. **Checks:** 347 backend tests green; ruff/format/mypy/frontend-lint/tsc clean; both PRs CI 4/4 green, squash-merged (#81 `01b2173`, #82 `3f16d5e`). **Not done (carried to next session):** acting on the 2026-06-17 architecture report + OQ-20 (the relation-lifecycle state-machine drawing) + the `glossary/model-tier-routing.md` "not yet built" drift — a `meta-architect:*` vault pass. **Lessons:** (a) *verify before you commit to a design option* — the owner's "decide after I verify" turned a plausible-but-wrong choice (persist spans) into the right one (search) by reading the actual write path; the deterministic-over-LLM instinct, applied to a design decision; (b) `/code-review` caught a self-review miss for the **fifth** consecutive substantive PR — the casefold-length assumption was load-bearing and invisible to a happy-path read; the multi-agent finder's edge-case angle is the safety net the single self-review structurally lacks; (c) a recorded soak *date* is a floor, not the gate — the gate is time-precise, so date arithmetic that ignores the publication time-of-day surprises you by hours on the drop day.

- _**Earlier sessions (Milestone 3 and before)** — see **[`docs/PLAN_ARCHIVE.md`](PLAN_ARCHIVE.md)**._

---

## How the two skills use this file

- **`/resume-session`** (session start) reads the **Session handoff** block, verifies the on-disk anchors it names, runs `git log`/`git status`, opens the spec sections listed, and confirms reality matches before work begins.
- **`/wrap-session`** (session end) runs the green-state checks (report-only), checks off completed tasks, strikes (does not delete) obsolete ones, appends a dated line to **Done in previous sessions**, refreshes **Blocked / questions** and **Decided**, and rewrites the **Session handoff** block to point at the next session. It then reminds you to commit on a feature branch (squash-merge hygiene).
- **When a spec change happened:** both skills stop and require `story-forge-poc-spec.md` + `PLAN_LONG.md` to be reconciled with this file before continuing.
