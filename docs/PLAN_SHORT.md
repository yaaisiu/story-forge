# PLAN_SHORT.md — Current milestone tactical plan

> Updated every working session. Read at start (`/resume-session`); update at end (`/wrap-session`).

<!-- ───────────────────────────── HANDOFF ─────────────────────────────
This block is the contract between `/wrap-session` (writes it) and
`/resume-session` (reads it). It always describes where the NEXT session
should start. Keep it short and literal. Do not delete the markers.
────────────────────────────────────────────────────────────────────── -->

## ▶ Session handoff (read this first)

- **Next step:** **M3.S4f — relation-review UI (frontend), the additive UI half of the S4a→S4b cut.** The backend shipped (S4e, PR #76): a React "decide on relations" surface consuming the two new endpoints — `GET /stories/{id}/relations` (committable relations: both endpoints accepted) and `POST /stories/{id}/relations/{relation_id}/decide` (`{action: "commit" | "reject"}`). The §3.3 5th human action: list each committable relation (surface `subject`/`predicate`/`object` + the resolved `subject_entity_id`/`object_entity_id`), let the author **commit** (write the edge) or **reject**, and on success the edge appears in the §3.4 graph viewer. **Pattern to mirror:** the S4b extraction-review feature (`frontend/src/features/extraction-review/` — keyboard reducer, hooks `useCandidates`/`useReviewCandidate` typed off the generated client, `CandidateCard`/`ReviewQueue`, the route + graph-viewer link). **First code = a failing component/hook test** (per the frontend test-first rule — list renders committable relations; commit/reject calls the right endpoint). **Confirm-at-build:** the keyboard scheme (§8.3 — S4b used J/K/A/N/M/R; relations need at least commit/reject/next — firm it with the owner) and whether the card shows resolved **entity names** (needs a names lookup) or just the surface strings the endpoint returns today.
- **Read before starting:** this file (the **Session-29 Decided** entry + the **Session-29 Done** top entry + the **relation-write cross-cutting item** now ✅ backend-resolved + the **Session-27 waiver-drop cross-cutting item**); **`architecture/proposals/m3-relation-write.md`** (now `accepted` — the full register + the data-flow + "but what if" for the UI's edge cases); spec **§3.3** (the 5th human action "decide on relations") + **§8.3** (keyboard scheme) + **§3.4** (the graph viewer edges now render) + **§9 M3**. For code: `frontend/CLAUDE.md` + `frontend/src/CLAUDE.md` (React/TS + component/state conventions) + `frontend/src/lib/api/CLAUDE.md` (regenerate the client when the contract changes); the S4b feature `frontend/src/features/extraction-review/` (the mirror); `frontend/src/lib/api/schema.d.ts` (the new `RelationView`/`DecideRelationRequest`/`RelationDecisionResponse` types + the two `relations` paths); root `CLAUDE.md` (Review model, merge flow, **plain-language owner questions**, **`/triage-advisory`**).
- **Verify on disk:** on **`main`** after this docs-close commit. **Present (S4e backend, PR #76):** `backend/src/story_forge/agents/relation_review.py` (`RelationReviewService` — the only edge writer), `backend/src/story_forge/adapters/postgres_relation_store.py`, the migration `…893f57edb237_create_staged_relations.py` (the `staged_relations` table), the two routes in `api/stories.py` (`list_relations` / `decide_relation`), `create_relation` now uses **`MERGE`** (idempotent) in `adapters/neo4j_repo.py`, `committed_entity_id`/`normalize_name`/`StagedRelation` in `domain/candidates.py`; **`docs/decisions/0005`**; `architecture/invariants.md` INV-1 broadened to edges; `architecture/proposals/m3-relation-write.md` (`status: accepted`); OQ-19 **struck** in `architecture/open-questions.md`; the new relations paths in `frontend/openapi.json` + `frontend/src/lib/api/schema.d.ts`. **Present (security waivers, dated — VERIFY against today):** `infra/osv/osv-scanner.toml` `ignoreUntil` python-multipart **2026-06-19** (fix 0.0.31 soaks **2026-06-18** — likely **droppable now**), starlette **2026-06-27**/jp82 **2026-06-26** (fixes 1.3.1/1.3.0 soak 06-26/25), + `infra/trivy/neo4j.trivyignore` `CVE-2026-50010` (condition-based). **Absent — S4f CREATES it:** a frontend relation-review feature (mirror `features/extraction-review/`) — hooks (`useRelations`/`useDecideRelation`), a card + queue, a route, and the graph-viewer link; no backend change expected (the contract is fixed).
- **Last session ended:** 2026-06-16 — **Session 29 (M3.S4e relation-write backend, PR #76 squash-merged to `main`).** Built test-first; resolved the whole DM-Rel register (table, normalised-exact resolution, idempotent MERGE-by-triple, explicit gate, INV-1 broadened); ADR 0005, proposal→accepted, OQ-19 struck. `/review-pr` (self) + the 7-finder `/code-review` both ran and were folded before merge — `/code-review` caught a real merge-target-not-persisted bug (fixed). CI 4/4 green; 331 backend tests pass. No red checks.
- **Open blocks/questions:** **(A) S4f confirm-at-build:** the relation-review keyboard scheme (§8.3) and whether the card resolves entity **names** (extra lookup) or shows the endpoint's surface strings. **(B) Drop the expiring security waivers (`/triage-advisory`; `/resume-session` step 3b flags them):** **python-multipart 0.0.31 soaked 2026-06-18 → likely DUE now** (bump via `/add-dependency`, drop the OSV waiver); **starlette 1.3.0/1.3.1 soak 2026-06-25/26**; **neo4j CVE-2026-50010** condition-based (drops when a 5.26.x image ships netty ≥4.1.135). **(C) `review-architecture` re-sync still overdue** — a dated post-S4e as-built `reports/` snapshot (the INDEX was hand-refreshed S29 but a full sweep is owed). **Cross-cutting, priority order:** **(1, TOP)** the S4f relation-review UI (this session); **(2)** the security-waiver drops (B); **(3)** **`GET /stories/{id}/graph` story-vs-project scoping** (§3.4 — now that edges render, the viewer's project-vs-story scope matters more). **Carry-forwards (unchanged):** **per-mention provenance for triple-deduped edges** (new, post-PoC — ADR 0005); spec **§10 q8** (bilingual `canonical_name_pl/en`); **§6.5 model-override dropdown** (OQ-14); **observability/logging** (OQ-15, INV-6 vacuous); the **envelope-unwrap dup**; the **error-to-string mapper** dup (n=4); 422-overload remap + window-and-stitch; orphaned-sandbox cleanup; re-structure delete-and-replace; **Issue #22**; **Issue #31** (meta-architect slug carve-out); M2.S1 review nits; **meta-architect ritual-wiring** (deferred per ADR 0002). **Note: the `CandidateView` merge-target-name gap is now adjacent to the S4e fix** — the override-merge target is persisted on the row, so a merge-target *name* in the UI is a smaller lift than before.

<!-- ─────────────────────────── END HANDOFF ──────────────────────────── -->

## Current milestone: Milestone 3 — Cascade matching (dedupe)

**Goal:** the §3.3 four-stage cascade so duplicate candidates become **one** entity
under the author's control — *"dedupe works, I control every decision, the graph is
clean"* (§9 M3). Stage 1 RapidFuzz → Stage 2 embedding cosine → Stage 3 LLM
`JudgeAgent` → Stage 4 human review queue. M3 **retires the temporary INV-8** (no
dedupe) and lands **INV-1's** first enforcer (the human gate).

**Design:** framed in the step-0 decompose — `architecture/proposals/m3-cascade-matching.md`
(milestone register **DM1–DM6 + DM7 + DM-rej all resolved** — DM1–DM4 S20, DM6 S19, DM5 S22,
DM7/DM-rej S23) + the S4a backend decompose `architecture/proposals/m3s4a-intercept-write-path.md`
(build register **DM-S4a-1..5 resolved**, S23) + the candidate-lifecycle state machine
(`architecture/state-machines/candidate-lifecycle.md`, finalises to `living` at S4a).
**DM6 decided (2026-06-11): (A) intercept-before-write** — extraction *stages* candidates,
Neo4j is written only on human accept (so M2.S4's CREATE-on-extract path is refactored).
**The DM6 write-path refactor + INV-8 retirement land with M3.S4a** —
when the human-accept path actually exists; INV-8 stays the live contract until then, and
the cascade is built proposal-only/unwired through M3.S2–S3 (no empty-graph window).

**Feature order** (re-sliced 2026-06-11 / S20 — Stage 1 and Stage 2 split into their own
sessions once the embedding stack's cost surfaced; see Decided):

1. **M3.S1 — `MatchingAgent` Stage 1 (RapidFuzz).** ✅ **DONE (PR #56, 2026-06-11).**
   Deterministic token-set ratio vs `canonical_name` + aliases; routes auto-merge /
   ambiguous / new by the §3.3 bands. Carried **DM1** (threshold home → `config.py`) +
   the **§6.7 HF-model channel** amendment. Proposes only; unwired (INV-1 honoured by
   construction, INV-8 untouched). _Was "Stage 1 + Stage 2"; re-sliced._
2. **M3.S2 — `MatchingAgent` Stage 2 (embeddings) + the `pgvector` read-path switch.**
   ✅ **DONE (PR #58, 2026-06-12).** `postgres_repo.py` `NULL AS embedding` → real
   `vector(768)` + `register_vector_async` (centralised in `db.connect`); per-mention
   vectors on `entity_mentions` (migration); Stage 2 = **max** cosine of candidate-context
   vs the entity's mention vectors (strict `>0.85`). Carried **DM2** (model
   `paraphrase-multilingual-mpnet-base-v2`, 768-dim, pinned by commit SHA via the §6.7
   HF-channel — `scripts/pin_hf_model.py`) + **DM3/DM4** (storage). Added the embedding
   library stack + the **torch OSV waiver**. **Foundation-only** (see Decided): EmbeddingAgent
   built but *not* wired into the live coordinator (mentions written `embedding=None`) — the
   embed-on-extract wiring lands M3.S4 with the cascade. Verify-at-build passed: model loads
   on the CPU host, emits 768 dims.
3. **M3.S3 — `JudgeAgent` Stage 3.** ✅ **DONE (PR #60, 2026-06-15).** LLM-as-judge
   (cloud_free via the router — `weight="medium"`/`task_type="judge"`; DM5 resolved),
   strict-JSON `{match, confidence, reasoning}` (degenerate body fails + retries),
   prompt-injection-safe, full Appendix C.3 field set. `classify_verdict` merges iff
   `match AND confidence > 0.8` (§3.3 amended with the clarifying merge rule). Carried
   the **rule-of-three fold** (`validate_with_retry` + `TaskWeight`/`Router` → `base.py`).
   Proposal-only/unwired; INV-8 untouched.
**M3.S4 was re-sliced 2026-06-15 (S23) into S4a + S4b** — the decompose
(`architecture/proposals/m3s4a-intercept-write-path.md`) showed the backend write-path refactor +
the React review-queue UI are too big for one conversation. The invariant flip is witnessed at the
**backend/API boundary**, so S4a is a coherent green slice (no empty-graph window) and S4b is purely
additive UI. See Decided 2026-06-15.

4. **M3.S4a — intercept-before-write (backend).** ✅ **DONE (PR #63, 2026-06-15).** New Postgres
   **`candidates`** staging table + the cascade **wired synchronously into the `ExtractionCoordinator`**
   (embed-on-extract → Matching Stage 1/2 → Judge Stage 3, fail-closed) + the human-accept write-path
   as API endpoints (`GET …/candidates` / accept→MERGE-or-CREATE to Neo4j + `entity_mention`(+vector) +
   a `candidate_decisions` evidence row / reject→remembered). **Retired INV-8 → landed INV-1's first
   enforcer + new INV-9** ("no automated stage writes the graph") + **ADR 0004** + finalised the
   candidate-lifecycle note to `living`, test-first — the flip test ("extraction stages, writes **zero**
   Neo4j nodes; the accept endpoint is the only writer") replaced M2.S4's "two identical → two nodes".
   `create_entity` → idempotent MERGE-on-id; accept path status-flip-last + deterministic ids (idempotent
   retries); store-down→503 + Neo4j-lifespan-close cross-cutting folded in. **Relations are staged
   (JSONB) but the edge write + re-point-on-merge is deferred to S4b.** Register DM-S4a-1..5 resolved
   (Decided 2026-06-15).
5. **M3.S4b — review-queue UI Stage 4 (frontend).** ✅ **DONE (PR #65, 2026-06-15).**
   `features/extraction-review/` with keyboard nav (§3.3 *entity* elements: quote ±200 chars,
   NEW-vs-MERGE, reasoning, top-3 alternatives, accept/change-target/create/reject), consuming S4a's
   endpoints. Keyboard scheme = §8.3 J/K/A/N/M/R (the §10 q9 pick). Hooks (`useCandidates`/
   `useReviewCandidate`, generated-client-typed), a pure keyboard reducer, `CandidateCard` +
   `ReviewQueue`, route + graph-viewer link. Browser-verified end-to-end (22 staged → accept/reject →
   21 nodes, queue drained). **Deferred out of S4b (tracked in cross-cutting):** the **decide-relations
   action + relation-write backend**, and the **§3.4 graph story-vs-project scoping**. **INV-2 consent
   gate deferred past M3** — *not* landed here.
6. **M3.S4c — intra-batch dedup: on-accept live re-match (backend).** ✅ **DONE (PR #67, 2026-06-15).**
   Decompose: `architecture/proposals/m3s4c-intra-batch-rematch.md` (register DM-S4c-1..6 resolved). Each
   human **accept** now re-runs the **deterministic** matcher (Stage 1 RapidFuzz + Stage 2 cosine, reusing
   the candidate's stored `context_embedding` + the mention vector just written; **no LLM**) over still-pending
   candidates against the just-accepted entity; a strong match (Stage 1 `>85%` **or** Stage 2 `cosine >0.85`)
   flips the staged `proposal` `new → merge` (+ target + provenance/alternatives). **Monotone** (only
   `new → merge`) → idempotent; writes **only the `candidates` table** — **INV-1/INV-9 hold** (re-match is the
   first automated *staging-proposal* writer, never the graph). New `agents/candidate_rematch.py`
   (`ReMatchService` reusing the matcher; match signal built from the accept's own data → **zero extra reads**;
   Stage-1-only when a vector is absent) + `postgres_candidate_store.update_proposal` (`status='review-queued'`
   guard) + an optional `rematch` collaborator on `CandidateReviewService.accept`, **fail-closed**
   (`contextlib.suppress` — a re-match failure never rolls back the accept; reject/replay don't fire it).
   Vault folds: `candidate-lifecycle` self-loop + INV-9 graph-vs-staging clarification; **ADR 0005 declined**
   (see Decided). Backend-only (S4b card already renders merges; queue refetches on accept). Spec §3.3 (amended
   S25). `/review-pr` + `/code-review` clean. Full suite 301 green.
7. **M3.S4d — manual handpick (backend entity-search + picker UI).** ✅ **DONE (PR #70, 2026-06-16).**
   `GET /stories/{id}/entities?q=` (**project-scoped** — DM-S4c-5) over Neo4j accepted entities via
   `Neo4jRepo.list_entities`, ranked with the **same RapidFuzz signal the matcher uses** (a shared
   `_rank` core that `top_alternatives` now delegates to) — **no score floor** (the diminutives
   handpick exists to catch score *below* cross-category noise, so reachability wins; the human picks);
   `q` never reaches Cypher (injection-safe). `EntityPicker` + `useEntitySearch` in the review card feed
   the existing `M`/merge-accept path; **last-pick-wins** (picking an alternative clears a handpick).
   **Supersedes** the deferred "arbitrary-entity search merge target" cross-cutting item. §3.4 scoping
   reconciled (both routes project-scoped). `/code-review` caught a handpick-precedence bug (fixed).
8. **M3.S4e — relation-write (backend).** ✅ **DONE (PR #76, 2026-06-16).** Resolves each staged relation's
   surface-form endpoints → committed entity ids and writes graph edges under an **explicit human gate**
   (the §3.3 5th action "decide on relations" — DM-Rel-1, owner S28). Built test-first: new `staged_relations`
   table (status lifecycle + evidence — DM-Rel-4), normalised-exact same-paragraph resolution reading the
   committed id from `candidates` (DM-Rel-2; the create-id derivation promoted to the shared
   `domain.candidates.committed_entity_id` helper), `create_relation` `CREATE` → idempotent **MERGE on
   `uuid5(subject_id, predicate, object_id)`** (DM-Rel-6 — one edge per fact across paragraphs, retry-safe),
   `RelationReviewService` (the only edge writer — **INV-1 broadened to edges**, INV-9 second instance) with
   TOCTOU re-resolve + held/self-loop/ambiguous guards (DM-Rel-7), two `decide-relations` endpoints. **ADR
   0005**; DM-Rel-2/4/5/6/7 confirmed at build as proposed. `/code-review` caught + fixed a real bug: a
   *changed* merge target wasn't persisted on the candidate row, so a relation endpoint resolved to the stale
   target — `set_status` now persists the committed target on merge-accept. Re-point of *written* edges stays
   **M4** (DM-Rel-5); per-mention provenance for triple-deduped edges is a post-PoC follow-up (ADR 0005).
9. **M3.S4f — relation-review UI (frontend).** ⏳ Deferred to follow S4e (the S4a→S4b cut). The React
   "decide on relations" surface consuming S4e's endpoints — list committable relations (both endpoints
   accepted), confirm / re-target / edit predicate / reject. Scope firms once S4e's endpoint contract lands.

Each ends green + committed; `/review-pr` (+ `/code-review` for substantive code) → PR
→ squash-merge, per feature.

---

## Completed milestone: Milestone 2 — Basic extraction (closed 2026-06-11, M2.S6 / PR #53)

_Retained as history — all six sessions done. M2's outcome: the extraction pipeline is
visible end-to-end in a browser, **with duplicates** (the no-dedupe INV-8 that M3
lifts)._

**Goal:** turn persisted paragraphs into an entity graph in Neo4j — **without any
dedupe** (every candidate = new entity, per spec §9). End-to-end demonstration of
the agent architecture (PreNER → Extraction → Neo4j write; no Matching/Judge yet —
those are M3), the multi-tier router (cloud_free default, paid cloud configurable,
OpenRouter as meta), and the spec §8.5 agent activity panel + spec §6.6 token /
GPU-time budget visible in the UI.

M2 is split into **6 sessions**, each sized for one Claude Code conversation. Each
ends green (lint/type/tests reported clean) and committed, and leaves a resume
anchor so the next session can pick up by reading this file. Order is by dependency
— do not reorder without updating the handoff block.

---

### Session 1 — PreNERAgent (spaCy, deterministic) `[x]`

_No LLM, no router. Per-paragraph spaCy baseline — spec §7 step 3. The
deterministic-first opener: smallest blast radius, gives M2.S3 a real candidate
set before ExtractionAgent is designed._

- [x] **Deps:** spaCy + `pl_core_news_lg` + `en_core_web_lg` pinned. The model wheels aren't on PyPI, so they became a new §6.7 **direct-URL wheel channel** (GitHub-release wheels, hash-locked in `uv.lock`, soak checked against the asset upload date) rather than going through `/add-dependency`'s PyPI path — spec amended, gate extended. See Decided.
- [x] **Failing test first:** `backend/tests/unit/agents/test_prener_agent.py` (path corrected from the handoff's `backend/tests/…` per `backend/CLAUDE.md` — agent tests live in `tests/unit/agents/`) — PL Appendix B.2 fixture + EN mirror; `PreNERAgent.extract` returns `CandidateSpan`s covering the obvious proper nouns, typed via the spaCy → §3.2 mapping.
- [x] `domain/extraction.py` — `CandidateSpan` (minimal: text, char_start, char_end, spacy_label, mapped_type); no §3.2 full-shape fields.
- [x] `agents/prener_agent.py` — `PreNERAgent.extract(paragraph_text, language)`; lazy per-language model load; PL/EN routing by language (no auto-detect); unsupported language → `ValueError`. Pure `map_spacy_label` + `candidates_from_entities` factored out so the mapping/offset logic is CI-tested without the models.
- [x] Per-paragraph dispatch: candidates returned in-process for the test to assert against (the entity store lands in M2.S4, as planned).

**Done when:** ✅ failing test passes; spaCy + 2 models pinned; PL + EN deterministic fixtures.
**Resume anchor:** ✅ `backend/src/story_forge/agents/prener_agent.py`; `backend/tests/unit/agents/test_prener_agent.py`; spaCy + 2 models (the latter in the optional `models` dependency group) in `backend/pyproject.toml`.

### Session 2 — Paid LLM adapter (OpenRouter) + router + budget tracking `[x]`

_Unlocks every downstream LLM agent. **All in-session decisions now made** (2026-06-02, via the
meta-architect dogfood — see Decided + `docs/decisions/0003` + `architecture/proposals/m2s2-llm-router-budget-cap.md`):
hand-rolled `httpx` adapters (not SDKs); provider order Ollama → OpenRouter → Grok → Anthropic →
Google → OpenAI with **OpenRouter the only paid route built now**; per-day USD cap; control-first
**pause-and-ask** on free-quota-exhaustion or cap (never silent paid escalation)._

- [x] **Grow the seam first:** extend `adapters/llm/base.py` — `CompletionResult` + the `LLMProvider` Protocol carry usage (`model`, `input_tokens`, `output_tokens`, nullable `gpu_seconds`, `cost_estimate`) and the §6.5 `cost_per_1k_tokens` / `rate_limit_kind`; **`adapters/llm/ollama.py` stops discarding** Ollama's `prompt_eval_count`/`eval_count` (INV-5 / OQ-7).
- ~~[ ] `adapters/llm/anthropic.py` — `AnthropicProvider` picked first…~~ **struck 2026-06-02:** not Anthropic-first; direct vendor adapters deferred (OpenRouter covers them). See Decided / ADR 0003.
- ~~[ ] `adapters/llm/openai.py` + `adapters/llm/grok.py` — scaffolded…~~ **struck 2026-06-02:** deferred to "as needed" (was M2.S6 for OpenRouter; vendor stubs no longer scaffolded in S2).
- [x] `adapters/llm/openrouter.py` — `OpenRouterProvider`, hand-rolled `httpx` mirroring `OllamaProvider` (OpenAI-compatible `/chat/completions`, bearer auth, model passthrough, injectable transport). The **first failing test** of the session: this adapter against a mocked transport, asserting the **system-derived usage fields** + a **cap-refusal path** from the start. (Cap-refusal landed as HTTP 402 → `BudgetExceededError`.)
- [x] `adapters/llm/router.py` — `LLMRouter.complete(weight, task_type)` per **amended** §6.5 (orchestrates the call, not `route()→provider` — see Decided): light → cloud_free on a GPU-less host; medium → cloud_free; heavy → cloud_strong preferring OpenRouter. **Error-discriminated** within-tier failover (429/5xx/**transport errors** → fail over; 401 → fail over but not quota; other 4xx → re-raise). Only **pure-429** cloud_free exhaustion → pause-and-ask (`QuotaExhaustedError`); cap → `BudgetExceededError`. Tier/provider/model **system-derived** (INV-7). _Schema-retry stays in the agent (ChunkingAgent pattern), not the router — see Decided._
- [x] Cost tracking per **amended** §6.6: one `llm_calls` Postgres table, one row per call **incl. refusals/failures**; record input/output **tokens whenever the provider returns them** (incl. Ollama's `eval_count`/`prompt_eval_count`), nullable `cost_estimate` (paid), nullable `gpu_seconds` (Ollama Cloud); daily / project / per-task-type aggregation read from it; per-day USD cap (`DAILY_BUDGET_USD`) **checked before dispatch** (fail-closed), bounded one-call overshoot documented.
- [x] Status endpoint surfacing budget + Ollama Cloud GPU-quota state (consumed by the M2.S5 agent activity panel). Declare non-2xx outcomes per `backend/CLAUDE.md` "API routes". **Scoped 2026-06-02 (own-review + Codex):** S2 surfaces budget/spend/remaining-USD + GPU-seconds **consumed** (from the ledger) + per-task-type breakdown. The **remaining** Ollama Cloud GPU-time *quota* (spec §6.5/§6.6 "time remaining" / "remaining session quota") needs Ollama Cloud's account API and lands with the **M2.S5** panel — accepted deferral (see Decided), consistent with ADR 0003's M2.S5 panel scope. The endpoint docstring marks the gap; no fabricated remaining-figure in the meantime.
- [x] **Deferred-egress marker:** a clear in-code note at the paid-egress point that the per-fragment consent UI is M2.S5 (D5 — no sensitive data, documented not gated).
- [x] Tests: `OpenRouterProvider` vs mocked transport (usage parsing); router decision matrix + **failover discrimination** (429 vs 401 vs 5xx **vs transport**); cost recording; cap → pause-and-ask path; end-to-end `OpenRouterProvider`→router→ledger. _**envelope-malformed vs schema-invalid** deferred to M2.S3 — the router meets a real Pydantic schema there; the malformed-`200`-envelope record/failover gap is tracked in cross-cutting._

**Done when:** a routed cloud_strong call (via `OpenRouterProvider`) against a mock returns and records a usage row; the cap path pauses-and-asks; status endpoint exposes the daily total.
**Resume anchor:** `adapters/llm/{openrouter,router}.py` exist; `base.py`/`ollama.py` carry usage; `llm_calls` table + writes; status endpoint live. (Direct vendor adapters intentionally absent.)

### Session 3 — ExtractionAgent `[x]`

_Spec §7 step 4. Cloud_free by default per §6.5 agent table; cloud_strong on hard
cases via the M2.S2 router._

> **Design settled 2026-06-08 — build directly from the accepted `architecture/proposals/m2s3-extraction-agent.md`** (register resolved; no owner-resolution step remains). Key decisions are folded into the tasks below: per-paragraph granularity (D4); single-paragraph agent, resumable batch driver → M2.S4 (D5); **`candidate_name`** surface form, *not* `canonical_name` (D1); typed `ProviderResponseError` for malformed-`200` envelopes (D2/OQ-10); soft-flag `evidence_quote` (G5); known-entities/custom-types as params now / PreNER hints deferred (D3).

- [x] `prompts/extraction.pl.j2` + `prompts/extraction.en.j2` — Appendix C.2 skeleton. **Render structure strictly from the trusted template; never reparse output rendered with user-supplied text** (encoded in `/review-pr` §4 prompt-injection-safety check; the same rule that hardened `ChunkingAgent` in Session 3).
- [x] Pydantic schema: `ExtractionProposal` with `entities: list[EntityCandidate]` + `relations: list[RelationCandidate]`; open-world types per §3.2 (string `type` field — examples constrain but no enum); validators for required fields + **non-empty `candidate_name`** (surface form per D1 — **not** `canonical_name`, which is the resolved bilingual name assigned at M3 merge); **soft-flag `evidence_quote`** (drop the quote if it isn't a whitespace-normalised substring of the paragraph, keep the candidate — G5); dangling relations accepted (M3/human resolves). _Follow-up: blank `subject`/`predicate`/`object` aren't yet rejected — see cross-cutting (external PR-#42 remark)._
- [x] `agents/extraction_agent.py` — load prompt → call **router** (`router.complete(messages, weight="medium", task_type="extraction", json_schema=…)`) → parse + validate → retry on schema violation (mirror `ChunkingAgent` retry shape, including the bias-control of giving up after N retries with a clear error). **Single-paragraph** (D5): the batch loop + pause-and-ask catcher are M2.S4; the agent propagates `Budget/QuotaExhaustedError`, never swallows them.
- [x] **Router OQ-10 (D2):** add a typed `ProviderResponseError` in `adapters/llm/base.py`, raised by adapters on a malformed-`200` envelope (missing fields, unparseable body, **and null `content`** — added in `/code-review` fold), that `router.py` records + fails over like a 5xx — distinct from a schema-invalid body (agent prompt-retry). Add the "envelope-malformed vs schema-invalid" router test.
- [x] Per-paragraph dispatch (D4) with known-entities/custom-types context per spec §7 step 4 ("Here are known entities: {existing}") **as params now** — the real Neo4j read is wired in M2.S4; first end-to-end passes empty lists. ~~**PreNER hints deferred** (parameter wired, unused until an eval — D3).~~ **As-built:** the `prener_hints` param was **dropped** (not wired-unused) per the `/code-review` fold — no speculative dead param; re-add with a concrete type when the eval lands (the D3 intent, minus the dead surface).
- [x] Tests: agent against a mocked **router**; happy path PL + EN; retry-on-malformed-JSON; schema-violation → bounded give-up; **empty-but-valid not retried**; **prompt-injection safety** — paragraphs containing `[ROLE]` markers or fake JSON can't forge structure or merge into a wrong entity. _Plus null-content envelope + grounded/ungrounded `evidence_quote` + direct `extract_json` tests added in the review fold._

**Done when:** ✅ against a mocked router, paragraph → validated entity + relation candidates; retry-on-bad-JSON proven; empty-but-valid not retried; injection-safe; `ProviderResponseError` records+fails-over. **All met; merged PR #42 (134 unit tests).**
**Resume anchor:** `agents/extraction_agent.py`, `prompts/extraction.{pl,en}.j2`, `EntityCandidate`/`RelationCandidate`/`ExtractionProposal` schemas, and `ProviderResponseError` exist.

### Session 4 — Neo4jAdapter writes (no dedupe) `[x]`

_Spec §9 M2: "Save entities to Neo4j without cascade (everything = new entity for
now)." Cascade matching (MatchingAgent, JudgeAgent, review queue — Stages 1–4) is
M3 in its entirety._

- [x] **Deps:** `/add-dependency` for the async `neo4j` Python driver. ✅ **Session 15:** `neo4j==5.28.4` (5.x LTS to match the 5.26 server; aged, OSV-clean).
- [x] `adapters/neo4j_repo.py` — async neo4j driver session helper (config from the existing M0 `.env` keys); `create_entity`, `create_relation`; node properties per §3.2 (id UUID app-side, type, canonical_name PL+EN, aliases, properties JSON, first_seen paragraph_id, embedding **nullable** — M3 fills it). ✅ **Session 15:** also `get_entity`/`count_entities`/`get_relations`/`delete_project_graph`; `CREATE`-not-`MERGE` (INV-8); open-world type backtick-escaped (Cypher-injection-safe); `properties` stored as JSON; `domain/graph.py` holds `GraphEntity`/`GraphRelation` (distinct from the agent's candidates).
- [x] Wire: ExtractionAgent output → Neo4jAdapter writes; **no Stage 1/2/3 logic** (every extracted candidate becomes a fresh node — the spec is explicit, and this exposes the duplicate problem M3 then solves). ✅ **Session 16 (PR #48):** pure `agents/extraction_graph.py` `proposal_to_graph` (provisional `canonical_name` = surface form in the project language, EN peer null; within-paragraph relation resolution, dangling dropped) + the `ExtractionCoordinator` batch driver + `POST /stories/{id}/extract`. Folded the blank `subject`/`predicate`/`object` validator (PR-#42 gap) here.
- [x] Persist `entity_mentions` in Postgres (paragraph_id ↔ entity_id back-reference per §6.4). **Write a new Alembic migration for the table** — it is *not* in M1's schema (M1 created only projects/stories/chapters/scenes/paragraphs + llm_calls; `entity_mentions` exists only in spec §6.4). Extend `EXPECTED_TABLES` in `tests/integration/test_migrations.py` to cover it. ✅ **Session 15:** migration created (`entity_id` no Postgres FK — cross-store, OQ-1); `EXPECTED_TABLES` extended; migration smoke test green. _(The repo insert fn + the write wiring are still Session 16.)_
- [ ] **Embedding read path stays deferred** — paragraphs continue to read `NULL AS embedding`. M3's MatchingAgent Stage 2 will turn it on alongside the entity-embedding writes; flag in code (don't refactor `postgres_repo` here).
- [x] Tests: integration against the neo4j compose service; round-trip read back what was written; `entity_mentions` back-reference holds; verify the no-dedupe property (two identical extractions produce two nodes). ✅ **Session 15:** round-trip + no-dedupe green vs live Neo4j. ✅ **Session 16:** the `entity_mentions` round-trip/cascade tests, the live `ExtractionCoordinator` persistence suite, the pure-mapping + batch-driver unit suites, and the route-contract suite — and the CI neo4j service makes them all **run at the gate**.
- [x] **[Session 16] Add the `neo4j` service to the backend CI job** (`.github/workflows/ci.yml`) + update the `integration` marker description (now needs Neo4j too). ✅ Done (PR #48) — backend job green with neo4j+postgres services; the netty/openssl CVE-rot the neo4j scan surfaced was waived (unreachable on loopback).
- [x] **[Session 16] Map router exit exceptions → declared HTTP** on the new write path; fold the ChunkingAgent→502 cross-cutting at the same time. ✅ Done: `ProviderResponseError`/`ExtractionError`/`httpx.HTTPError` → 502; `BudgetExceeded`/`QuotaExhausted` are caught by the driver → **202-paused** (the OQ-2 + owner pause-response decision), not an HTTP error. ChunkingAgent now maps `ProviderResponseError`→`ChunkingError`→502.
- [x] **[Session 16] Resumable batch driver** (OQ-2) — the batch driver (not the agent) catches the router's pause-and-ask and resumes from the last-done graph write. ✅ Done: `ExtractionCoordinator`; checkpoint = a committed `entity_mentions` row, written **last** (after entities + relations) so a relation failure isn't falsely checkpointed (external-review fold).

**Done when:** ✅ end-to-end ingest (upload → structure → extraction → graph write) completes against a real neo4j; round-trip test green; **the integration test runs in CI** (neo4j service present). **All met; merged PR #48 (188 tests).**
**Resume anchor:** ✅ `adapters/{neo4j_repo,postgres_mention_store}.py`, `agents/{extraction_graph,extraction_coordinator}.py`, `entity_mentions` migration, `POST /stories/{id}/extract`, CI neo4j service all exist on `main`.

### Session 5 — Frontend graph viewer + agent activity panel `[x]`

_Spec §3.4 (read-only cytoscape) + §8.5 (agent activity panel — "makes the
multi-model + agent architecture legible to anyone browsing the UI"). No editing
— that's M4._

- [x] **Deps:** `/add-dependency` for cytoscape (npm); confirm pinnable + OSV-clean + ≥14d. ✅ **Session 17:** `cytoscape==3.33.4` (22 d, OSV-clean, ships own types).
- [x] `frontend/src/features/graph-viewer/` — read-only cytoscape view over the M2.S4 graph; force-directed, colored by entity type; click → node details side panel (canonical_name, type, aliases, first_seen paragraph). ✅ `GraphViewer`/`GraphCanvas`/`NodeDetailsPanel` + pure `graphElements` mapper; cytoscape isolated in `GraphCanvas` (jsdom-untestable boundary). _first_seen shown as a value, not a link — the text-reader drill-down target isn't built (cross-cutting)._
- [x] `frontend/src/features/agent-activity/` — persistent small panel per §8.5: last agent run + chosen tier + chosen model + latency + cost-this-call + daily total + paid spend. ✅ Consumes `GET /llm/status` (extended with `last_call` this session). _Ollama Cloud quota **remaining** still deferred (needs their account API — tracked); the panel shows GPU-seconds **observed**, no fabricated figure._
- [x] TanStack Query hooks for the graph read + status polling; honest stale times (graph: stale-while-revalidate; activity panel: short polling interval). ✅ `useStoryGraph` (staleTime 30s), `useLlmStatus` (5s poll), `useExtractStory`.
- [x] E2E test under jsdom: upload → structure → mocked extraction → graph viewer renders nodes; activity panel updates after the mocked run. ✅ Extended `e2e.test.tsx` (GraphCanvas mocked — the canvas is browser-smoke territory).
- [x] Real-browser smoke walk (lesson from Session 6): the activity panel renders the data CI can't see — verify the column shows what a stranger would expect. ✅ **Done:** live extraction → cytoscape renders force-directed + colour-by-type + labelled edges; node-details populates; panel shows real latency (≈19.8 s), tier/model, `success`; duplicate nodes visibly demonstrate INV-8.

**Done when:** ✅ in a real browser, the M2 pipeline produces a visible graph and the activity panel shows the chosen tier + cost. **All met; merged PR #51 (201 backend + 51 frontend tests). Backend read surface (latency ledger, `GET /stories/{id}/graph`, `/llm/status`+`last_call`) shipped alongside; OQ-9 resolved option (a).**
**Resume anchor:** ✅ `frontend/src/features/{graph-viewer,agent-activity}/` exist on `main`; cytoscape pinned; `/llm/status`+`/stories/{id}/graph` consumed.

### Session 6 — Direct vendor adapters (as needed) + integration polish `[x]`

_Spec §6.5 + §9 M2. **Scope shifted 2026-06-02:** `OpenRouterProvider` was pulled forward into
M2.S2 (now the preferred paid route — ADR 0003), so S6 is no longer "build OpenRouter." It becomes
the optional direct-vendor adapters + the integration polish that closes M2._

> **⚠️ M2-roll decision (raised 2026-06-11, Session 17):** S6's **acceptance criterion** ("Done
> when" below — a stranger uploads → sees chapters/scenes → the entity graph → which agent/tier/model
> ran) was **met by S5's browser smoke**, and its headline (OpenRouter) shipped in S2. The genuine
> *remainder* is thin and mostly "as-needed"/polish: direct vendor adapters (**may ship none** if
> OpenRouter suffices), the §6.5 router/model **UI dropdown**, **log-redaction verified under a real
> call** (§6.7 — currently only unit-tested), and the **manual triplet smoke + smoke-procedure doc**.
> **Resolved 2026-06-11 → option (b), thin S6** (PR #53): the real-provider smoke ran green (Ollama
> Cloud + an OpenRouter free model; the §6.7 key-leak grep clean), the **UI dropdown is deferred as a
> feature** (see Decided 2026-06-11 + the struck task below + cross-cutting), and direct vendor
> adapters **ship none**. **Still owed before M3 starts:** the **cross-cutting curation review**
> (`docs/AGENTS.md` §3) and the `/wrap-session` handoff rewrite. Original options kept for history:
> (a) declare M2 done now and carry the leftovers into M3 cross-cutting, or (b) run a thin S6 to land
> them before rolling.

- [x] **Direct vendor adapters, only if needed** — **shipped none** (2026-06-11): OpenRouter covers the paid route (ADR 0003); no specific model gap or cost/latency case forced a direct adapter. Re-open under cross-cutting when one does. ~~`adapters/llm/{grok,anthropic,google,openai}.py`~~ intentionally absent.
- ~~[ ] Router: UI dropdown per spec §6.5 surfacing the chosen model and reason (the provider-order routing already lands in M2.S2).~~ **Deferred 2026-06-11 (M2→M3 roll):** a *feature*, not thin polish — the §8.5 panel already surfaces "which model/why"; the remaining half is a user **model-override** control + per-task-type prefs that touches INV-7. See Decided 2026-06-11 + cross-cutting; lands as its own decompose step-0 (with/after M3).
- [x] Integration polish — **log redaction of API keys per §6.7 verified under a real call + documented** (2026-06-11, PR #53): found the backend emits *no* operational logs (so §6.7 holds vacuously today; fixed the false `backend/AGENTS.md` structlog line) and added the manual key-leak smoke as the regression guard for when logging lands. ~~error mapping / status-endpoint-against-a-real-provider~~ → the store-down→500 + status-endpoint follow-ups are already tracked in **cross-cutting** (the M2.S4/S5 review notes), not re-done here.
- [x] Manual smoke against a real **doublet** (Ollama Cloud + OpenRouter free model — *not* a triplet: no direct Anthropic adapter exists, OpenRouter is the only paid route) where keys are configured locally; smoke procedure documented in `backend/AGENTS.md`. Run 2026-06-11: both providers 200, key-leak grep clean. `scripts/check_openrouter.py` added (mirrors `check_ollama_cloud.py`).

**Done when:** M2 acceptance per spec §9 — a stranger can upload → see chapters/scenes → see the entity graph (with duplicates, that's the point) → see which agent/tier/model ran for each step.
**Resume anchor:** M2 done; handoff rolls to M3 in the block above.

---

### Cross-cutting (carried into M3 — curated at the M2→M3 roll 2026-06-11; see `docs/CLAUDE.md` §3)

> **M2→M3 curation pass (2026-06-11).** Every item walked by hand per `docs/CLAUDE.md` §3. The M2-resolved items below stay struck as history (§4). Open items carried into M3; those that **bite M3 directly:** the **embedding read path** (lands *with* MatchingAgent Stage 2), the **agent-retry-loop / envelope-unwrap rule-of-three dup** (M3's matching/judge agent is the n=3 trigger for branch (a)), **store-down→503** (its old "lands with M2.S5" pointer was stale — M2.S5 shipped without it; re-pointed to M3), **graph story-vs-project scoping** (the §3.4 world-toggle needs a backend), the **`last_call()` tiebreaker** (if M3 introduces concurrent LLM calls), and the **§6.5 model-override dropdown** (its own decompose step-0). The **vault-reflection** item resolves in this session's gate 2 (the meta-architect run).

- [ ] **§6.5 model-override dropdown** (user picks "which provider/model for this task" + per-task-type preferences). Deferred from M2.S6 as a *feature*, not thin polish (see Decided 2026-06-11). The §8.5 panel already does the *surfacing*; this is the *override* control, which touches **INV-7** (system-derived routing) and needs an INV-7/spec reconciliation + router plumbing + a preferences store. Do as a `meta-architect:decompose-requirement` step-0, alongside or after M3. (M2→M3 roll note, 2026-06-11.)
- [x] ~~**Reflect the M2→M3 roll decisions in the `architecture/` vault.** The **dropdown deferral** (a new open decision touching INV-7) and the **observability-logging gap** (a new open question) are recorded in the plan but **not yet** in `architecture/open-questions.md` / `INDEX.md` — by design, since the vault is written only by the `meta-architect:*` skills (`docs/decisions/0002`). Fold both at the next meta-architect run: the M3 `review-architecture` catch-up sweep + the `decompose-requirement` step-0.~~ ✅ **Resolved 2026-06-11 (Session 19, gate 2):** the meta-architect run folded both — **OQ-14** (model-override dropdown vs INV-7) + **OQ-15** (operational logging absent ⇒ INV-6 vacuous) added to `architecture/open-questions.md`, `INDEX.md` regenerated; the dropdown is also carried in cross-cutting above. (M2→M3 roll, 2026-06-11.)
- [ ] **meta-architect workflow integration** — the three skills (`initialize`/`decompose`/`review`) are shipped and **two live runs are done** (Session 9 `initialize` → vault; Session 10 `review`+`decompose` → ADR 0003), but the skills are still **not wired into the rituals**. Deferred by design (`docs/decisions/0002`): wire on evidence *after living with the vault*, not before. Hypothesis to test: bootstrap-detect a missing `architecture/` vault in `/resume-session`; run `decompose-requirement` at feature "step 0" before the failing test; run `review-architecture` in `/wrap-session` alongside `/review-pr`. **Evidence so far (2 runs):** both produced real value (vault + ADR 0003) *and* a real maintenance cost (the accepted-proposal/decision-reconciliation drift — now mitigated by decompose §5b + `/review-pr` §2). Carry-ins: ~~(a) add a vault/docs-PR review lens to `/review-pr`~~ ✅ **done Session 10** (broadened to the decision-reconciliation lens — `/review-pr` §2 + root `AGENTS.md` + decompose §5b); (b) **[Issue #31](https://github.com/yaaisiu/story-forge/issues/31)** — meta-architect slug/filename carve-out for `PROJECT`/`INDEX`/`CHANGELOG` (still open). (Session 8 note; updated Sessions 9 + 10, 2026-06-02.) **Session 28 (retro):** fixed a smaller framework wart — the `decompose-requirement` + `review-architecture` skills said "read the matching template from `templates/`" but no `proposal.md`/`review.md` template exists (the section structure lives in each skill body); wording corrected to point at `vault-note.md` for frontmatter + name the skill-body as the single home (no duplicate template). Also **fresh evidence toward the OQ-3 ritual-wiring refinement** (run `review-architecture` at *resume* when a session opens with a decompose): Session 28 opened with a decompose on a stale-INDEX vault and had to correct the "Next steps" by hand — wiring itself stays deferred per ADR 0002, evidence accumulates.
- [x] ~~**[Issue #68](https://github.com/yaaisiu/story-forge/issues/68) — frontend `npm audit` HIGH on vite 8.0.10 (GHSA-fx2h-pf6j-xcff + GHSA-v6wh-96g9-6wx3).**~~ ✅ **Resolved 2026-06-16 (PR #73, Session 27):** `vite` 8.0.10 → **8.0.16** (published 2026-06-01, 15 days old → soaked; OSV-clean), via `/add-dependency`. `npm audit --audit-level=high` green; the 2 residual react-router *low* advisories don't fail the high gate (out of scope). Issue #68 closed.
- [ ] **Drop the Session-27 time-boxed security waivers as their fixes soak (`/triage-advisory`).** Clearing the security-job reds (PRs #72/#73) needed soaked bumps + **fix-first time-boxed waivers** for residuals whose fixes were still inside the 14-day soak. Drop each (bump to the now-soaked fix, delete the `[[IgnoredVulns]]` block + `WAIVERS.md` row) when it bakes: **python-multipart 0.0.31 — soaks 2026-06-18** (GHSA-v9pg-7xvm-68hf, low); **starlette 1.3.0 — soaks 2026-06-25** (GHSA-jp82-jpqv-5vv3, low); **starlette 1.3.1 — soaks 2026-06-26** (GHSA-82w8-qh3p-5jfq, **HIGH** DoS). Plus the **condition-based** neo4j Trivy waiver **CVE-2026-50010** (netty TLS hostname-verification bypass) — drop when a Neo4j 5.26.x image ships netty ≥4.1.135.Final. `/resume-session` step 3b now flags the dated ones; the `ignoreUntil` CI re-red is the backstop. (Session 27, 2026-06-16.)
- [ ] Update spec §10 if any open question hardens (extraction granularity, etc.)
- [x] ~~**`POST /stories/{id}/extract`: store-down surfaces as 500, and the Neo4j driver has no lifespan-close.** The route maps agent/transport failure to 502, but a *store* outage (Neo4j/Postgres connectivity error mid-ingest) propagates as an unhandled 500; and `main.py` builds the `Neo4jRepo` driver at import with no FastAPI lifespan to `close()` on shutdown.~~ ✅ **Resolved 2026-06-15 (M3.S4a, PR #63):** the extract + accept routes map `(psycopg.OperationalError, neo4j ServiceUnavailable)` → **503** (fail-closed — a connectivity blip mid-cascade must never degrade to a silent NEW); list/reject map `OperationalError` → 503; `main.py` gained a FastAPI **lifespan** that `close()`s the Neo4j driver on shutdown. (M2.S4 `/code-review` + external-review note, 2026-06-10; re-pointed to M3 at the 2026-06-11 curation; landed with the write-path refactor.)
- [x] ~~**Relation decide-on-relations UI + the relation-write backend (deferred past M3.S4b — Session 25).**~~ ✅ **Backend resolved 2026-06-16 (M3.S4e, PR #76);** the **S4f UI** is now a numbered session (feature-order item 9). S4a *stages* relation data but nothing wrote graph edges, and a relation can only be written once **both** endpoints are accepted (re-pointing on a merge). S4b was originally scoped to own the decide-relations UI + this backend slice; at Session-25 start we **deferred both** to keep S4b a clean single-conversation additive-UI slice that closes M3 on *entity* dedupe (the §9 M3 outcome). Resolve as its own slice: a backend relation-write path (edge written only when both endpoints are accepted; re-point on merge) + the §3.3 5th human action (decide-relations) in the review queue. (Session 25 scope decision, 2026-06-15.) **Priority raised 2026-06-15 (Session 25, M3.S4c decompose):** M3.S4c/S4d make merges *more frequent*, and a merge with no relation-write **orphans the candidate's staged relations** — so this slice now gates a clean graph more than before; sequence it soon after S4c/S4d (owner kept it deferred for now, eyes open). **Now live (Session 26):** S4c shipped (PR #67) — on-accept re-match makes merges routine, so this is the **top backend cross-cutting** to sequence next (after or alongside S4d). **Session 27:** S4d shipped (PR #70) — a handpick-**merge** orphans the candidate's staged relations the same way; M3 *entity* dedupe is now complete (S4a–S4d), so this relation-write slice is **the** next M3 piece, and the natural next session. **Session 28 — DECOMPOSED + DECIDED** (`architecture/proposals/m3-relation-write.md`, register mirrored in Blocked / OQ-19): owner framed it an **M3 slice**; **DM-Rel-1 = explicit human gate** (the §3.3 5th action, not auto-write); build **split backend-now / UI-next**. The decompose reframed it smaller than feared — edges write **lazily** (resolve each surface endpoint to its candidate's *committed* entity id), so "re-point on merge" mostly dissolves (only an M4 entity↔entity merge re-points a written edge); a real must-fix surfaced (`create_relation` `CREATE` doubles an edge on retry → deterministic-id `MERGE`). Remaining register (DM-Rel-2/4/5/6/7) confirm-at-build. **Next: the S4e backend slice, test-first** (the relation-write flip test).
- [x] ~~**Review-queue "merge with…" is alternatives-only — no arbitrary-entity search (deferred, Session 25).** The Stage-4 `M` (change-target) action lets the reviewer retarget a merge, but the only targets offered are the cascade's top-3 `alternatives`.~~ ✅ **Promoted into a planned slice 2026-06-15 (Session 25): M3.S4d — manual handpick** (`GET /stories/{id}/entities?q=` project-scoped + a picker; decompose `architecture/proposals/m3s4c-intra-batch-rematch.md` DM-S4c-5/6). No longer "later" — it's the false-negative safety net paired with M3.S4c's on-accept re-match.
- [ ] **`CandidateView` carries no merge-*target* name — Stage-2/3 merges show a generic label (Session 25).** The review card resolves the proposed merge target's name by matching `target_entity_id` against the staged top-3 `alternatives` — but those are ranked by **RapidFuzz** while a Stage-2/3 merge target is chosen by **embedding cosine / judge** (`candidate_staging._cascade` + `matching_agent.stage2`), a different signal, so the target need not be in the list. `CandidateView` (`api/stories.py`) exposes only `target_entity_id`, no name, so on those paths the badge falls back to `Merge → an existing entity` (the `/code-review` should-fix; the raw-UUID was folded out in S4b). Proper fix: add `target_canonical_name` to `CandidateView` (resolve it at stage time from the in-scope `existing`/`snapshot` entities, or at read time) so the reviewer sees *what* they're merging into on every path. Backend slice + openapi regen — track here, do when the §3.3 review UX is tightened. (Session 25 `/code-review` finding.)
- [ ] **Per-screen `reviewMessage`/`extractMessage`/`messageForError` error-to-string mappers duplicate a pattern (now 4 screens — Session 25).** `ReviewQueue`, `GraphViewer`, `UploadScreen`, `OutlineEditor` each carry a near-identical `ApiError → string` switch (differing only in which statuses they special-case). At n=4 this is past the rule-of-three; a shared `errorToMessage(error, overrides?)` in `lib/api/` with per-call status overrides would fold them. Pre-existing convention, *not* S4b's surgical job — fold when one of these screens next needs an error-message change (don't pre-emptively refactor all four). (Session 25 `/code-review` reuse finding.)
- [ ] **`GET /stories/{id}/graph` returns *project*-scoped data under a story URL.** The graph is keyed by `project_id` (the §6.4 multi-tenancy seam), so the route resolves the story → its project and returns that project's whole graph. Correct while it's one-story-per-project (today's as-built — `api/stories.py` "Each upload creates its own project for now"), but the moment a project holds two stories, `/stories/A/graph` and `/stories/B/graph` return identical graphs, and spec §3.4's "this story only / whole world" toggle has no backend to drive it. Resolve when multi-story projects (or project selection) land: either a genuine story filter (entities `first_seen` in the story's paragraphs) or a `/projects/{id}/graph` route matching the data. (M2.S5 `/code-review` + `/review-pr` altitude finding, 2026-06-11.) **Session 27:** M3.S4d's new `GET /stories/{id}/entities?q=` deliberately adopts the *same* project scope (consistent with this route), so the world-toggle question is unchanged — both move together when multi-story projects land.
- [ ] **`PostgresCostStore.last_call()` ordering has no tiebreaker.** `ORDER BY created_at DESC LIMIT 1` decides the §8.5 panel's "most recent call"; `created_at` defaults to `now()` (transaction-start time), and there's no monotonic column (the PK is a random uuid). Unreachable on the as-built **sequential single-user** extraction path (rows land seconds apart), but two rows sharing a `created_at` would make "most recent" nondeterministic. Lands with the same concurrency work spec §6.6 already defers ("a true reserve-then-reconcile lands only if batched concurrent calls do") — add a monotonic tiebreaker (a `bigserial`, or order by a real insertion sequence) **if/when** concurrent LLM calls are introduced. (M2.S5 `/code-review` finding, 2026-06-11.)
- [x] ~~**Embedding read path** — `adapters/postgres_repo.py` reads `NULL AS embedding` (paragraphs never store embeddings in M1). When the embedding pipeline lands (**M3, alongside MatchingAgent Stage 2**…), add the `pgvector` dep + `register_vector_async`, switch `get_paragraph`/`list_paragraphs` to `SELECT ... embedding`, and start writing the column. (Codex review note, 2026-05-20.)~~ ✅ **Resolved 2026-06-12 (M3.S2, PR #58):** `pgvector==0.4.2` added; `register_vector_async` centralised in `db.connect` across all three connect sites; the 3 paragraph reads un-stubbed to `SELECT ... embedding`; the new write path is `entity_mentions.embedding` (paragraphs have no producer yet — the cascade uses per-mention vectors). See Decided.
- [ ] **Orphaned sandbox files on upload failure** — `api/stories.py` writes the original file to the sandbox *before* the `Project`/`Story` inserts; if the DB write fails the transaction rolls back but the file is left on disk, pointing at a story that never persisted. Harmless on a single-user local app, so deferred. Fix when it bites or when a delete-story feature lands (clean up the file on insert failure, or write-after-commit). (Own-review note, 2026-05-21.)
- [ ] **Window-and-stitch for `auto` on long text** — `agents/chunking_coordinator.py` currently raises `ChunkingTooLongError` above `max_input_words` (a 422). The proper fix is a deterministic pre-split on paragraph boundaries, per-window agent calls, offsetting paragraph indices, and stitching the chapters/scenes back together — accepting some seam imperfection that the user corrects in the UI confirm. Deferred until the auto path is actually exercised on a real 50k-word input. (Session 4 windowing-guard decision, 2026-05-25.)
- [ ] **Re-structure semantics: delete-and-replace** — `POST /stories/{id}/structure` returns 409 if the story already has a tree. A re-run with a different mode ("I didn't like auto, let me redo manual") needs delete-and-replace, which the Session 1 repo doesn't have (Update was deferred). Session 6 added a *narrow* update — `update_story_raw_text` in the repo + an override-and-persist branch on the structure endpoint — covering the manual editor's "user accepts/edits" use case (spec §7 step 2). The full delete-and-replace UX (redo a different mode after the first persist) is still deferred. Add when the frontend wants it. (Session 4 endpoint-shape decision, 2026-05-25; narrowed Session 6, 2026-05-26.)
- [x] ~~**Backend advisory scan in CI (PyPI parity for `npm audit`).**~~ ✅ **Built 2026-06-08 (PR #44, Session 14).** `osv-scanner` step vs `backend/uv.lock`, **fail-on-any**, scanner **digest-pinned** (the `google/osv-scanner-action` root `action.yml` is a no-`runs:` stub and its reusable workflows do diff/SARIF scans — neither a scoped fail-on-any step; a digest pin is stronger than the planned SHA-pin, D6 intent preserved). Scoped waivers in `infra/osv/` (toml + `WAIVERS.md`, mirroring the Trivy split; no active waivers). `starlette` 1.0.0→1.0.1 explicit pin was the live self-test (gate red on 1.0.0, green on 1.0.1). Spec §6.7 amended; README + `/review-pr` §5 reconciled. (Session 5 note, 2026-05-25; decomposed Session 13; built Session 14.)
- [ ] **`/stories/{id}/structure` 422 is overloaded.** Two distinct failure modes return 422 with different body shapes: FastAPI's auto-generated request-validation errors (shape: `HTTPValidationError`) and the explicit `ChunkingTooLongError` raised when the input exceeds `max_input_words` (shape: `{detail: str}`). The Session 5 PR documented the unambiguous error codes (400/404/409/413/415/502) in OpenAPI but left 422 alone because declaring it would overwrite FastAPI's validation shape. Pick one: either remap `ChunkingTooLongError` to 413 (payload too large — semantically truer; the body *is* too long for the agent) or to a free status code in the 4xx range, so 422 stays purely validation-shaped and the frontend can discriminate the two cases by type rather than body inspection. Lands cleanly with the "window-and-stitch for auto on long text" cross-cutting fix above, which removes the error path entirely for the auto mode. (Session 5 Codex-review fold-in note, 2026-05-25.)
- ~~**LLM router doesn't record/fail-over on a malformed-but-`200` provider envelope.**~~ ✅ **Resolved 2026-06-08 (M2.S3, PR #42):** typed `ProviderResponseError` added in `adapters/llm/base.py`, raised by the Ollama + OpenRouter adapters on a malformed-`200` envelope (missing fields, unparseable body, **and null `content`** — the null case caught by `/code-review`), and `router.py` records a failure row + fails over like a 5xx; the "envelope-malformed vs schema-invalid" split is proven by router tests. Closes the §6.6 "one row per call" gap (INV-5). (M2.S2 own-review should-fix → built M2.S3.)
- ~~**ChunkingAgent doesn't map `ProviderResponseError` (malformed-`200`) to its route's 502.**~~ ✅ **Resolved 2026-06-10 (M2.S4, PR #48):** `ChunkingAgent` now catches `ProviderResponseError` around the provider call → `ChunkingError` (the structure route already maps that to 502), so a malformed-200 during chunking is a 502, not an unhandled 500. + test. Original below. M2.S3 added a typed `ProviderResponseError` (adapters raise it on a malformed-`200` envelope). `ExtractionAgent` is behind the router, which records + fails over; but `ChunkingAgent` holds a *raw* `OllamaProvider` and its retry loop catches only `ValidationError`, so a malformed-`200` during chunking propagates as `ProviderResponseError` past the route's `except ChunkingError` → an unhandled **500**, though `POST /stories/{id}/structure` advertises **502** for LLM failure. **Not a regression** (pre-M2.S3 the same body raised a raw `KeyError` → also 500). Cheap fix: catch `ProviderResponseError` in `ChunkingAgent` → `ChunkingError` (the route already maps that to 502). The deeper fix is to move chunking behind the router like extraction (gets failover + the `llm_calls` row too) — lands naturally with **M2.S4**. (M2.S3 `/code-review` finding, deferred 2026-06-08.)
- ~~**`RelationCandidate` accepts blank `subject`/`predicate`/`object` as success.**~~ ✅ **Resolved 2026-06-10 (M2.S4, PR #48):** added the same non-empty `field_validator` (covering all three fields) used for `candidate_name`, so blank relation output is retried not accepted; + an agent-retry test and a direct-validation test. Original below. `agents/extraction_agent.py:92-94` types the three relation fields as plain `str` with **no non-empty validator** (only `candidate_name` has one). A model returning `{"subject":"","predicate":"","object":"",…}` validates as a successful `ExtractionProposal`, so the agent's retry loop **stops** and the future M2.S4 graph writer gets a relation with no usable endpoint or type. **Distinct from the intentionally-accepted dangling relation** — a dangling endpoint still needs a *non-empty* surface form or id; only its *resolution* (match/create) is deferred to M3. Fix: add the same non-empty `field_validator` used for `candidate_name` to `subject`/`predicate`/`object`, so blank output is retried not accepted; + a test. A real gap **both** M2.S3 review passes missed (tests cover blank `candidate_name` + empty-lists, never blank relation fields). Small — fold into **M2.S4** (where it bites) or a quick standalone PR. (External review remark on merged PR #42, 2026-06-08.)
- **Agent retry loop + adapter envelope-unwrap are duplicated (revisit at rule-of-three).** Two altitude dups surfaced in the M2.S3 `/code-review`: (a) the `render → call → validate → retry-on-ValidationError → give-up` loop is near-verbatim in `chunking_agent.py` and `extraction_agent.py` (only the schema/weight/grounding differ); (b) the `response.json()` + envelope-unwrap-or-raise-`ProviderResponseError` block is near-identical in `ollama.py` and `openrouter.py` (only the dict path differs). **(a) ✅ Resolved 2026-06-15 (M3.S3, PR #60):** JudgeAgent was the n=3 trigger — the loop folded into `agents/validation.py` `validate_with_retry(...)`; chunking + extraction refactored onto it (each keeps its own schema/call/give-up error); `TaskWeight` + a `Router` Protocol promoted into `base.py` at the same time. **(b) still open** — deliberately per-copy at n=2; **fold at the first direct vendor adapter** (Anthropic/Grok/…, "as needed"), since each new copy re-implements the INV-6/OQ-4 no-body-leak rule by hand, and a `_unwrap_content(...)` in `base.py` would touch that security-sensitive guarantee. (M2.S3 `/code-review` altitude findings, 2026-06-08.)
- **Repo-root `scripts/` is outside CI's ruff scope.** The backend ruff config is `src = ["src", "tests"]` and CI runs `ruff check .` from `backend/`, so `scripts/*.py` (e.g. `check_dependency_age.py`) are never linted — a pre-existing `UP017` (`datetime.UTC`) sits there uncaught. Either add repo-root `scripts/` to a lint step or fix the one nit. Left untouched in PR #25 to stay surgical. (M2.S1 own-review nit, 2026-06-01.)
- ~~**[sooner-than-later — bites M2.S3] Spec §6.5 router contract is stale vs as-built; reconcile before/at M2.S3.**~~ ✅ **Resolved 2026-06-08 (M2.S3 step 0, stop-and-amend flow):** spec §6.5 router block amended `route(self, task) -> LLMProvider` → `complete(messages, *, weight, task_type, json_schema=None) -> CompletionResult` (orchestrating API, ADR 0003), and the envelope-vs-schema split written into both the §6.5 pseudocode and the §6.5/§11 "Failover" paragraphs (malformed-`200` envelope → failover via `ProviderResponseError`; schema-invalid → agent prompt-retry). Reconciled: `README.md` failover line, plan line 64 now accurate. Left intact as history: ADR 0001 (superseded-in-part) + the M2.S2 Done/Decided log lines. Original framing (for history): two §6.5 drifts the first router consumer would read as contradictions — (1) the `route()→LLMProvider` signature vs as-built `complete(...)`, and (2) the "malformed response schema → retry the prompt" lump conflating envelope-malformed (→ failover) with schema-invalid (→ retry prompt), the OQ-10 distinction. (Codex no-PR-flow review + M2.S3 decompose G6.)
- ~~**[doc-hygiene, contingent on confirmation] Simplify the §0 concurrent-agents rules if Codex Desktop is fully gone.**~~ ✅ **Resolved 2026-06-10 (PR #50):** owner confirmed Codex Desktop is retired (the only residual is the GitHub-wired Codex PR review). Root `AGENTS.md` §0 rewritten to "Commit hygiene on the working tree" — dropped the two-agents / `.codex/RUNTIME_NOTES.md` / UNC-boundary framing, kept stage-explicit-paths + surface-foreign-changes as general hygiene; `.codex/` deleted; the Review-model note + the Windows/UNC symlink caveat trimmed. Original below.
- ~~**[deprioritized 2026-06-08 — contingent on Codex still reviewing] Mirror the failover/error-taxonomy review lesson into the Codex `/review-pr` skill.**~~ ✅ **Resolved 2026-06-10 (won't-do; PRs #49/#50):** moot — the GitHub Codex PR reviewer reviews the diff and runs **no** repo skill, and the Codex-side `.agents/skills/review-pr/` (the only thing that consumed a `lessons.md`) is **deleted** (PR #50). The durable lessons live in the primary `.claude/skills/review-pr/` gate (PR #49 also de-emphasised Codex there + removed the dead UNC-artifact bullet). Nothing left to mirror into. Original below.
- ~~**INV-5 "every terminal edge" over-claims ledger coverage vs OQ-10.**~~ ✅ Handled in-vault 2026-06-08: `architecture/invariants.md` INV-5 narrowed to "every terminal edge **the router currently handles**" + an explicit "known coverage gap (OQ-10)" clause, so a reader can't mistake INV-5 as total before the `ProviderResponseError` work lands. (Codex no-PR-flow review; folded during the M2.S3 decompose since it's a vault file.)
- ~~**[doc-hygiene, low priority] M2.S2 proposal depicts the superseded `route()` router API.**~~ ✅ **Resolved 2026-06-09 (Session 15 architecture sweep):** both spots in `architecture/proposals/m2s2-llm-router-budget-cap.md` now read `complete()` — the stations table (line 139, annotated "as-built / amended §6.5") and the mermaid (line 183). Original: it showed `router.route()` + `LLMRouter.route`, the pre-build API superseded by the as-built `complete(messages, *, weight, task_type, json_schema=None)` (ADR 0003; spec §6.5 amended PR #39). (PR #39 self-review should-fix → fixed during the pre-M2.S4 `review-architecture` sweep.)
- **`CandidateSpan` has no `char_start <= char_end` validator** (`domain/extraction.py`). Internal deterministic data from spaCy, so optional — a model validator would just self-document the invariant. Add if a future producer of `CandidateSpan` is less trustworthy than spaCy. (M2.S1 own-review nit, 2026-06-01.)
- ~~CORS allowlist excludes `http://127.0.0.1:5173`~~ ✅ Resolved 2026-05-26 (Session 6 wrap, PR #20): allowlist now includes both `localhost` and `127.0.0.1` for ports 5173 and 3000. §6.7 amended with rationale. See Decided.
- ~~Dev DB needs `alembic upgrade head` before first real run~~ ✅ Resolved 2026-05-26 (Session 6 wrap, PR #20): one-line note added to `backend/CLAUDE.md` "Running locally". See Decided.

### Blocked / questions

- ~~**M3 relation-write register** (`architecture/proposals/m3-relation-write.md`, mirrored OQ-19)~~ ✅ **fully resolved 2026-06-16 (Session 29, M3.S4e / PR #76; proposal now `accepted`, OQ-19 struck, ADR 0005).** DM-Rel-1 = **explicit human gate** (owner; *rejected:* auto-write, hybrid bulk-confirm). DM-Rel-2/4/5/6/7 confirmed at build as proposed: normalised-exact same-paragraph resolution reading the committed id from `candidates` (create-id derivation promoted to `domain.candidates.committed_entity_id`); a `staged_relations` table; M3 writes / M4 re-points; idempotent `MERGE` on `uuid5(subject_id,predicate,object_id)`; dangling-to-known held. Invariant: **INV-1 broadened** to cover edges (not INV-10 minted); INV-9 second instance. **New follow-up (carried):** per-mention provenance for triple-deduped edges — edge-id-from-triple collapses repeated mentions to one edge (clean graph), so post-PoC we need a way to enumerate all occurrences of a relationship (the `staged_relations` rows retain per-paragraph provenance; the graph edge does not).
- ~~**M3 cascade register** (`architecture/proposals/m3-cascade-matching.md`, mirrored OQ-16).~~ ✅ **Fully resolved 2026-06-15 (S23).** ~~DM6~~ ✅ intercept-before-write (A); ~~DM1~~ ✅ thresholds → `config.py` (S20); ~~DM2~~ ✅ `paraphrase-multilingual-mpnet-base-v2` 768-dim, SHA `4328cf26…`, pinned via the §6.7 HF-channel (S20; **implemented S21, PR #58**); ~~DM3/DM4~~ ✅ per-mention vectors on `entity_mentions`, max cosine (S20; **implemented S21, PR #58**); ~~DM5~~ ✅ JudgeAgent tier = cloud_free via `weight="medium"`/`task_type="judge"` (S22, **implemented PR #60**); ~~DM7~~ ✅ review-queue UX → build §3.3 Stage-4 elements + keyboard nav in S4b, **INV-2 consent gate DEFERRED past M3** (persona-justified; keyboard scheme an S4b-time pick) (S23); ~~DM-rej~~ ✅ **remember rejections** — the `rejected` edge writes a `candidate_decisions` evidence row the matcher consults before re-queueing (S23). Also live: spec §10 q8 (multilingual `canonical_name_pl/en`, surfaces at merge — the spec's to resolve).
- ~~**M3.S4c intra-batch-dedup register** (`architecture/proposals/m3s4c-intra-batch-rematch.md`, mirrored OQ-18).~~ ✅ **Resolved 2026-06-15 (Session 25):** ~~DM-S4c-1~~ ✅ **slice S4c (live re-match, backend-only) + S4d (manual handpick)**; ~~DM-S4c-2~~ ✅ re-match runs **synchronously in the accept handler + incremental** (vs just the newly-accepted entity); ~~DM-S4c-3~~ ✅ **auto-flip on Stage 1 `>85%` OR Stage 2 `cosine >0.85`** (owner chose the +Stage-2 option over the proposed Stage-1-only), **no live Stage-3 judge**; ~~DM-S4c-4~~ ✅ **monotone** re-proposal (only `new → merge`, never the reverse) → idempotent — kept as the transition guard, **not** a new INV-10; ~~DM-S4c-5~~ ✅ handpick **project-scoped** (the §6.4 tenancy key) — **supersedes** the deferred "arbitrary-entity search" cross-cutting; ~~DM-S4c-6~~ ✅ `GET /stories/{id}/entities?q=` over Neo4j accepted entities via `accepted_entity_reader`. Spec §3.3 amended (on-accept re-match + handpick clarifications). **INV-1/INV-9 hold** (re-match writes only the `candidates` table). **Built — S4c shipped (PR #67, 2026-06-15, Session 26); ADR 0005 declined at build** (owner): a contained change (staging-only, INV-9 holds), already fully recorded in the proposal + spec §3.3, so the rationale lives in Decided + the vault INV-9 graph-vs-staging clarification rather than a separate decision file. **Relation-write stays deferred** (the merge-orphans-relations cross-cutting, now top backend priority — S4c made merges routine). **S4d (manual handpick) is the next slice.**
- ~~**M3.S4a build-detail register** (`architecture/proposals/m3s4a-intercept-write-path.md`, mirrored OQ-17).~~ ✅ **Resolved 2026-06-15 (S23):** ~~DM-S4a-1~~ ✅ new Postgres `candidates` staging table; ~~DM-S4a-2~~ ✅ add **INV-9** "no automated stage writes the graph"; ~~DM-S4a-3~~ ✅ resume checkpoint = "candidates staged" + a zero-candidate marker (mentions move to accept-time); ~~DM-S4a-4~~ ✅ a focused append-only **`candidate_decisions`** evidence table now, **defer** the full §4.2 `edit_history` (text-edit dataset) to the editing milestone; ~~DM-S4a-5~~ ✅ no retention at PoC (rejected-memory is a feature; unreviewed backlog the only growth risk). **ADR 0004** (DM6 intercept-before-write, fuller MADR) to author **test-first with the M3.S4a code**.
- ~~**[Issue #2](https://github.com/yaaisiu/story-forge/issues/2) — invalid neo4j container image tag.**~~ ✅ Resolved 2026-05-21 (PR #3, squash `6694e23`): neo4j → `5.26.25-community-ubi10`, 3 bundled-netty CVEs waived. See Decided + Done.
- ~~**[Issue #4](https://github.com/yaaisiu/story-forge/issues/4) — pgvector (+ ollama) images CVE-stale.**~~ ✅ Resolved 2026-05-21 (PR #6, squash `4cb92a2`): pgvector → `0.8.2-pg17-trixie` (7-day bend), ollama → `0.24.0`, both scoped-waivered; `WAIVERS.md` register added. `main` fully green. See Decided + Done.
- ~~**Decide in Session 2 — language-detection library:** `langdetect` vs `fasttext`.~~ ✅ Resolved 2026-05-21: chose **`langdetect`** (no out-of-band model artifact, ample accuracy for PL/EN prose). See Decided + Done.
- ~~**Decide in Session 3 — auto-chunking trigger heuristic:** which threshold decides "long enough → use Ollama Cloud instead of local"?~~ ✅ Resolved 2026-05-22: config knob `CHUNKING_LOCAL_MAX_WORDS=4000` (conservative); but moot on this GPU-less host where cloud_free is the default tier anyway (see Decided). See Decided.

### Decided

- **2026-06-16 — Session 29 (M3.S4e relation-write backend, PR #76 merged): graph edges now write under an explicit human gate; INV-1 broadened; a real merge-target bug fixed by `/code-review`.** Built the relation-write backend test-first, resolving the whole DM-Rel register. **(1) DM-Rel-4 = a `staged_relations` table** (owner) — a per-paragraph-occurrence row with a `staged|written|rejected` lifecycle, resolved endpoint ids, and the committed edge id; *considered & rejected:* keeping the inert JSONB blob (can't carry per-relation status/evidence for the explicit gate). Relations stage here atomically in `persist()`; `paragraph_processed.relations` is left vestigial. **(2) DM-Rel-6 edge id = `uuid5(subject_id, predicate, object_id)`** (owner) — the same fact across two paragraphs collapses to **one** edge (clean graph, §9 M3); *considered & rejected:* id-from-staged-row (preserves per-mention edges but shows two parallel edges for one fact). Owner-flagged **follow-up:** the triple-id loses per-mention provenance on the graph side → post-PoC need a way to enumerate all occurrences (the staged rows retain it). **(3) DM-Rel-2/5/7 confirmed at build as proposed:** normalised-exact same-paragraph resolution reading the committed id from `candidates`; M3 writes / M4 re-points; dangling-to-known held. The create-id derivation was **promoted to the shared `domain.candidates.committed_entity_id`** so the accept path and resolution can't drift (the DM-Rel-2 `verify-at-build` flag). **(4) Invariant: broaden INV-1** ("…and every relation edge") rather than mint INV-10 — one human-gate principle for nodes + edges; INV-9 gains its second witnessed instance. **ADR 0005** records the slice + its accepted costs; proposal → `accepted`, OQ-19 struck. **(5) No spec amendment** — §3.3 already lists "decide on relations"; the gate *mechanism* didn't force a §3.2/§3.3 change. **(6) `/code-review` caught a real bug:** when a reviewer *changes a merge target*, the candidate row's `target_entity_id` was never persisted (only the decision row was), so `committed_entity_id` resolved a same-paragraph relation endpoint to the stale target → edge to the wrong entity. *Fixed:* `set_status` now persists the committed target on merge-accept (still status-last); regression + cross-paragraph-collapse tests added. **Reviews:** `/review-pr` (self) + the 7-finder `/code-review`, both folded before merge. 331 tests pass.

- **2026-06-16 — Session 28 (planning/decompose, vault-only): relation-write framed + designed; the central gate decided; a meta-architect skill wording fixed.** No production code. **(1) Framing (owner):** the relation-write work is an **M3 slice** — relations are part of §9 M3's "the graph is clean" (and §3.3 lists "decide on relations" as a Stage-4 human action), *not* an M3→M4 milestone roll (M3's *entity* dedupe is done, S4a–S4d). So we stay in M3 and add a relation-write slice; no milestone roll. **(2) `meta-architect:decompose-requirement` step-0** → `architecture/proposals/m3-relation-write.md` (+ glossary `referential-integrity`, OQ-19, learning-log; INDEX/changelog regenerated; the stale INDEX "Next steps" that framed S4c as next — though S4c/S4d shipped — corrected). **Key architectural finding (the reframe):** a staged relation names its endpoints as **surface strings** with no entity id until accept, so edges must be written **lazily** by resolving each endpoint to its candidate's *committed* entity id — which means the feared "re-point edges on merge" **mostly dissolves** (the edge is born pointing at the survivor; only an M4 accepted-entity↔entity merge re-points a *written* edge — DM-Rel-5). Concrete must-fix surfaced: `neo4j_repo.create_relation` uses `CREATE`, so a retried accept **doubles** the edge → needs a deterministic id + `MERGE`-on-id (DM-Rel-6). **(3) DM-Rel-1 decided (owner): an EXPLICIT human gate** over relations — the §3.3 5th action ("decide on relations"), *not* auto-write-on-both-endpoints-accepted. *Considered & rejected:* auto-write (lightest, but commits a hallucinated predicate/direction even when both nodes are right — the exact LLM error human-in-the-loop exists to catch); hybrid bulk-confirm (kept as a fallback if the explicit gate proves heavy). Implies broadening INV-1 (or minting INV-10) to put the human gate on edges — framed, decided at build. **(4) Slice (owner): split BACKEND now / UI next** (the proven S4a→S4b cut) — the backend (staged_relations table + resolution + idempotent edge + decide-relations endpoints + the invariant flip) is test-first witnessable at the API boundary; the React relation-review surface follows. *Considered & rejected:* one combined slice (too big for one conversation, the same reason S4 was split). Remaining register **DM-Rel-2/4/5/6/7 carried as proposed, confirm-at-build** (the S4a pattern). **(5) Spec consistency:** §3.3 already lists "decide on relations" as a human action, so the explicit gate is spec-faithful — **no amendment needed now** (a precise-mechanism amendment may ride the build, like S4c amended §3.3). **(6) Retro (1 change, owner ran `/retro`):** the `decompose-requirement` + `review-architecture` skills instructed "read the matching template from `templates/`", but **no `proposal.md`/`review.md` template exists** (only adr/component/state-machine/glossary-term/project + the canonical `vault-note.md`); the section structure already lives in each skill's body. Fixed the wording in both `meta-architect/skills/*/SKILL.md` to point at `vault-note.md` for frontmatter and state that proposal/review structure is the skill-body's single home. *Considered & rejected:* adding the two templates (would duplicate the skill-body section list — a second source of truth that drifts). Second finding (review-architecture-at-resume not run on this decompose-opening session) → **note-only**: fresh evidence toward OQ-3's deferred ritual-wiring, no skill change (wiring stays deferred per ADR 0002).

- **2026-06-16 — Session 27 (M3.S4d build + security treadmill): two design calls, a multi-PR security detour, and a retro skill.** **(1) Handpick ranking — no score floor.** The `GET /stories/{id}/entities?q=` search ranks with the matcher's own RapidFuzz (`search_entities` reuses a shared `_rank` that `top_alternatives` now delegates to — behaviour-preserving). The *planned* score floor was **dropped during the build** after measuring: the duplicates handpick exists to catch — "Kasia"↔"Katarzyna" (43), "Bronek"↔"Bronisław" (53) — score *below* unrelated cross-category noise ("Warszawa"↔"Wars" 67), so any floor that cuts noise also hides the target. Resolution: rank, cap at top-N, keep low matches reachable; the human (INV-1) picks. *Considered & rejected:* a floor (re-introduces the matcher's blind spot); substring/Cypher `CONTAINS` (worse for diminutives + would put `q` in Cypher). **(2) Handpick precedence — last-pick-wins.** `mergeTarget = handpicked ?? pickedAlternative` gave the handpick *static* precedence, so after handpicking you couldn't switch back to a top-3 alternative (`/code-review` caught it). Fixed: picking an alternative clears the handpick (+ its active highlight), so the most recent choice wins. **(3) Security treadmill (owner-decided approach).** On opening PR #70, CI surfaced two reds beyond the known vite #68 — a backend **OSV SCA** wave (new advisories against pinned `starlette`/`python-multipart`) and, after fixing it, an **unmasked** Trivy **neo4j** netty HIGH (CVE-2026-50010, sequential-unmask). Owner chose **soaked bumps + fix-first time-boxed waivers**: `starlette` 1.0.1→1.2.0 + `python-multipart` 0.0.27→0.0.30 + `vite` 8.0.10→8.0.16 (all soaked) clear 3 of 4 SCA HIGHs and the vite HIGH; residuals whose fixes are still in soak are waived with `ignoreUntil` + reachability (127.0.0.1 single-user, no public TLS → the DoS/MITM CVEs aren't remotely reachable) + dated drop-when (tracked in cross-cutting). Merged as PRs #72 (SCA + neo4j), #73 (vite); #68/#71 closed. *Considered & rejected:* wait ~11 days for the cleanest fixes to soak (blocks the feature); scope the pre-push age-hook (a non-security micro-opt — the local hook checks pin *age*, not CVEs; CI is the real gate). **(4) Retro — `/triage-advisory` skill + `/resume-session` step 3b.** The genuine security risk surfaced is a **forgotten waiver** (a known-vulnerable dep shipping behind a green board). New `/triage-advisory` skill owns the advisory lifecycle (enumerate gates + waivers → fix-first via `/add-dependency`/`/pin-image`, else time-boxed waiver, **and the drop-revisit** that removes a waiver when its fix lands); `/resume-session` gained a cheap **date-only waiver-expiry scan** (step 3b) so expiring waivers surface at session start, with the `ignoreUntil` CI re-red as backstop. Root `AGENTS.md` security baseline points at it. *Considered & rejected:* a settings.json SessionStart hook (heavier infra; resume is already the mandatory ritual); a new treadmill *rule* (the knowledge already lives in `/review-pr` §5/§8 + the waiver files).

- **2026-06-15 — Session 26 (M3.S4c build): ADR 0005 declined; a plain-language communication rule added.** Two non-obvious calls made during the build. **(1) ADR 0005 declined (owner).** S4c introduces the project's first *automated* writer that mutates a *staged proposal* after staging (re-match flips `new → merge` in the `candidates` table). The proposal had deferred to the owner whether that shift is ADR-worthy. Decided **no separate ADR**: it is a contained change — staging-only, never the graph, so **INV-9 holds** (its line is graph-vs-staging) and INV-1 holds (the human still commits every merge) — and the full reasoning already lives in the accepted proposal + spec §3.3. The rationale is recorded in this plan + a one-line **INV-9 graph-vs-staging clarification** in `architecture/invariants.md` + the `candidate-lifecycle` self-loop, rather than a new decision file. *Considered & rejected:* author ADR 0005 (more ceremony than a staging-only, already-documented change earns). **(2) Communication rule (retro).** Added a bullet to root `AGENTS.md` "How to communicate with me": **lead with plain language when asking the owner to decide** — state the actual tradeoff a non-specialist would follow and unpack any internal shorthand (ADR, `INV-N`, `DM-S4c-N`, slugs) rather than assuming it's shared, keeping the precise terms for the durable written record. Earned by a real correction this session (the first ADR-0005 question was too jargon-dense; the owner asked for "less lingo" — not because they're a beginner, but because dense shorthand packed together trips them up). *Considered & rejected:* treat it as a one-off (the tendency is general and owner-facing, so a durable rule fits).

- **2026-06-15 — Session 25 (M3.S4c decompose): intra-batch dedup designed; spec §3.3 amended.** The S4b browser walk made a real gap concrete — a single first-pass extraction of the sample story staged `Janek` ×3 (and `Marta`/`Konrad`/`Order` ×2) as independent NEW proposals → after accept, **duplicate Neo4j nodes the review queue could not merge**, undercutting §9 M3's "the graph is clean". The cascade only dedupes *cross-pass* (against the accepted graph at stage time); this is the *intra-batch* gap M3.S4a's own "but what if" had named and deferred. **`meta-architect:decompose-requirement`** → `architecture/proposals/m3s4c-intra-batch-rematch.md` (+ glossary `intra-batch-dedup`, OQ-18, learning-log ×2). Two additive mechanisms on S4a/S4b, **neither touching the graph** (INV-1/INV-9 *hold* — re-match is the project's first automated *staging-proposal* writer, and INV-9's line is graph-vs-staging): **(a) on-accept live re-match** (deterministic Stage 1/2 over still-pending candidates each accept → dupes flip `new → merge`; backend-only, no LLM) + **(b) manual handpick** (entity-search endpoint + picker for matcher false negatives). **Register resolved (owner):** ~~DM-S4c-1~~ split **S4c** (re-match, backend-only) + **S4d** (handpick); ~~DM-S4c-2~~ synchronous-in-accept + incremental; ~~DM-S4c-3~~ auto-flip on Stage 1 `>85%` **OR** Stage 2 `cosine >0.85` (*owner chose +Stage-2 over my proposed Stage-1-only* — catches more semantic matches; still **no live judge**), ~~DM-S4c-4~~ **monotone** (`new → merge` only) → idempotent, kept as transition guard not a new INV-10; ~~DM-S4c-5~~ handpick **project-scoped** (*considered & rejected:* cross-project/world — crosses §6.4, needs the M4 world-graph) — supersedes the deferred arbitrary-search item; ~~DM-S4c-6~~ `GET /stories/{id}/entities?q=` via `accepted_entity_reader`. *Considered & rejected:* one combined S4c+S4d session (too big); relation-write before S4d (~~deferred~~ kept deferred — but its priority rises, since merges now orphan relations). **Spec §3.3 amended** (on-accept re-match + manual-handpick clarifications + the deterministic-only cost note) via the stop-and-amend flow; vault + plan reconciled. **No code, no ADR yet** — ADR 0005 (if warranted) authors test-first with the S4c code. First S4c code = the failing re-match flip test.

- **2026-06-15 — Session 24 (M3.S4a): intercept-before-write shipped + merged (PR #63).** The M3 milestone-defining flip, test-first. **(1) The flip** — `ExtractionCoordinator` no longer writes the graph: it *stages* each candidate into a new Postgres `candidates` table with the §3.3 cascade's proposal (embed-on-extract → Matching S1/S2 → Judge S3, fail-closed), writing **zero** Neo4j nodes. The graph is written **only** by the human-accept path (`CandidateReviewService` → `POST …/candidates/{id}/accept|reject`). The flip is witnessed by the rewritten coordinator unit test + an integration test ("extract → 0 nodes, accept → 1 node"), replacing M2.S4's two-identical-→-two-nodes test. **(2) Invariants** — **INV-8 retired** (superseded by INV-1), **INV-1's first enforcer landed** (the accept handler), **INV-9 added** ("no automated stage writes the graph" — the coordinator holds no graph writer); `architecture/invariants.md` + `candidate-lifecycle.md` (→ `living`) + `overview`/`open-questions`/`glossary`/`INDEX` reconciled; **ADR 0004** authored. **(3) Design** — `create_entity` is now an idempotent MERGE-on-id (+ `add_alias` for accept-merge), so the accept path (Neo4j → mention(+vector) → evidence → **status-flip-last**) with **deterministic accept-path ids** is idempotent on a crash-before-flip retry; merge re-validates the target (TOCTOU → 409). New tables `candidates`/`candidate_decisions`/`paragraph_processed` (resume marker, fixes M2's zero-candidate reprocess wart); `PostgresCandidateStore`, `AcceptedEntityReader` (accepted-graph snapshot read once/run, the C4 mitigation). **(4) Scope** — relations are *staged* (JSONB on the marker) but the edge write + re-point-on-merge is **deferred to S4b** (a relation needs both endpoints accepted); **INV-2 consent gate deferred past M3**; §10 q8 (bilingual peering) surfaced as deferred (accept-create reuses the M2 provisional rule). Register DM-S4a-1..5 resolved. **(5) Cross-cutting folded:** store-down→503 (psycopg `OperationalError` + neo4j `ServiceUnavailable`) + a FastAPI lifespan closing the Neo4j driver. **(6) JudgeAgent hardened** (the `/code-review` catch): its give-up contract is now *total* — a terminal transport/envelope failure → `JudgeError`, so a judge outage fail-closes the candidate to the human instead of 502'ing the batch. **(7) Reviews:** `/review-pr` + multi-agent `/code-review` both ran and folded (judge fail-closed gap, idempotent re-accept `entity_id`, DM-rej docstring overclaim, dead-code removal); the GitHub Codex secondary was usage-limited (best-effort, not blocked on). 287 backend + 51 frontend tests pass; ruff/format/mypy/tsc clean; CI green 4/4; squash-merged `4c607cf`. **Retro (2 changes, approved):** (a) *"regenerating the typed API client does not reconcile its consumers — on a backend route shape change, grep `frontend/src` for the old + new field names"* → `frontend/src/lib/api/AGENTS.md` + `/review-pr` §2 (a renamed `ExtractResponse` field left 3 frontend test fixtures red in CI, not review); (b) *"make an agent's give-up error total (wrap transport/envelope → give-up) so a consumer can fail-close on one exception type"* → `backend/src/story_forge/AGENTS.md` (Agents) + `/review-pr` §1. *Considered & rejected:* note-only for both (each miss was concrete and the class is general). **Vault derived notes** (INDEX priority queue item 13) flagged for the next `meta-architect:review-architecture` re-sync.

- **2026-06-15 — Session 23 (governance-only, no feature code): M3.S4 re-sliced → S4a + S4b; DM7/DM-rej + the M3.S4a build register resolved; vault reconciled.** A pre-code milestone-boundary session — two `meta-architect` skills + the owner decisions that gate the milestone close. **(1) `meta-architect:review-architecture`** (`architecture/reports/2026-06-15-architecture-review.md`, no blockers) cleared the Session-22-flagged vault drift: DM5 folded to ✅ (PR #60), `task_type` label `"judging"`→`"judge"`, the §3.3 confident-yes merge-rule shorthand, and the `overview.md` snapshot moved to "M3.S1–S3 built, proposal-only/unwired, INV-8 still live". **(2) M3.S4 re-sliced into S4a (backend) + S4b (frontend UI)** — the invariant flip is witnessed at the **backend/API boundary** (a test asserts extraction writes zero Neo4j nodes; the accept endpoint writes one), so S4a is a coherent green slice with **no empty-graph window** and S4b is purely additive. *Considered & rejected:* keep M3.S4 whole (too big for one conversation — risks a half-flipped `main`); a three-way split (awkwardly separates the INV flip from the write-path it depends on). **(3) DM7 → INV-2 consent gate DEFERRED past M3** — persona-justified (single local user, full trust); the cascade wires with no consent prompt; the keyboard scheme (§10 q9) becomes an S4b-time pick. Rationale: INV-2 had already slipped its landing target three times (M2.S2→M2.S5→M3); the fix for an indefinitely-deferred guard is an *explicit dated deferral with a rationale*, not a fourth re-point. **(4) DM-rej → remember rejections** — the `rejected` terminal edge writes a `candidate_decisions` evidence row the matcher consults before re-queueing (a default suppression, not a hard ban). **(5) `meta-architect:decompose-requirement` on M3.S4a** (`architecture/proposals/m3s4a-intercept-write-path.md`) + its register **DM-S4a-1..5 resolved by the owner:** a new Postgres **`candidates`** staging table (DM-S4a-1); **add INV-9** "no automated stage writes the graph" (DM-S4a-2); resume checkpoint = "candidates staged" + a zero-candidate marker, since `entity_mentions` (the M2.S4 checkpoint) move to accept-time (DM-S4a-3); a focused append-only **`candidate_decisions`** evidence table now, **deferring** the full §4.2 `edit_history` text-edit dataset to the editing milestone (DM-S4a-4, *considered & rejected:* build §4.2 now — premature + shape-mismatch for a graph decision; status-on-`candidates`-only — too weak for INV-3 reversibility); no retention at PoC (DM-S4a-5). **ADR 0004** (DM6 intercept-before-write, fuller MADR form) to author **test-first with the S4a code**, stating the accepted costs (graph empty until review; ungated Stage-3 egress under the INV-2 deferral). **Retro:** no skill/`CLAUDE.md` change earned its place; the one process data-point — *run `review-architecture` at `/resume-session` (not `/wrap-session`) when a session opens with a decompose* — was folded into the vault's ritual-wiring placement-evidence note (`open-questions.md`), sharpening the ADR-0002 deferred-integration evidence. No code touched (green-state code gates N/A). The wrap is the only commit.

- **2026-06-15 — Session 22 (M3.S3): Stage 3 `JudgeAgent` (LLM-as-judge) shipped + merged (PR #60), proposal-only; DM5 resolved; rule-of-three folded; §3.3 amended.** Test-first. **(1) JudgeAgent** — router-wired like `ExtractionAgent` (`complete(weight="medium", task_type="judge")` → cloud_free per §6.5); strict §3.3 verdict schema `{match: bool, confidence: float∈[0,1], reasoning: non-empty str}` (a parseable-but-degenerate body — out-of-range confidence, blank reasoning — **fails and retries**, never passes); `classify_verdict` proposes a merge **iff `match AND confidence > 0.8`** (strict edge mirrors Stage 1/2; **fail-closed — a confident NO never auto-merges**). Proposal-only/**unwired** — INV-8 still live; the cascade coordinator that maps a `JudgeError` give-up to "uncertain → review queue" lands M3.S4. **(2) DM5 resolved** — judge tier = cloud_free via `weight="medium"`/`task_type="judge"` (spec §6.5-settled; *confirmed*, not re-decided). **(3) §3.3 amended** (stop-and-amend, owner-approved): a Stage-3 merge-rule clarification — `confidence` is the judge's certainty the two are the *same*, so merge needs `match=true AND confidence > 0.8`; the box's "confidence > 0.8 → merge" is the confident-yes shorthand; the cascade stays fail-closed (no automated stage commits). **(4) Prompt fidelity — the multi-agent `/code-review` catch:** the prompt was first built from §3.3 *prose* and silently dropped **Appendix C.3**'s existing-entity `aliases` (the diminutive signal Stage 3 exists to use) + candidate `type`/`properties`; the single `/review-pr` missed it, `/code-review` caught it; folded to match C.3 in full (both PL/EN), with tests asserting every field reaches the model. `match_stage3_confidence` (default 0.8) joined `config.py` **with its reader** (no dead config). **(5) Rule-of-three fold:** `agents/validation.py` `validate_with_retry` — the shared `render → call → validate → retry-on-ValidationError → give-up` loop; `ChunkingAgent` + `ExtractionAgent` refactored onto it (each keeps its own schema/call/error). `TaskWeight` + a `Router` Protocol promoted into `adapters/llm/base.py` from `extraction_agent`'s local mirror. Cross-cutting (a) **resolved**; (b) the envelope-unwrap dup stays open (trigger: first direct vendor adapter). **(6) Reviews:** `/review-pr` + `/code-review` both ran; the GitHub Codex secondary didn't (usage-limited) — best-effort, not blocked on. 202 unit tests pass; ruff/format/mypy clean; CI green 4/4; squash-merged `88efb7b`. **Retro (1 change, approved):** encoded *"build an agent prompt from the spec's Appendix C template (the complete field set), cross-check §3 — not the §3 prose alone"* into `backend/src/story_forge/AGENTS.md` (Prompts) **+** a `/review-pr` §2 mirror — the C.3 omission is a building-time foot-gun that recurs for every future agent prompt (the V2 editing agents each have a C-template too). *Considered & rejected:* note-only (the miss was concrete and the class is general). **Vault drift flagged** (DM5 resolved / `task_type="judging"`→`"judge"` / the merge-rule clarification) for the next `meta-architect:review-architecture` — the vault is writer-restricted to those skills, so not hand-edited.
- **2026-06-12 — Session 21 (M3.S2): Stage 2 embedding-cosine matcher + the pgvector foundation shipped (PR #58), foundation-only.** Test-first, decisions DM2/DM3/DM4 (resolved S20) now implemented. **(1) Foundation-only scope (the session's key call):** EmbeddingAgent is built + unit-tested but **not wired into the live extraction coordinator** — mentions are recorded with `embedding=None`, and the embed-on-extract wiring lands **M3.S4** alongside the cascade + the DM6 write-path refactor. *Rationale:* wiring it now would pull the optional ~2 GB torch stack into the live path + CI and add CPU-slow per-mention compute — a behaviour change in a session where INV-8 ("no dedupe / no behaviour change") is still live; it also mirrors the established precedent that the other heavy optional NLP stack (spaCy/PreNER) is built-but-unwired (`extraction_agent.py` passes PreNER hints as empty lists). *Rejected:* wire-now-with-graceful-degradation (adds a conditional heavy dep + a degradation branch to the working M2 extraction path). **(2) Deps:** `pgvector==0.4.2` (core); optional `embeddings` group (`sentence-transformers==5.5.1`, `torch==2.12.0`, `transformers==5.9.0`, `huggingface-hub==1.17.0`), ~2 GB, CI-skippable like the spaCy `models` group. `torch`'s `GHSA-rrmf-rvhw-rf47` (CVE-2025-3000, **MEDIUM CVSS 5.3** — the GHSA DB field says LOW but the scanner buckets it MEDIUM; aligned the docs to what the gate reports) is waived in the scoped `infra/osv/` register: `torch.jit.script` memory corruption, unreachable here (inference-only `encode`), and **no fixed version exists** (affects all ≤2.12.0) — unfixable-by-pin + unreachable, the two conditions a waiver requires. **(3) Model pin (§6.7 HF-channel):** `paraphrase-multilingual-mpnet-base-v2` pinned by commit SHA `4328cf26…` (commit dated 2025-08-19, soak met) via `scripts/pin_hf_model.py`'s `snapshot_download`; the script imports `MODEL_REPO_ID`/`MODEL_REVISION` from `EmbeddingAgent` so the pin script and runtime loader can't drift. Verify-at-build passed: loads on CPU, emits 768 dims. **(4) Persistence:** `entity_mentions.embedding vector(768)` migration (`down_revision` was the *real* head `2b5e854961e1`, not the handoff's stale `d721987b4168` — the latency migration had moved it); `register_vector_async` centralised in a new `db.connect` helper routed through all three connect sites; reads coerce the returned numpy array → `list[float]` via Pydantic; writes wrap `pgvector.Vector(...)` (the dumper isn't registered for bare lists). **(5) Stage 2:** `MatchingAgent.stage2` = max cosine of a candidate's context vector vs an entity's mention vectors, **strict `>0.85`** (mirrors Stage 1's strict upper edge); `match_stage2_cosine_merge` joined `config.py` with its reader. Pure `cosine_similarity` CI-tested without the model. **(6) Reviews folded:** `/review-pr` caught a flawed `stage2` sentinel (`-1.0` → `float("-inf")`, so an entity at cosine −1.0 is still selected; + regression test); `/code-review` caught a connection leak in `db.connect` (close the conn if `register_vector_async` raises). **Retro:** generalised the `backend/AGENTS.md` detect-secrets note from Alembic-revision-ids-only to **any high-entropy hex literal** (incl. a pinned model/commit SHA, which tripped the hook this session).
- **2026-06-11 — Session 20 (M3.S1): Stage 1 RapidFuzz matcher shipped (PR #56); M3.S1 re-sliced to Stage 1 only; register DM1–DM4 resolved; §6.7 HF-model channel added.** First M3 code, test-first. **(1) Stage 1 `MatchingAgent`** — deterministic RapidFuzz token-set ratio of a candidate surface form vs existing entities' `canonical_name` + aliases, routing to auto-merge-proposed (`>85`) / ambiguous (`60–85` → Stage 2) / new-proposed (`<60`) per §3.3; proposes only (INV-1 honoured by construction; unwired, INV-8 untouched). Pure `classify` band logic CI-tested without RapidFuzz; RapidFuzz imported directly (the deterministic-local-compute exception, like `PreNERAgent`/spaCy). `rapidfuzz==3.14.5` pinned. **(2) DM1** — the §3.3 thresholds get one config home (`config.py`, pydantic-settings, spec defaults, env-overridable, global; only Stage 1's land now, Stage 2/3 join with their readers — no dead config). **(3) DM2/DM3/DM4 resolved:** embedding model `paraphrase-multilingual-mpnet-base-v2` (768-dim — **verified** SHA `4328cf26…`); per-mention vectors on `entity_mentions`, Stage 2 = **max** cosine; storage co-located with the mention that already keys Neo4j↔Postgres. **(4) §6.7 amended** — a new **HuggingFace-model pinning channel** (pin the model by immutable HF commit-revision SHA via `snapshot_download` in an optional group, library stack as ordinary PyPI pins; soak verified procedurally against the commit date; OSV N/A for weights) — mirrors the spaCy GitHub-wheel channel. Policy defined now; the concrete pin lands M3.S2. **(5) Re-slice (owner):** M3.S1 = **Stage 1 only**; Stage 2 + the DM6 write-path refactor → M3.S2+. *Rationale, from new findings:* `torch` carries a fail-on-any (LOW, `jit.script` unused, no-fix) advisory needing a scoped OSV waiver, the embedding stack is ~2 GB, and the DM6 refactor *retires INV-8* (graph empty until the S3/S4 queue) — bundling would bury a security waiver + a behaviour change in one large diff. *Rejected:* one big M3.S1 PR (as originally planned). **(6) Self-review caught a real bug** — `classify` used `score >= 85` for merge, but §3.3 writes merge as strictly `>85` with 85 in the 60–85 Stage-2 band; fixed to `>` (the more fail-closed branch) + corrected the test that had encoded the wrong boundary. **Retro:** no rule change — the session's friction ("stalling": over-analysis before producing verifiable output) is an application of existing guidance ("when you have enough information to act, act"), not a gap.
- **2026-06-11 — Session 19 (M2→M3 roll gates): M3 cascade decompose step-0 + DM6 decided (intercept-before-write).** Both owed roll gates ran before any M3 code. **(1) Cross-cutting curation** — the list re-scoped M2→M3 by hand (`docs/AGENTS.md` §3); items that bite M3 noted (embedding read path, agent-dup rule-of-three, store-down→503, graph scoping, `last_call()` tiebreaker, §6.5 dropdown); the stale "lands with M2.S5" pointer on store-down re-pointed to M3. **(2) Architecture catch-up** — `meta-architect:review-architecture` (`architecture/reports/2026-06-11-architecture-review.md`: no blockers; headline `risk` = INV-2's consent gate lost its M2.S5 landing target and the M2.S6 smoke fired real paid egress gate-less — accepted at PoC scale, re-pointed to the M3 review-queue UI) + `meta-architect:decompose-requirement` step-0 (`architecture/proposals/m3-cascade-matching.md`, register OPEN; the vault's first state machine `architecture/state-machines/candidate-lifecycle.md`). Vault reflection of the roll decisions folded (OQ-14 dropdown/INV-7, OQ-15 logging/INV-6-vacuity, OQ-16 the M3 register) — discharges the tracked cross-cutting item. **(3) DM6 decided (owner):** the M3 cascade **gates the graph write — (A) intercept-before-write**. Extraction will *stage* candidates (Postgres); Neo4j is written **only** on human accept in the Stage-4 queue. This **retires INV-8** (no-dedupe scaffolding) rather than layering on it, and lands **INV-1's** first enforcer; cost accepted = M2.S4's CREATE-on-extract write path is refactored, and the graph is empty until review (vs M2's deliberate duplicate-filled graph). *Rejected:* (B) write-then-dedupe — a dirty graph until review contradicts INV-1. **Deferred to the M3-code session (test-first):** ADR 0004 (this pipeline decision) + the actual INV-8→INV-1 fold in `architecture/invariants.md` + finalising the candidate-lifecycle note — the invariant flip is witnessed by the failing test, not asserted ahead of it. Register items DM1–DM5, DM7, DM-rej stay OPEN (embedding model, vector storage, thresholds home, review-queue UX, rejected-candidate memory).
- **2026-06-11 — M2→M3 roll (thin S6): model-override dropdown deferred; observability-logging need recorded.** Resuming on `main` after Session 17 (M2.S5 merged). **(1) Roll decision (owner):** run a **thin S6** — real-provider smoke + §6.7 key-leak verification + smoke-procedure doc — to close M2, then roll to M3. **(2) The §6.5 "which provider/model for this task" dropdown is deferred — it is a feature, not thin polish.** The §8.5 panel already does the *surfacing* half ("which tier/model ran, and why"); the remaining half is a real **user model-override control + persisted per-task-type preferences**, which collides with INV-7 (routing is currently *system-derived, never caller-asserted* — that near-miss was deliberately closed in M2.S2). Building it needs an INV-7/spec reconciliation ("system-derived *unless* the user explicitly overrides"), router plumbing for the override + its interaction with weight-tiering and failover, and a preferences store. *Owner call: not in a thin session* — no rush given free Ollama Cloud credits cover real runs. **Lands as its own scoped piece** (a `meta-architect:decompose-requirement` step-0, alongside or after M3); carried in cross-cutting. *Rejected:* surfacing-only (the panel already covers it — near-zero value); cram-into-thin-S6 (rushes an invariant-touching feature). **(3) Triplet→doublet:** with no direct vendor adapters shipping (OpenRouter is the only paid route — ADR 0003), the real smoke exercises **Ollama Cloud (cloud_free) + OpenRouter free model (cloud_strong)** — both adapters, both tiers; an Anthropic-direct leg can't exist (that adapter intentionally isn't built). **(4) Observability/operational logging recorded as a later need** (`docs/PLAN_LONG.md`): the backend emits no operational logs today, so §6.7 "keys never logged" is currently *vacuous*. Noted that training data comes from the `llm_calls` ledger + planned `edit_history` (the data layer), **not** stdout logs — a common conflation — and that the S6 leak-check becomes the redaction regression guard the moment logging lands.
- **2026-06-11 — Session 17 (M2.S5 step 0): OQ-9 (latency) resolved → option (a), record `latency_ms`.** The §8.5 panel promises a *latency* column and INV-5 + the m2s2 proposal both said a usage row records latency, but the as-built `llm_calls`/`LlmCallRecord` and spec §6.6's enumeration recorded none — a three-home source-of-truth conflict. **Chosen:** add `latency_ms` (elapsed time around `provider.complete`, system-derived; recorded for every dispatched call, null only for a pre-dispatch budget refusal that never reached a provider — external PR-#51 review fold) — a one-column Alembic migration on `llm_calls` + capture in the router; the §8.5 panel shows it. *Rejected:* option (b) trim the panel/INV-5 to match as-built — the spec §8.5 panel already promises latency, so trimming would shrink a stated portfolio-visible feature to dodge a one-column migration. **Reconciled this pass:** spec §6.6 enumeration amended; `architecture/invariants.md` INV-5 latency caveat + `architecture/open-questions.md` OQ-9 struck-resolved; `architecture/INDEX.md` watch line updated. (Stop-and-amend flow; OQ-9 had been carried as "decide before M2.S5".)
- **2026-06-10 — Session 16: M2.S4 completed + merged (PR #48); OQ-1/OQ-2 resolved; CVE-rot waived across 3 images; 2 retro rules.** The wiring half, test-first, squash-merged green. **(1) OQ-1 (two-store write consistency) — option (c):** write **Neo4j-then-Postgres** and **accept eventual inconsistency** at PoC (single user, reversible); no reconciliation/outbox. *Rejected:* a startup repair sweep (code for a rare local-crash window) and an outbox (over-engineered for the PoC). **(2) OQ-2 (resumable ingest) — option (a):** the **batch driver** (`ExtractionCoordinator`), not the agent, owns the budget/quota pause-and-ask; the single-paragraph agent keeps *propagating* it (M2.S3 D5). Resume keys off committed `entity_mentions`. *Rejected:* swallow-in-agent (couples batching into the fragment-agnostic agent) and no-recovery/re-upload (throws away a long story's work). **(3) Pause response = HTTP 202 + partial progress, re-POST resumes** (owner) — *not* a 402 error; budget/quota are caught by the driver and never reach the route as an error. Only hard failure (`ProviderResponseError`/`ExtractionError`/`httpx.HTTPError`) → 502. **(4) Checkpoint written last (external-review fold):** the `entity_mentions` row (the resume marker) is written **after** entities **and** relations, so a transient Neo4j relation-write failure can't leave a "done" paragraph with lost relations — the original entities→mention→relations order had that silent-data-loss bug, which is also why a new `/review-pr` "checkpoint-ordering" rule was added. **(5) `extraction_model` default = `gpt-oss:120b-cloud`** (cloud_free, heavier than chunking's 20b — extraction is richer reasoning); tunable via `.env`. **(6) CVE-rot waived, not fixed:** the neo4j CI service brought the security job into focus and the sequential-Trivy scan unmasked the same OpenSSL `PKCS7_verify` UAF (CVE-2026-45447) across neo4j→pgvector→ollama plus 2 new neo4j netty HIGHs — all unreachable on a 127.0.0.1 single-user deployment, all verified against the GitHub Advisory DB / OpenSSL advisory (the netty advisory's *second* vulnerable range was the one covering our 4.1.132 — the first range didn't). Pre-existing infra rot, not M2.S4 code; Issue #22 treadmill. **(7) Retro (2 rules, both approved):** `/review-pr` gained a **checkpoint/resume-marker ordering** check (a marker written mid-sequence lets a later step fail over a "done" record — enumerate *each* inter-write window; the P1 bug *both* my review passes missed and external review caught) and a **multi-range CVE-advisory** check (confirm which of an advisory's several vulnerable ranges your version falls in). *Considered & rejected:* note-only (both trace to concrete this-session misses and generalize).
- **2026-06-09 — Session 15: M2.S4 foundation built (unmerged branch); two latent infra bugs found by running it; 4 retro changes.** A build session; the foundation is on `feat/m2s4-neo4j-writes`, **not merged** (half-feature + the integration test would be dead at the CI gate without a Neo4j service — merging it would read as "covered" when it isn't). **(1) Driver: `neo4j==5.28.4`** — the **5.x LTS** driver line to match the 5.26-community server (same-major = lowest compatibility risk). *Rejected:* the newer 6.x line (targets the 2025.x servers we don't run). **(2) Graph modelling:** `domain/graph.py` `GraphEntity`/`GraphRelation` are the *persisted* shapes, **distinct from the agent's `*Candidate`** (candidate has a surface `candidate_name`, no id/canonical/embedding — the M2→M3 boundary). The adapter uses **`CREATE`, never `MERGE`** (the load-bearing keyword for INV-8 no-dedupe — a `MERGE`-on-name would silently violate it); the open-world relation `type` is **backtick-escaped** when interpolated (Cypher-injection-safe — the analogue of prompt-injection hardening, since the type is LLM-produced); `properties` is stored as a **JSON string** (Neo4j can't hold a nested map), `aliases` as a native list. *Rejected:* a global `MATCH (n) DETACH DELETE n` test cleanup (unsafe on a shared instance) → per-test `project_id` scoping instead. **(3) `entity_mentions` migration:** `paragraph_id` is a real FK (cascades with the tree); **`entity_id` carries NO Postgres FK** — it references a Neo4j node, the OQ-1 cross-store seam; `span_start`/`span_end`/`confidence` nullable (the LLM path yields an evidence quote, not reliable offsets). **(4) Two latent infra bugs, found only by *running* it:** the compose Neo4j had **crash-looped since M0** (`NEO4J_server_metrics_enabled` is Enterprise-only; Community rejects it under strict validation — nothing had ever booted it, since the security job only Trivy-*scans* the image) → removed; and **CI has no Neo4j service** (the plan's claim was false) → deferred to Session 16. **(5) §6.7 Neo4j telemetry:** disabled `client.allow_telemetry` + `dbms.usage_report.enabled`, the two default-on channels, **verified against `SHOW SETTINGS`** on the live 5.26 server rather than guessing the setting names (guessing wrong re-triggers the crash). **(6) Retro (4 changes, all approved):** sharpened **both** `/resume-session` (verify infra *runs* — boot the service / grep `ci.yml` — not just that config exists) **and** `/wrap-session` (don't *write* an unverified infra anchor into the handoff — the authoring-side fix, since both false claims were born there), after the handoff twice asserted false infra state this session; corrected the `backend/CLAUDE.md` `down_revision` pragma note (both revision lines can trip detect-secrets — pragma both); added a `backend/CLAUDE.md` note that a TDD integration test for a new module must import cleanly (or it breaks the fast-tier *collection*, not just the integration run). *Considered & rejected:* note-only on the infra-claim pattern (it recurred **twice this session** — clears the bar easily).
- **2026-06-08 — Session 14: backend SCA gate built + merged (PR #44); client DevSecOps notes folded; retro adds verify-at-build rule.** **(1) Scanner pinned by digest, not an Action (deviation from proposal D6, intent preserved):** `google/osv-scanner-action`'s root `action.yml` is a no-`runs:` metadata stub and its reusable workflows do a PR *diff* scan (misses an advisory already on `main`) or a SARIF-upload recursive scan — neither a scoped fail-on-any lockfile *step*. Built the gate as the **digest-pinned `ghcr.io/google/osv-scanner` container** (`scan source -L uv.lock --config=…`), which is *stronger* than the planned SHA-pin (a tag/digest can't move). D6's intent (pin the scanner immutably + soak it) held; the mechanism changed. *Considered & rejected:* the reusable workflow (wrong semantics/heavier), a hand-installed pinned binary (more YAML, weaker than a digest). **(2) Fail-on-any, scoped waivers in a sibling `infra/osv/`** (`osv-scanner.toml` enforced + `WAIVERS.md` register, condition-based expiry) — mirrors the Trivy split; no active waivers (green by *fixing* starlette, not suppressing). **(3) Client DevSecOps notes → `docs/PLAN_LONG.md`** new "Security & DevSecOps hardening — later" section: the *research-takes-precedence-over-feature-work* principle + two not-scheduled items (adopt Anthropic's `security-review` as a standing gate; an automated architecture-conformance CI check from the vault invariants). *Considered & rejected:* a self-hosted Gitea+runners CI (out of scope for a public GitHub portfolio; the client said skip it). **(4) Retro:** added a **verify-at-build** rule to `meta-architect:decompose-requirement` — a register decision resting on un-inspected external-tool behaviour (an Action's interface, a CLI's lockfile support, an API shape) is a *hypothesis* tagged `verify-at-build`, not settled, so a plan-first builder doesn't trust it blindly. *Considered & rejected:* note-only (the class recurred in spirit — both "SHA-pin the Action" and "parses uv.lock natively" needed build-time confirmation). *Worked well (no change):* my own `/review-pr` §2 grep sweep caught a stale acceptance-banner home my first reconciliation pass missed — the rule earned its keep.
- **2026-06-08 — Session 13: M2.S3 ExtractionAgent built + merged (PR #42); SCA decompose accepted; retro folds field-validator symmetry.** **(1) M2.S3 built test-first + merged (PR #42, green):** `ExtractionAgent` (first `LLMRouter` consumer) + `prompts/extraction.{pl,en}.j2` + `EntityCandidate`/`RelationCandidate`/`ExtractionProposal` schemas + the OQ-10 typed `ProviderResponseError` (both adapters raise on a malformed-`200` envelope; router records + fails over like a 5xx). Shared `extract_json` extracted to `agents/json_output.py`. **(2) Reviews — the layered gate earned its keep:** `/review-pr` (primary) caught typing/nits; **`/code-review` (the 2nd gate) caught a real bug `/review-pr` missed** — a `content: null` 200 envelope crashed the router with an uncaught `ValidationError` (no ledger row, no failover); folded as a null-content guard. Also folded: `_Router` protocol `weight` → `Literal` (would've broken M2.S4 wiring), **dropped the dead `prener_hints` param** (D3 intent minus the speculative surface), hardened `extract_json` for inline fences, blank `evidence_quote` → `None`. **(3) External review (post-merge) caught a gap both my passes missed:** blank `subject`/`predicate`/`object` validate as success (only `candidate_name` had a non-empty validator) → tracked cross-cutting (fix in M2.S4 or a quick PR). **(4) SCA decompose accepted** (`meta-architect:decompose-requirement`, triggered by a `starlette` 1.0.0 Dependabot alert CI didn't catch — Trivy scans only images, nothing scans `uv.lock`): owner approved the G1–G7 cluster — **osv-scanner**, **fail-on-any + scoped waivers**, SHA-pinned Action, reuse the Trivy waiver split (`infra/osv/`), `npm audit` left HIGH/CRITICAL, **§6.7 baseline control (no new INV)**, explicit `starlette==1.0.1` pin. `architecture/proposals/backend-dependency-advisory-scan.md` now `accepted`; **build deferred to next session (one branch); the spec §6.7 amendment lands *with* the build** (else it claims a gate CI doesn't run). Glossary +2 (`software-composition-analysis`, `defense-in-depth`). **(5) Retro:** folded **field-validator symmetry** into `/review-pr` §4 (when a schema validates one field for an invariant, audit every sibling field with the same semantic — the blank-relation gap slipped both my passes). *Considered & rejected:* note-only (the class is concrete + recurred this session — the null-content and blank-relation misses share the "degenerate-but-parseable passes as success" theme).
- **2026-06-08 — Session 12: M2.S3 design settled + spec §6.5 amended + review-model adapted + CVE wave-4 (PRs #39, #40).** A design/governance session, no production code. **(1) M2.S3 register resolved** (owner walked it after a by-hand `meta-architect:decompose-requirement` step 0 → accepted `architecture/proposals/m2s3-extraction-agent.md`): **per-paragraph** granularity (agent fragment-agnostic; spec §10 Q1 stays open); **single-paragraph agent**, resumable batch driver → M2.S4 (pause-and-ask propagates, never swallowed); **`candidate_name`** surface form, *not* `canonical_name` (D1 — canonical/PL-EN naming is M3); typed **`ProviderResponseError`** for malformed-`200` envelopes (D2/OQ-10 — failover-the-provider, distinct from schema-invalid→retry-the-prompt); **soft-flag `evidence_quote`** (drop quote, keep candidate — *rejected* hard-reject as punishing paraphrase); known-entities/custom-types as params now (Neo4j read wired M2.S4) / **PreNER hints deferred** (no speculative prompt complexity). **(2) Spec §6.5 router contract amended** (stop-and-amend): `route(self, task) -> LLMProvider` → `complete(messages, *, weight, task_type, json_schema=None) -> CompletionResult`; §6.5 + §11 "Failover" split envelope-malformed (→ failover) from schema-invalid (→ agent retry). Reconciled across README/plan/vault; ADR 0001 + M2.S2 history left intact. *Why:* the spec still described the pre-M2.S2 `route()` API the first router consumer would read. **(3) Review model adapted** (root `AGENTS.md` "Review model"): ChatGPT sub cancelled → Codex review is **secondary/best-effort, never block on it**; **`/review-pr` is the primary gate**; **`/code-review`** (multi-agent) added for substantive *code*; solo-review-blind-spot note for decision-state-flip PRs. *Considered & rejected:* ripping out all Codex references (over-reacting to a "we'll see"; §0 concurrent-agent rules kept as good hygiene pending confirmation). **(4) CVE-2026-42504 waived** (PR #40, wave 4 of Issue #22): Go-stdlib `mime.WordDecoder.DecodeHeader` DoS, compiled into pgvector-gosu + ollama; attribution verified vs NVD + pkg.go.dev/vuln/GO-2026-5038; reachability nil (127.0.0.1, trusted client); handled the pgvector→ollama sequential-unmask in one pass. **(5) Retro:** sharpened `/review-pr` §2 (same-PR proposal status-flip homes — INDEX row/next-steps + the plan task a decision changes) + the solo-review discipline note, after external review caught 3 §5b misses my self-review claimed clean. *Considered & rejected:* note-only/no-rule-change (the class recurred — #34 then #39 — so it earned the sharpening).
- **2026-06-02 — M2.S2 build: implementation-shape choices + the GPU-quota deferral (PR #36).** Confirmed while building the router/ledger (some with the owner mid-session, the rest folded from own-review + Codex on PR #36): **(1) The `LLMRouter` orchestrates `complete(weight, task_type)`** — agents call the router, not a provider; failover, the budget gate, and ledger recording have no other sensible home. (Rejected: the spec's literal `route()→LLMProvider`, which would scatter those cross-cutting concerns into every agent.) **(2) A `CostStore` Protocol** keeps the router unit-testable against an in-memory fake; the Postgres `llm_calls` impl is integration-tested separately. **(3) `PostgresCostStore` commits on its own short-lived connection per write** (not the request transaction) so a *failure* row survives a request that rolls back on that same failure — the §6.6 "explain why a batch stopped" trail must not vanish with the error it records. **(4) Provider 402 (out of credit) → `BudgetExceededError`** (same pause-and-ask type as our pre-dispatch cap); transport errors + 429/5xx fail over; 401 fails over but isn't quota; **only pure-429 cloud_free exhaustion → `QuotaExhaustedError`** (a bad key/outage/network failure surfaces the real error, not a false "quota spent" — Codex Risk fold-in). **(5) Schema/envelope-validation retry stays in the agent** (the ChunkingAgent pattern); the router owns transport + spend. The malformed-`200`-envelope record/failover gap is tracked as an M2.S3 cross-cutting follow-up. **(6) GPU-quota *remaining* deferred to M2.S5** (Codex Risk fold-in): `GET /llm/status` surfaces budget/spend/remaining-USD + GPU-seconds *consumed* + per-task breakdown now; the Ollama Cloud *remaining-quota* figure (spec §6.5/§6.6) needs their account API and lands with the M2.S5 panel — accepted deferral, consistent with ADR 0003's panel scope, marked in the endpoint docstring (no fabricated figure meanwhile). *Spec judged unchanged:* §6.5/§6.6 state the V1 goal "quota visible at all times"; this is sequencing its delivery, not contradicting it — so no spec amendment, only this recorded deferral.
- **2026-06-02 — Process (retro, Session 10): the decision-reconciliation discipline, encoded in three places.** The six-pass review saga on PR #34 was one recurring class — a *resolved decision left stale in another home* — and it cleared the retro bar easily (repeatable friction; it's also where my own `/review-pr` failed, rating the accepted-doc contradictions "banner covers it"). Three cross-referencing changes (all approved): **(1) `/review-pr` §2 — a "decision reconciliation" lens:** when a PR records/resolves a decision (flips a doc `proposed→accepted`, amends the spec, lands an ADR), verify by **exhaustive whole-repo grep, not the diff** — every home of the fact (spec's *other* sections, README, every `AGENTS.md`, plans, vault), the **tracking/registry/nav/status** notes that phrase it differently (ADR lists, priority queues, as-built snapshots, doc pointers), the **accepted artifact's whole body** (diagram/config/edge-cases, not just a banner), and **dated artifacts'** status; leave append-only history intact. Subsumes the Session-9-deferred vault lens (no-duplication + tense-honesty). **(2) Root `AGENTS.md` "Where knowledge lives" — the proactive authoring mirror:** reconcile a decision across every home in one pass, at authoring time, verified by sweep. **(3) Meta-architect `decompose-requirement` §5b — "reconcile the proposal to resolved state":** when the owner decides, bring the whole proposal (update-in-place) to resolved — register→Decided (incl. overrides), de-activate rejected options across diagram/config/edge-cases, strike the mirrored OQs, reconcile host-repo homes, grep-sweep. *Considered & rejected:* a standalone "resolve-a-decision" skill (a rule + the review check is enough — no new ritual earns its place); leaving it at `/review-pr` only (the authoring-side rule prevents the rework the review-side merely catches).
- **2026-06-02 — M2.S2 LLM-router decisions, settled via the meta-architect dogfood (ADR 0003; spec §6.5/§6.6 amended).** The architect's `review-architecture` + `decompose-requirement` passes (artefacts: `architecture/reports/2026-06-02-architecture-review.md`, `architecture/proposals/m2s2-llm-router-budget-cap.md`) framed the options; the owner resolved them. The cluster: **(1) Provider order = Ollama → OpenRouter → Grok → Anthropic → Google → OpenAI** — *not* Anthropic-first (overturns ADR 0001). OpenRouter leads the paid routes because one OpenAI-compatible endpoint reaches most vendor models. *Considered & rejected:* Anthropic/OpenAI-primary (ADR 0001's original). **(2) Hand-rolled `httpx` adapters, not vendor SDKs** (D2) — uniform shape, zero new deps, injectable-transport tests, visible swappability; cost = hand-written per-provider usage parsing. *Rejected:* 5 vendor SDKs (5 dependencies to vet/pin/soak, divergent shapes, buries the swappability). **(3) Build only `OllamaProvider` (extended) + `OpenRouterProvider` in M2.S2;** direct vendor adapters deferred to "as needed" (pulls OpenRouter forward from M2.S6). **(4) Control-first budget (G1):** on cloud_free-quota-exhaustion **or** daily-cap, the router **pauses and asks the user** — never silent paid escalation. Per-day USD hard cap, fail-closed (checked before dispatch). *Rejected:* auto-escalate-to-cheapest-paid-within-cap (owner chose control over unattended-batch convenience). **(5) `local_small` degrade target on a GPU-less host = cheapest cloud_free model** (resolves the live intra-spec contradiction §6.5 step-5-vs-hardware; OQ-3). **(6) INV-5 return-shape grows** — `CompletionResult`/Protocol carry usage; `OllamaProvider` stops discarding Ollama's `eval_count`/`prompt_eval_count` (OQ-7). One `llm_calls` table, nullable per tier (D4); tier/provider/model system-derived not caller-echoed (INV-7). **(7) Paid-egress consent gate deferred to M2.S5** (D5) — no sensitive data; documented at the egress point, not gated now (proposed temporary INV-9 dropped). **(8) Google/Gemini added** to the provider set. Recorded in **ADR 0003** (supersedes ADR 0001's provider-priority + quota-degradation consequences; three-tier model stands); spec §6.5/§6.6 amended; the architecture vault reconciled (OQ-3/6/7/8 struck, INV notes updated). *Process note:* this is the **first time the meta-architect produced design evidence that fed real product decisions** — treated as the dogfood evidence ADR 0002 §4 wants before any ritual-wiring (still deferred; the vault is now *navigable* from root `AGENTS.md` + `architecture/AGENTS.md`, which is awareness, not automation).
- **2026-06-02 — Session 9: meta-architect dogfood interlude — `architecture/` vault initialized + cross-agent hygiene (PRs #29/#30/#32, issue #31).** A cluster of linked decisions from the first live run of the plugin. **(1) The vault is committed** (not gitignored) — ADR 0002's default; it reads to an outsider as "we dogfooded our architect here," consistent with public-portfolio hygiene. *Considered & rejected:* gitignore (would hide the architecture work from the portfolio narrative). Landed as PR #30 (squash `bfbd37f`): `PROJECT` + `overview` (nine-layer system seed, grounded in the as-built present), `invariants` (8 named, INV-8 flagged temporary), `open-questions` (operator's review-then-strategize queue + 5 seed-pass gaps + a *reference* to spec §10), a 14-term cross-linked `glossary/`, plus `learning-log`/`CHANGELOG`/`INDEX` and empty dirs for later runs. **(2) The vault references, never copies, the spec/plans/code** — this was the run's core stress-test, and **two Codex review passes** sharpened it: no-duplication (the `overview` Data layer had restated the §6.4 schema, `cascade-matching` the §3.3 algorithm → both trimmed to the architectural *reading* + a §-reference) and tense-honesty (`PROJECT` Identity + `invariants` INV-2 described unbuilt extraction/router/consent features in present tense → reframed as-built-vs-planned, matching INV-1). My own `/review-pr` + a same-session fold of a "Built in M2.S2" overclaim came first; Codex's passes caught the rest. **(3) Codex = PR-review only; the boundary is documented both ways.** Codex runs on host-Windows over a UNC view of this WSL checkout (no WSL shell), so it reviews but does not edit Story Forge code; its own runtime notes (`.codex/RUNTIME_NOTES.md` + `AGENTS.md` §0) landed as PR #32 when they surfaced uncommitted in the shared tree — kept out of the vault PR to stay surgical. **(4) Concurrent-agent shared-tree rule** added to root `AGENTS.md` §0 (retro outcome): stage explicit paths (never `git add -A`); surface foreign uncommitted changes, never absorb or delete them. **(5) Deferred:** wiring the meta-architect skills into the rituals (ADR 0002) — now explicitly carrying the vault-review lens for `/review-pr` and **issue #31** (slug/filename carve-out for the uppercase notes). *Considered & rejected:* encoding the vault-review lens into `/review-pr` now (one data point; honors ADR 0002's integrate-after-living-with-it).
- **2026-06-02 — meta-architect plugin incubated in-repo + `AGENTS.md` made canonical + self-improving Codex review (PR #27, squash `f3fdeef`).** A cluster of decisions. **(1) Incubate the architect as in-repo dev tooling.** A domain-agnostic product architect — an agent persona + three skills (`initialize-project-architecture`, `decompose-requirement`, `review-architecture`) producing only Markdown design artefacts into an Obsidian vault, teaching architecture vocabulary, never writing production code. It is **dev tooling, not a product feature** → deliberately *not* in `story-forge-poc-spec.md`; lives as a self-contained `meta-architect/` directory so extraction later = moving the folder. Wired via committed `.claude/settings.json` (`extraKnownMarketplaces` relative `./meta-architect` + `enabledPlugins`). Rationale + sequencing in **`docs/decisions/0002`**. *Considered & rejected:* standalone repo from day one (couldn't dogfood while building); global `~/.claude` install (not reproducible); scattering skills into `.claude/skills/` (muddies extraction); hard-wiring the workflow integration now (premature — validate after the first run). **(2) The plugin's own framework, as its own MADR ADRs:** nine analysis layers with **Compliance/Audit split out of Security** (`meta-architect/decisions/0001`), a **glossary-as-knowledge-graph** (one note per term, not one file — `0002`), and a three-mode **vault update model** (regenerated / append-only / update-in-place) dropping a misleading `frozen` status (`0003`). **(3) `AGENTS.md` is canonical** in every directory; each `CLAUDE.md` is a **symlink** to it — one source of truth, read by Codex (`AGENTS.md`) and Claude (`CLAUDE.md`) alike — gratuitous tool-naming neutralized (factual infra like `.claude/settings.json` / `${CLAUDE_PLUGIN_ROOT}` kept). Dev env is Linux/WSL/macOS (symlink-aware; noted in `AGENTS.md`). **(4) A self-improving Codex review skill** (`.agents/skills/review-pr/`) that reads/appends its own `lessons.md`; Claude's `/review-pr` learned to filter Codex's Windows/UNC cross-environment artifacts (false-positive filemode/symlink diffs). **Reviewed** via `/review-pr` (own) + Codex — converged on the vault-only Bash guardrail; folded that + a stale `proposals/<slug>/decisions.md` path; the filemode "change" was a Codex Windows/UNC artifact (no fix needed — canonical `git ls-files -s` = `100755`). **Sequencing:** build now (this PR); first live `initialize` run next session; integrate into the rituals only after learning from it.
- **2026-06-01 — Process (retro): cwd-robust gate invocation + "a retro is a reflective pass".** Two fixes from this session's retrospective (which itself was nearly skipped — the user had to prompt for it twice, which is *why* the second fix exists). **(1) cwd foot-gun killed at the source.** Running `python3 scripts/check_dependency_age.py` from `backend/` fails with "No such file" (the script lives at repo root); it bit 3× this session and once in Session 3, where we wrongly dismissed it as "the from-repo-root note already guards it." The note didn't — recurrence proved it. `/add-dependency` and `/wrap-session` now invoke it cwd-independently as `python3 "$(git rev-parse --show-toplevel)/scripts/check_dependency_age.py"`, verified working from `backend/`. The script was already cwd-independent (`Path(__file__)`); the bug was always the *invocation path*, so this corrects the Session-3 misdiagnosis rather than touching the script. **(2) `/wrap-session` step 2 clarified:** a retro is a *reflective pass* (what ran, what recurred, what helped), not merely folding an explicit user directive — conflating the two is how the cwd gotcha survived a session. *Considered & rejected:* hardening the script itself (already cwd-independent); a Makefile/task-runner for all gates (over-engineering for one script).
- **2026-06-01 — M2.S1 PreNERAgent + the §6.7 direct-URL-wheel channel + doc-over-memory rule (PR #25 feature, this PR governance).** Several linked decisions. **(1) GitHub-release wheels as a new §6.7 dependency channel.** spaCy's pretrained models (`pl_core_news_lg`, `en_core_web_lg`) aren't on PyPI — only versioned wheels on GitHub Releases. Rather than relax the baseline, ran the security-baseline-amendable flow: spec §6.7 gained a "Direct-URL wheel channel" clause (PEP 508 `name @ url`, exact-by-construction, SHA-256-locked in `uv.lock`), `check_dependency_age.py` learned to gate these on the GitHub **asset** upload date, `/add-dependency` documents the procedure. OSV is the one genuinely-N/A rule (artifacts not indexed); residual risk bounded by official publisher + locked hash + weights-only payload. *Considered & rejected:* `spacy download` (unpinned runtime fetch); a manual WAIVERS-style hole (the gate enforces it automatically instead). **(2) Asset date, not tag date (Codex P2).** A wheel can be added to/replaced on an older release after its tag was cut; since uv locks the new bytes, the tag's `published_at` would let a fresh artifact pass the soak. Gate reads the matching asset's `updated_at`. The model assets date 2024-09-29 vs the tag's 2024-09-30 — proof the two differ. **(3) Models in an optional `models` dependency group.** The wheels are ~950 MB; default `uv sync` (and CI) skips them, the model-loading tests auto-skip via `importlib.find_spec`, and the pure `candidates_from_entities`/`map_spacy_label` seam keeps `extract()`'s logic CI-tested without them (own-review #1). `spacy` itself stays a core dep (needed for mypy). `uv sync --group models` locally. **(4) Layering carve-out.** spaCy is imported directly in `agents/prener_agent.py` (not behind a Protocol) — documented exception in `src/story_forge/CLAUDE.md` for deterministic local NLP; revisit a `NerPipeline` Protocol only when a second NER backend appears (e.g. the finetuned model below). **(5) PreNER is recall-first, deliberately imperfect.** It surfaces candidates; the LLM ExtractionAgent (M2.S3) + §3.3 Stage-4 human review correct types, and the accumulated corrections are the training data for a **custom finetuned spaCy model** later (recorded in `PLAN_LONG`). So tests assert the agent's contract, not model accuracy — encoded in `backend/CLAUDE.md`. **(6) Process: document in the repo, not agent memory.** Durable knowledge (decisions, conventions, follow-ups) goes into the spec/plan/`CLAUDE.md`/skills — the file the next contributor consults — never only in private agent memory. Encoded in root `CLAUDE.md` + `/retro` + `/wrap-session`; the three memories this session accrued were migrated here and into those files, then cleared. *Also recorded:* web research runs in the **main loop**, since background subagents can't surface permission prompts and get web/shell tools auto-denied (root `CLAUDE.md` session guidance). **Reviewed** via `/review-pr` (own) + Codex; both folded — CI-coverage seam (#1), GitHub-token/403 gate robustness (#3), layering note (#2), asset-date gating (Codex P2). CI green across all four jobs; squash-merged `e80ef94`.
- **2026-05-27 — Wave-3 CVE response + post-PR-23 waiver audit + /review-pr §5/§8 amendments.** Four linked actions, all chained from one Codex catch. **(1) Wave-3 CVE response (PR #23, squash `0057044`).** Three new HIGH gosu Go-stdlib CVEs (CVE-2026-39823 `html/template` `<meta>` XSS; CVE-2026-39825 `net/http/httputil` ReverseProxy; CVE-2026-39826 `html/template` `<script>` XSS) hit pgvector and ollama together via the sequential-Trivy unmask pattern; waived per-image with reachability rationales. Tripped the WAIVERS.md "build our own image if this happens a third time" threshold (recorded 2026-05-26 after wave 2). Evaluation tracked in [Issue #22](https://github.com/yaaisiu/story-forge/issues/22), pgvector-scoped — gosu is shipped by the *postgres* image, not by pgvector, so the recipe only escapes the treadmill if `postgres:17-trixie` rebuilds faster than pgvector does (that's the issue's load-bearing first task). **(2) Codex P2 fold.** Initial CVE-2026-39823 rationale named `net/url`; Codex caught that the CVE is actually in `html/template` (verified against pkg.go.dev/vuln GO-2026-4982 + NVD). Outcome unchanged (gosu doesn't render templates) but the *justification* needed to match the real code path. My own `/review-pr` missed it. **(3) Existing-waiver audit (user-requested, this wrap commit).** Verified 39 pre-PR-23 CVE rationales against NVD: **39/39 package attributions correct** — Codex's catch was isolated, not a repo-wide pattern. Five minor description tightenings folded (none changing reachability): CVE-2025-61723 "std" → "encoding/pem"; CVE-2026-25679 IPv6-specific → general host/authority validation; CVE-2026-33811 DoS → double-free + crash severity; CVE-2026-29111 dropped over-claimed "arbitrary code execution" (actual is DoS per NVD); CVE-2026-40356 "integer overflow" → "integer underflow". Running the audit *while the Codex catch was fresh* gave empirical confidence the bug-class was an isolated incident. **(4) Process amendments from retro** (both approved). `/review-pr` §5 gains: *"verify each CVE's package attribution against an authoritative source (pkg.go.dev/vuln / NVD); Trivy's title text can reference a different CVE and silently mislead"* — memorable example: CVE-2026-39823's title began with "CVE-2026-27142 fixed a vulnerability in which URLs were not correctly...". `/review-pr` §8 gains: *"sequential-Trivy unmask — don't declare ready-to-merge on first green scan when a PR changes a Trivy waiver or image pin"* — pattern is 3-for-3 (#2, #4, #23). `/pin-image` already encodes the scan-all-images-up-front check for new pins; `/review-pr` §8 carries the equivalent for waiver-only PRs. **Considered + rejected:** a new `/verify-waiver` skill (insufficient recurrence — fresh waivers ~3× per year; the check belongs as a bullet in `/review-pr`); a separate "Last audited" field in WAIVERS.md (redundant with "Last reviewed", which is now 2026-05-27); an ADR for the wave-3 escalation (the durable record — WAIVERS.md preamble + Issue #22 + PR #23 squash body — already serves).
- **2026-05-27 — M2 sliced into 6 resumable sessions + §3.3 drift corrected.** Three linked decisions. **(1) Slice shape:** S1 PreNERAgent (spaCy, deterministic) → S2 paid LLM adapters (~~Anthropic-first~~ → OpenRouter-first, overturned 2026-06-02 — ADR 0003) + `LLMRouter` + budget tracking → S3 ExtractionAgent → S4 Neo4jAdapter writes (no dedupe) → S5 frontend graph viewer (cytoscape) + agent activity panel (§8.5) → S6 OpenRouterProvider + polish. Order follows spec §7's pipeline order (Pre-NER step 3 before LLM extraction step 4), so dependency is the slice rule, not a reordering. **(2) M2.S1 = PreNERAgent (deterministic) over "router first".** Considered & rejected: opening with paid-adapter + router (the alternative the Session 6 wrap-handoff floated). Router-first would couple three decisions in one session (which paid provider, where the budget knob lives, router shape) and ship no extraction; PreNER-first is standalone, deterministic, follows the project preference for deterministic methods over LLM where they suffice, and gives ExtractionAgent (S3) a real input set before it's designed. Also considered: read the spec sections first and decide afterward — the right move and the one we took, which confirmed §7 step 3 places Pre-NER ahead of step 4 LLM extraction. **(3) §3.3 cascade-matching drift in the M1→M2 handoff: corrected.** The Session 6 wrap-handoff parenthetically scoped "§3.3 Stages 1+2+3 as M2 scope; Stage 4 review queue is M3" — but spec §9 M2 says literally "Save entities to Neo4j without cascade (everything = new entity for now)" and §9 M3 lists MatchingAgent (Stages 1+2), JudgeAgent (Stage 3), **and** the review queue (Stage 4) all together. Per the workflow rule, spec wins. The slice now matches: M2 has *no* cascade work at all; every extracted candidate becomes a fresh Neo4j node. Spec is unchanged (it was already right); only the plan was sloppy. This is the kind of drift `/resume-session` exists to catch — flagging it here as a process win, not just a fix.
- **2026-05-26 — Session 6: five linked decisions (frontend upload + outline UI, M1 close).** (1) **shadcn install path: B-now / A-when-needed.** Plain Tailwind alone for Session 6; hand-vendor primitives at first real need, documented in `frontend/src/components/ui/CLAUDE.md` at first use. Chose over the official CLI macro because the CLI bulk-installs deps without going through `/add-dependency`'s §6.7 discipline and writes files into the repo opaquely. The minimal `cn()` helper at `src/lib/utils.ts` matches shadcn's canonical location so first-vendored primitives can import it unmodified. (2) **Outline editor scope: Option B (raw_text override + persist on the existing structure endpoint).** Considered & rejected: read-only editor (would miss the "outline editable in UI" milestone goal); full delete-and-replace + PATCH (overshoots one conversation). Manual mode is deterministic so local-preview ↔ backend-parse drift risk is low; auto/hybrid edit-preview still deferred (needs an LLM dry-run path). Spec §7 step 1/2 amended to record the response/body shape. (3) **Defense-in-depth empty-`raw_text` guard, two layers.** My `/review-pr` Should-fix #1 (UI disables submit when manual/hybrid textarea is empty + inline hint) and Codex's P1 (hook drops the override when raw_text is empty/whitespace) caught the *same* destructive-overwrite bug with *different* fix mechanisms. Folded both — the UI guard prevents the click, the hook guard catches anything that bypasses the UI (programmatic caller, future regression). (4) **CORS allowlist also accepts `http://127.0.0.1:5173` and `http://127.0.0.1:3000`** (§6.7 spec amended with rationale, root `CLAUDE.md` updated, `config.py` default + `.env.example` extended). Real-browser smoke test caught that Vite binds to `127.0.0.1` by default but the original allowlist only named `localhost` — uploads succeeded backend-side but the browser rejected the response. Same loopback socket, same trust boundary; both name forms point at it. Kills the class rather than the instance — the cross-cutting CORS follow-up is removed. (5) **Planning docs reorganized.** `PLAN_SHORT.md` / `PLAN_LONG.md` moved into `docs/`; new `docs/CLAUDE.md` documents the handoff-block contract, the Decided/Blocked/Done structure, and the cross-cutting curation rule (carried by hand at each milestone roll — nothing moves automatically). Path references in root `CLAUDE.md`, `/resume-session`, `/wrap-session`, and README reconciled.
- **2026-05-26 — Process (retro): three rule additions from Session 6.** (1) **`/review-pr` §8 amendment**: "same bug, different fixes — consider folding both." When `/review-pr` and Codex flag the same bug class with different suggested mechanisms (UI guard vs hook guard, in our case), the default isn't "pick one and dedup the other" — it's *consider folding both as defense-in-depth*, especially for destructive bugs (data loss, silent overwrite). Two independent reviewers converging on a class while diverging on mechanism is the signal the bug deserves belt-and-suspenders. Example: empty-`raw_text` overwrite — both folds landed (PR #18). (2) **`backend/CLAUDE.md` "Running locally" note**: before first run against the dev backend, `uv run alembic upgrade head` from `backend/`. Tests use a throwaway DB so this gap was invisible until the verify-skill smoke walk 500'd on `relation "projects" does not exist`. Idempotent; safe to re-run. (3) **§6.7 CORS amendment** (described in Decided above) — recorded here as a process change because it removes the cross-cutting follow-up and makes both `localhost` / `127.0.0.1` first-class loopback origins for dev. **Considered + rejected:** a `/pin-image` amendment about "if it bit you once, expect the next CVE wave in days not weeks" (the WAIVERS.md preamble we wrote *this session* already records the empirical pattern + the "build our own image at occurrence 3" threshold — a `/pin-image` line on top would be ceremony); a `/verify` shadow about "handoff to user for GUI surface" (one-off in this no-Chromium env, working around the skill's PASS/FAIL/BLOCKED/SKIP vocabulary by saying "PASS (backend) | HANDOFF (GUI)" was fine and didn't cost real time); a "cross-layer scope expansion" rule (AskUserQuestion handled it; no recurring friction to encode).
- **2026-05-26 — Session 6: pgvector CVE-rot recurrence threshold recorded.** The pgvector image (`pgvector/pgvector:0.8.2-pg17-trixie`) was hit by a *second* CVE-staleness wave in 5 days against the same 2026-05-15 rebuild (Issue #4 → PR #19). Resolved by extending `infra/trivy/pgvector.trivyignore` with 11 new HIGHs (1 krb5 OS + 10 Go-stdlib in `gosu`), all reachability-justified. A pattern note in `infra/trivy/WAIVERS.md` calls out the recurrence explicitly: if this happens a *third* time, strongly consider building our own image (`postgres:17-trixie` + `apt-get install postgresql-17-pgvector`) so we own the rebuild cadence. Documented so a reader knows we're aware of the treadmill, not surprised by it.
- **2026-05-25 — Session 5: six linked decisions (frontend shell + typed client).** (1) **shadcn/ui deferred to Session 6** — Session 5's failing test is "app shell renders under router + query provider", which doesn't need any shadcn primitive. Installed Tailwind alone; primitives land when the upload/outline UI actually consumes them. Smaller, more honest diff for the shell session. (2) **M0 `/health` page removed, not converted.** It was M0 connectivity scaffolding, not a feature. Converting it to a TanStack Query hook would have invented a feature we don't need (Karpathy "no speculative features") — Session 6's upload screen replaces this view anyway. (3) **`openapi.json` captured to a file committed in the repo** (`frontend/openapi.json`), regenerated via `backend/scripts/dump_openapi.py`. Keeps the frontend build hermetic (no live-backend dependency in CI / for frontend-only contributors) and surfaces backend schema drift as a visible PR diff. (4) **`openapi-typescript@7.13.0` runs via `npx`, not installed.** Upstream peer is `typescript@^5.x` while the project is on `typescript@6.0.3`; `npx -y openapi-typescript@7.13.0` (pinned in the `generate:api` npm script) sidesteps the `ERESOLVE` cleanly without project-wide `--legacy-peer-deps`. Trade-off acknowledged: not visible to `scripts/check_dependency_age.py` / `npm audit`; pin-time age + OSV verified manually and documented in `frontend/src/lib/api/CLAUDE.md`. Revisit when upstream supports TS 6. (5) **Shared `ErrorResponse(BaseModel)` + `responses={...}` on both `/stories` routes** (folded from Codex P2). Every `HTTPException` the routes raise (400/404/409/413/415/502) now appears in OpenAPI typed as `ErrorResponse`, so Session 6's hooks can discriminate failure modes by status code with proper types. 422 is deliberately left undeclared to preserve FastAPI's auto-generated `HTTPValidationError` shape; the overload (`ChunkingTooLongError` also raises 422 with a `{detail: str}` body) is tracked as a cross-cutting follow-up. (6) **Vitest wired into the frontend CI job** (`.github/workflows/ci.yml` `- name: Vitest`). The shell render test existed in the first commit but no CI step invoked it; folded from own-review (B1). A test that runs only locally is half a test.
- **2026-05-25 — Session 5: QuerySentinel makes test wrappers load-bearing.** The shell test originally mounted `<QueryClientProvider>` around an `<AppShell>` that has no TanStack Query usage — the wrapper was decorative, and the test docstring's claim ("proves both providers are wired correctly") was a lie. Folded via a tiny in-test `<QuerySentinel />` that calls `useQueryClient()` (throws without a provider). Verified by removing the provider in the test and confirming the test fails with the missing-provider error. The provider chain is now defended from Session 5, not from "whenever Session 6 happens to add a real hook."
- **2026-05-25 — Process (retro): three rule additions from Session 5.** (1) Added a **`/review-pr` §4 bullet**: "FastAPI routes declare every non-2xx outcome they raise in `responses={...}`" — Codex caught the gap on `/stories/{id}/structure`; my own `/review-pr` missed it. Also documents the 422 trap (declaring 422 clobbers FastAPI's validation typing). (2) Added a **`/review-pr` §6 bullet**: "Render-test wrappers must be load-bearing" — every provider / context / fixture wrapping a render test must be exercised by the asserted-on subtree or by an explicit sentinel component. (3) Added a **`/review-pr` §6 bullet**: "A new test runner / category / glob is wired into CI before merge" — the test-driven rule is structurally bypassed if CI doesn't execute the test. (4) Mirrored the FastAPI rule as an **authoring** rule in `backend/src/story_forge/CLAUDE.md` (new `## API routes` section). The review and authoring sides now both name the rule, so it's caught both when written and when reviewed. **Considered + rejected:** a rule about "grep for existing CI steps before adding a duplicate plan item" (near-miss with `npm audit`; due diligence caught it on its own — not skill-worthy); a "what to do when a peer-dep conflict makes the pin unworkable" branch in `/add-dependency` (edge case enough that adding it would be over-engineering until we hit it twice).
- **2026-05-25 — Chunking default lowered: `gpt-oss:120b-cloud` → `gpt-oss:20b-cloud`.** Verified against Ollama's current cloud catalog (the `:20b-cloud` tag is confirmed available, same gpt-oss family, "Low Usage" tier vs the 120b's "Medium Usage" — lighter quota, same Ollama API, same JSON structured-output behavior). Rationale: **chunking is a structural JSON task** (find `##`/`###` boundaries, return paragraph ranges with one-sentence summaries), not deep Polish prose generation; a 6× smaller model in the same family is sufficient and meaningfully cheaper to run. Spec §6.5 / §5 hardware tier / §9 example references softened to cite the new default ("`gpt-oss:20b-cloud` for chunking, larger variants for heavier tasks"); not a tier change (still cloud_free). Bump back to 120b / try `qwen3.5:cloud` if proposal quality is short on real long-form PL text — kept the option open in the .env.example comment. Considered + rejected: `qwen3.5:cloud` (better PL breadth — 201 languages — but Medium-Usage tier with no quota savings, multimodal weight we don't need; the right default for *later* prose-quality work, not chunking).
- **2026-05-25 — Process (retro): two `/review-pr` amendments from Session 4.** (1) Added a "dispatch-level confusion in nested structures" bullet to `/review-pr` §1: when code branches on a property at one level of a tree, check the invariant isn't at a neighboring level. Codex caught the hybrid bug — `_fill_untitled` dispatched on chapter-untitled and silently discarded an explicitly-titled scene that lived *inside* an implicit chapter (`### Cold Open` before any `##`). My own `/review-pr` ran after the fold and didn't list this class. (2) Clarified `/review-pr` §3 Simplicity bullet: an abstraction that net-removes existing debt (type-ignores, repeated boilerplate, branches) is a refactor, not speculation; simplicity argues *for* it. Example: the `OutlineProposer` `Protocol` introduced in the same PR removed 11 `# type: ignore[arg-type]` markers. **Considered + rejected:** a rule encoding AskUserQuestion cadence (the tool description already says reserve it for genuine forks — I asked once for a conventional default, self-discipline observation only); a rule forcing `/review-pr` *before* Codex's review (hard to encode without implementation friction, surfaced as awareness).
- **2026-05-25 — Session 4: five linked decisions (chunking modes + persistence).** (1) **Markers are heading *levels*, not literal "Chapter"/"Scene" words** — `##` opens a chapter and the trailing text is the title (`## The Crossing`, not the word "Chapter"); `#` is the story title and is not a structural boundary; `###` opens a scene. Plan wording clarification, not a spec change (§3.1 already showed `##`/`###` as the marker shape; the literal title was just an example). (2) **Hybrid fills only untitled *scenes* (per-scene dispatch).** First pass dispatched per-chapter and lost explicit scene anchors inside implicit chapters (Codex P1 catch); the corrected rule preserves every explicit `##`/`###` verbatim. (3) **Windowing: guard now, window-and-stitch later.** `auto`/`hybrid`-fill LLM paths raise `ChunkingTooLongError` above `max_input_words` (a constructor knob, not a new env setting); manual mode handles any size. The proper pre-split + per-window + stitch sub-system is a tracked cross-cutting follow-up — premature to build before auto is actually run on a 50k-word input. (4) **Endpoint = one-shot build+persist; re-structure returns 409.** `POST /stories/{id}/structure?mode=...` reads the story's stored raw text and persists the tree in a single call. The accept/edit loop (§7 step 2 "user accepts/edits before save") is a frontend interaction — deferred to Session 6 where the editor exists. 409 over silent-append on a second run; delete-and-replace is a frontend-driven follow-up. (5) **Outline carries `summary`.** The agent produces summaries and the DB columns already exist; dropping them would be lossy. Manual mode leaves them `None`, which is correct.
- **2026-05-25 — Session 4: DB write lock is taken *after* the LLM build, not before.** The structure route's `SELECT ... FOR UPDATE` originally wrapped the whole handler — including the seconds-long LLM call. Folded into the same PR after `/review-pr`: build the outline first (no DB state needed beyond the story.raw_text we already read), then take the lock for the small writes window, re-check `list_chapters` under the lock, then insert. The 409 race is still closed (concurrent POSTs serialize on the lock; the second sees the persisted tree). Honest trade: a true contention test needs parallel connections, which the txn-sharing `db_conn` fixture doesn't fit — defense-in-depth verified by reading.
- **2026-05-22 — Process (retro): created `/review-pr` skill, wired into CLAUDE.md Merge flow (Session 3).** A Story Forge PR-review skill that does a correctness/bug-finding pass *plus* the project lens (spec/plan fidelity, layering, §6.7 gaps CI can't catch, test discipline, hygiene, merge-readiness); report-only, complements Codex (PR #11, `d90ed76`). Hardened mid-session after Codex caught two things its first version missed — added an *output-schema strength* check (malformed-but-parseable must fail, not pass as success) and a *prompt-injection safety* check (never recover prompt structure by reparsing output rendered with untrusted content). Referenced from CLAUDE.md's Merge flow as the explicit "your own review" step. Considered + rejected: hardening `check_dependency_age.py` for cwd-independence — on inspection it already uses `Path(__file__)` and is cwd-independent; the failures were invocation errors (running `python3 scripts/…` from `backend/`), which the "from repo root" note already guards.
- **2026-05-22 — Chunking refocus + cloud_free default (Session 3).** Two linked decisions. (1) **GPU-less dev host** → the local_small tier is impractical (CPU-only 9B too slow for interactive use), so chunking defaults to **cloud_free** (Ollama Cloud); local_small stays a config swap for a GPU-backed host. Because both tiers speak the Ollama API, this is a host/key flip in `OllamaProvider`, not a code-path change. Spec §6.5 (hardware para + agent table) and §7 step 2 amended with rationale. (2) **LLM chunking is not the primary path.** §3.1 already defines three modes; manual (`## Chapter`/`### Scene` markers) + hybrid are deterministic, free, and more reliable than a small model on *marked* text — and paragraphs are already deterministic (`domain/parsing.py`). So `ChunkingAgent` is reframed as the auto/**fallback** path for *unmarked* text + the canonical first agent demonstrating the agent pattern (prompt + Pydantic schema + retry); the deterministic manual/hybrid chunker (Session 4) is the everyday primary path. Session order unchanged (3 = LLM auto, 4 = manual/hybrid + persistence). The `CHUNKING_LOCAL_MAX_WORDS=4000` threshold is kept as a config knob (conservative; meaningful only when a local tier is configured), not a hardcoded magic number. **Tests stay mocked** — the smallest Ollama Cloud model is the runtime default and fine for manual smoke checks, but agent unit tests use a mocked `LLMProvider` per `backend/src/story_forge/CLAUDE.md`.
- **2026-05-21 — Language detection: `langdetect==1.0.9` (Session 2).** Over `fasttext`: `langdetect` is pure-Python and ships its own profiles (no `lid.176` model artifact to download out-of-band — pinnable, offline-friendly, no extra supply-chain surface), and PL/EN is an easy discrimination on 5k–50k-word prose where fasttext's short-text accuracy edge doesn't apply. Seeded (`DetectorFactory.seed = 0`) for deterministic output.
- **2026-05-21 — Upload creates a new `Project` per file (Session 2).** A `Story` needs a `project_id` and there is no project-selection UI/endpoint yet, so the upload route creates a `Project` (name = filename stem, language = detected) alongside the `Story`. The detected language lives on `Project` (the `Story` model has no language field). Revisit when project management lands.
- **2026-05-21 — Sandboxed uploads stored without a DB path column (Session 2).** Original bytes are written to a dedicated dir (UUID-named, `0o700` dir / `0o600` file, no exec — spec §6.7), and the parsed text is stored in `Story.raw_text`; the saved path is *not* tracked in Postgres. Satisfies §6.7 with no spec/migration change. The dir chmod is explicit (not `mkdir(mode=…)`, which is umask-masked and ignored for an existing dir).
- **2026-05-21 — Per-request DB connection, no pool (Session 2).** `adapters/db.py` `get_connection` opens one `AsyncConnection` per request (commit on success / rollback on error). A pool (`psycopg_pool`) is deferred as speculative for a single-user local app. `libpq_kwargs` preserves URL query options (`sslmode`, timeouts) so a managed-Postgres URL still works (Codex P1); `conftest` reuses the same helper.
- **2026-05-21 — Process (retro): PR-first / `/wrap-session`-last, encoded in the wrap skill.** The session-close order is commit → PR → CI + Codex review → fold fixes → squash-merge → *then* `/wrap-session` (which lands the separate `docs: close Session N` commit). Added a preflight step 0 to `wrap-session` and reframed its step 8 (commit the wrap's *own* bookkeeping, not the feature). Also added a `backend/CLAUDE.md` test convention: bind credential-like values to a variable so `detect-secrets` doesn't flag them (prefer over baseline/pragma edits).
- **2026-05-21 — Issue #4 resolved: pgvector → `0.8.2-pg17-trixie`, ollama → `0.24.0`, both scoped-waivered (PR #6, `4cb92a2`).** pgvector pinned at 6 days old (the pre-approved §6.7 bend); trixie is the cleanest base pgvector ships (9 OS CVEs vs bookworm 13; older 0.8.1-trixie = 42; no UBI variant). Fixing pgvector unmasked a second stale image — ollama `0.22.1` (sequential Trivy masking, same as neo4j→pgvector in #2). ollama bumped to `0.24.0` (freshest aged stable, 7 days, no bend), which dropped the CRITICAL + 6 others (17 → 12 HIGH, 0 CRIT). Residual CVEs no tag can fix are waived per-CVE: pgvector's OS CVEs (no fresher rebuild exists) + bundled `gosu`/Go-stdlib; ollama's compiled-in Go-stdlib + `buger/jsonparser` — all assessed not network-reachable on a 127.0.0.1 single-user deployment (gnutls unused; mostly DoS; cert-validation needs MITM). Added **`infra/trivy/WAIVERS.md`**, a consolidated review register of every waived CVE across images (fixed-in versions + drop conditions), backreferenced from each scoped `.trivyignore`; waivers stay per-image, never repo-wide.
- **2026-05-21 — Process (retro): amended `/pin-image` with three lessons from Issue #4.** (1) When a local Trivy scan can't be read back — large images auto-background into a sandbox whose stdout/file-writes never reach disk — stop iterating locally; scaffold a header-only waiver, push, and harvest the exact CVEs from the CI Trivy log (CI is the source of truth). (2) Scan *every* image the security job scans up front — a red image masks staleness in every later image (bit us twice: neo4j→pgvector, pgvector→ollama). (3) Record each waiver in `WAIVERS.md`, not just the scoped file.
- **2026-05-20 — §6.7 exception pre-approved for Issue #4: pin `pgvector:0.8.2` even if <7 days old.** Rationale (user): it's a CVE-*fix* release from a known official publisher; a serious flaw in a ~6-day-old release would most likely already be public, and a still-unknown one is subtle enough that the age-soak wouldn't catch it regardless — so waiting only prolongs exposure to the *known* CVEs the bump fixes. Trivy-cleanliness + the documented waiver remain the real gates. Record the exception in the PR / waiver file when applied. (This is the "no exceptions without explicit conversation" clause being exercised, not removed.)
- **2026-05-21 — Docker image age soak lowered 14 → 7 days (images only; packages stay 14).** Base images come from known signed official publishers and the dominant risk is *known CVEs*, where a fresher rebuild is safer — so Trivy CVE-cleanliness is the primary gate and a 7-day soak still allows a compromised-tag alert to surface. Spec §6.7 amended (with rationale), root `CLAUDE.md` updated, new `/pin-image` skill encodes it. `check_dependency_age.py` untouched (it only scans PyPI/npm, never images).
- **2026-05-21 — neo4j pinned to `5.26.25-community-ubi10` + scoped netty waiver (Issue #2).** UBI-10 base reports 0 OS CVEs vs 14 (incl. 2 CRITICAL gnutls) on Debian `-trixie`; the 3 HIGH netty CVEs Neo4j bundles itself (no base variant fixes them) are waived with per-CVE justification in `infra/trivy/neo4j.trivyignore`, wired to the neo4j Trivy step only. All DoS/parsing-desync, unreachable on a 127.0.0.1 single-user deployment. Drop the waiver when Neo4j bumps netty ≥ 4.1.133.
- **2026-05-21 — Process (retro): created `/pin-image` skill** for the image-pinning ritual (Docker Hub tag/date lookup → 7-day check → **dockerized Trivy** variant scan → base choice / scoped-waiver → apply to compose + ci.yml). Corrects the stale M0 "no trivy in WSL" assumption: `docker run aquasec/trivy image …` works locally and is the verify-before-push path. `add-dependency` gained a one-line "images → use /pin-image" pointer.
- **2026-05-20 — Primary keys are app-generated UUIDs (`uuid4`), not serial integers.** Paragraph/entity IDs are referenced cross-store (Neo4j `source_paragraph_id`, `entity_mentions.paragraph_id`), so store-independent stable IDs avoid sequence coordination between Postgres and Neo4j. DB carries `gen_random_uuid()` as a safety default; the repo inserts the model's id explicitly.
- **2026-05-20 — The document-tree migration is hand-written raw SQL (`op.execute`), not `op.create_table`.** A faithful 1:1 transcription of spec §6.4 — including `vector(768)` — without pulling a pgvector SQLAlchemy type into the dep set just to express one column. `target_metadata` stays `None`; no ORM layer (repo is raw async psycopg, columns map 1:1 to domain fields).
- **2026-05-20 — Repo scope is Create/Read/Delete for now; generic Update deferred.** The realistic update is renumbering `order_index` on reorder, which lands with chunking persistence (Session 4); not added speculatively.
- **2026-05-20 — Recipe for adding an `.env`-backed setting recorded in `backend/CLAUDE.md`** (retro outcome): placeholder in `.env.example` → hand the user an append command → dependent tests stay red until set. Captures the friction this session so Neo4j creds / LLM keys don't re-derive it.
- **2026-05-20 — Integration tests use a dedicated `story_forge_test` DB, fixture-managed in the same Postgres instance.** A pytest session fixture creates it, runs `alembic upgrade head`, yields, then drops it. Chosen over an ephemeral compose service / testcontainers: no new dependency, dev data untouched, deterministic. CI gets a pgvector service container.
- **2026-05-20 — Sibling ordering column is `order_index` (plain integer ordinal).** Avoids `order` (SQL reserved word → forced quoting); same name used end-to-end (DB / Pydantic / JSON), no mapping layer. Integer ordinal renumbered on reorder; fractional/lexical rank deferred as speculative. Spec §6.4 amended (inline note), SQL updated.
- **2026-05-20 — M1 sliced into 6 resumable sessions** (this file's structure). Each session is one-conversation-sized, ends green + committed, and records a resume anchor in the handoff block. Driven by `/wrap-session` (end) and `/resume-session` (start).
- **2026-05-20 — API client generator: `openapi-typescript`** (over `orval`/`hey-api`). Emits TS types + a tiny typed `openapi-fetch` client from the backend's `openapi.json`; we hand-write the TanStack Query hooks in `src/lib/api/`, keeping them plain/commented for an outsider. Chosen for the smallest dependency surface and most legible data layer in a portfolio repo.

### Done in previous sessions

- **2026-06-16 — Session 29: M3.S4e shipped (PR #76) — the knowledge graph now has human-gated *edges*, completing §9 M3's "clean graph" for relations.** Built the relation-write backend test-first, the §3.3 5th human action ("decide on relations"). **The path:** extraction already staged each paragraph's relations as surface-form triples, but nothing wrote a Neo4j edge (`create_relation` had zero callers). Now a new `RelationReviewService` (the only edge writer) resolves a staged relation's two surface endpoints to the **committed** entity id of the same-paragraph accepted candidate each names (normalised-exact — DM-Rel-2), and writes the edge under an explicit human `decide` action; held/self-loop/ambiguous endpoints are never committable (DM-Rel-7). A new **`staged_relations` table** (additive migration) gives each relation a `staged|written|rejected` lifecycle + audit (DM-Rel-4); `create_relation` went `CREATE` → **idempotent `MERGE` on `uuid5(subject_id, predicate, object_id)`** so the same fact across paragraphs is one edge and a retried decide never doubles (DM-Rel-6). Two endpoints (`GET …/relations`, `POST …/relations/{id}/decide`) with full `responses=`; OpenAPI + typed client regenerated. **Invariant:** broadened **INV-1** to cover edges (not a new INV-10) + INV-9's second witnessed instance; **ADR 0005**; proposal → `accepted`, OQ-19 struck, INDEX refreshed. **No spec amendment** (§3.3 already lists the action). **Reviews:** `/review-pr` (self) caught a self-loop test that wasn't exercising its branch (fixed + added ambiguity + cross-paragraph-collapse tests) and stale "PROPOSED/OPEN" vault status (reconciled); the 7-finder **`/code-review` caught a real bug** — a *changed* merge target was never persisted on the candidate row, so `committed_entity_id` (reused for relation resolution) resolved a same-paragraph endpoint to the **stale** target → edge to the wrong entity; **fixed** by persisting the committed target on merge-accept (status-last) + a regression test. Other finder candidates were refuted by the passing real-Neo4j tests (the "invalid Cypher `$props.id`" claim — edges write fine) or out of the single-user-local threat model (two-reviewer races). **Checks:** backend ruff/format/mypy/pytest (331 passed + 4 model-skips) green; PR #76 CI 4/4 (backend/frontend/security/ollama-smoke); squash-merged to `main`. **Judgment calls (owner-visible):** dropped `staged_relations.project_id` (story_id scopes every query; a paragraph can have relations with zero candidates, so no clean source); `relation_id` isn't story-scoped in the decide route — kept consistent with the existing candidate routes. **Carried follow-up:** per-mention provenance for triple-deduped edges (post-PoC). **Retro (1 change, owner-approved):** added a **"shared-helper reuse"** bullet to `/review-pr` §1 — when a PR routes an existing shared helper to a new consumer, verify the data the helper reads is maintained correctly for the *new* call site (grounded in this PR's `committed_entity_id`/override-merge bug, which the single self-review missed and `/code-review`'s cross-file tracer caught). *Considered & rejected:* a new rule for the "test green for the wrong reason" lesson (already covered by `/review-pr` §6). **Lessons:** (a) reusing a helper across a new caller (`committed_entity_id`) can *promote* a benign pre-existing imprecision (the unpersisted override-merge target) into a real bug — `/code-review`'s cross-file tracer earned its keep, the **sixth** consecutive substantive PR where the multi-agent pass caught a self-review miss; (b) a "drop a guard's test isn't enough" check — a passing test (`test_self_loop_is_dropped`) was green for the *wrong reason* (ambiguity, not self-loop), the kind of false-confidence only an adversarial re-read catches.

- **2026-06-16 — Session 28: planning/decompose (vault-only, no production code) — the M3 relation-write slice designed and its central decisions made, leaving the build a clean test-first start.** A deliberate pre-code session at the "finish M3 for relations" boundary. Ran `/resume-session` (clean — on `main`, anchors verified, the edge-write gap confirmed real: `neo4j_repo.create_relation` exists but the accept path never calls it; one security waiver flagged — python-multipart fix soaks 2026-06-18). **Owner framed relation-write as an M3 slice** (relations are part of §9 M3's "clean graph"; §3.3 lists "decide on relations" as a Stage-4 human action) — not an M3→M4 roll. Ran **`meta-architect:decompose-requirement`** → `architecture/proposals/m3-relation-write.md`: nine-layer/nine-station pass, Mermaid data-flow, a sketched relation-lifecycle, a 7-entry register (DM-Rel-1..7), the "but what if" set, gaps-for-PO; added glossary `referential-integrity` + a learning-log line + OQ-19; regenerated INDEX (and **corrected its stale "Next steps"** that still framed S4c as next though S4c/S4d shipped). **The decompose's payoff:** the dreaded "re-point edges on merge" **mostly dissolves** under intercept-before-write — an endpoint is a surface string with no entity id until accept, so edges write lazily by resolving to the *committed* id (born pointing at the survivor); only an M4 accepted-entity↔entity merge re-points a written edge. It also surfaced a real must-fix: `create_relation`'s `CREATE` doubles an edge on a retried accept → needs deterministic-id `MERGE`. **Owner decisions:** DM-Rel-1 = **explicit human gate** (the §3.3 5th action, not auto-write); build **split backend-now/UI-next** (the S4a→S4b cut); remaining register carried as-proposed for build. Spec §3.3 already covers the gate → no amendment now. **`/retro` (owner-run):** fixed the false "read the matching template" pointer in the `decompose-requirement` + `review-architecture` skills (no `proposal`/`review` template exists — the structure lives in each skill body; pointed them at `vault-note.md` for frontmatter), *considered & rejected* adding duplicate templates. **Checks:** docs/vault/skills only — backend/frontend untouched, so code gates are no-ops; nothing red. The wrap is the only commit. **Lessons:** (a) a step-0 decompose on a branchy "completion" slice earns its keep — the lazy-resolution reframe (re-point dissolves) and the `CREATE`-doubles-edge must-fix would both have surfaced mid-implementation otherwise, the most expensive place to find them; (b) a milestone-boundary decompose wants a fresh vault — the INDEX was stale because the rituals don't run `review-architecture` at resume yet (OQ-3), so the stale "Next steps" had to be caught by hand; (c) the meta-architect skills told the operator to read a template that doesn't exist — a small but every-run friction worth a one-line honesty fix.
- **2026-06-16 — Session 27: M3.S4d shipped — manual handpick (full-stack), completing M3 *entity* dedupe; plus an unplanned security-treadmill detour (3 dep PRs) and a retro skill.** **(1) The feature (PR #70), test-first.** New `GET /stories/{id}/entities?q=` lets the author search **all** of a project's accepted entities and pick **any** as the merge target — the safety net for a true duplicate the cascade + on-accept re-match miss. Project-scoped (resolve story→project, §6.4); ranks `q` in Python with the **same RapidFuzz signal the matcher uses** (a shared `_rank` core that `top_alternatives` now delegates to, behaviour-preserving), so the human's search ≈ the machine's match; **no score floor on purpose** (measured: the diminutives handpick exists to catch score *below* cross-category noise, so reachability wins; the human/INV-1 picks); `q` **never reaches Cypher** (only `project_id` binds — injection-safe by construction); 404 declared, OpenAPI + typed client regenerated. Frontend `EntityPicker` + `useEntitySearch` feed the existing `M`/merge-accept path; the window keyboard scheme already yields to the search input; **last-pick-wins** (picking an alternative clears a handpick — the precedence bug `/code-review` caught, fixed). Promoted `_canonical`→`canonical_for_language` so the route resolves the same project-language name the matcher ranks on. **(2) The security treadmill.** Opening the PR surfaced two reds beyond the known vite #68: a backend **OSV SCA** wave (new advisories vs pinned `starlette`/`python-multipart`) and, after fixing it, an **unmasked** Trivy **neo4j** netty HIGH (CVE-2026-50010 — the sequential-unmask `/review-pr` §8 warns of). Cleared with owner-approved **soaked bumps + fix-first time-boxed waivers**: `starlette` 1.2.0 + `python-multipart` 0.0.30 + `vite` 8.0.16 (all soaked) clear 3-of-4 SCA HIGHs + the vite HIGH; residuals still in soak waived with `ignoreUntil` + reachability (127.0.0.1 single-user, no public TLS) + dated drop-when. Merged as PRs **#72** (SCA + neo4j Trivy) and **#73** (vite); **#68 + #71 closed**. Full local Trivy sweep confirmed pgvector + ollama clean (no further unmask). **(3) Checks:** backend ruff/format/mypy/pytest (306 passed + 4 spaCy skips), frontend lint/format/build/vitest (109) — all green on the merged `main`; feature CI 4/4 after rebase. `/review-pr` + `/code-review` both ran (the latter caught the precedence bug); re-confirmed clean on the final byte-identical code. **Retro (1 new skill + 1 amendment):** the real risk the waivers exposed is a **forgotten waiver** → new **`/triage-advisory`** skill owns the advisory lifecycle incl. the **drop-revisit**, and **`/resume-session` step 3b** adds a date-only waiver-expiry scan so expiring waivers surface at session start (CI `ignoreUntil` re-red as backstop); root `AGENTS.md` points at it. **Lessons:** (a) a *measured* threshold beats a planned one — the score-floor would have re-introduced the exact blind spot the feature exists to fix; only checking real RapidFuzz scores caught it. (b) the SCA/Trivy treadmill reds on a post-gap merge against *unchanged* pins, and fixing one gate unmasks the next — scan all gates/images up front (the `/triage-advisory` discipline). (c) a time-boxed waiver is a deliberate "ignore"; the security hole is forgetting to drop it — so the drop-revisit needs to be a *ritual*, not a memory. (d) `/code-review` caught a self-review miss for the **fifth** consecutive substantive PR.
- **2026-06-15 — Session 26: M3.S4c shipped — on-accept live re-match (intra-batch dedup), backend-only, test-first, squash-merged (PR #67).** Closes the gap Session 25's browser walk surfaced: a first pass against the empty graph stages `Janek` ×3 as three un-mergeable NEW proposals. Now each human **accept** re-runs the **deterministic** matcher (Stage 1 RapidFuzz + Stage 2 cosine — **no LLM**) over the still-pending candidates against the just-accepted entity; a strong match (Stage 1 `>85%` **OR** Stage 2 `cosine >0.85`) flips their staged `proposal` `new → merge` (+ target + provenance/alternatives), so duplicates light up in the queue automatically. New `agents/candidate_rematch.py` (`ReMatchService` reusing `MatchingAgent` stage1/stage2; the match signal is built entirely from the accept's own data — entity id + surface name + the mention vector just written — so it adds **zero extra reads**; Stage-1-only when a vector is absent, mirroring the staging fail-closed gate) + `postgres_candidate_store.update_proposal` (a `status='review-queued'` SQL guard) + an optional `rematch` collaborator on `CandidateReviewService.accept`, called *after* the status flip and **fail-closed** (`contextlib.suppress` — a re-match failure never rolls back the accept; `reject()` and the terminal-noop replay don't fire it). **Monotone** (`new → merge` only, never re-points an existing merge) → idempotent. Writes **only the Postgres `candidates` table** — never Neo4j — so it is the first automated writer of a staged proposal yet **INV-1/INV-9 hold** (the human commits every merge; INV-9's line is graph-vs-staging). Vault folds (witnessed by the flip test): `candidate-lifecycle` gains the `review-queued → review-queued` self-loop; `invariants.md` gets the INV-9 graph-vs-staging clarification; OQ-18 records **ADR 0005 declined**. Tests: re-match unit (flip per stage, no-flip, monotone skip, Stage-1-only, idempotent) + accept-path unit (rematch after status flip, fail-closed, reject/replay don't fire) + the integration **flip test** (accept `Janek` → 2 pending Janeks flip to merge, graph node count unchanged by re-match). **Reviews:** `/review-pr` + multi-agent `/code-review` (high) both ran, **no confirmed defects** — the one candidate ("raw cosine into `confidence`") refuted as matching the existing `candidate_staging` convention and unreachable on the `>0.85` flip path. Full suite **301 passed**; ruff/format/mypy --strict clean. **Merge-readiness:** CI green on backend/security/ollama-smoke; the **frontend `npm audit` job was red on a pre-existing, unrelated vite advisory that also reds `main`** (diagnosed, filed as **Issue #68**) — squash-merged `3326f6a` under the green-main exception, stated in the commit body. **Retro (1 change):** added a "lead with plain language when asking the owner to decide" rule to root `AGENTS.md` (see Decided). **Lessons:** (a) building the match signal from the accept's own data kept re-match a zero-extra-read, fail-closed enhancement — the simplest thing that closed the gap; (b) the green-main exception is exactly for an unrelated dependency-treadmill red like #68 — diagnose + track + state it, don't scope-creep the fix into an unrelated PR; (c) a jargon-dense owner question is friction worth a durable rule, not just an in-the-moment reword.
- **2026-06-15 — Session 25: M3.S4b review-queue UI shipped (PR #65); the milestone's first end-to-end browser run surfaced a real dedup gap → M3.S4c decomposed + spec §3.3 amended.** Two arcs. **(1) M3.S4b — the Stage-4 human gate (frontend), test-first.** `features/extraction-review/`: two generated-client-typed TanStack Query hooks (`useCandidates` queue fetch + `useReviewCandidate` accept/reject mutation — accept invalidates the queue *and* the story-graph key so the graph fills as the author commits; reject invalidates only the queue), a **pure keyboard reducer** (`reviewQueue.ts` — §8.3 J/K nav, A accept, N new, M merge-cycle, R reject) kept out of the components, a presentational `CandidateCard` (quote ±200, NEW-vs-MERGE proposal, reasoning, top-3 alternatives) and the `ReviewQueue` container (states, per-candidate pending, a window keydown listener with an editable-target guard), wired into routing + a graph-viewer link. Strict TDD for the logic (hooks + reducer: red→green); shaped-with-test for the JSX. `/review-pr` + multi-agent `/code-review` both ran and folded: a merge-target with no resolvable name (a Stage-2/3 cosine/judge target ∉ the fuzzy top-3) renders "an existing entity" not a raw UUID; reject stopped invalidating the graph; the keyboard guard; per-candidate pending. 95 frontend tests green; tsc/eslint/prettier clean; CI 4/4; squash-merged `2039c22`. **(2) The browser verify earned its keep.** Running the real pipeline end-to-end (sample story → structure → extract → review queue → accept/reject; 22 candidates staged → 21 nodes, queue drained) confirmed S4b works — *and* surfaced what unit tests can't: a single first pass against an empty graph stages recurring entities (`Janek` ×3) as independent NEW proposals the queue **cannot merge**, leaving duplicate nodes — undercutting §9 M3's "the graph is clean". (Also exposed two already-documented `frontend/CLAUDE.md` gotchas in the flesh: Vite's 5174 fallback and `127.0.0.1`-vs-`localhost` CORS — diagnosed from the docs, no rule change.) **(3) M3.S4c decompose** (`meta-architect:decompose-requirement` → `architecture/proposals/m3s4c-intra-batch-rematch.md`): **on-accept live re-match** (each human accept re-runs the *deterministic* Stage 1/2 matcher over still-pending candidates → dupes flip `new → merge`; backend-only, no LLM; INV-1/INV-9 *hold* — it writes only the staging table) + **manual handpick** (entity-search endpoint + picker for matcher false negatives). Register resolved (owner): split **S4c** (re-match) + **S4d** (handpick); auto-flip on Stage 1 `>85%` **OR** Stage 2 `cosine >0.85` (owner *overrode* my Stage-1-only proposal for more recall; no live judge); monotone re-proposal (guard, no INV-10); project-scoped handpick (supersedes the deferred arbitrary-search item); relation-write kept deferred (priority raised — merges orphan relations). **Spec §3.3 amended** (on-accept re-match + manual-handpick clarifications) via stop-and-amend; plan + vault reconciled across every home; OQ-18 struck. **Retro (1 change):** *"when verifying a register resolution by grep, key on the **register/OQ id** (e.g. `OQ-18`), not just the proposal slug, and use `grep -A/-B` context"* → `/review-pr` §2 — a manual §2 sweep keyed on the slug passed a stale `INDEX.md` "register OQ-18 OPEN" line as clean (the slug was on the wrapped line *above*); the `/review-pr` re-run, which grepped the id, caught it. **Lessons:** (a) actually *running the app* surfaces product-level gaps (single-pass duplicates) that a green test suite never will — the browser verify is not optional polish; (b) the layered review gate caught self-review misses for the *fourth* consecutive substantive PR (the §2 id-grep miss this time), so running both `/review-pr` and `/code-review` (and re-running `/review-pr` on decision-flip docs) keeps earning its place; (c) a line-scoped grep misses stale framing when a wrapped list entry splits the slug from the `OPEN`/`proposed` word — grep the id too.
- **2026-06-15 — Session 24: M3.S4a shipped — intercept-before-write, the cascade's invariant flip, test-first, squash-merged green (PR #63).** The milestone-defining change: extraction stops writing the graph and *stages* each candidate into a new Postgres `candidates` table with the §3.3 cascade's proposal (embed-on-extract → Matching S1/S2 → Judge S3, fail-closed) — **zero** Neo4j nodes; the graph is written **only** by the human-accept endpoints (`GET …/candidates`, `POST …/accept|reject`), the first code enforcer of INV-1. This **retired the temporary INV-8** (no-dedupe `CREATE`-on-extract) and **added INV-9** ("no automated stage writes the graph"); the flip is witnessed by the rewritten coordinator test + an integration test ("extract → 0 nodes, accept → 1 node"), replacing M2.S4's two-identical-→-two-nodes test. `Neo4jRepo.create_entity` became an idempotent **MERGE-on-id** (+ `add_alias` for merge); the accept path writes Neo4j → `entity_mention`(+context vector) → `candidate_decisions` evidence → **status-flip-last**, with deterministic accept-path ids making a crash-before-flip retry idempotent (merge re-validates the target → 409 on TOCTOU). New `candidate_decisions` (INV-3 evidence; the §4.2 `edit_history` text-edit dataset stays deferred) + `paragraph_processed` resume marker (fixes M2's zero-candidate reprocess wart). **ADR 0004** authored; `architecture/invariants.md` / `candidate-lifecycle.md` (→ `living`) / `overview` / `open-questions` / `glossary` / `INDEX` reconciled. **Scope honesty:** relations are *staged* (JSONB) but the edge write + re-point-on-merge is **S4b**; INV-2 consent gate **deferred past M3**; §10 q8 (bilingual peering) surfaced as the spec's deferred call. **Cross-cutting folded:** store-down→**503** (psycopg `OperationalError` + neo4j `ServiceUnavailable`) + a FastAPI lifespan that closes the Neo4j driver. **Reviews — the layered gate paid off again:** `/review-pr` passed but the multi-agent **`/code-review` caught a real fail-closed gap the single pass missed** — `JudgeAgent` propagated the router's transport/envelope errors raw, so a judge outage escaped the cascade's `except JudgeError` and 502'd the whole batch; fixed by making the judge's give-up contract *total* (transport → `JudgeError`). The GitHub Codex secondary was usage-limited (best-effort, not blocked on). **CI surfaced** what review didn't: the `ExtractResponse` field rename (`entities_written`/`relations_written` → `candidates_staged`) left three **frontend test fixtures** on the old shape, red in the frontend `tsc` build — fixed + pushed. **Checks:** 287 backend + 51 frontend tests pass; ruff/format/mypy/tsc clean; CI green 4/4; squash-merged `4c607cf`. **Retro (2 changes):** (a) *"regenerating the typed API client ≠ reconciling its consumers — grep `frontend/src` for the old+new field names on any route-shape change"* → `frontend/src/lib/api/AGENTS.md` + `/review-pr` §2; (b) *"make an agent's give-up error total so a consumer can fail-close on one exception type"* → `backend/src/story_forge/AGENTS.md` (Agents) + `/review-pr` §1. **Lessons:** (a) an API-shape change has *code consumers* (hooks, components, **test fixtures**) that the regenerated client doesn't update — they fail `tsc`, not lint, so they slip a review that only reads the diff; grep them. (b) when one component fail-closes on another's failure, the `except` must cover the error classes that flow *past* the inner component's own give-up error (the router's transport/envelope errors), not just that error — otherwise "provider unavailable" is the one failure that *isn't* fail-closed. (c) the multi-agent `/code-review` caught what `/review-pr` missed for the **third** consecutive substantive PR (#36, #60, #63) — the "run both for substantive code" rule keeps earning its place.
- **2026-06-15 — Session 23: governance-only (no feature code) — M3.S4 re-sliced and the milestone's remaining decisions resolved, ahead of the milestone-closing build.** A deliberate pre-code session at the M3.S4 boundary: the decompose showed the review-queue session is too big for one conversation, so it was split and the open owner decisions settled, leaving the next session a clean test-first start. Ran two `meta-architect` skills by hand (they're deferred from the rituals — ADR 0002). **`review-architecture`** (`architecture/reports/2026-06-15-architecture-review.md`, no blockers) cleared the drift the Session-22 wrap flagged: DM5 folded to ✅ (PR #60), the `task_type` label `"judging"`→`"judge"`, the §3.3 confident-yes merge-rule shorthand, and the `overview.md` snapshot brought up to "M3.S1–S3 built, proposal-only/unwired, INV-8 still live". **`decompose-requirement`** on the backend half produced `architecture/proposals/m3s4a-intercept-write-path.md`. **Decisions (owner):** M3.S4 → **S4a (backend write-path refactor + cascade wiring + INV-8→INV-1 flip + ADR 0004, test-first) + S4b (React review-queue UI)** — the invariant flip is testable at the API boundary, so S4a is a green slice with no empty-graph window; **DM7 → INV-2 consent gate deferred past M3** (persona-justified; its fourth re-point replaced by an explicit dated deferral); **DM-rej → remember rejections** (a `candidate_decisions` evidence row the matcher consults); and the S4a build register **DM-S4a-1..5** — a new `candidates` staging table, a new **INV-9** ("no automated stage writes the graph"), a staged-candidates resume checkpoint (mentions move to accept-time), a focused `candidate_decisions` evidence table now (deferring §4.2's full `edit_history` to the editing milestone), and no retention at PoC. **Checks:** no code touched (green-state code gates N/A); the vault + plan are the deliverables; the wrap is the only commit. **Retro:** nothing earned a skill/rule change; the one data-point — *run `review-architecture` at resume, not wrap, when a session opens with a decompose* — was folded into the vault's ritual-wiring evidence note. **Lesson:** at a branchy milestone-closing boundary, spending a whole conversation on the drift sweep + decompose + decisions (and *not* code) is the cheap way to keep the heavy build test-first and `main` never half-flipped — the re-slice and the five build-detail decisions would otherwise have surfaced mid-implementation, the most expensive place to discover them.
- **2026-06-15 — Session 22: M3.S3 shipped — `JudgeAgent` Stage 3 (LLM-as-judge), test-first, squash-merged green (PR #60).** The cascade's Stage-3 rung: an LLM judge that, for a candidate Stages 1–2 left *ambiguous*, returns a strict verdict `{match, confidence, reasoning}` and routes it to a merge proposal or "new/uncertain". Router-wired like `ExtractionAgent` (`weight="medium"`/`task_type="judge"` → cloud_free, **DM5 resolved**); the schema is strict (a degenerate body — out-of-range confidence, blank reasoning — fails and retries); `classify_verdict` merges only on a confident *yes* (`match AND confidence > 0.8`, strict, fail-closed — a confident *no* never auto-merges). The prompt follows spec **Appendix C.3** in full (PL + EN), injection-safe. **Proposal-only/unwired** — INV-8 is still the live no-dedupe contract until M3.S4, so nothing reaches the graph. Carried the **rule-of-three fold**: the per-agent retry loop became `agents/validation.py` `validate_with_retry`, with Chunking + Extraction refactored onto it, and `TaskWeight` + a `Router` Protocol promoted into `adapters/llm/base.py`. **Reviews — the layered gate earned its keep again:** `/review-pr` (primary) passed, but the multi-agent **`/code-review` caught a real spec-fidelity gap the single pass missed** — the prompt, built from §3.3 *prose*, had dropped Appendix C.3's existing-entity `aliases` (the diminutive signal Stage 3 exists for) + candidate `type`/`properties`; folded to match C.3 before merge. The GitHub Codex secondary reviewer was usage-limited (didn't run) — best-effort, not blocked on. **Spec §3.3 amended** (stop-and-amend, owner-approved) with the Stage-3 merge-rule clarification. **Checks:** 202 unit tests pass, ruff+format+mypy clean, CI green 4/4. **Retro (1 change):** encoded *"build an agent prompt from the spec's Appendix C template, not the §3 prose alone"* into `backend/src/story_forge/AGENTS.md` + `/review-pr` §2 — a building-time guard so the C.3-omission class is prevented at authoring, not only caught at review. **Lessons:** (a) the spec specifies each agent's prompt in *two* homes — §3 prose and the authoritative Appendix C template — and building from the prose alone silently drops fields; (b) with the secondary reviewer unavailable, the multi-agent `/code-review` is the safety net that catches what one self-review can't — direct evidence the "run `/code-review` for substantive code" rule pays for itself.
- **2026-06-12 — Session 21: M3.S2 shipped — `MatchingAgent` Stage 2 (embedding cosine) + the pgvector foundation, test-first, squash-merged green (PR #58).** Built Stage 2 and the whole pgvector persistence under it, **foundation-only**: the embedding stack is built + tested but stays out of the live extraction path until the cascade is wired (M3.S4) — mentions are recorded with `embedding=None`, mirroring the spaCy/PreNER built-but-unwired precedent and honouring INV-8 (no behaviour change yet). The mid-session call (foundation-only vs wire-now-with-graceful-degradation) was surfaced as a `/plan`-mode question and decided with the owner before any code. **Shipped:** `pgvector==0.4.2` (core) + an optional `embeddings` group (`sentence-transformers`/`torch`/`transformers`/`huggingface-hub`, ~2 GB, CI-skippable); a scoped **torch OSV waiver** (`GHSA-rrmf-rvhw-rf47`, MEDIUM, `torch.jit.script` unreachable, no fix); the §6.7 HF-model pin (`paraphrase-multilingual-mpnet-base-v2`, 768-dim, by commit SHA via `scripts/pin_hf_model.py`, verified-at-build on CPU); an `entity_mentions.embedding vector(768)` migration; `register_vector_async` centralised in a new `db.connect` helper across all three connect sites; the paragraph reads un-stubbed from `NULL AS embedding` to the real column; and `MatchingAgent.stage2` (max cosine, strict `>0.85`, `match_stage2_cosine_merge` in config), proposal-only/unwired. **Reviews:** `/review-pr` caught a flawed `stage2` sentinel (`-1.0` → `float("-inf")`; + regression test); `/code-review` caught a `db.connect` connection leak on the registration-failure path. Both folded before merge. **Checks:** 231 unit+integration pass (incl. 2 new pgvector round-trip tests), ruff+mypy clean, dep-age + OSV green, full CI green. **Retro:** generalised the `backend/AGENTS.md` detect-secrets note to cover any high-entropy hex literal (the pinned model SHA tripped it, where before only Alembic ids were documented). **Lessons:** (a) when a step bundles a behaviour change (here, wiring a heavy optional dep into the live path) under foundation work, *surface it as an explicit scope question* — `/plan` + AskUserQuestion caught it before code, exactly where the cost is lowest. (b) a handoff that hardcodes an infra id (`down_revision`) drifts when later work moves the head — verify against `alembic heads` at write time, don't trust the literal.
- **2026-06-11 — Session 20: M3.S1 shipped — `MatchingAgent` Stage 1 (RapidFuzz), test-first, squash-merged green (PR #56).** The first M3 code: a deterministic Stage-1 matcher (token-set ratio of a candidate surface form vs existing entities' `canonical_name` + aliases → auto-merge-proposed / ambiguous / new-proposed by the §3.3 thresholds), proposing only — nothing reaches the graph (INV-1), and INV-8 is untouched (still the live no-dedupe contract until M3.S4). Pure `classify` band logic is CI-tested without RapidFuzz; the agent imports RapidFuzz directly (the deterministic-local-compute exception). Resolved the register's gating decisions: **DM1** (thresholds → `config.py`), **DM2** (`paraphrase-multilingual-mpnet-base-v2`, 768-dim verified), **DM3/DM4** (per-mention vectors on `entity_mentions`, max cosine). Amended spec **§6.7** with a new **HuggingFace-model pinning channel** (immutable commit-revision SHA via `snapshot_download`) for M3.S2's embedding model. **Re-sliced** the session: M3.S1 = Stage 1 only; Stage 2 + the DM6 write-path refactor (which *retires INV-8*) move to M3.S2+, once the embedding stack's real cost surfaced — `torch`'s fail-on-any (LOW, unreachable) CVE needs a scoped OSV waiver, the stack is ~2 GB, and the write-path change is a behaviour shift (empty graph until the review queue), none of which belonged buried in the Stage-1 diff. **Self-review caught a real spec-fidelity bug** (`classify` used `>=85`, but §3.3 makes merge strictly `>85` with 85 in the Stage-2 band) and folded the fix + the corrected test before the PR. `rapidfuzz==3.14.5` pinned (OSV-clean). Checks: 164 unit pass, ruff+mypy clean, dep-age + full CI green. **Retro:** no rule change — the session's friction was over-analysis-before-output ("stalling"), which existing guidance already covers ("when you have enough information to act, act"). _Lesson: when a session's scope hides a security waiver or a behaviour change behind unrelated work, split it — a small reviewable PR beats a big one with a buried decision._
- **2026-06-11 — Session 19: the two M2→M3 roll gates ran before any M3 code, and the first M3 design decision (DM6) is made — a docs/vault-only governance session.** No production code. **(gate 1 — cross-cutting curation, `docs/AGENTS.md` §3)** walked every `### Cross-cutting` item by hand, re-titled the list M2→M3, and recorded a dated curation note flagging which items bite M3 (embedding read path, the agent-retry/​envelope-unwrap rule-of-three — M3's matching/judge agent is the n=3 trigger, store-down→503, graph story-vs-project scoping, `last_call()` tiebreaker, the §6.5 dropdown); fixed one stale pointer (store-down said "lands with M2.S5" — M2.S5 shipped without it → re-pointed to M3). **(gate 2 — architecture catch-up, the vault lagged M2.S5 *and* M2.S6)** ran `meta-architect:review-architecture` → `architecture/reports/2026-06-11-architecture-review.md` (no blockers; headline `risk`: **INV-2's consent gate lost its M2.S5 landing target** and the M2.S6 smoke **fired real paid egress gate-less** — the OQ-6 fail-open-by-sequencing window, now realised; accepted at PoC scale, re-pointed to the M3 review-queue UI; plus freshness drift), then `meta-architect:decompose-requirement` step-0 → `architecture/proposals/m3-cascade-matching.md` (register **OPEN**: nine-layer/nine-station pass on the §3.3 cascade, Mermaid data-flow, 8-entry decision register, a but-what-if pass — intra-batch dupes, review-gate TOCTOU, fail-closed embedding/judge outages, relation re-pointing on merge, rejected re-surfacing). Drew the vault's **first state machine**, `architecture/state-machines/candidate-lifecycle.md`. **(vault reflection of the roll decisions — discharges the tracked cross-cutting item)** added OQ-14 (model-override dropdown vs INV-7), OQ-15 (operational logging absent ⇒ INV-6 *vacuously* true), OQ-16 (the M3 register); regenerated INDEX; +5 learning-log lines (vacuous truth, accepted-deferral-vs-silent-drift, temporary-invariant hand-off, cheapest-first cascade, gate-the-write-vs-dedupe); folded the review's recommended freshness fixes (honest as-built, decision-independent): INV-2 consent gate re-pointed, INV-5/OQ-9 `latency_ms`→as-built M2.S5, overview→M0–M2.S6 built/M3 next/Monitoring ✅. **(decision — DM6, owner)** the M3 cascade **gates the graph write — (A) intercept-before-write**: extraction *stages* candidates (Postgres), Neo4j is written **only** on human accept in the Stage-4 queue. This **retires the temporary INV-8** rather than layering on it, and lands **INV-1's** first enforcer; cost accepted = M2.S4's CREATE-on-extract path is refactored and the graph is empty until review. *Rejected (B) write-then-dedupe* (a dirty graph until review contradicts INV-1). **ADR 0004 + the INV-8→INV-1 fold in `invariants.md` + finalising the candidate-lifecycle note are deferred to the M3-code session, test-first** — the invariant flip is witnessed by the failing test, not asserted ahead of it. Register items DM1–DM5, DM7, DM-rej stay OPEN. **Checks:** docs/vault only — no backend/frontend code touched, so the code gates are no-ops; pre-commit (detect-secrets, whitespace, EOF) passed. **Retro: no skill/CLAUDE.md changes** — the rituals served well (the `review-architecture` sweep caught the INV-2 consent-gate drift the plan alone would have missed; the decompose framed DM6 cleanly); the only friction was cosmetic (very long single-tool writes read as stalls — a working-style note, not a rule). **Lessons:** (a) a deferral with a *named landing milestone* rots into silent drift the moment that milestone passes unmet — a milestone-boundary `review-architecture` sweep is what catches the slip (INV-2's lapsed M2.S5 target). (b) an invariant can be *vacuously* satisfied (INV-6 "keys never logged" holds only because nothing logs) — name the vacuity so it's re-examined when the precondition arrives. (c) the milestone roll is the right moment to draw the first state machine — the candidate lifecycle becomes live code in M3, and DM6 (gate-the-write) is exactly the kind of branchy call a step-0 decompose exists to surface before code.
- **2026-06-11 — Session 18: M2 closed via a thin M2.S6, squash-merged green (PR #53) — the M2→M3 roll decision made; the entity-extraction milestone is done.** A roll/governance session (no production code): decide M2.S6, run it thin, and tee up M3. **(roll decision, owner)** option **(b) thin S6** — close M2's genuine remainder rather than carry it into M3 cross-cutting. **(what shipped)** `scripts/check_openrouter.py` (a standalone OpenRouter connectivity smoke mirroring the M0 `check_ollama_cloud.py` — OpenRouter is *not* wired into the running app, so this is how its real `cloud_strong` egress is exercised; model from `OPENROUTER_MODEL`, no hardcoded default since free ids churn); an honesty-fix to `backend/AGENTS.md` (it claimed "structlog … never log auth headers" but **structlog isn't a dependency and nothing logs at all** — so §6.7 "keys never logged" holds *vacuously* today); a documented **"Manual real-provider smoke + §6.7 key-leak check"** procedure (the regression guard for when logging lands); a new `docs/PLAN_LONG.md` **"Operational logging & observability — later"** section (operational logging is a real later gap, distinct from training-data capture — which is the `llm_calls` ledger + planned `edit_history`, *not* stdout logs, a conflation worth naming). **(smoke verified, real keys)** Ollama Cloud + an OpenRouter free model both returned 200; the key-leak grep over the captured output was clean for both. **(deferred, with rationale)** the **§6.5 model-override dropdown** is a *feature*, not thin polish — the §8.5 panel already does the *surfacing*; the remaining half is a user model-override control + per-task-type preferences that collides with **INV-7** (routing is system-derived, never caller-asserted — closed in M2.S2), so it needs an INV-7/spec reconciliation + router plumbing + a preferences store. Recorded in Decided + cross-cutting; lands as its own `decompose-requirement` step-0 (with/after M3). Direct vendor adapters: **shipped none** (OpenRouter suffices). **(reviews)** `/review-pr` (primary gate) ran the §2 reconciliation sweep and caught the S6 roll-decision blockquote still reading "pending" after the dropdown bullet was struck → folded; a cross-cutting tracker added so the **architecture-vault reflection** of these roll decisions (the vault's snapshot predates M2.S4 *and* M2.S5) isn't forgotten at the next meta-architect run. **Checks:** all 4 CI jobs green on PR #53; `scripts/check_openrouter.py` ruff-clean. **Retro:** **no skill/CLAUDE.md changes** — the reflection surfaced two honesty issues (a prior handoff mislabeling a feature as "thin polish"; the latent structlog doc-drift), both fixed this session and both already covered by — or structurally uncatchable by — existing rules (the dropdown mis-scope was caught precisely *because* `/resume-session` mandates reading the spec, not memory; the latent doc-drift isn't in any diff, so no `/review-pr` rule could fire on it). *Worked well, kept:* `/review-pr` §2 and `/resume-session`'s read-the-spec rule both earned their keep. **Lessons:** (a) a "thin/optional polish" self-label in a roll-handoff isn't load-bearing — read each remaining item's actual spec requirement + invariants before trusting the estimate (a UI control that lets the user assert what was system-derived is a feature). (b) a convention file (`AGENTS.md`) stating an unimplemented capability in present tense is real portfolio drift a stranger would believe — but it lives outside any diff, so it's caught by working-nearby or a periodic sweep, not PR review.
- **2026-06-11 — Session 17: M2.S5 completed, reviewed, and squash-merged green (PR #51) — the read-only graph viewer + agent-activity panel are live; M2's pipeline is visible end-to-end in a browser.** **(step 0)** Resolved **OQ-9 (latency) → option (a)** via the stop-and-amend flow: spec §6.6 records `latency_ms`, reconciled across the vault (INV-5/OQ-9/INDEX) + plan. **(backend surface, test-first)** `llm_calls.latency_ms` (migration `2b5e854961e1`) captured by the router as the elapsed monotonic duration around each provider attempt (`None` only for a pre-dispatch budget refusal); `GET /stories/{id}/graph` (thin `GraphNode`/`GraphEdge` projection, story→project resolution, 404 declared; `Neo4jRepo.list_entities` added); `GET /llm/status` extended with `last_call`. OpenAPI snapshot + typed client regenerated. **(frontend)** `cytoscape==3.33.4` (vetted via `/add-dependency`); `features/graph-viewer/` (force-directed, colour-by-type, node-details panel, extraction trigger; cytoscape isolated in `GraphCanvas`, pure mapping in `graphElements`); `features/agent-activity/` (§8.5 panel over `/llm/status`); hooks (`useStoryGraph` SWR, `useLlmStatus` poll, `useExtractStory`); route lazy-loaded (main bundle 226→83 kB gzip). **(reviews)** `/review-pr` + 7-angle `/code-review` (folded: `nodeLabel` reuse, E2E `?mode=manual` precision, monotonic-clock docstring; two cross-cutting deferrals tracked — story-URL/project-data scoping, `last_call` tiebreaker). **External PR review (P2) caught a spec/as-built contradiction both my passes missed** — spec said `latency_ms` "always recorded" but the code records `None` for pre-dispatch refusals → amended the wording (not the code; recording a fabricated value would be dishonest) across all four homes. **(browser smoke)** verified live: cytoscape renders force-directed + colour-by-type + labelled edges, node-details populates, panel shows real latency (≈19.8 s for `gpt-oss:120b-cloud`)/tier/model/`success`, and duplicate nodes visibly demonstrate the no-dedupe INV-8 the M3 cascade will resolve. **Checks:** ruff/format/mypy --strict clean; **201 backend + 51 frontend** tests; all 4 CI jobs green (one post-merge `main` failure was a transient ghcr.io image-pull timeout in the SCA step — re-run green; not a code issue). **Retro (4 changes, all approved):** `/review-pr` §2 gained an **absolute-quantifier check** (a spec amendment's "always/every/never/all" must match the code's exception path — the exact PR-#51 miss); `backend/AGENTS.md` got a **collation-mismatch** troubleshooting note (host-glibc upgrade blocks the local integration tier); `frontend/AGENTS.md` got a **smoke-walk CORS/port note** (Vite→5174 falls outside the §6.7 allowlist); `frontend/src/AGENTS.md` got a **test-first-for-React nuance** (strict for logic/hooks; presentational components shaped-then-tested, every behaviour covered + untestable surface isolated). **Lessons:** (a) an absolute quantifier in a spec amendment is exactly where as-built drift (a nullable field, an early return) hides — grep the exception path before writing "always". (b) "PR green, main red" is usually an infra flake (registry pull timeout) on a job that does external I/O — diagnose + re-run, don't assume a regression. (c) isolating the one jsdom-untestable surface (`GraphCanvas`) keeps the rest unit-testable and the browser-smoke scope tiny.
- **2026-06-10 — Session 16: M2.S4 completed, reviewed, and squash-merged green (PR #48) — the entity graph is now written.** Finished the wiring half on `feat/m2s4-neo4j-writes`, test-first. **(built)** `agents/extraction_graph.py` `proposal_to_graph` (pure candidate→graph mapping: provisional `canonical_name` = surface form in the project language with the EN peer null, within-paragraph relation resolution, dangling endpoints dropped, no dedupe); `domain.EntityMention` + `postgres_repo` insert/list + `list_story_paragraphs`; `adapters/postgres_mention_store.py` (own-connection commit = durable resume checkpoint); the resumable `ExtractionCoordinator` batch driver; and `POST /stories/{id}/extract` (200 complete / 202 paused+partial, re-POST resumes / 502 hard-failure), wired in `main.py` (router → cloud_free). **(folded gaps)** blank `subject`/`predicate`/`object` validator on `RelationCandidate` (PR-#42); `ChunkingAgent` `ProviderResponseError`→`ChunkingError`→502. **(CI)** added a `neo4j` service to the backend job so the graph integration tests run at the gate (they were dead without it) + updated the `integration` marker. **(reviews)** `/review-pr` + `/code-review` (folded: dedup the `entity_mentions` INSERT via delegation, `.env.example` parity, a docstring honesty note); **external review caught a real bug both my passes missed** — the resume checkpoint (`entity_mentions`) was written *before* the relation write, so a transient Neo4j relation failure left a "done" paragraph with its relations lost forever → reordered to write the checkpoint last; also mapped router transport (`httpx.HTTPError`) → 502. **(decisions)** OQ-1 resolved (Neo4j-then-Postgres, accept eventual inconsistency), OQ-2 resolved (driver owns the pause-and-ask → 202), `extraction_model` default `gpt-oss:120b-cloud`. **(security)** the new neo4j scan + sequential-Trivy unmask surfaced CVE-rot — 2 neo4j netty HIGHs + the same OpenSSL `PKCS7_verify` UAF (CVE-2026-45447) on all three image bases — all waived with per-CVE reachability (unreachable on a 127.0.0.1 single-user deployment), verified against the GitHub Advisory DB / OpenSSL advisory; `WAIVERS.md` updated. **(architecture)** reconciled the vault in-PR — overview M2.S4 planned→built, OQ-1/OQ-2 struck-resolved, INV-8 gained an as-built "Enforced at", INDEX next-steps → M2.S5. **Checks:** ruff/format/mypy --strict clean, **188 tests** pass (3 spaCy skips), all 4 CI jobs green. **Retro (2):** `/review-pr` gained a checkpoint/resume-marker ordering check and a multi-range CVE-advisory check. **Lessons:** (a) when a persisted write is a resume marker, it must land after every step it certifies — and you must check *each* inter-write window (I checked entity→mention, framed it benign, and missed the non-benign mention→relation window). (b) A CVE advisory can carry several vulnerable ranges; confirm which one your version is in (the netty fix was in the *second* range). (c) Folding the CVE waivers into the feature PR (vs splitting) kept main green but turned one merge into a 3-round sequential-Trivy chase — expected and flagged, but real.
- **2026-06-09 — Session 15: M2.S4 foundation built + green on an unmerged branch; two latent infra bugs found by running it; pre-M2.S4 architecture sweep; 4 retro changes.** Started M2.S4 (Neo4j writes, no dedupe), test-first, on `feat/m2s4-neo4j-writes` — **deliberately not merged** (half-feature, and the integration test would be dead at the CI gate without a Neo4j service). **(build)** `neo4j==5.28.4` (5.x LTS to match the 5.26 server); `domain/graph.py` (`GraphEntity`/`GraphRelation`, distinct from the agent's candidates); `adapters/neo4j_repo.py` (create/get entity+relation, count, delete — `CREATE`-not-`MERGE` for INV-8, backtick-escaped open-world type = Cypher-injection-safe, `properties` as JSON); the **`entity_mentions` Alembic migration** (`entity_id` carries no Postgres FK — the OQ-1 cross-store seam). Integration tests (entity/relation round-trip + the no-dedupe property) **green against live Neo4j**; migration smoke test green. **(two latent infra bugs, found only by *running* it — the value of "let's check how it works")** the compose Neo4j had **crash-looped since M0** — `NEO4J_server_metrics_enabled` is an Enterprise-only setting Community rejects under strict validation; nothing had ever booted it (the security job only Trivy-*scans* the image, `docker compose config` only validates syntax) — **fixed**; and **CI has no Neo4j service** (the plan's "CI's neo4j service runs" was false) — deferred to Session 16. §6.7 Neo4j telemetry (`client.allow_telemetry`, `dbms.usage_report.enabled`) disabled + **verified via `SHOW SETTINGS`** (not guessed). **(architecture)** ran `meta-architect:review-architecture` as a pre-M2.S4 sweep — found `overview.md` 3 sessions stale (M2.S2/S3 still "planned"), INV-5's OQ-10 gap unflipped, the `entity_mentions`-not-in-schema trap, and the INV-8-via-`CREATE` + router-error→HTTP forward risks; folded the approved drift-fixes (overview/invariants/PROJECT/open-questions/INDEX + the m2s2 `route()`→`complete()` doc-hygiene, both spots). **(spec)** amended §6.4's `:Entity` node cypher line to list `first_seen_paragraph_id` — it was in §3.2 + the code but omitted from the node illustration (an incompleteness, not a design change). **Checks:** ruff/format/mypy --strict clean, **161 tests** pass (3 spaCy-model skips), dep-age clean. **Committed** as 4 clean commits (`docs(architecture)` / `docs(plan)` / `fix(infra)` / `feat(graph)`); **nothing pushed/merged.** **Retro (4 changes):** sharpened **both** `/resume-session` and `/wrap-session` to verify infra *runs* (boot the service / grep `ci.yml`), not just that config files exist — the handoff twice asserted false infra state this session (`entity_mentions` "already in schema"; "CI runs neo4j"), so the fix is both the reader (resume) and the author (wrap); corrected the `backend/CLAUDE.md` `down_revision`-pragma note (both revision lines can trip detect-secrets); added a `backend/CLAUDE.md` note that a TDD integration test for a new module must import cleanly or it breaks the fast-tier *collection*. **Lessons:** (a) *scanned/config-valid ≠ runs* — a service only ever image-scanned and config-validated can be broken for milestones; smoke-run it. (b) a handoff "Verify on disk" that only checks file presence misses live infra state — the recurring false-claim pattern is why the retro touched both ritual skills. (c) a failing test that imports a not-yet-created module takes the *unit* tier down too (pytest imports before deselecting by marker), so create the module skeleton alongside the red. (d) wrapping mid-feature (foundation green, unmerged) is a legitimate, explicitly-agreed exception to the merge-then-wrap order.
- **2026-06-08 — Session 14: backend SCA gate built + merged (PR #44); client DevSecOps notes folded into PLAN_LONG; one retro change.** Built the accepted `backend-dependency-advisory-scan` proposal on one branch, test-first (the gate's own red→green self-test). **(build)** New `osv-scanner` step in the CI `security` job scans `backend/uv.lock` **fail-on-any**; scanner pinned by **immutable image digest** (`ghcr.io/google/osv-scanner@sha256:64e8…`, v2.3.8) — *not* a GitHub Action: `google/osv-scanner-action`'s root `action.yml` is a no-`runs:` stub and its reusable workflows do diff/SARIF scans, neither a scoped fail-on-any step; a digest pin is stronger than the proposal's planned SHA-pin (D6 intent preserved, mechanism changed). Scoped waiver scaffold in a sibling `infra/osv/` (`osv-scanner.toml` enforced via `--config` + `WAIVERS.md` register, condition-based "drop when a fixed version clears soak"); no active waivers. **(fix = self-test)** `starlette` 1.0.0→1.0.1 explicit pin in `pyproject.toml` (17 days old, OSV-clean) cleared GHSA-86qp-5c8j-p5mr — the gate went **red on 1.0.0, green on 1.0.1** in CI, exactly as designed. **(reconcile)** spec §6.7 amended (new "Backend dependencies advisory-scanned" bullet + the M0 CI enumeration); README local-gates snippet; `/review-pr` §5 extended to SCA waivers + scanner-pin-drift; the architecture vault (proposal whole body, INDEX, OQ-13) brought fully to resolved; the M2 cross-cutting "backend advisory scan" item struck. **(reviews)** `/review-pr` folded a stale acceptance-banner §2 finding *its own grep sweep caught* (the rule earned its keep); `/code-review` found no real bugs (its candidates — "are the `osv-scanner` flags right?" — were refuted by the live CI red→green + a local `--config` waiver test). External review's two notes addressed: P1 (gate red on commit 1) is the intermediate branch state only — squash-merge lands gate+fix atomically, `main` never red; P2 (decision-state drift) folded. **(client notes)** A practitioner's DevSecOps notes folded into `docs/PLAN_LONG.md` (new "Security & DevSecOps hardening — later"): the research-takes-precedence principle + two not-scheduled items (adopt Anthropic `security-review` as a standing gate; an automated architecture-conformance CI check). The self-hosted-Gitea suggestion was deliberately not pursued (public-GitHub-portfolio model; client said skip). **Checks:** ruff/format/mypy --strict clean, **161 tests** pass, dep-age clean; PR #44 merged green (all 4 CI jobs). **Retro (1 change):** added a **verify-at-build** rule to `meta-architect:decompose-requirement` — a register decision resting on un-inspected external-tool behaviour is a hypothesis tagged `verify-at-build`, not settled. **Lessons:** (a) a decompose register can assert external-tool *mechanics* ("SHA-pin the Action", "parses uv.lock natively") that only survive contact with the tool — verify before trusting. (b) a security gate's *failing* state is its test — landing the gate red-then-fixing-green on a branch (squash-merged) is legitimate test-first, not a broken build. (c) the proactive `/review-pr` §2 grep sweep caught the stale home a spot-by-spot reconcile missed — run it before claiming clean. **Follow-up (PR #46, post-wrap):** moved every GitHub Action off the deprecated Node 20 (`checkout@v4→v5`, `setup-python@v5→v6`, `setup-node@v4→v5`, `setup-uv@v6→v7`; each Node-24 confirmed via `action.yml` `runs.using`, soaked floating major chosen over too-fresh newer majors) — surfaced as a CI warning during the wrap, fixed in two passes (the fix-N-unmasks-N+1 shape: grep *every* "Node.js 20" line up front).
- **2026-06-08 — Session 13: M2.S3 ExtractionAgent built + merged (PR #42); SCA gate decomposed + accepted; one retro change.** The first M2.S3 *code* session. **(build, test-first)** `ExtractionAgent` — the first agent wired to the `LLMRouter`: render prompt → `router.complete(weight="medium", task_type="extraction", json_schema)` → validate → retry on schema failure (ChunkingAgent shape); per-paragraph, single-paragraph (batch driver → M2.S4); `Budget`/`Quota` pause-and-ask propagate. Output = `ExtractionProposal` (surface-form `candidate_name`, open-world `type`, dangling relations accepted, soft-flagged `evidence_quote`). Plus `prompts/extraction.{pl,en}.j2` (injection-safe) and the **OQ-10 typed `ProviderResponseError`** (both adapters raise on a malformed-`200` envelope — missing fields, unparseable body, **null content**; router records a failure row + fails over like a 5xx). Shared `extract_json` moved to `agents/json_output.py`. **(reviews)** `/review-pr` then `/code-review` (high) — the **second gate caught a real bug the first missed**: a `content: null` 200 envelope crashed the router uncaught; folded a guard + tests. Also folded: `_Router` `weight` → `Literal` (unblocks M2.S4 wiring), dropped the dead `prener_hints` param, hardened `extract_json`, blank `evidence_quote` → `None`. **(post-merge external review)** caught a gap *both* my passes missed — blank relation `subject`/`predicate`/`object` validate as success; tracked cross-cutting (fix M2.S4 or a quick PR). **(SCA decompose)** a `starlette` 1.0.0 Dependabot alert (medium; CI didn't catch it — Trivy scans only images) triggered a `meta-architect:decompose-requirement` on a backend dependency-advisory scan; owner approved the G1–G7 cluster (osv-scanner, fail-on-any + scoped waivers, SHA-pinned Action, §6.7 baseline control, explicit `starlette==1.0.1` pin) — proposal `accepted`, **build deferred to next session (one branch), §6.7 amendment lands with it**. **Checks:** ruff/format/mypy --strict clean, **134 unit tests** pass; PR #42 merged green (all 4 CI jobs). **Retro (1 change):** folded **field-validator symmetry** into `/review-pr` §4 — when a schema validates one field for an invariant, audit every sibling field with the same semantic (the blank-relation miss). **Lessons:** (a) the layered review (own `/review-pr` → `/code-review` → external) earned its keep — each caught something the prior missed, all of the "degenerate-but-parseable output validates as success" family. (b) a non-empty validator on one schema field is a *smell to check its siblings*, not a box ticked. (c) `meta-architect:decompose` is useful beyond features — it framed a CI/security baseline decision cleanly.
- **2026-06-08 — Session 12: M2.S3 *design* settled + spec §6.5 amended + review-model adapted + CVE wave-4 — docs/spec/governance only, no product code (PRs #39, #40).** Set up M2.S3 so the next session writes code with zero ambiguity. **(decompose, step 0)** Ran `meta-architect:decompose-requirement` on M2.S3 by hand → `architecture/proposals/m2s3-extraction-agent.md`; the owner walked the register, so it's now `accepted`/resolved: per-paragraph granularity, single-paragraph agent (resumable batch driver → M2.S4), `candidate_name` (surface form, not `canonical_name`), typed `ProviderResponseError` for malformed-`200` envelopes (OQ-10), soft-flag `evidence_quote`, known-entities as params now / PreNER hints deferred. Glossary +2 (`prompt-injection`, `poison-message`); OQ-11/OQ-12 resolved, OQ-2/OQ-10 advanced; INV-5 ledger-coverage claim narrowed for the OQ-10 gap. **(spec)** Amended §6.5 router contract `route(self, task) -> LLMProvider` → `complete(messages, *, weight, task_type, json_schema=None) -> CompletionResult` and split §6.5/§11 "Failover" (envelope-malformed → failover vs schema-invalid → agent retry), via the stop-and-amend flow + a whole-repo reconcile (README, plan, vault); ADR 0001 + M2.S2 history left intact. **(process)** ChatGPT subscription cancelled → adapted the merge flow: `/review-pr` is the **primary** review gate, Codex review **secondary/best-effort** (never block on it), `/code-review` added for substantive code; recorded in root `AGENTS.md` "Review model". **(security)** A pre-existing fresh CVE — **CVE-2026-42504** (Go-stdlib `mime.WordDecoder.DecodeHeader` DoS, GO-2026-5038) — surfaced on the docs PR's Trivy scan (re-scan vs the live DB); diagnosed as not-introduced-by-the-PR, verified attribution vs NVD + pkg.go.dev, scoped-waived for pgvector + ollama in one pass (handling the sequential-unmask), green `main` restored (PR #40, wave 4 of Issue #22). **Review:** self-`/review-pr` on PR #39 caught + folded one §5b heading miss; **external review** then caught **three** more §5b/reconciliation misses (the INDEX row, the next-steps, the plan's `canonical_name` task) my pass had claimed clean — all folded before merge. **Checks:** no code touched, so backend/frontend gates N/A; both PRs merged fully green (backend/frontend/security/ollama-smoke); pre-commit passed on every commit. **Retro (2 changes):** sharpened `/review-pr` §2 with the *same-PR proposal status-flip* class (grep the proposal's own slug + status words across `INDEX.md` + both plan files; diff each decision against the task it changes) and added a *solo-review-blind-spot* note to the review model (run the §2 sweep as an explicit pass on decision-state-flip PRs). **Lessons:** (a) flipping a proposal `proposed→accepted` leaves its stalest copies in the *navigation/plan* homes you wrote earlier the same session — keyword-grepping the *decision* misses them; grep the *status*. (b) Sole reviewer = a real blind spot; self-review claimed §2 clean and still shipped three stale homes — `/code-review` + an explicit reconciliation pass are the mitigations. (c) A docs-only PR can still go red on a freshly-disclosed image CVE; the green-main "pre-existing/unrelated/diagnosed" exception + `/pin-image` waiver flow handled it without scope-creep.
- **2026-06-02 — Session 11 complete: M2.S2 built — paid LLM tier + router + cost ledger (PR #36, squash `919f9ce`).** First product code on the M2.S2 decisions (ADR 0003). Shipped, test-first, in 6 commits: **(seam)** `adapters/llm/base.py` grows `Usage` on `CompletionResult` + `cost_per_1k_tokens`/`rate_limit_kind` on the Protocol + `estimate_cost` + the pause-and-ask exceptions (`BudgetExceededError`/`QuotaExhaustedError`); `OllamaProvider` stops discarding token counts (INV-5). **(adapter)** `OpenRouterProvider` — hand-rolled `httpx`, OpenAI-compatible, system-derived usage + USD estimate, HTTP 402→`BudgetExceededError`. **(router)** `LLMRouter.complete(weight, task_type)` — tier selection, error-discriminated failover (429/5xx/**transport**→fail over, 401→fail over but not quota, other 4xx→re-raise, 402→don't escalate), fail-closed `DAILY_BUDGET_USD` gate (free tiers never blocked), pure-429 cloud_free exhaustion→`QuotaExhaustedError`. **(ledger)** `llm_calls` migration + `PostgresCostStore` (one row per call incl. refusals/failures; **commits independently** so a failure row survives a request rollback). **(api)** `GET /llm/status` for the M2.S5 panel. Router wired into an agent in M2.S3 (ExtractionAgent); the cost store is wired now (status endpoint reads it live). **Checks:** 128 backend tests (unit + integration vs real Postgres) + 29 frontend; ruff/format/mypy --strict + eslint/prettier/tsc/vite-build clean; no new deps (hand-rolled httpx). All four CI jobs green pre-merge. **Review:** `/review-pr` (own) found + tracked the malformed-`200`-envelope record/failover gap (deferred to M2.S3); **Codex** caught two real router bugs my pass missed — `httpx.RequestError` (transport) uncaught, and every exhausted `cloud_free` tier mislabelled "quota" regardless of cause — both folded with tests, plus the GPU-quota-remaining→M2.S5 deferral (reconciled in plan + Decided; spec goal judged unchanged) and the `api_key` test-literal binding nit. **Retro (2 changes):** sharpened `backend/AGENTS.md` on `detect-secrets` (credential var name must avoid the keyword; alembic revision ids need `# pragma: allowlist secret` — 3 blocked commits this session), and added a `/review-pr §1` **failover/error-taxonomy** check (enumerate HTTP status families *and* transport/timeout errors; verify the exhaustion branch classifies by actual cause — citing this PR). **Lessons:** (a) error-discrimination code needs the *full* exception taxonomy enumerated — catching `HTTPStatusError` but not `RequestError` drops every network failure, and "all failed → quota exhausted" is dishonest when the cause was a bad key; both shipped and both were review-caught, now a written `/review-pr` check. (b) recording a *failure* must outlive the request that fails — the cost store commits on its own connection. (c) `detect-secrets` foot-guns (keyword-in-var-name, alembic hex ids) are now documented so the next migration/adapter-test doesn't re-discover them.
- **2026-06-02 — Session 10: M2.S2 router/budget decisions settled via meta-architect dogfood (PR #34, squash `8f14dbd`) — docs-only, no product code.** Second live dogfood. Ran `review-architecture` (the OQ-A drift sweep over M0→M2.S1 + ADRs 0001–0002 — caught a stale ADR-0001 consequence, the INV-5 token-discard seam, the INV-2 consent-vs-egress lag, and a live intra-spec §6.5 contradiction) then `decompose-requirement` on the M2.S2 `LLMRouter` + budget cap (nine-layer pass, data-flow + LLM-call state machine, decision register, "but what if" surface). The architect *framed*; the **owner decided** — the first time the architect's artefacts fed real product decisions. **Decisions** (in **ADR 0003**, superseding ADR 0001's provider-priority + quota-degradation consequences; spec §6.5/§6.6 amended): provider order Ollama→OpenRouter→Grok→Anthropic→Google→OpenAI; **OpenRouter the only paid adapter built in M2.S2**, direct vendor adapters deferred; **hand-rolled `httpx`** (no SDKs, no new deps); **control-first** per-day USD cap, fail-closed, **pause-and-ask** on quota-exhaustion/cap (never silent paid escalation); **INV-5** usage row keeps tokens whenever the provider returns them (incl. Ollama's eval counts); egress consent **deferred to M2.S5** (no sensitive data, documented); Google/Gemini added. The vault was reconciled to the decisions and made **navigable** (`architecture/AGENTS.md` + a root `AGENTS.md` pointer — awareness, not ritual-wiring, which stays deferred per ADR 0002). **Review:** own `/review-pr` + **six Codex passes** — every pass the *same class* (a resolved decision left stale in another home), at progressively smaller scope: accepted-doc body → its long tail → the fact in *other files* (spec §5/§9, README, AGENTS) → *tracking/registry/nav* notes that phrase it differently → a *dated report* framed as live. No P1s; all folded; CI green on every commit; squash-merged. **Retro (the real payoff):** the recurring class became three cross-referencing rules — a **decision-reconciliation lens** in `/review-pr` §2 (verify-by-whole-repo-grep, incl. tracking notes + accepted-doc-fully-resolved + dated-artifact-status; subsumes the Session-9-deferred vault lens), a proactive **authoring rule** in root `AGENTS.md` "Where knowledge lives", and a **§5b resolution step** in the meta-architect `decompose-requirement` skill. **Lessons:** (a) the dogfood *works* — the architect's decompose/review produced decisions a stranger can audit; (b) an *accepted* design artefact is a maintenance surface — resolving its register isn't enough, the whole body + every host-repo home + the status-tracking notes must be swept, **proactively**, or review finds the leftovers (six times); (c) reconciling the authoritative source is not reconciling the decision — a fact has many homes, some keyword-invisible.
- **2026-06-02 — Session 9: meta-architect dogfood interlude — `architecture/` vault initialized (PRs #29/#30/#32; issue #31) — docs-only, no product code.** First live use of the in-repo meta-architect (`docs/decisions/0002`). The session opened by *catching drift*: `/resume-session` found a forgotten Session-8 improvement (the three `review-pr` retro lessons) sitting uncommitted in the tree → landed it cleanly as **PR #29** (squash `b0925a8`) before anything else. Then ran `initialize-project-architecture`: interview (operator self-rated **novice** → verbose teaching tier; vault **committed**; both readers), read-only scan, classified Story Forge as a web app whose centre of gravity is the §7 ingest pipeline, and scaffolded the `architecture/` vault — `PROJECT`, a nine-layer `overview` grounded in the **as-built present** (M0→M2.S1 done; M2.S2+ planned), 8 named `invariants`, `open-questions` (the operator's *review-then-strategize* queue), a 14-term cross-linked `glossary/`, `learning-log`/`CHANGELOG`/`INDEX` — landed as **PR #30** (squash `bfbd37f`). **Reviews:** `/review-pr` (own) verified the no-duplication promise, wikilink/frontmatter integrity, and as-built fidelity by grep, not eyeball; **two Codex passes** then caught real drift — a "Built in M2.S2" overclaim, the `overview` Data layer + `cascade-matching` restating spec schema/algorithm (trimmed to references), present-tense descriptions of unbuilt features (`PROJECT` Identity, INV-2 → reframed planned-vs-built), and a source-of-truth registry row that implied Alembic owns the Neo4j schema (split Postgres/Neo4j). All folded; each fold swept for its class. **Cross-agent hazard, handled:** Codex (host-Windows/UNC) edited `AGENTS.md`/`.codex/` in the *shared* working tree mid-session; kept out of PR #30 and landed separately as **PR #32** (squash `ec31031`). **Checks:** docs/markdown only (no `.py`/`.ts`/deps); full CI green on all three PRs before each squash-merge; `main` green. **Retro:** added a **§0 concurrent-agents** shared-tree rule to root `AGENTS.md` (stage explicit paths, surface-don't-absorb foreign changes) — mutual with the Codex-side `.codex/` notes; deferred a vault-review lens for `/review-pr` to the meta-architect-integration batch; filed **issue #31** (slug/filename carve-out). **Lessons:** (a) the resume ritual earned its place again — the forgotten #29 change would otherwise have polluted a later PR. (b) a vault that *references* sources of truth is harder than it looks: two review passes were needed to strip copied schema/algorithm and fix tense — the no-duplication and as-built-honesty disciplines are now the durable review checks for vault PRs. (c) two agents on one working tree is a structural, recurring condition, not a one-off — now a written rule on both sides. (d) dogfooding produced a real framework fix on its first run (issue #31), which is the point of incubating in-repo.
- **2026-06-02 — Session 8: meta-architect plugin built + merged (PR #27, squash `f3fdeef`) — no product code; dev tooling + governance.** Designed and shipped a domain-agnostic architect plugin via human-in-the-loop co-design: an agent persona (`agents/meta-architect.md`) carrying the doctrine (nine layers incl. a Compliance/Audit split, nine stations, source-of-truth, invariants, state machines, MADR ADRs with auto-escalation, a knowledge-graph glossary, progressive disclosure), three skills (`initialize`/`decompose`/`review`), six note templates, three design ADRs, a README, and a worked JIT-SSH example. Incubated **in-repo** as a self-contained `meta-architect/` folder (extract later by moving it), auto-loaded via committed `.claude/settings.json`; **deliberately out of the product spec** (it's tooling). Also made **`AGENTS.md` canonical** across all 7 directories (each `CLAUDE.md` → symlink; tool-agnostic naming) so Codex reads the same rules Claude does, and added a **self-improving Codex `review-pr` skill** (`.agents/skills/`) plus a cross-environment-reconciliation lesson in Claude's `/review-pr`. **Checks:** no backend/frontend code touched; CI green on all four jobs (backend, frontend, ollama-smoke, security/detect-secrets) across the fold commits. **Review:** `/review-pr` (own) caught a stale `proposals/` path + the Bash-guardrail gap; **Codex converged** on the guardrail (independent confirmation) and flagged a filemode diff that proved a **Windows/UNC false positive** (canonical `git ls-files -s` = `100755`); all real findings folded, the false positive taught to `/review-pr`, and Codex self-improved its own `lessons.md` after feedback. **Lessons:** (a) Claude Code's "iterate standalone, package later" path fits incubation — build in-repo, extract by moving one folder. (b) one source of truth beats a synced copy — `AGENTS.md`↔`CLAUDE.md` symlinks, not duplication. (c) two reviewers in different environments are defense-in-depth *and* a noise source — convergence confirms a finding; divergence can be one reviewer's environment (verify filemode/symlink diffs against canonical git). (d) build the tool before wiring it into the process — the workflow integration is a hypothesis to test next session, not something to hard-code now. **Retro:** done inline — the process improvements this session were made *during* the work and are already merged; nothing left to formalize.
- **2026-06-01 — Session 7 complete: M2.S1 PreNERAgent — deterministic spaCy baseline (PR #25, squash `e80ef94`).** Opened Milestone 2. Shipped `domain/extraction.py` (`CandidateSpan`), `agents/prener_agent.py` (`PreNERAgent.extract` + pure `map_spacy_label`/`candidates_from_entities`, lazy per-language model load, unsupported-language `ValueError`), and PL/EN Appendix-B.2 tests that assert the agent's **contract, not model accuracy** (recall-first baseline — the LLM + human review fix types in M2.S3, and corrections feed a future custom spaCy finetune; `en_core_web_lg` mislabels "Old Bronek" as ORG, which is fine). **Dependency pinning** was the session's hard part: spaCy models aren't on PyPI, so they became a new §6.7 **direct-URL wheel channel** — GitHub-release wheels pinned as PEP 508 `name @ url`, SHA-256-locked in `uv.lock`, soak enforced against the GitHub **asset** upload date (not the tag date — Codex P2), with `check_dependency_age.py` extended to gate it automatically (GitHub-token-authenticated, 403-aware) rather than waiving it. The ~950 MB model wheels live in an optional `models` dependency group so CI stays lean and the model-loading tests auto-skip; a pure `candidates_from_entities` seam keeps `extract()`'s logic CI-tested without the models (own-review #1). **Checks:** ruff + format + mypy --strict clean; CI path 68 passed / 3 skipped, local (`--group models`) 71 passed; dep-age gate green; all four CI jobs green pre-merge. **Review:** `/review-pr` (own) found the CI-coverage gap (#1), a layering note (#2), and GitHub-403 gate fragility (#3) — all folded; Codex's P2 (asset-date vs tag-date) folded with spec/skill/code reworded consistently. **Process (the user's directive, encoded this wrap):** document durable knowledge in project files, not agent memory — rule added to root `CLAUDE.md` + `/retro` + `/wrap-session`, and this session's three memories migrated into the plan/docs then cleared; also recorded that web research runs in the main loop (background subagents get web/shell auto-denied). **Lessons:** (a) a non-PyPI dependency can still be held to the baseline by *extending the gate*, not punching a hole — the channel stays enforced. (b) a "test that runs only in CI when a 950 MB model is present" is no CI test at all; factoring a pure seam restored coverage. (c) gating on the right timestamp matters — tag-publish ≠ asset-upload, and uv locks the asset. (d) the retro itself was nearly skipped — folding the user's explicit doc directive felt like "the retro" but isn't; the reflective pass (now enforced in `/wrap-session` step 2) is what surfaces recurring friction like the cwd-invocation foot-gun, which had survived since Session 3.
- **2026-05-27 — Wave-3 CVE response + post-PR-23 waiver audit + /review-pr §5/§8 amendments (no production code; infra/governance + process).** Sequential to the same-day M2 planning slice. **Wave 3** of HIGH gosu Go-stdlib CVEs (CVE-2026-39823 `html/template` `<meta>` XSS; CVE-2026-39825 `net/http/httputil` ReverseProxy; CVE-2026-39826 `html/template` `<script>` XSS) hit pgvector and ollama together via the sequential-Trivy unmask pattern; both waived in **PR #23** (squash `0057044`) with per-image reachability rationales (gosu renders no templates, runs no HTTP; ollama returns JSON, is not a reverse proxy). Tripped the WAIVERS.md "build our own pgvector image if this happens a third time" threshold; evaluation tracked in **[Issue #22](https://github.com/yaaisiu/story-forge/issues/22)** (pgvector-scoped — first task is verifying whether `postgres:17-trixie` rebuilds faster than pgvector, since gosu is shipped by the postgres image). **Codex P2 fold:** initial CVE-2026-39823 rationale named `net/url`; Codex caught that the CVE is actually in `html/template` per Go vuln-DB GO-2026-4982 / NVD; my own `/review-pr` missed it. Outcome unchanged (gosu doesn't render templates); rationale tightened. **Existing-waiver audit (user-requested, this wrap commit):** verified 39 pre-PR-23 CVE rationales against NVD; **39/39 package attributions correct** (the Codex catch was isolated, not a repo-wide pattern); 5 minor description tightenings folded (CVE-2025-61723 "std"→"encoding/pem"; CVE-2026-25679 IPv6-specific→general host validation; CVE-2026-33811 DoS→double-free severity; CVE-2026-29111 dropped over-claimed code-exec claim; CVE-2026-40356 overflow→underflow). **`/review-pr` retro amendments** (both approved): §5 *"verify CVE attribution against pkg.go.dev/vuln or NVD, not Trivy's title"* (the gap Codex caught); §8 *"sequential-Trivy unmask: don't declare ready-to-merge on first green scan when a PR changes a waiver/pin"* (pattern 3-for-3: #2, #4, #23). **Considered + rejected:** a new `/verify-waiver` skill (insufficient recurrence — lives as a bullet in `/review-pr`); a separate "Last audited" field in WAIVERS.md (redundant); an ADR for the wave-3 escalation (durable record already exists). **Lessons:** (a) my `/review-pr` checked CVE IDs and reachability *coherence* but did NOT cross-verify package attributions against authoritative sources — encoded as the §5 amendment. (b) sequential-Trivy unmask is now a 3-for-3 confirmed pattern — encoded as the §8 amendment so the next waiver-only PR doesn't re-surprise. (c) running the audit in the same wrap commit *while the Codex catch was fresh* was the user's call and the right one; deferring would have left lingering doubt.

- **2026-05-27 — M2 planning slice committed (no production code).** Restructured `docs/PLAN_SHORT.md` for **Milestone 2 — Basic extraction**: replaced the M1 session list with six M2 sessions (S1 PreNERAgent → S2 paid LLM adapters + `LLMRouter` + budget tracking → S3 ExtractionAgent → S4 Neo4jAdapter no-dedupe writes → S5 frontend graph viewer (cytoscape) + agent activity panel (§8.5) → S6 OpenRouterProvider + polish), each with concrete resume anchors and a named failing-test seam. Rewrote the handoff block to point at **M2.S1 = PreNERAgent** (deterministic, spaCy, no LLM — spec §7 step 3) with the first failing test named (`backend/tests/test_prener_agent.py` against an Appendix B.2 PL fixture + EN mirror). **Drift caught:** the Session 6 wrap-handoff parenthetically scoped "§3.3 Stages 1+2+3 as M2", but spec §9 places *all* cascade matching (MatchingAgent + JudgeAgent + review queue, Stages 1–4) in M3 — M2's spec line is literally "Save entities to Neo4j without cascade (everything = new entity for now)." The slice now matches spec; rationale in Decided. **Process win:** `/resume-session` ritual surfaced the drift before any M2 code was written — exactly the value of the pre-code spec-read step. **Considered + rejected:** opening with paid-adapter + router (would have coupled three decisions into one session and shipped no extraction); reading the spec only after slicing (less safe — the drift would have been caught later or not at all). No code touched; no green-state gates to re-run. **Lesson:** an inherited handoff's "likely shape" parenthetical is guidance, not gospel — verify each bullet against the spec before encoding it as the new slice. Retro skipped by choice (no friction surfaced; the drift catch is already a process win recorded in Decided).

- **2026-05-26 — Session 6 complete: frontend upload + outline editor — M1 closed (PR #18, squash `ee1834f`; bundled with PR #19 pgvector waiver wave 2, squash `e207ed7`, that landed mid-session).** Shipped the M1 browser-side flow end to end: upload a `.txt`/`.md`/`.docx`, see the detected language + paragraph count, continue to a manual-mode outline editor seeded with the uploaded source via router state, edit `##`/`###` markers with a live preview (TS mirror of the backend's `domain/chunking.parse_manual_outline`), submit, see persisted chapter/scene/paragraph counts. **Frontend:** `src/lib/api/client.ts` (typed fetch helpers + `ApiError(status, detail, body)` for status-discriminated UX), `useUploadStory` / `useStructureStory` hooks, `UploadScreen` (picker + drag-drop + 415/413/400-specific error copy + "Continue to outline →" Link carrying `rawText` via router state), `OutlineEditor` (mode picker, manual/hybrid textarea + live preview, 404/409/422/502-specific error copy, empty-editor-disabled-submit guard with inline hint, post-success disabled + "Outline built ✓" relabel), `outlineParse` (TS mirror parser with 6 unit tests pinning the rules), `cn()` helper at canonical `src/lib/utils.ts` (shadcn-shaped, ready for first hand-vendored primitive), `e2e.test.tsx` at `src/test/` walking the full M1 flow under jsdom. `Landing.tsx` removed; `/` now hosts `UploadScreen`. **Backend (Option B):** `StoryUploadResponse` echoes `raw_text` so the editor opens pre-seeded; `POST /stories/{id}/structure` accepts optional `{raw_text?}` body that overrides + persists in the same transaction via new `update_story_raw_text` repo helper (lock window stays small — override write post-lock). **Tests:** 29 frontend (4 hook + 3 upload + 6 editor + 6 parser + 1 e2e + 1 shell + 2 hook defense-in-depth) + 75 backend (+2 new: raw_text in upload response, manual override-and-persist); all gates clean (ruff/format/mypy --strict 21 src files; eslint/prettier/tsc/vite-build/dep-age). All four CI jobs green pre-merge. **Review:** `/review-pr` (run *before* Codex per the Session 4 bias-control) caught one destructive bug (empty-`raw_text` overwrite — Should-fix #1) and three smaller items (dead `postJson` helper, post-success UX, e2e test location). Codex independently caught the *same* destructive bug class (P1) with a *different* fix mechanism (drop override at hook layer vs disable submit at UI layer); both folded as defense-in-depth in commits `dc3350e` and `67a08a4`. **Mid-session decisions** (5 linked, in Decided): shadcn B-now/A-when-needed (plain Tailwind + `cn()` at canonical location); Option B outline scope; double-fold of the empty-`raw_text` bug; §6.7 CORS amended to also accept `127.0.0.1` loopback origins (kills the smoke-test-only failure class); planning docs reorganized into `docs/`. **Retro** (3 changes): `/review-pr` §8 "same bug, different fixes — consider folding both"; `backend/CLAUDE.md` "Running locally" notes the dev-DB `alembic upgrade head` step; the CORS §6.7 amendment. Considered + rejected: `/pin-image` "second wave expected" amendment (WAIVERS.md already records the pattern); `/verify` "handoff to user" shadow (one-off); cross-layer scope rule (AskUserQuestion handled it). **Smoke test:** real-browser walk at `http://localhost:5173` confirmed .txt + .md upload → editor seeded → build → persisted; .bin → 415-specific copy. Caught the CORS 127.0.0.1 issue and the unmigrated-dev-DB gap — both folded in this wrap commit. **Lessons:** (a) the bias-controlled order (`/review-pr` *before* Codex) keeps working — Codex caught the same bug class I did but with a different mechanism; the win was *folding both*, not picking. (b) real-browser smoke-testing catches an entire class CI can't (response visible-to-JS vs server-returned). (c) one-conversation sessions can stretch across layers (frontend session → backend route extension) when surfaced explicitly via `AskUserQuestion`.

- **2026-05-25 — Session 5 complete: frontend shell + typed API client (PR #16, squash `aa3d745`).** Shipped the React/Vite shell M0 never built — no feature UI, deliberate scope discipline. `src/app/` — `AppShell` (router-only, easy to test under `MemoryRouter`), `routes` table, `Landing` placeholder with a stable `data-testid` so Session 6 can swap copy without touching the shell test, `createQueryClient()` factory with conservative defaults (no retries, no refetch-on-focus). `src/App.tsx` rewritten as the production composition root (`BrowserRouter` + `QueryClientProvider` + `AppShell`); the M0 `useEffect(fetch)` `/health` page **removed**, not converted. Tailwind v4 via `@tailwindcss/vite` (no PostCSS config); shadcn primitives deferred to Session 6 when an actual feature consumes them. `src/lib/api/schema.d.ts` — TypeScript types generated by `openapi-typescript@7.13.0` from a committed `frontend/openapi.json` snapshot (`backend/scripts/dump_openapi.py` dumps the schema; `npm run generate:api` codegens the client). Frontend build stays hermetic — no live-backend dependency in CI. `AppShell.test.tsx` — failing-test-first, mounts under `MemoryRouter` + `QueryClientProvider`, with an in-test `QuerySentinel` calling `useQueryClient()` so the provider wrapper is *load-bearing* (verified by removing the provider and confirming the test fails). New CI step `- name: Vitest` in the frontend job runs it on every PR. **Backend (folded from Codex P2):** both `/stories` routes now declare their non-2xx responses via a shared `ErrorResponse(BaseModel)` (400/404/409/413/415/502); 422 deliberately left undeclared so FastAPI's auto-generated `HTTPValidationError` typing survives — overload (`ChunkingTooLongError` shares 422 with a different body shape) tracked as cross-cutting. **Deps** (all OSV-clean, all ≥14d, `npm audit --audit-level=high` 0 vulns): runtime `@tanstack/react-query@5.100.10`, `react-router-dom@7.15.0`; dev `vitest@4.1.6`, `@testing-library/react@16.3.2`, `@testing-library/jest-dom@6.9.1`, `jsdom@29.1.1`, `tailwindcss@4.3.0`, `@tailwindcss/vite@4.3.0`. `openapi-typescript@7.13.0` runs via `npx` (peer-conflict with `typescript@6.0.3`); pin lives in the `generate:api` script string, rationale in `frontend/src/lib/api/CLAUDE.md`. **Tests:** 74 backend passed (no behavior change, only OpenAPI metadata grew) + 1 frontend passed; ruff + format + mypy --strict + eslint + prettier + tsc + vite build all clean. All four CI jobs green pre-merge. **Review:** own `/review-pr` (run before Codex notes per the Session 4 retro lesson) caught one blocking (CI didn't run vitest) and four smaller items (decorative QueryClientProvider, stale `package.json` description, test filename, `routes.tsx` docstring inaccuracy) — all folded. Codex P2 (missing `responses=` on stories routes) folded next, with the upload route's same gap folded alongside for consistency. **Process (retro):** three rule additions — a `/review-pr` §4 bullet on FastAPI `HTTPException` → `responses=` (the gap Codex caught), mirrored as an *authoring* rule in `backend/src/story_forge/CLAUDE.md` (new `## API routes` section); a `/review-pr` §6 bullet "render-test wrappers must be load-bearing" (the QuerySentinel lesson); a `/review-pr` §6 bullet "a new test runner/category/glob is wired into CI before merge". Two cross-cutting notes added to PLAN_SHORT.md: backend PyPI advisory scan parity for `npm audit` (frontend has it, backend doesn't), and the 422-overloading follow-up. **Lesson:** running `/review-pr` *before* Codex's notes arrived (the Session 4 bias-control) worked — I got an independent list of my own findings (B1/S1/S2/N1/N2) that I can fairly compare to Codex's (one P2, in a different category). The two reviewers caught **non-overlapping** classes of issue this PR, which is exactly the value of running both.
- **2026-05-25 — Session 4 complete: chunking modes + outline persistence (PR #13, squash `ceb832d`).** Shipped the §3.1 three-mode dispatch and the §7 step-2 endpoint. `domain/chunking.py` — pure `parse_manual_outline` on heading *levels* (`##` chapter, `###` scene, `#` story-title dropped; preamble + scene-before-chapter land in implicit untitled nodes so no text is lost) + `outline_to_tree` (assigns `order_index` and threads FK ids chapter→scene→paragraph; covered by direct unit tests). `agents/chunking_coordinator.py` — three-mode dispatch over an `OutlineProposer` `Protocol` (`ChunkingAgent` satisfies it structurally, tests need no `# type: ignore`); hybrid fills only untitled *scenes* so every explicit `##`/`###` survives — including a `### Cold Open` before any `##`. `proposal_to_outline` enforces the end-index-vs-count upper bound (closes Session 3 carry-forward). `ChunkingTooLongError` guards LLM paths above `max_input_words` (windowing-deferred). `POST /stories/{id}/structure?mode=auto|manual|hybrid` — one-shot build+persist; 404/409/422/502 mapped; the DB write lock (`SELECT ... FOR UPDATE`) is taken *after* the LLM build so it spans only the writes window (folded after `/review-pr`). `main.py` DI-wires `OllamaProvider` (cloud_free default) → `ChunkingAgent` → `ChunkingCoordinator` on `app.state`. New settings (all working defaults): `chunking_local_max_words` (closes Session 3 named-knob carry-forward), `chunking_model`, `ollama_cloud_host`. Incidental refactor: promoted the blank-line rule to shared `domain.parsing.split_paragraphs` (third consumer arrived). **74 tests pass** (48 unit + 26 integration); ruff + format + mypy --strict (21 src files) clean; dep-age clean (no new deps); all four CI jobs green. **Review:** Codex flagged two P1s pre-merge — hybrid losing pre-chapter `###` anchors (the dispatch-on-wrong-level bug) and the 409 guard being non-atomic; both folded with regression tests + a `get_story_for_update` lock helper. Own `/review-pr` then surfaced lock-during-LLM, missing `outline_to_tree` unit test, `Literal`-vs-`str` on the response, the `OutlineProposer` `Protocol` (removed 11 `# type: ignore[arg-type]`), and the comment self-referencing the review — all folded. **Process (retro):** two `/review-pr` amendments — added a "dispatch-level confusion in nested structures" bullet to §1 (with the hybrid bug as the memorable example), and clarified §3 Simplicity to say an abstraction that net-removes existing debt is a refactor, not speculation. **Lesson:** Codex caught the hybrid bug class my first review missed, and my `/review-pr` ran *after* the fold so it couldn't be evaluated for independence — running it without reading Codex's notes first is the bias-control I want next session.
- **2026-05-22 — Session 3 complete: LLM provider abstraction + ChunkingAgent (PR #10, squash `ef4a07a`).** Shipped the agent-orchestration spine (spec §6.5) and the first agent end-to-end against a mocked provider. `adapters/llm/base.py` — `LLMProvider` Protocol + `Message`/`CompletionResult`/`ModelTier` (scoped to `complete()`; cost/rate-limit metadata deferred to the router/budget work that will consume them). `adapters/llm/ollama.py` — `OllamaProvider`, one adapter for both the local-small and cloud-free tiers (host/key swap), Ollama `/api/chat`, JSON-Schema `format`. `prompts/` — Jinja2 loader that splits the *template source* on `[ROLE]` markers then renders each segment (so untrusted story text can't forge turns) + `chunking.{en,pl}.j2` (Appendix C.1). `agents/chunking_agent.py` — `ChunkingProposal` schema (with an ordered/non-negative `paragraph_range` validator), `select_chunking_tier`, and the agent (load → call → parse + validate → retry on bad JSON or schema violation). **Spec amended** (§6.5 hardware para + agent table, §7 step 2): on a GPU-less host the local 9B is impractical, so chunking defaults to **cloud_free**; `local_small` is a config swap when a GPU is configured. **Deps:** `jinja2==3.1.6` added; `httpx==0.28.1` promoted to runtime. **54 tests pass** (agent against a mocked `LLMProvider`: happy path, retry-on-malformed-JSON/schema-violation/reversed/negative range, code-fence strip, give-up, PL/EN routing, injection-safety, tier selection; `OllamaProvider` via `httpx.MockTransport`: request shape, `format` omission, bearer auth, 5xx); ruff + mypy --strict + dep-age clean; `main` green. **Review:** own-review via the new `/review-pr` skill caught a jinja2-in-agent layering smell (fixed: `prompts` raises its own `PromptNotFound`); two Codex notes folded pre-merge — the prompt-injection redesign and the `paragraph_range` validator (upper-bound-vs-count deferred to Session 4 persistence). **Process (retro):** created `/review-pr` (PR #11, `d90ed76`) and wired it into CLAUDE.md's Merge flow. **Lesson:** I waved prompt-injection through as "inherent to the feature" in my own review; an external reviewer caught it — the fix (derive structure from the trusted template, never reparse user-rendered output) is now a `/review-pr` check and a saved memory.
- **2026-05-21 — Session 2 complete: story upload → validate → sandbox → parse → detect → persist (PR #8, squash `c692cba`).** `POST /stories/upload` (spec §7 step 1) accepts `.txt`/`.md`/`.docx`, validates (extension allowlist → 415, Content-Type with params stripped, 10 MiB cap → 413, empty/unparseable → 400), sandboxes the original (UUID-named, `0o700` dir / `0o600` file, no exec — §6.7, dir chmod explicit so it holds for a pre-existing dir), parses to raw text + paragraph blocks, detects PL/EN, and persists a `Project` (detected language) + `Story` (raw text). I/O-pure `domain/parsing.py` + `domain/language.py` (seeded `langdetect`); `adapters/{db,upload_storage}.py`; thin `api/stories.py` wired in `main.py`. New deps exact-pinned ≥14d, no advisories: `langdetect==1.0.9`, `python-docx==1.2.0`, `python-multipart==0.0.27`. **38 tests pass** (parser/detection/storage/db units + upload integration: happy txt→en & docx→pl, 415/413/400, sandbox write+perms, charset-param accept); ruff + mypy --strict + dep-age clean; `main` green (all four CI jobs). **Review:** two Codex notes folded in pre-merge — `libpq_kwargs` now preserves URL query options (sslmode/timeouts) and the route strips Content-Type parameters; own-review also de-duplicated `conftest`'s copy of `libpq_kwargs` and filed a cross-cutting note for orphaned-sandbox-file cleanup on upload failure (deferred, harmless single-user). **Process (retro):** encoded PR-first/`wrap-session`-last in the wrap skill (preflight step 0 + reframed step 8) and added a `backend/CLAUDE.md` test convention (bind secret-keyword values to a variable so detect-secrets doesn't flag them). **Lesson:** a happy-path test that hard-codes a clean `Content-Type` ("text/plain") hides the real-world parameterized form ("text/plain; charset=utf-8") — test the input the browser actually sends.
- **2026-05-21 — Issue #4 closed: both CVE-stale images fixed, `main` fully green (infra/governance, no M1 code).** pgvector `0.8.1-pg17-bookworm` → `0.8.2-pg17-trixie` (the pre-approved 7-day-rule bend for a CVE-fix release; trixie carries 9 OS CVEs vs bookworm's 13, and older is strictly worse — 0.8.1-trixie = 42). Fixing it unmasked a second stale image, ollama `0.22.1` (sequential-Trivy masking, the same pattern neo4j→pgvector showed in #2) → bumped to `0.24.0` (freshest aged stable, 7 days, no bend; dropped the CRITICAL + 6 others, 17 → 12 HIGH). Residual CVEs that no tag can fix are waived per-CVE in scoped `infra/trivy/{pgvector,ollama}.trivyignore` — pgvector's OS CVEs (no fresher rebuild) + bundled `gosu`/Go-stdlib, and ollama's compiled-in Go-stdlib + `buger/jsonparser` — each with a reachability assessment (none network-reachable on a 127.0.0.1 single-user non-root deployment: gnutls unused since Postgres TLS is OpenSSL, no DTLS, no systemd daemon; ollama CVEs are mostly DoS a trusted local caller self-inflicts, the two cert-validation ones need MITM of outbound TLS). Added `infra/trivy/WAIVERS.md`, a consolidated review register (every waived CVE across neo4j/pgvector/ollama with fixed-in versions + drop conditions) for periodic re-checking. Applied to `docker-compose.yml`, the CI backend `postgres` service container, the wrapper `infra/ollama/Dockerfile`, and both Trivy scan steps. pgvector waiver verified locally (dockerized Trivy → exit 0); ollama CVE list enumerated from the CI Trivy run. Squash-merged (PR #6, `4cb92a2`), Issue #4 closed. **Process (retro):** amended `/pin-image` — local-Trivy-unreadable → push-and-harvest-from-CI fallback; scan all images up front; record waivers in `WAIVERS.md`. **Lesson:** the sequential-Trivy unmask is now a confirmed *pattern*, not a one-off — fixing image N reveals staleness in image N+1, so sweep every image the security job scans before declaring it will go green. And in this environment local Trivy on large images is unusable (sandboxed background), so CI is the authoritative scanner.
- **2026-05-21 — Issue #2 fixed + image-pinning process hardened (infra/governance, no M1 code).** neo4j repointed from the nonexistent `5.26.25-community-bullseye` to `5.26.25-community-ubi10` in `docker-compose.yml` (neo4j + neo4j-init) and `ci.yml`; UBI-10 zeroes the 14 Debian OS CVEs, and the 3 bundled-netty HIGH CVEs are waived with per-CVE justification in `infra/trivy/neo4j.trivyignore` (scoped to the neo4j scan step). Verified with **dockerized Trivy** (exact CI flags → exit 0) and `docker compose config`; CI confirmed the neo4j scan step passes. Squash-merged (PR #3, `6694e23`), Issue #2 closed. The fix unmasked a separate pre-existing pgvector CVE-staleness problem → filed **[Issue #4](https://github.com/yaaisiu/story-forge/issues/4)** (security CI stays red on it until ~05-22 when the fix image ages in); merged with that red accepted as diagnosed/unrelated per the green-main norm. **Process (retro):** lowered the image age soak 14 → 7 days (images only) — spec §6.7 amended with rationale, root `CLAUDE.md` updated; created the `/pin-image` skill (corrects the stale "no trivy in WSL" assumption — dockerized Trivy is the local verify path) and added an `add-dependency` pointer. **Lesson:** an aged exact pin has *two* rot modes — a nonexistent tag (Session 1) **and** CVE-staleness as new advisories land against frozen packages (this session); and sequential Trivy steps mask downstream image failures, so scan *all* compose images locally up front.
- **2026-05-20 — Session 1 complete: persistence foundation.** Postgres document tree (Project → Story → Chapter → Scene → Paragraph, spec §6.4) shipped end to end: I/O-pure Pydantic domain models (`domain/models.py`, UUID PKs, `order_index`); a raw-SQL Alembic migration (cascade FKs, `vector(768)`, `CREATE EXTENSION vector`, child-lookup indexes, reversible downgrade); an async-psycopg repo (`adapters/postgres_repo.py`, C/R/D). Integration-test harness landed: `tests/conftest.py` owns a throwaway `story_forge_test` DB (create → `alembic upgrade head` → yield → drop) with a transactional `db_conn`, and an `integration` marker so unit tests need no Postgres. CI backend job gained a `pgvector/pgvector:0.8.1-pg17-bookworm` service container + `TEST_DATABASE_URL`. Docs: README "Running tests" + status → M1; `backend/CLAUDE.md` test-DB lifecycle. Backend gate green — 11 tests (1 unit + 10 integration), ruff + mypy clean. Incidental: fixed the Alembic ruff post-write hook (`console_scripts` → `exec`, it had never run) and refreshed `.secrets.baseline` for the CI throwaway password + Alembic URL placeholder. **Process (retro):** added the "Adding a setting that tests/app read from `.env`" recipe to `backend/CLAUDE.md`; rejected splitting incidental fixes into separate commits (squash-merge collapses them anyway; the commit body discloses them). **Merge + review:** addressed two Codex review notes before merge — a test-DB drop guard (refuse to `DROP` the app DB or any non-`*test*` name; unit-tested) and documenting/tracking the deferred embedding read path. Squash-merged to `main` (PR #1, `ff95dc2`); private repo created. **Lesson:** the first-ever CI run surfaced two latent M0 bugs (nonexistent `trivy-action@0.32.0` tag → fixed to `v0.36.0`; invalid `neo4j` image tag → [Issue #2](https://github.com/yaaisiu/story-forge/issues/2)). An exact pin that is ≥14 days old can still be a tag that does not exist — only a real pull/scan proves it. Standing up the remote + CI at M0 (not M1) would have caught these earlier.
- **2026-05-20 — Session 1 groundwork (no production code).** Amended spec §6.4: `order` → `order_index` with a naming/ordering convention note (squash-merged to `main`). Added the `/retro` process-retrospective skill, referenced from `/wrap-session`. Decided the test-DB approach (fixture-managed `story_forge_test`). Diagnosed why `docker compose up postgres` failed (missing root `.env`); user created `.env`/`backend/.env` themselves and verified Postgres + pgvector running locally. Clarified `backend/.env.example` DATABASE_URL password sourcing (and refreshed `.secrets.baseline` rather than using a leak-prone allowlist pragma). Hardened secret handling: tracked `.claude/settings.json` denies agent `Read`/`Edit`/`Write` on `.env`, plus matching `CLAUDE.md` security clauses. Expanded Session 1 task list with the test harness + CI Postgres service + docs work that must precede the schema. Fixed a `/wrap-session` ordering bug — the `/retro` prompt now runs *before* the bookkeeping steps so their output is captured (it previously ran last, leaving the wrap stale).
- **2026-05-20 — M1 planning.** Restructured this file into 6 sessions; added `/wrap-session` and `/resume-session` skills and referenced them in root `CLAUDE.md`. No production code written.
- **2026-05-19 — M0 (Setup) complete.** Repo skeleton scaffolded end-to-end:
  - Foundation: `LICENSE` (MIT), `SECURITY.md`, `.gitignore`, setup-first `README.md`, root `CLAUDE.md`.
  - Governance: `PLAN_LONG.md`, this file, `docs/decisions/0001-three-tier-llm-strategy.md`.
  - Infra: `docker-compose.yml` (Neo4j, Postgres+pgvector, Ollama, one-shot `neo4j-init`), `infra/ollama/Dockerfile` (non-root wrapper), `infra/neo4j/init.cypher` (constraints + indexes), `infra/postgres/init/01-pgvector.sql`.
  - Env: split `.env.example` (repo-root + `backend/.env.example`).
  - Backend: `pyproject.toml` (hatchling, exact pins ≥14d), `src/story_forge/{main,config,__init__}.py`, Alembic harness, async `/health` test via `httpx.ASGITransport`.
  - Frontend: Vite + React 19 + TypeScript 6 skeleton, eslint flat config, prettier, `/health` smoke page.
  - Scripts: `check_dependency_age.py` (strict on ranges + age), `check_ollama_cloud.py`.
  - CI: GitHub Actions with `backend`, `frontend`, `security` (incl. Trivy on 3 images), and `ollama-cloud-smoke` jobs.
  - Pre-commit: detect-secrets v1.5.0 (with baseline), ruff lint + format, whitespace hooks, dep-age as pre-push.
  - Lockfiles generated: `backend/uv.lock`, `frontend/package-lock.json`, `.secrets.baseline`.
  - Verification clean: compose config, ruff/ruff-format/mypy-strict/pytest, eslint/prettier/build, dep-age (24/24 deps), detect-secrets.
  - Deferred: local Trivy scan (no `trivy` in WSL — CI handles it); `pre-commit run --all-files` (no git repo yet, per Phase 2 rule).

---

## How the two skills use this file

- **`/resume-session`** (session start) reads the **Session handoff** block, verifies the on-disk anchors it names, runs `git log`/`git status`, opens the spec sections listed, and confirms reality matches before work begins.
- **`/wrap-session`** (session end) runs the green-state checks (report-only), checks off completed tasks, strikes (does not delete) obsolete ones, appends a dated line to **Done in previous sessions**, refreshes **Blocked / questions** and **Decided**, and rewrites the **Session handoff** block to point at the next session. It then reminds you to commit on a feature branch (squash-merge hygiene).
- **When a spec change happened:** both skills stop and require `story-forge-poc-spec.md` + `PLAN_LONG.md` to be reconciled with this file before continuing.
