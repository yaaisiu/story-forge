# PLAN_SHORT.md — Current milestone tactical plan

> Updated every working session. Read at start (`/resume-session`); update at end (`/wrap-session`).

<!-- ───────────────────────────── HANDOFF ─────────────────────────────
This block is the contract between `/wrap-session` (writes it) and
`/resume-session` (reads it). It always describes where the NEXT session
should start. Keep it short and literal. Do not delete the markers.
────────────────────────────────────────────────────────────────────── -->

## ▶ Session handoff (read this first)

- **Next session — MILESTONE PLANNING: what goes into the next milestone, and how the work divides** *(owner ask, Session 101; a thinking/planning unit — no production code)*. **Graph quality is closed and archived; there is deliberately no current milestone.** The roadmap now sits at an explicit **fork** (`docs/PLAN_LONG.md` → *"◀ THE FORK"*): go **deeper on extraction** (re-extraction, the eval baseline, relation deep-modelling) or move to **V2 Editing** (spec §4). **Do not lock the fork this session** — it has a recorded prerequisite (see Open blocks). What this session *can* do without that evidence, and should: **(a)** frame the fork properly — name what evidence would actually settle it, so the sample run has a question rather than a vibe; **(b)** design that run as a concrete session (which text, what to measure, what "extraction is fine" would look like); **(c)** sketch candidate slices for **both** branches so whichever wins already has a shape — applying the root-`AGENTS.md` **completeness sweep** (enumerate the full operation set *before* committing to slice boundaries); **(d)** run the **BACKLOG review** a roll normally includes — there are now ten freshly-aged items plus two promotion candidates. **Sizing input:** Graph quality was **8 slices but 33 sessions** (~40 % went to security treadmill + one out-of-sequence tooling detour); the slicing was sound, the overhead wasn't planned for. Size the next milestone with that in view.
- **Read before starting:** this file (handoff + **Decided Session 101** + the live cross-cutting/Blocked lists); **`docs/PLAN_LONG.md`** — especially the new **"◀ THE FORK"** section (the prerequisite, the two promotion candidates, the sizing note) and the *Preparation gate* section above it, whose Session-66 shape is the model for a second prep gate; **`docs/BACKLOG.md`** (its routing-rule header, the two promotion candidates, and the new *"Aged-out cross-cutting items"* section); **`architecture/reports/2026-07-23-architecture-review.md`** (the roll sweep — `/resume-session` §3c triages it *this* session, see Open blocks) and **`architecture/open-questions.md` OQ-35**; root `CLAUDE.md` (one-unit-per-conversation, owner-holds-merge). Spec sections only as the fork's branches need them — **§5** (deferred extraction work) and **§4** (V2 Editing) — read them when sketching that branch's slices, not before.
- **Verify on disk:** on **`main`** after this wrap's PR merges (the roll + wrap ship together on `chore/milestone-roll-graph-quality` — confirm it landed). **`docs/PLAN_SHORT.md` is ~22 KB** (was 235 KB) and has **no `## Current milestone:` task section** — its milestone heading reads *"not yet chosen"*; that absence is correct, not drift. **`docs/PLAN_ARCHIVE.md`** carries a `## Archived 2026-07-23 (Session 101): Graph quality` batch at the **top**. **`docs/BACKLOG.md`** has an *"Aged-out cross-cutting items — routed here by the two-roll rule"* section holding **10** items. **`architecture/reports/2026-07-23-architecture-review.md`** exists. **`docs/AGENTS.md` §3** contains "two-roll rule"; root `AGENTS.md` contains "Establish the facts that constrain the options"; `meta-architect/skills/review-architecture/SKILL.md` step 7 mentions mirroring `watch` findings. **`architecture/AGENTS.md`** says "INV-1…INV-10" **and** carries the "Bump a note's `updated:` in the same edit" rule. **`architecture/PROJECT.md`** Calibration says **"Primary reader: NOT the operator"** and **"Language: English only"**. The `updated:`-vs-last-commit scan should return **only `invariants.md`** (a known detector false positive — deliberately set to its true content date, then committed later). `meta-architect/templates/project.md` must **NOT** contain "Polish". **Note for the anchor-checker:** the old handoff's `grep -rl "brief description"` anchor is **retired** — that phrase now legitimately appears in more files because the DM-IH-8 supersession notes *quote* the superseded promise; a higher count is expected, not a missed home.
- **Last session ended:** 2026-07-23 — **Session 101: the Graph-quality → next-milestone ROLL, all three parts done; the milestone is closed and archived, and the owner answered all four open architecture questions.** The retro mined Sessions 68–100 for three cross-session rules; the architecture sweep found the vault's two strategic notes a **whole milestone stale** (re-synced) and closed both queued inputs; the archive move took `PLAN_SHORT.md` 235 KB → 21.5 KB with 0 bullets lost or duplicated. The cross-cutting list was re-curated under the new two-roll rule — 10 aged items to the backlog. **No production code touched, so green state is unchanged from Session 100** (738 backend + 542 frontend tests, all gates clean). **No reds.** The wrap's own retro then caught two process gaps the owner's question exposed (`watch` findings had no home; the two-roll rule had no answer for an unchosen milestone) — both closed. The owner then answered the sweep's four open questions (Decided 101(b)); the load-bearing one is that **he does not read the vault notes**, which retires the "teach the operator" purpose, drops Polish from them, and exposed a Story-Forge assumption hardcoded into the now-shared `meta-architect` plugin.
- **Open blocks/questions:** **(1) The fork is NOT to be decided this session.** Its prerequisite is recorded in `docs/PLAN_LONG.md`: **run fresh sample stories/articles end-to-end first**, because the Oakhaven graph has been hand-curated across S4–S7 and no longer shows what the *pipeline* produces — deciding from it reads the wrong evidence. Planning *around* the fork is exactly this session's job; *locking* it is not. **(2) `/resume-session` §3c triage of the fresh architecture report should CONFIRM, not discover** — every blocker/risk finding is resolved-or-tracked, and **all four questions it raised for the owner were answered in the same session** (Decided 101(b); OQ-35 struck accordingly). No open architecture question carries into this session. **(3) A NAMED vault-maintenance unit now exists — schedule it, don't let it drift.** The owner's answers created concrete work with no home yet, and it must not become a "do it when something touches that file" carry (the escape hatch `docs/AGENTS.md` §3 exists to kill): **(i)** write the two `architecture/components/` notes the owner asked for — `LLMRouter` (failover, budget cap, tier fallback) and `EntityEditService` (eight witnessed INV-9 writer-paths); **(ii)** strip the Polish glosses from the **22** glossary notes that carry them (English-only is now the calibration). Both are `meta-architect:*` writes (vault notes are writer-restricted, ADR 0002). Sized as one small unit; the planning session should either slot it into the next milestone or run it standalone. **(4) Security follow-up, still open and actionable as its own session** (unchanged, and re-verified against Docker Hub at this session's resume): **pgvector** pinned `0.8.2-pg17-trixie` with **`0.8.5-pg17-trixie` published 2026-07-08** (soaked), and **ollama** pinned `0.24.0` with much newer tags — both carry condition-based waivers whose drop-when is "a rebuilt image ships the fix", so a batch may now be droppable. Method in cross-cutting: diff raw pinned-vs-candidate Trivy findings, then `/pin-image`; treat the version jumps as real changes and verify the stack boots + the integration tier passes. **(5) Other live cross-cutting (carried):** the `docs/code` reference refresh (now also covering S7 + the `LabelVocabularyReader` cache) and the `get_story`-outside-503-guard fix across the *edit* routes — note S100 surfaced that the **reader** route shares the same shape and is not covered by that item.

<!-- ─────────────────────────── END HANDOFF ──────────────────────────── -->

> **Earlier milestones — archived.** Milestones 1–4 (M0 Setup, M1, M2 Basic extraction, M3 Cascade
> matching, M4 V1 polish), **Public readiness** (V1 → public portfolio) **and Graph quality** (curate
> the graph you have) — their full task breakdowns, Decided entries, and Done logs — live in
> **[`docs/PLAN_ARCHIVE.md`](PLAN_ARCHIVE.md)** (dated, append-only). This file carries **only the
> current milestone**; the milestone-roll ritual (`/wrap-session`, `docs/AGENTS.md` §7) moves the
> just-closed milestone there.

## Current milestone: _not yet chosen_

Graph quality closed at the Session-101 roll. The next milestone is **deliberately unchosen** — the
fork between deeper **extraction** work (spec §5: re-extraction, an eval baseline, relation
deep-modelling) and **V2 Editing** (§4) has a recorded prerequisite: **run fresh sample stories
end-to-end first**, because the working graph has been hand-curated across S4–S7 and no longer shows
what the *pipeline* produces. Deciding from it would read the wrong evidence. See the handoff block
above; the milestone's task section lands here once the fork is settled.

---

### Cross-cutting (do as the relevant session touches it — curated at the Graph-quality→next roll 2026-07-23, Session 101; see `docs/AGENTS.md` §3)

> **Graph-quality→next-milestone curation pass (2026-07-23, Session 101).** Every item walked by hand
> per `docs/AGENTS.md` §3 — and for the first time under the **two-roll rule** added the same session.
> **Resolved this milestone → moved to `docs/PLAN_ARCHIVE.md` (§7):** the Session-59 vault motivation
> reframe and the DM-IH-8 tooltip homes (both closed by this roll's `review-architecture` sweep), the
> Session-27 waiver-drop task (fully closed at S99), the setuptools OSV waiver (S99), the
> `CandidateView` merge-target name (S75), and the two-review-queues shared-machinery item (S79). Also
> struck: the meta-architect workflow-integration item (its live part landed at S68; the residual
> hypotheses are owned by ADR 0002 §4 and Issue #31, not by this list) and the perennial "update spec
> §10 if an open question hardens" (owned by the spec's own stop-and-amend flow, not a task).
> **Aged out under the two-roll rule → `docs/BACKLOG.md`:** ten items that had survived two or more
> rolls untouched, every one carrying the "fix when the relevant code is next touched" escape hatch.
> Because the incoming milestone is unchosen, the rule's other branch (name it as a task there) was
> unavailable, so all ten took the BACKLOG branch — where they get reviewed and promoted at the next
> roll rather than carried silently again. **Carried forward live —** the four condition-based image /
> OSV waiver items (all raised *this* milestone, all still actionable — the pgvector + ollama re-scan
> is the one with a soaked candidate available today), the `docs/code` reference refresh, and the
> `get_story`-outside-the-503-guard fix (S100 found the **reader** route shares the shape, widening it).

- [ ] **Drop the ollama `x/crypto/ssh` wave-6 waivers (CVE-2026-39832 + CVE-2026-39831) when a patched ollama image soaks (`/triage-advisory`).** CVE-2026-39832 added 2026-06-27 (Session 70, PR #161); **CVE-2026-39831 added 2026-07-09 (Session 80, PR #184** — a fresh `x/crypto/ssh` FIDO/U2F presence-check bypass, GHSA-89gr-r52h-f8rx, *unmasked* by the pgvector krb5 waiver letting the sequential scan reach ollama). Both against the unchanged `ollama/ollama:0.24.0` pin, waived on the same unreachability as their wave-6 `x/crypto/ssh` siblings (ollama opens no SSH client / uses no FIDO keys; 127.0.0.1 JSON inference). **Condition-based** drop: when an `ollama/ollama` tag rebuilt on `golang.org/x/crypto ≥0.52.0` soaks ≥7 days, bump via `/pin-image` and drop these + the sibling wave-6 ssh/net waivers together. Re-scan to confirm (not date-checkable). (Session 70, 2026-06-27; extended S80.)
- [ ] **Drop the pgvector krb5 waivers (CVE-2026-40356 + CVE-2026-40355) when a rebuilt pgvector image ships the `deb13u1` krb5 (`/triage-advisory`).** CVE-2026-40355 added 2026-07-09 (Session 80, PR #184) — a fresh krb5 NULL-ptr-deref DoS (GHSA-8qgv-wm66-hrmc), sibling of the already-waived CVE-2026-40356, **same** Debian fix `1.21.3-5+deb13u1` (not yet baked into a pgvector rebuild). Both waived on the same unreachability (no Kerberos service in the container; Postgres uses OpenSSL for TLS; linked libs only; 127.0.0.1 single user). **Condition-based** drop: when a rebuilt `pgvector/pgvector:0.8.2-pg17-trixie` (or a newer soaked tag) ships krb5 ≥`1.21.3-5+deb13u1`, drop both. Re-scan to confirm (part of the standing "pgvector CVE-rot" set in `infra/trivy/WAIVERS.md`). (Session 80, 2026-07-09.)
- [ ] **Re-scan the pgvector + ollama pins against their much-newer soaked tags — the new §3b tag-freshness check flagged both (`/triage-advisory`).** The Session-99 retro added a session-start check for *condition-based* image waivers (a newer tag is the only way their drop-condition can become true), and its **first run immediately flagged two stale pins**: **pgvector** is pinned at `0.8.2-pg17-trixie` while **`0.8.5-pg17-trixie` has been out since 2026-07-08** (soaked), and **ollama** is pinned at `0.24.0` while **`0.30.2` has been out since 2026-06-03** (long soaked). Both images carry condition-based waivers whose drop-when is literally *"when a rebuilt image ships the fix"* — pgvector krb5 + gosu Go-stdlib, and the ~20 ollama `x/crypto/ssh` / `x/net` / `x/image` entries — so a rebuild that old may well clear a large batch of them. **Do this as its own security session:** for each image, diff raw Trivy findings pinned-vs-candidate (does the bump clear waivers, and does it introduce anything new?), then bump via `/pin-image` and delete every waiver it retires. Treat the version jumps as real changes, not pure rebuilds — `0.8.2 → 0.8.5` and `0.24.0 → 0.30.2` may move Postgres/ollama behaviour, so verify the stack still boots and the integration tier passes before merging. Note this is exactly the rot the retro identified, found on two *more* images. (Session 99, 2026-07-23.)
- [ ] **Drop the ollama `x/image` wave-7 waivers (CVE-2026-46602 + CVE-2026-46604) when a patched ollama image soaks (`/triage-advisory`).** Two `golang.org/x/image` TIFF-decoder DoS HIGHs compiled into `ollama/ollama:0.24.0` (x/image v0.22.0), waived Session 98 (PR #218) — *unmasked* once the setuptools OSV red cleared and the sequential `security` job reached the ollama scan (OSV runs before Trivy, so its failure had masked the ollama scan for days). Same unreachability as the wave-6 Go-module siblings (reachable only by decoding a crafted TIFF fed to a multimodal model; 127.0.0.1 single trusted user, self-inflicted). **Condition-based** drop: when an `ollama/ollama` tag rebuilt on `golang.org/x/image ≥0.43.0` soaks ≥7 days, bump via `/pin-image` and drop these together with the sibling wave-6 ssh/net waivers. Re-scan to confirm (not date-checkable). (Session 98, 2026-07-21.)
- [ ] **Refresh the `docs/code` reference notes for the graph-quality features — frontend AND now the backend edge-write surface (`/document-code changed`).** The Graph-quality slices added structure the reference layer doesn't yet describe — **frontend:** S2 navigation (`graphFilters.ts`, `useDebouncedValue`), S3b (`EdgeEvidencePanel`, edge-tap), S4b (`hooks/useReviewQueue.ts`, `features/duplicate-review/`, `useDuplicateSuggestions`/`useDismissDuplicate`, `MergeConflictFields`, the `/duplicates` route), **S5a** (the new `features/entity-panel/` — `EntityEditPanel`, `entityEditForm`, `useEntityEditForm`, and the six relocated helpers; `features/graph-viewer/selectionGuard.ts`; the deleted `NodeDetailsPanel`; `ReaderEntityPanel` recomposed as a thin wrapper); **backend (S5b-be):** the new pure `domain/relation_rekey.py`, `EntityEditService.retarget_relation`, the `PATCH …/relations/{edge_id}` route + `RetargetRelationRequest`, and the `GraphRelation.edge_uid` field; **S5b-fe:** the new `features/graph-viewer/edgeEditForm.ts` + `useEdgeEditForm.ts`, `lib/api/useRetargetRelation.ts`, the `EdgeEvidencePanel` extended read-only → editable, and the `selectionGuard.ts` generalization (`selectedNodeId` → `selectedId`, now shared by node + edge). **backend (S6a-1):** the new pure `domain/label_synonyms.py` (the label self-join + `label_dismissal_id`), `adapters/label_vocabulary_reader.py` (`LabelVocabularyReader` + the `LabelEncoder` Protocol), `adapters/postgres_label_dismissal_store.py`, `Neo4jRepo.list_predicate_vocabulary`/`list_type_vocabulary`, the `label_pair_dismissals` table, and the `GET/POST/DELETE …/label-vocabulary` routes + `name_normalise_suggest_floor` knob. **backend (S6a-2):** the new pure `domain/predicate_rename.py` (`plan_predicate_rename`), `EntityEditService.rename_predicate` + `relabel_entity_type` (+ the `PredicateRenameSummary`/`TypeRelabelSummary` results), `Neo4jRepo.relabel_entity_type` (the one net-new graph writer) + the `EntityGraphEditor` Protocol gaining `get_relations`/`relabel_entity_type`, and the `POST …/label-vocabulary/rename` route + `RenameLabelRequest`/`RenameSummaryResponse`. **frontend (S6b):** the new `features/normalise-names/` (`normaliseNames.ts`, `LabelPairCard`, `NormaliseNamesQueue`), the three `lib/api/` hooks (`useLabelVocabulary`, `useRenameLabel`, `useDismissLabel`), and the `/stories/:id/normalise-names` route + graph-viewer link. **backend (S6c):** the new pure `domain/name_similarity.label_match_score` (the subset-intolerant label scorer) alongside `name_match_score`. **backend (S7):** the new pure `domain/entity_summary.py` (`summarise_relations` + `RelationSummaryLine`/`EntityRelationSummary` — the §3.5 graph-derived tooltip summary) and the `ReaderEntity` response model gaining `relations`/`relation_overflow` (the reader route now also reads `get_relations`). **frontend (S7):** the **new `src/components/ui/` directory** — the first occupant of the folder `frontend/src/AGENTS.md` documents — holding `QueueProgress` and `EmptyQueueNext`, both consumed by all four review queues; and `decorations.tooltipText` extended to render the summary. **backend (S7 follow-up):** `LabelVocabularyReader` gained an app-lifetime **embedding cache** keyed by bare label (shared across projects) plus an `asyncio.Lock` serialising the cold-cache encode — a behavioural change the reference note's description of that adapter should mention, since it is why a vocabulary load costs ~1.7 s rather than ~14 s. Nothing compiles `docs/code/*.md` against the code, so they've drifted silently across S2/S3/S4/S5/S6. Refresh the relevant notes (+ `CODE_GUIDE`) via `/document-code changed` — do as a focused pass, or fold at the milestone roll. (Session 79 step-3a backstop; S5a added Session 81; backend edge-write surface added Session 82; S5b-fe added Session 83; S6a-1 added Session 92; S6a-2 apply surface added Session 95; S6b frontend added Session 96; S6c label scorer added Session 97; S7 tooltip summary + the new `components/ui/` added Session 100.)
- [ ] **Guard `get_story` / `get_project` inside the declared-503 `try` across the entity-edit routes (`api/stories.py`).** Every edit route (`edit_entity_route`, `add_relation_route`, `remove_relation_route`, `retarget_relation_route`, merge, delete) calls `story = await get_story(conn, story_id)` **before** its `try/except (OperationalError, ServiceUnavailable)`, so a Postgres outage on that read raises a bare **500** while the route **declares 503** — the `/review-pr` §4 undeclared-500 lens. Pre-existing and *systemic* across the file (not introduced by S5b-be, which mirrors the pattern), so fix it in **one focused pass** over all the edit routes rather than piecemeal — move the story/project lookups inside the guard (or a shared dependency that maps the outage). Surfaced by the S5b-be `/review-pr` (Session 82); do when the edit-route error surface is next touched. (Session 82 own-review finding.) **Note:** the S6a-1 `label-vocabulary` **read** routes (Session 92) already guard `get_story` *inside* the declared-503 `try`, so they don't add to this debt — this item stays scoped to the pre-existing *edit* routes; S6a-2's apply endpoints must guard likewise.

### Blocked / questions

- _**Earlier resolved registers / questions (Milestones 1–4, Public readiness, Graph quality)** — see **[`docs/PLAN_ARCHIVE.md`](PLAN_ARCHIVE.md)**._

### Decided

- **2026-07-23 — Session 101 (b): four owner answers to the architecture sweep's open questions — one confirmation, one sharpening, and a real recalibration of who the vault is written for.** Raised at the wrap as findings needing the owner; answered the same session, so none carried forward. **(a) The vault's `updated:` dates stay a real freshness signal** — *"there are no people to edit those notes, only you are taking care of them, so you need to do it right."* All **12** mismatched dates corrected in the same pass (each set to its last content-bearing commit, verified by diff), and `architecture/AGENTS.md` gained the standing rule: bump `updated:` in the same edit that changes a note's body, always — with the content-vs-commit distinction spelled out so a wikilink repoint doesn't cause a false bump. *Rejected:* demoting the date to coarse with git as the source (a wrong date is worse than none, and there is no human to blame for the drift — the agent is the only maintainer). **(b) The vault's empty `components/` altitude gets populated** — *"we should keep it updated and usable"* — so per-component notes are wanted, not deliberately skipped; the two that pay (`LLMRouter`, `EntityEditService`) are a **named task**, not a write-it-when-something-touches-it carry. **(c) The persona is confirmed but now has an expiry.** Still one person, own machine, fully trusted — *and* the owner wants it *"usable one day, and not only for me."* No spec amendment (§2.3 scopes the **PoC**), nothing to build now; what changes is that "single-user forever" is no longer safe to build **irreversibly** against — a shortcut that would be *wrong* under many users and expensive to undo now deserves a note when taken. The live example is the S7 label-embedding cache, keyed on the bare label and shared across projects: harmless under one trusted user, a shared mutable structure below the tenancy key if the aspiration lands. **(d) The biggest change — the vault is not written for the owner.** He states plainly he does not read these notes, retiring the bootstrap assumption that it partly exists to teach *him* architecture vocabulary. The two real readers are an outside visitor reading cold and the agents that orient from the vault. The accessibility bar is **unchanged** (*"informative, usable for the beginner"*) but the **Polish glosses go** — they existed to teach the operator in his first language. English only going forward; the **22** existing glossary notes carrying Polish are a named stripping task. **This surfaced a leak in the now-shared `meta-architect` plugin:** `templates/project.md` **hardcoded** "glossary always carries the Polish term too", and three more files defaulted to "EN + PL" — a Story-Forge assumption that rode along into generic tooling now installed in repos with no connection to Polish. All four neutralised to defer to each project's own calibration, and the template now also asks *who actually reads these notes* rather than assuming the operator does.

- **2026-07-23 — Session 101: the Graph-quality → next-milestone ROLL — three parts, plus three process rules and a spec-free architecture re-sync (branch `chore/milestone-roll-graph-quality`).** Not a build session; the milestone's closing ritual (`/wrap-session` §5c). **(a) Mined before moving.** `/retro` in milestone-roll mode read the whole departing batch (Sessions 68–100) as a corpus and found three cross-session patterns no single session's retro could see, all owner-approved: the **two-roll rule** (`docs/AGENTS.md` §3 — an item that has survived two rolls untouched gets a binary call, never a third free carry; earned because nine items were 2+ rolls old, and Session 96 hit the error-mapper item's own fold-trigger and *added the seventh copy*); **establish the constraining facts before putting a decision to the owner** (root `AGENTS.md` — Sessions 97 and 98 each cost two decision rounds for the same reason, and the record itself noticed they shared a form while only the two instances got written down); and the **Review-model evidence updated from three sessions to 13-of-14 production-code sessions**, with the reason it doesn't converge recorded (the five new `/review-pr` lenses prevent *recurrence* of known classes — S92/S95 wrote routes correctly because of the S75 lens — but nearly every PR brings a *novel* class, so the second gate never becomes skippable as `/review-pr` grows). *Considered + rejected:* mandating a browser walk before merging a user-facing slice — the milestone's highest-yield step, but already universal, documented in `frontend/AGENTS.md`, and the one case it missed wouldn't have been caught by mandating it. **(b) The architecture sweep found a whole stale milestone.** `overview.md` and `PROJECT.md` both still read "the project is now in a Public-readiness milestone" — closed at the Session-68 roll — so the entire Graph-quality record (S0–S7, ADRs 0010/0011, INV-10) was missing from the vault's as-built snapshot: the fifth recurrence of doc-freshness drift and the second to reach a roll uncaught, which is exactly the evidence that wired the sweep into the roll (ADR 0002 §4). Both re-synced. Both queued inputs closed — **DM-IH-8 recorded as SUPERSEDED, not wrong**, across five homes (option (a) was correct for a read-only slice; what S7 found was that the *spec line* had never been updated to match it), and the Session-59 motivation residue cleared — where the item's own "~6 files" estimate was **wrong in both directions**: one live "personal tool" lens, plus two *as-built overclaims* ("the real Wody Święte draft") its keyword grep couldn't see. **(c) The archive move** took `PLAN_SHORT.md` **235 KB → 21.5 KB**, verified byte-honest (98 dense bullets before; 75 archive / 10 backlog / 13 live after; 0 lost, 0 duplicated). The §3 re-curation walked all 24 cross-cutting items in the two-roll rule's first run: 6 resolved → archive, 2 struck as owned elsewhere, **10 aged out → `docs/BACKLOG.md`**, 6 carried live. **(d) Two INV-1 near-misses recorded** (both already tracked): the **normalise-names gate is nominal** — the author approves a graph-wide rename with no evidence shown, the milestone's own thesis unapplied to its own slice, and the sweep independently supports promoting it; and the **review-queue cursor is index- not identity-anchored**, a TOCTOU at the human gate live on all four queues. **(e) The wrap's own retro caught two gaps the process missed and the owner's question found** — `watch` findings had no home outside a dated report nobody re-reads (`review-architecture` now mirrors them into `open-questions.md`), and the two-roll rule's first branch is unavailable when the incoming milestone is unchosen (`docs/AGENTS.md` §3 now names BACKLOG as the default in that case). **No production code, no spec amendment, no ADR.** `PLAN_LONG.md` reconciled: Graph quality marked complete, and **the fork opened as an explicit roadmap section** with its prerequisite and the milestone-sizing note (8 slices, 33 sessions, ~40 % unplanned overhead).

- _**Earlier decisions (Milestones 1–4, Public readiness, Graph quality)** — see **[`docs/PLAN_ARCHIVE.md`](PLAN_ARCHIVE.md)**._

### Done in previous sessions

- **2026-07-23 — Session 101: the Graph-quality → next-milestone milestone ROLL (governance/planning; no production code).** Ran the roll per `docs/AGENTS.md` §7 / `/wrap-session` §5c, in its three ordered parts. **(1) Mined before moving** — `/retro` in milestone-roll mode read Sessions 68–100 as a corpus and produced three owner-approved rules: the **two-roll rule** for cross-cutting items that have outlived two milestones, a root-`AGENTS.md` rule to **establish the facts that constrain the options before putting a decision to the owner**, and a milestone-scale evidence update to the required-`/code-review` gate (13 of 14 production-code sessions had it catch a real defect the self-review missed) together with *why* it doesn't converge. **(2) The architecture sweep** (`meta-architect:review-architecture`, the first run under the ADR-0002 §4 roll wiring) found no blockers but one severe drift: the vault's two strategic notes were a **whole milestone stale**, so all of Graph quality was missing from its as-built snapshot — re-synced, along with both queued reconciliation inputs (the DM-IH-8 tooltip supersession; the Session-59 motivation residue, where the predicted "~6 files" turned out to be one lens plus two as-built overclaims a keyword grep had missed). Two INV-1 near-misses recorded — a *nominal* human gate on the normalise-names screen and a TOCTOU-prone review-queue cursor — both already tracked in `docs/BACKLOG.md`. **(3) The archive move** took `PLAN_SHORT.md` from **235 KB to 21.5 KB** — back under the soft flag and well clear of the 256 KB single-read limit it was approaching — with the whole Graph-quality record moved verbatim into `docs/PLAN_ARCHIVE.md` and verified byte-honest (0 bullets lost, 0 duplicated). The cross-cutting re-curation walked all 24 items under the new rule; ten that had survived two-plus rolls untouched went to the backlog, where they get reviewed and promoted rather than carried silently again. **No production code was touched**, so the green-state checks are unchanged from Session 100 (738 backend + 542 frontend tests, all gates clean). **The wrap's own retro then caught two gaps the process itself had missed** — surfaced by the owner asking whether *every* sweep finding had been fixed or marked: `watch`-severity findings had no home outside a dated report nothing re-reads, and the brand-new two-roll rule had no answer for a roll where the next milestone isn't chosen yet. Both closed. **Lesson:** a report is only as good as the thing that re-reads it — the sweep had a forcing function for its serious findings and none at all for its quiet ones, and the difference was invisible until someone asked the obvious question.

- _**Earlier sessions (Milestones 1–4, Public readiness, Graph quality — through Session 100)** — see **[`docs/PLAN_ARCHIVE.md`](PLAN_ARCHIVE.md)**._

---

## How the two skills use this file

- **`/resume-session`** (session start) reads the **Session handoff** block, verifies the on-disk anchors it names, runs `git log`/`git status`, opens the spec sections listed, and confirms reality matches before work begins.
- **`/wrap-session`** (session end) runs the green-state checks (report-only), checks off completed tasks, strikes (does not delete) obsolete ones, appends a dated line to **Done in previous sessions**, refreshes **Blocked / questions** and **Decided**, and rewrites the **Session handoff** block to point at the next session. It then reminds you to commit on a feature branch (squash-merge hygiene).
- **When a spec change happened:** both skills stop and require `story-forge-poc-spec.md` + `PLAN_LONG.md` to be reconciled with this file before continuing.
