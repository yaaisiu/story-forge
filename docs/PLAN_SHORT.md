# PLAN_SHORT.md — Current milestone tactical plan

> Updated every working session. Read at start (`/resume-session`); update at end (`/wrap-session`).

<!-- ───────────────────────────── HANDOFF ─────────────────────────────
This block is the contract between `/wrap-session` (writes it) and
`/resume-session` (reads it). It always describes where the NEXT session
should start. Keep it short and literal. Do not delete the markers.
────────────────────────────────────────────────────────────────────── -->

## ▶ Session handoff (read this first)

- **Next step — item 4 (code documentation) is DONE; the remaining public-readiness work is two unordered sessions, owner's pick:** **Session 5 — Security & CI posture writeup** (tell the supply-chain + CI story for an outsider: OSV+Trivy gates, the 14-day/7-day soak rule, the time-boxed-waiver lifecycle, the daily scheduled scan, path-scoped CI + branch protection — currently only in internal records; its own doc or an expanded `SECURITY.md`, linked from README), **or Session 6 — Doc hygiene** (confirm the ADR/`AGENTS.md`/spec/plan map is coherent + navigable; **+ the owner-flagged README "architecture vault is Obsidian-friendly" note**; **+ a `CONTRIBUTING.md` decision**). Both are docs-only, no production code. **First work step:** none is a failing test — Session 5 drafts the security/CI doc outline; Session 6 sweeps the doc map. **Unit boundary:** land one session and stop. _Slice the picked session finer when picked up (a branchy one gets a `meta-architect:decompose-requirement` first slice)._
- **Read before starting:** this file (the `## Current milestone: Public readiness` section — **sessions 5/6** + the live **cross-cutting** list); root `CLAUDE.md` (**Public-portfolio hygiene**; **§4 Directory-level AGENTS.md**). For **Session 5**: spec **§6.7**, `SECURITY.md`, `infra/osv/WAIVERS.md` + `infra/trivy/WAIVERS.md`, `.github/workflows/ci.yml`, the `pin-image`/`add-dependency`/`triage-advisory` skills. For **Session 6**: `docs/decisions/`, the README **Project map** (now links both `docs/CODE_GUIDE.md` *and* `docs/code/`), `architecture/AGENTS.md` (the Obsidian-vault framing), and the `AGENTS.md` set.
- **Verify on disk:** on **`main`** after the **Session-62 Slices-B+C** merge (PR #148, squash **`04263d7`**). **The `docs/code/` reference layer exists** — `ls docs/code/` = `README.md` + `backend-domain.md` + `backend-agents.md` + `backend-adapters.md` + `backend-api.md` + `frontend-features.md` + `frontend-data-layer.md` (6 notes + index); README's Project map links it (`grep -c "docs/code/" README.md` ≥ 1) and `CODE_GUIDE.md` points at it. **`/document-code` has a `reference` mode** (`grep -n "reference \[" .claude/skills/document-code/SKILL.md` hits) and **`code-scribe.md` rule 6 names the *reference* doc kind** (`grep -n "Reference docs" .claude/skills/document-code/code-scribe.md`). **The code-docs freshness forcing function exists** — `/review-pr` §2 has the `docs/code` lens (`grep -n "docs/code/.*reference home" .claude/skills/review-pr/SKILL.md`) and `/wrap-session` has **§3a** (`grep -n "3a. Code-docs freshness" .claude/skills/wrap-session/SKILL.md`). **`docs/BACKLOG.md`** "code documentation generation" item carries the **"Second stone landed (PR #148)"** line. **Milestone item 4 DONE (Slices A–C); sessions 5/6 remain.** **Spec §7 Step 3** = DEFERRED (unchanged). **alembic head** = **`c0c7904a555c`** (the 2026-06-24 `drop_projects_world_id` migration; parent `d5971b7919ab` — the real tip). **No new migration this milestone** (docs-only). **Security waivers (NEUTRAL floor dates — `/resume-session` step 3b computes drop-readiness against the resume day; do NOT inherit a verdict; the daily scan re-reds on a waiver's `ignoreUntil`):** **starlette 1.3.1** clears CVE-2026-54282 (floor **2026-06-25**) + CVE-2026-54283 (floor **2026-06-26**, **HIGH** DoS) — binding floor **2026-06-26**, droppable via `/triage-advisory` once ≤ the resume date; OSV `osv-scanner.toml` `ignoreUntil` **2026-06-26/-27**; **pydantic-settings** floor **2026-07-04** (→ 2.14.2); torch (no fix). **Condition-based (re-scan, not date):** neo4j jackson-databind **CVE-2026-54512/54513** + netty **CVE-2026-50010** + shiro **CVE-2026-49268**; the gosu/ollama Go-module wave-5/6 waivers. _(Session 5 — the security writeup — is the natural time to also sweep the waiver state.)_
- **Last session ended:** 2026-06-25 — **Session 62 (Public-readiness Session 4, Slices B+C): the docstring sweep proved the in-code docs were already done, so the work became the agent-authored `docs/code/` reference layer (PR #148 merged green, `04263d7`).** Slice B's survey *contradicted* the plan's premise — frontend `features/` 35/36 well-documented (0 real gaps), backend ~64% richly documented — so the sweep filled the **one** genuine gap (`upload_story`) and churned nothing. The owner's "do we actually have code docs?" surfaced that the **standalone descriptive** layer was missing → Slice C built `docs/code/` (6 per-layer narrative notes + index, authored by one `general-purpose` agent per layer under `code-scribe`; 180 links verified; wired into `CODE_GUIDE`/README). Stop-and-amend: `code-scribe` rule 6 now licenses a drift-disciplined *reference* doc; `/document-code` gained a `reference` mode. **Retro (owner-chose defense-in-depth):** a `/review-pr` §2 lens **+** `/wrap-session` §3a backstop now force a structural code change to refresh the reference notes; `/document-code` §4a hands parallel writers their sibling filenames. `main` green (full CI ran — code-bearing PR).
- **Open blocks/questions:** **Owner pick first:** Session **5 security-CI writeup** / **6 doc-hygiene** (unordered; item 4 done). **PoC→next-phase preparation gate (owner, S60; in `docs/PLAN_LONG.md`):** *before deciding the next **build** work* (after public-readiness, before Graph-quality), run a deliberate **cleaning/preparation pass** — review the PoC spec + author a spec for the new work, review `PLAN_LONG`, adjust the `AGENTS.md`/`CLAUDE.md` set, consider new skills/agents. Don't lose this gate. **README "architecture vault is Obsidian-friendly" note (owner, S60)** → folded into the **Session-6 doc-hygiene** task. **DON'T-FORGET (S59):** propagate the motivation reframe to the vault's residual homes (`proposals/*.md` + `INDEX.md`) at the **next `review-architecture` sweep** — cross-cutting, low urgency. **Direction DECIDED** (owner, S54): **Public-readiness → Graph-quality polish → V2 Editing**. **Standing process question (deferred S51):** whether `/code-review` becomes a *required* second gate for substantive code. **ADR-0002 evidence note (S58):** the 4th doc-freshness recurrence is strong evidence to wire `review-architecture` into the roll ritual — owner wants a **dedicated session**. **Cross-cutting carried (live):** the vault-propagation item; the **plan-trim learning-loop** prep (S61); §6.5 model-override dropdown; meta-architect ritual-wiring (ADR 0002); waiver-drop tracking (starlette → 1.3.1); the review-queue UX nits (`CandidateView` merge-target-name, error-mappers n=5, shared `useReviewQueue`); orphaned-sandbox cleanup; **re-structure delete-and-replace + delete-story**; window-and-stitch + 422-overload remap; `last_call()` tiebreaker; envelope-unwrap dup (b); repo-root `scripts/` ruff scope; `CandidateSpan` validator. **`docs/BACKLOG.md`** holds the post-PoC list incl. the Session-54 graph-quality items + the S60 ones + the S61 agentic-architecture note; auto-chunker silent content-loss is the most serious; OQ-28 homes the architectural read. **Carry-forwards (post-PoC / when-touched):** per-mention provenance for triple-deduped edges (ADR 0005); OQ-14 / OQ-15; Issue #22; Issue #31; M2.S1 review nits.

<!-- ─────────────────────────── END HANDOFF ──────────────────────────── -->

## Current milestone: Public readiness (V1 → public portfolio)

**Goal:** V1 — the ingest→graph→viewer PoC (M0–M4) — is **feature-complete** (the M4
multi-story live smoke PASSED, Session 54). Before the next *build* milestone, **lock the
portfolio presentation**: make the public repo something a stranger can land on and
understand — an honest spec, a compelling README with demo artifacts, navigable code docs,
and clean doc hygiene. **Planning / docs / spec milestone — little-to-no production code.**
Direction owner-DECIDED (Session 54): **Public-readiness → Graph-quality polish → V2
Editing** (`docs/PLAN_LONG.md` amended; M0–M4 ticked, the two new milestones inserted before
V2).

**Slicing — skeleton + first session detailed (owner's call, S55 roll); later sessions are
sliced finer when picked up (a branchy one gets a `meta-architect:decompose-requirement`
step-0 as its own first slice):**

1. ✅ **Done (global Session 57, 2026-06-25; PR #138 squash-merged `d0ee135`).** **Session 1
   — the PreNER spec↔reality stop-and-amend** (the milestone opener; spec/docs only, no
   production code). Ran the stop-and-amend: spec **§7 Step 3** now marks PreNER
   deferred/dormant for the PoC (the pipeline lives in §7, not §6/§3 as the old handoff
   loosely labelled it), with §6.1/§6.5/§9 carrying the "built, not wired → §7 Step 3"
   marker (§6.4 left as-is — a tier use-case, not a pipeline-active claim) and the doc homes
   (`PLAN_LONG.md`, `BACKLOG.md`, the struck cross-cutting item) reconciled. The vault's
   PreNER-active framing is routed to **Session 2** (writer-restricted, ADR 0002). The spec
   §6 pipeline diagram (Step 3) + §3 step 4 describe the
   spaCy `PreNERAgent` as an *active* stage; in code it is **dormant** — the live `/extract`
   is LLM-only (the PreNER-hint param passed empty, injection "deferred until a real eval
   exists"). Run the **stop-and-amend flow**: propose the exact edits → owner approval →
   amend the spec to mark Step 3 deferred/dormant for the PoC (with a pointer to the
   data-flywheel + the `docs/BACKLOG.md` spaCy-without-LLM eval) → reconcile every §3/§6/stack
   home (spec lines ~332/475/579/650/694/766). **Drift correction (S55 roll):** the
   world-graph §3.6/§9 amend the old handoff paired with this is **already done** (Session 49,
   PR #119) — only the PreNER amend remains. First step = draft the spec edits for approval
   (no failing test; it's a reconciliation, not a feature).

2. ✅ **Done (global Session 58, 2026-06-25; PR #140 squash-merged `842bd54`).** **Architecture
   vault re-sync** — ran `meta-architect:review-architecture` (report-only; vault only).
   `reports/2026-06-25-architecture-review.md`: **no blockers.** Reconciled the **PreNER-as-live-stage**
   drift (`PROJECT.md`/`overview.md` → built-but-dormant, **code-verified** dormant against
   `extraction_agent.py`) to the amended §7 Step 3; refreshed `overview.md`'s stale M4 block (S3b/S3c/
   multi-story shipped, V1 feature-complete) and the `INDEX.md` "Next" framing; bumped `invariants.md`
   (body code-verified honest — INV-9's six writer-paths, INV-3 executed); BUILT banner on
   `m4-multi-story`; forward "what if" → **OQ-28** (Graph-quality inputs). Self-`/review-pr` caught two
   freshness leaks the sweep itself introduced (a stale proposal date; an un-cleared handoff-flagged
   INDEX "Next: M4") — **both folded**, and the **`review-architecture` skill gained a §6b self-check**
   (retro) so the freshness-fixing sweep stops leaking freshness debt. _(Original task description in
   `docs/PLAN_ARCHIVE.md` once this milestone rolls.)_

3. ✅ **Done (Slice A — Session 59, PR #142; Slice B — Session 60, 2026-06-25, PR #144 squash
   `3bf057c`).** **README overhaul + demo artifacts** — the headline portfolio piece. **Sliced A/B.**
   - [x] **Slice A — README prose (done Session 59, 2026-06-25; PR #142, squash `1a4ed87`).**
     Portfolio-lead rewrite (`What is this` → `Demo` → `Architecture` w/ Mermaid pipeline
     diagram, real agent inventory, multi-model routing, layered backend, brief
     secure-by-default → `Quickstart` → `Development` → `Project map`); stale facts corrected
     (status M1→V1 complete; agent/routing/ADR pointers). **Plus an owner-surfaced honesty
     amend that rode along:** the "genuine personal tool / the author writes Wody Święte"
     framing overclaimed → stop-and-amend reframed **spec §2.1** (Wody Święte = test fixture;
     PoC content LLM-generated; personal use aspirational) + reconciled vault `PROJECT.md`/
     `overview.md`.
   - [x] **Slice B — screenshot capture (done Session 60, 2026-06-25; PR #144, squash `3bf057c`).**
     Captured one real ingest→review→graph run over the Oakhaven samples and embedded six annotated
     shots in the README Demo (`docs/screenshots/`): `chunking`, `agent-activity`,
     `review-queue-judge` (judge declines a bad merge), `review-queue-merge` (cross-story merge),
     and `graph-this-story` / `graph-whole-project` (the two scope views, trimmed). Added a third
     sample `docs/samples/oakhaven-3.md` ("Chapter Seven: The Boarding"), **ingested into the
     existing two-story Oakhaven project** so the run exercises cross-story cascade matching.
     Rewrote the Demo prose (three samples; screenshot-led walk; honest graph-quality caption — the
     PoC extracts generously, curation is at the human gate, precision/de-dup is the next milestone)
     and corrected an owner-caught over-narrowing (merge-into-existing is intra-story via on-accept
     re-match, not cross-story-only — spec §3.3). Folded two `docs/BACKLOG.md` graph-quality notes.

4. ✅ **Done (Slices A–C; Slice A Session 61, Slices B+C Session 62, 2026-06-25).** **Code
   documentation** — make the codebase legible to a stranger. **Owner-reframed (Session 61)
   from "add docstrings" into "build a reusable skill+agent toolset that creates *and updates*
   code docs, then apply it" — at a portfolio "good enough" bar, prefer less-but-certain over
   speculative layers.** Sliced:
   - [x] **Slice A — the toolset + one proof slice (done Session 61, 2026-06-25; PR #146 squash
     `35a4f9f`).** Built `.claude/skills/document-code/` (the `/document-code` skill + `code-scribe`
     doctrine) — **reuses the built-in `Explore` (survey) + `general-purpose` (apply) agents; adds
     no new registered agent**; doctrine = document-only-what-the-code-verifiably-shows /
     prefer-omission-to-a-guess / doc-surface-only / navigate-don't-restate / Karpathy-minimal;
     diff-driven `changed` update mode. Proved it by authoring **`docs/CODE_GUIDE.md`** (navigation
     -only newcomer guide, linked from README's Project map) and iterating the skill once. The
     toolset is the **first lightweight stone** of the `docs/BACKLOG.md` "code documentation
     generation" item (noted there).
   - [x] **Slice B — the docstring sweep (done Session 62, 2026-06-25; folded into PR #148).** Ran
     `/document-code frontend/src/features` + a backend `api/` survey. Reality contradicted the
     premise: the whole-codebase survey found in-code docs already portfolio-grade (frontend
     `features/` 35/36 well-documented, **0 real gaps**; backend ~64% richly documented, ~16 minor
     class-doc gaps). The sweep was a near-no-op — filled the **one** genuine gap (`api/stories.py`
     `upload_story` docstring) and left the well-documented surface untouched (conservative,
     no-churn). The survey's real value was proving the in-code docs were *already there*.
   - [x] **Slice C — the agent-authored code-reference layer (done Session 62, 2026-06-25; PR #148
     squash `04263d7`).** Owner reframed item 4's remaining goal from "add docstrings" (Slice B
     showed those were done) to the missing piece: **standalone documentation in `docs/` that
     *describes* the code** for a reader/another agent, and a demonstration of the agentic system
     producing it. Built the **`docs/code/` reference layer** — one narrative note per layer (backend
     `domain`/`agents`/`adapters`/`api`; frontend `features`/`data-layer`) + an index, authored by
     one `general-purpose` agent per layer under the `code-scribe` doctrine; 180 cross-links all
     verified; wired into `CODE_GUIDE.md` + README. **Doctrine stop-and-amend:** `code-scribe` rule 6
     now recognises a drift-disciplined *reference* doc (describe at module altitude, never copy
     signatures); `/document-code` gained a `reference` mode + `changed`-mode refresh. **Forcing
     function (retro):** a `/review-pr` lens + `/wrap-session` §3a backstop so a structural code
     change refreshes the reference notes rather than letting them rot.

5. **Security & CI posture writeup** — make the project's *supply-chain + CI story* legible to a
   visitor (owner-added 2026-06-25, Session 59 resume scope review). The machinery is real but
   currently lives only in internal records (spec §6.7, `SECURITY.md`, `infra/*/WAIVERS.md`, the
   `pin-image`/`add-dependency`/`triage-advisory` skills): the **CVE approach** (OSV + Trivy gates,
   the 14-day/7-day soak rule, the time-boxed-waiver lifecycle with dated drop-whens, the daily
   scheduled scan) and the **CI approach** (path-scoped heavy jobs, always-on secret scan, branch
   protection — Session 56). This session tells that story for an outsider (its own doc or an
   expanded `SECURITY.md`, linked from the README) rather than leaving it as an implied
   "secure-by-default infra" line. _Slice finer when picked up._

6. **Doc hygiene** — confirm the ADR / `AGENTS.md` / spec / plan map is coherent and navigable
   from the README; decide whether a `CONTRIBUTING.md` earns its place for a solo-author public
   repo. **Also (owner-flagged S60):** mention in the README that the `architecture/` vault is
   **Obsidian-friendly** (it's an Obsidian-compatible Markdown vault with `[[wikilinks]]`), so a
   visitor knows they can open it in Obsidian for the graph/backlink view. _Slice finer when picked up._

_Then: the **Graph-quality** milestone (opens with a backlog-triage conversation over the 10
Session-54 findings — auto-chunker silent content-loss is the most serious), then **V2
Editing**._

---

> **Earlier milestones — archived.** Milestones 1–4 (M0 Setup, M1, M2 Basic extraction,
> M3 Cascade matching, **M4 V1 polish**) — their full task breakdowns, Decided entries, and
> Done logs — live in **[`docs/PLAN_ARCHIVE.md`](PLAN_ARCHIVE.md)** (dated, append-only). This
> file carries **only the current milestone**; the milestone-roll ritual (`/wrap-session`,
> `docs/AGENTS.md` §7) moves the just-closed milestone there.

---

### Cross-cutting (carried into Public readiness — curated at the M4→Public-readiness roll 2026-06-25; see `docs/CLAUDE.md` §3)

> **M4→Public-readiness curation pass (2026-06-25, Session 55).** Every item walked by hand per `docs/CLAUDE.md` §3. **Moved to `docs/PLAN_ARCHIVE.md` (§7):** all M4-resolved items (the `GET …/graph` story-vs-project scoping, the `world_id` cleanup, the embedding read path, store-down→503, the relation-write + decide-UI items, the M4 register/confirm-at-build Blocked entries) *plus* the M2/M3-era resolved leftovers that had lingered inline as history (the router-envelope / blank-relation fixes, the §6.5-contract reconcile, the CORS + alembic notes, the M2/M3 register entries) — the M2→M3 and M3→M4 curation-note blockquotes went with them. **The one carried item that bites *this* milestone:** the **PreNER spec drift** — it *is* Session 1 (the stop-and-amend). The rest are latent: the review-UX folds (`CandidateView` merge-target-name, the n=5 error-mappers, the shared `useReviewQueue`) fire only when a review screen next changes (a Graph-quality / V2 concern); §6.5 model-override dropdown, meta-architect ritual-wiring, window-and-stitch, the 422-overload, `last_call()` tiebreaker, the envelope-unwrap (b) dup, the repo-root `scripts/` ruff gap, and the `CandidateSpan` validator are "fix when the relevant code is next touched." **Graduated in urgency (Session 54):** re-structure delete-and-replace + a **delete-story** capability — the silent-truncation repair had no clean recovery path because neither exists. **Confirmed-resolved-and-archived this roll:** two `[ ]` items had never been struck despite being done — **DM-Rel-5** (edge re-point on an entity↔entity merge — verified live in `domain/entity_merge.py`, shipped PR #102/S40) and **OQ-20** (the relation-lifecycle state machine — verified `architecture/state-machines/relation-lifecycle.md` exists, drawn S33). Both were code/file-verified at this roll, struck, and moved to `docs/PLAN_ARCHIVE.md`'s resolved group.

- [x] ~~**Spec drift: §6 step 3 / §3 step 4 describe the spaCy PreNER baseline as an *active* pipeline stage, but it is dormant in code (surfaced Session 54 smoke).**~~ ✅ **Resolved 2026-06-25 (Public-readiness Session 1):** stop-and-amend ran — spec **§7 Step 3** now marks PreNER deferred/dormant for the PoC, with the §6.1/§6.4-adjacent/§6.5/§9 homes carrying the "built, not wired → §7 Step 3" marker, and the doc homes (`PLAN_LONG.md`, `BACKLOG.md`) reconciled to past tense. _Original finding (kept for history):_ The spec's §6 pipeline diagram ("Step 3: Pre-NER baseline (per paragraph) — PreNERAgent (spaCy) extracts simple entities") and §3 ("Step 4 can be cheaper if pre-NER (Step 3) already produced good candidates") read as if PreNER runs before the LLM extractor. In reality the live `/extract` path is **LLM-only**: `PreNERAgent` is built but wired into nothing in the extraction coordinator (the `known_entities`/PreNER-hint param is passed empty), and the code itself says PreNER injection is "deferred until a real eval exists." This is spec-vs-code drift, not a feature deferral — resolve via the **stop-and-amend flow** (propose → owner approval → amend spec to mark Step 3 deferred/dormant for the PoC, with a pointer to the data-flywheel + the eval backlog item → reconcile any other §3/§6 homes). The eval that *would* wire it is captured in `docs/BACKLOG.md` ("the spaCy PreNER baseline *without* the LLM"). Do as its own focused task; not mid-smoke. (Owner flagged Session 54, 2026-06-24.)
- [ ] **Propagate the Session-59 motivation reframe to the vault's remaining homes (at the next `review-architecture` sweep).** The honesty amend reconciled the vault *source* notes by hand (`PROJECT.md` business driver, `overview.md` Layer 2 — "genuine personal tool / the author writes Wody Święte" → designed-for / aspirational / PoC content LLM-generated), but two homes were deliberately left: the `proposals/*.md` decompositions still use **"personal tool"** as a design-driver *lens* (~6 files — `m2s2`, `m2s3`, `m3-cascade-matching`, `backend-dependency-advisory-scan`, …), and `INDEX.md` is a *derived* note. Both should be re-synced when `meta-architect:review-architecture` next runs (it's the authorized vault writer — ADR 0002; this is the Session-57 "route to the sweep" pattern, applied to the residual after the source-note hand-fix). **Low urgency** — the proposals use the phrase as an analysis lens, not an as-built overclaim — but tracked so the next sweep closes it. `architecture/CHANGELOG.md` is append-only history and stays as-is. (Session 59, 2026-06-25.)
- [ ] **§6.5 model-override dropdown** (user picks "which provider/model for this task" + per-task-type preferences). Deferred from M2.S6 as a *feature*, not thin polish (see Decided 2026-06-11). The §8.5 panel already does the *surfacing*; this is the *override* control, which touches **INV-7** (system-derived routing) and needs an INV-7/spec reconciliation + router plumbing + a preferences store. Do as a `meta-architect:decompose-requirement` step-0, alongside or after M3. (M2→M3 roll note, 2026-06-11.)
- [ ] **meta-architect workflow integration** — the three skills (`initialize`/`decompose`/`review`) are shipped and **two live runs are done** (Session 9 `initialize` → vault; Session 10 `review`+`decompose` → ADR 0003), but the skills are still **not wired into the rituals**. Deferred by design (`docs/decisions/0002`): wire on evidence *after living with the vault*, not before. Hypothesis to test: bootstrap-detect a missing `architecture/` vault in `/resume-session`; run `decompose-requirement` at feature "step 0" before the failing test; run `review-architecture` in `/wrap-session` alongside `/review-pr`. **Evidence so far (2 runs):** both produced real value (vault + ADR 0003) *and* a real maintenance cost (the accepted-proposal/decision-reconciliation drift — now mitigated by decompose §5b + `/review-pr` §2). Carry-ins: ~~(a) add a vault/docs-PR review lens to `/review-pr`~~ ✅ **done Session 10** (broadened to the decision-reconciliation lens — `/review-pr` §2 + root `AGENTS.md` + decompose §5b); (b) **[Issue #31](https://github.com/yaaisiu/story-forge/issues/31)** — meta-architect slug/filename carve-out for `PROJECT`/`INDEX`/`CHANGELOG` (still open). (Session 8 note; updated Sessions 9 + 10, 2026-06-02.) **Session 28 (retro):** fixed a smaller framework wart — the `decompose-requirement` + `review-architecture` skills said "read the matching template from `templates/`" but no `proposal.md`/`review.md` template exists (the section structure lives in each skill body); wording corrected to point at `vault-note.md` for frontmatter + name the skill-body as the single home (no duplicate template). Also **fresh evidence toward the OQ-3 ritual-wiring refinement** (run `review-architecture` at *resume* when a session opens with a decompose): Session 28 opened with a decompose on a stale-INDEX vault and had to correct the "Next steps" by hand — wiring itself stays deferred per ADR 0002, evidence accumulates. **Session 58 (2026-06-25):** the **doc-freshness-as-a-state-machine** drift recurred a **4th time** (`overview.md` stale at the V1→Public-readiness roll, plus a stale INDEX "Next") — each occurrence caught only by a *manual* sweep because the sweep isn't wired into the roll ritual. The owner's call (retro): the 4th recurrence is **strong evidence the wiring should land**, but it's an architectural change (amends ADR 0002) deserving **its own focused session**, not a retro fold — so revisit the wiring deliberately later, not now. (The interim mitigation — a §6b self-check in the `review-architecture` skill so the sweep stops *leaking* freshness debt — landed Session 58.)
- [ ] **Drop the Session-27 time-boxed security waivers as their fixes soak (`/triage-advisory`).** Clearing the security-job reds (PRs #72/#73) needed soaked bumps + **fix-first time-boxed waivers** for residuals whose fixes were still inside the 14-day soak. Drop each (bump to the now-soaked fix, delete the `[[IgnoredVulns]]` block + `WAIVERS.md` row) when it bakes: **python-multipart 0.0.31 — soaks 2026-06-18** (GHSA-v9pg-7xvm-68hf, low); **starlette 1.3.0 — soaks 2026-06-25** (GHSA-jp82-jpqv-5vv3, low); **starlette 1.3.1 — soaks 2026-06-26** (GHSA-82w8-qh3p-5jfq, **HIGH** DoS). Plus the **condition-based** neo4j Trivy waiver **CVE-2026-50010** (netty TLS hostname-verification bypass) — drop when a Neo4j 5.26.x image ships netty ≥4.1.135.Final. `/resume-session` step 3b now flags the dated ones; the `ignoreUntil` CI re-red is the backstop. (Session 27, 2026-06-16.)
- [ ] Update spec §10 if any open question hardens (extraction granularity, etc.)
- [ ] **`CandidateView` carries no merge-*target* name — Stage-2/3 merges show a generic label (Session 25).** The review card resolves the proposed merge target's name by matching `target_entity_id` against the staged top-3 `alternatives` — but those are ranked by **RapidFuzz** while a Stage-2/3 merge target is chosen by **embedding cosine / judge** (`candidate_staging._cascade` + `matching_agent.stage2`), a different signal, so the target need not be in the list. `CandidateView` (`api/stories.py`) exposes only `target_entity_id`, no name, so on those paths the badge falls back to `Merge → an existing entity` (the `/code-review` should-fix; the raw-UUID was folded out in S4b). Proper fix: add `target_canonical_name` to `CandidateView` (resolve it at stage time from the in-scope `existing`/`snapshot` entities, or at read time) so the reviewer sees *what* they're merging into on every path. Backend slice + openapi regen — track here, do when the §3.3 review UX is tightened. (Session 25 `/code-review` finding.)
- [ ] **Per-screen `reviewMessage`/`extractMessage`/`decideMessage`/`messageForError` error-to-string mappers duplicate a pattern (now 5 screens — Session 25, updated S30).** `ReviewQueue`, `GraphViewer`, `UploadScreen`, `OutlineEditor`, and now `RelationQueue` (`decideMessage`, M3.S4f) each carry a near-identical `ApiError → string` switch (differing only in which statuses they special-case). At n=5 this is well past the rule-of-three; a shared `errorToMessage(error, overrides?)` / `apiErrorFallback(error, default)` in `lib/api/` with per-call status overrides would fold the *skeleton* (non-ApiError fallback + the final `error.detail || HTTP status` line) while each screen keeps its own status→string switch. Pre-existing convention — fold when one of these screens next needs an error-message change (don't pre-emptively refactor all five). (Session 25 `/code-review` reuse finding; n bumped S30.)
- [ ] **The two review queues (`extraction-review` / `relation-review`) share copied machinery with two latent nits (Session 30).** `RelationQueue` mirrors `ReviewQueue` by copy (not a shared `useReviewQueue` hook): `isEditableTarget` (n=2), the no-deps window-keydown effect, the render-time `selectedIndex` clamp, the per-card `pending` guard. Two verified-real behaviours ride along in **both**: (a) the **keyboard A/R path is not gated by an in-flight mutation** while the buttons are — a fast double-key re-fires the decision for the selected item (harmless: deterministic edge/entity ids + the backend's `already_decided` no-op); (b) a **stale decide/review error banner persists across J/K navigation** until the next mutation. Left unfolded in S4f because fixing them *here only* would diverge the two queues — the right fix is a **shared** one (extract a `useReviewQueue` hook, or gate the keyboard handler in both) at the n=3 trigger or when a queue next needs a behaviour change. (M3.S4f `/code-review`, 2026-06-17.)
- [ ] **`PostgresCostStore.last_call()` ordering has no tiebreaker.** `ORDER BY created_at DESC LIMIT 1` decides the §8.5 panel's "most recent call"; `created_at` defaults to `now()` (transaction-start time), and there's no monotonic column (the PK is a random uuid). Unreachable on the as-built **sequential single-user** extraction path (rows land seconds apart), but two rows sharing a `created_at` would make "most recent" nondeterministic. Lands with the same concurrency work spec §6.6 already defers ("a true reserve-then-reconcile lands only if batched concurrent calls do") — add a monotonic tiebreaker (a `bigserial`, or order by a real insertion sequence) **if/when** concurrent LLM calls are introduced. (M2.S5 `/code-review` finding, 2026-06-11.)
- [ ] **Orphaned sandbox files on upload failure** — `api/stories.py` writes the original file to the sandbox *before* the `Project`/`Story` inserts; if the DB write fails the transaction rolls back but the file is left on disk, pointing at a story that never persisted. Harmless on a single-user local app, so deferred. Fix when it bites or when a delete-story feature lands (clean up the file on insert failure, or write-after-commit). (Own-review note, 2026-05-21.)
- [ ] **Window-and-stitch for `auto` on long text** — `agents/chunking_coordinator.py` currently raises `ChunkingTooLongError` above `max_input_words` (a 422). The proper fix is a deterministic pre-split on paragraph boundaries, per-window agent calls, offsetting paragraph indices, and stitching the chapters/scenes back together — accepting some seam imperfection that the user corrects in the UI confirm. Deferred until the auto path is actually exercised on a real 50k-word input. (Session 4 windowing-guard decision, 2026-05-25.)
- [ ] **Re-structure semantics: delete-and-replace** — `POST /stories/{id}/structure` returns 409 if the story already has a tree. A re-run with a different mode ("I didn't like auto, let me redo manual") needs delete-and-replace, which the Session 1 repo doesn't have (Update was deferred). Session 6 added a *narrow* update — `update_story_raw_text` in the repo + an override-and-persist branch on the structure endpoint — covering the manual editor's "user accepts/edits" use case (spec §7 step 2). The full delete-and-replace UX (redo a different mode after the first persist) is still deferred. Add when the frontend wants it. (Session 4 endpoint-shape decision, 2026-05-25; narrowed Session 6, 2026-05-26.)
- [ ] **`/stories/{id}/structure` 422 is overloaded.** Two distinct failure modes return 422 with different body shapes: FastAPI's auto-generated request-validation errors (shape: `HTTPValidationError`) and the explicit `ChunkingTooLongError` raised when the input exceeds `max_input_words` (shape: `{detail: str}`). The Session 5 PR documented the unambiguous error codes (400/404/409/413/415/502) in OpenAPI but left 422 alone because declaring it would overwrite FastAPI's validation shape. Pick one: either remap `ChunkingTooLongError` to 413 (payload too large — semantically truer; the body *is* too long for the agent) or to a free status code in the 4xx range, so 422 stays purely validation-shaped and the frontend can discriminate the two cases by type rather than body inspection. Lands cleanly with the "window-and-stitch for auto on long text" cross-cutting fix above, which removes the error path entirely for the auto mode. (Session 5 Codex-review fold-in note, 2026-05-25.)
- **Agent retry loop + adapter envelope-unwrap are duplicated (revisit at rule-of-three).** Two altitude dups surfaced in the M2.S3 `/code-review`: (a) the `render → call → validate → retry-on-ValidationError → give-up` loop is near-verbatim in `chunking_agent.py` and `extraction_agent.py` (only the schema/weight/grounding differ); (b) the `response.json()` + envelope-unwrap-or-raise-`ProviderResponseError` block is near-identical in `ollama.py` and `openrouter.py` (only the dict path differs). **(a) ✅ Resolved 2026-06-15 (M3.S3, PR #60):** JudgeAgent was the n=3 trigger — the loop folded into `agents/validation.py` `validate_with_retry(...)`; chunking + extraction refactored onto it (each keeps its own schema/call/give-up error); `TaskWeight` + a `Router` Protocol promoted into `base.py` at the same time. **(b) still open** — deliberately per-copy at n=2; **fold at the first direct vendor adapter** (Anthropic/Grok/…, "as needed"), since each new copy re-implements the INV-6/OQ-4 no-body-leak rule by hand, and a `_unwrap_content(...)` in `base.py` would touch that security-sensitive guarantee. (M2.S3 `/code-review` altitude findings, 2026-06-08.)
- **Repo-root `scripts/` is outside CI's ruff scope.** The backend ruff config is `src = ["src", "tests"]` and CI runs `ruff check .` from `backend/`, so `scripts/*.py` (e.g. `check_dependency_age.py`) are never linted — a pre-existing `UP017` (`datetime.UTC`) sits there uncaught. Either add repo-root `scripts/` to a lint step or fix the one nit. Left untouched in PR #25 to stay surgical. (M2.S1 own-review nit, 2026-06-01.)
- **`CandidateSpan` has no `char_start <= char_end` validator** (`domain/extraction.py`). Internal deterministic data from spaCy, so optional — a model validator would just self-document the invariant. Add if a future producer of `CandidateSpan` is less trustworthy than spaCy. (M2.S1 own-review nit, 2026-06-01.)
- [ ] **Make the next `PLAN_SHORT` trim a *learning loop*, not just a move (owner, S61 resume).** Before the next round of `PLAN_SHORT` trimming/archiving (the §7 roll move), **prepare the process first** so we have the skills/agents/settings to do more than relocate data: as content moves out, **analyse it in the context of the rest of the system** and actively look for things worth improving — patterns to harden into a skill, work to hand to an agent, or a new feedback loop for our evolving system. The current `/wrap-session` §7 step is a pure *move*; the owner wants the trim to also *mine* the archived material for process improvements. Scope/shape the tooling (a dedicated skill? an analysis agent pass at roll time?) as its own preparation task **before** the next milestone roll that triggers a trim. (Owner note, 2026-06-25, Session 61.)

### Blocked / questions

- **Review-gate policy after Codex retirement (deferred 2026-06-24).** The GitHub-wired Codex PR review is gone (owner has no subscription), so there is **no automated second reviewer**. `/review-pr` (self) stays the primary gate and `/code-review` (multi-agent) stays *recommended* for substantive code. **Open:** should `/code-review` become a **required** second gate for substantive (non-docs) code, or stay optional? Settle in a later session (see root `AGENTS.md` Merge-flow "Review model").
- _**Earlier resolved registers / questions (Milestones 1–4)** — see **[`docs/PLAN_ARCHIVE.md`](PLAN_ARCHIVE.md)**._

### Decided

- **2026-06-25 — Session 62 (Public-readiness Session 4, Slices B+C): docstring sweep (a near-no-op) + the agent-authored `docs/code/` reference layer (docs/skill/one-docstring; PR #148 squash-merged `04263d7`).** **Slice B** ran the toolset over the in-code gaps and the survey **contradicted the plan's premise**: a whole-codebase Explore survey found docs already portfolio-grade (frontend `features/` 35/36, **0 real gaps**; backend ~64% richly documented, ~16 minor class-doc gaps), so the "sweep" filled exactly **one** genuine gap (`api/stories.py` `upload_story`) and left good docs untouched (no churn). The owner then asked the real question — *do we actually have code documentation, or do we need a loop?* — which surfaced that the **standalone, descriptive** layer was missing. **Slice C decisions:** (a) **build an agent-authored narrative *reference* layer** (`docs/code/`, one note per layer + index) over the alternatives — chosen because it *demonstrates the agentic system comprehending its own code*, the portfolio thesis; rejected a generated `pdoc`/TypeDoc reference (showcases a generator, adds deps, dry), a both/hybrid (most upkeep), and deepening the single `CODE_GUIDE` (less reference feel); (b) **GitHub-friendly relative Markdown links, not `[[wikilinks]]`** (public repo renders on GitHub; Obsidian still opens relative links); (c) **stop-and-amend the `code-scribe` doctrine** — rule 6 now licenses a drift-disciplined *reference* doc (describe at module altitude, never copy signatures/behaviour), because the new layer is exactly the "restate" the doctrine had forbidden; `/document-code` gained a `reference` mode + `changed`-mode refresh. **Forcing function (retro, owner chose defense-in-depth):** a **`/review-pr` §2 lens** (a structural code change that leaves a `docs/code` note stale gets flagged → `/document-code changed`) **plus a `/wrap-session` §3a backstop** — so the reference layer can't silently rot the way arch-reports/waivers needed their own forcing functions; rejected a deterministic git-hook (can't cheaply tell structural from cosmetic — would fire noisily). **Skill fix (retro):** `/document-code` §4a now hands each parallel writer agent the *planned* sibling filenames up front, removing the six-edit cross-link cleanup this run cost. *Considered & rejected:* manufacturing docstring work after the survey showed none was needed (conservative doctrine forbids churn); chaining the build straight past the owner's reframe (the question *was* the unit boundary). **Self-`/review-pr`:** ready, no blocking findings — sampled 8 high-specificity doc claims against the code (all true), 180 links resolve. **Checks:** doc surface + one docstring — full CI ran (code-bearing PR) **green**; backend gates green at wrap (542 passed).
- **2026-06-25 — Session 61 (Public-readiness Session 4, Slice A): code-documentation toolset, proven on one slice (docs/tooling only — no production code; PR #146 squash-merged `35a4f9f`).** Owner reframed the bare "add docstrings" task into **build a reusable skill+agent toolset that creates *and updates* code docs**, at a portfolio "good enough" bar (*prefer less-but-certain over speculative layers*). **Decisions:** (a) **register no new agent** — built-in `Explore` (survey) + `general-purpose` (apply) cover both phases; the only genuinely-new piece is the *conservative documentation doctrine* (`code-scribe.md`), a persona the executor *adopts* (the meta-architect pattern, lighter — no plugin/marketplace ceremony, Story-Forge-specific) — landed only after the owner pushed **twice** to check existing agents first; (b) **standalone docs = a thin nav guide** (`docs/CODE_GUIDE.md`, navigation-only — reading order, request-trace, dir map, links to the always-current `AGENTS.md`/spec; never restating them, so it can't drift) over a fuller code guide; (c) **prove on one slice + iterate once** this session (authored the guide → refined the skill: *cite filenames by checking them, a fabricated ADR link was caught only at verify*), with the **docstring sweep deferred to the next conversation**. **Side-notes handled:** an **agentic-system architecture note** is real-but-distributed (no consolidated home; vault `components/` empty) → recorded in `docs/BACKLOG.md` (not urgent); the toolset noted as the **first lightweight stone** of the BACKLOG "code documentation generation" item. **Retro (owner-approved one rule):** root `AGENTS.md` §1 "Simplicity first" gained **"Reuse before building — especially tooling"** (survey built-in subagents / project skills / the meta-architect plugin before minting a new agent/skill) — earned by the twice-prompted reuse redirect. *Considered & rejected:* a bespoke registered doc-writer agent (built-ins + a doctrine suffice); a fuller restating code guide (drift risk — against "less but certain"); building the toolset only / a full sweep this session (owner chose build+prove+iterate, sweep next). **Self-`/review-pr`** caught 3 guide should-fixes (a self-contradictory reading-order, a §7-conflating request-trace + omitted embedding step, a stale BACKLOG home) — all folded. **Checks:** docs/tooling-only — code gates n/a; CI docs-skip (`changes` + `secret-scan`) green.
- **2026-06-25 — Session 60 (Public-readiness Session 3, Slice B): README Demo screenshots (docs-only; PR #144 squash-merged `3bf057c`).** Captured + embedded six annotated screenshots completing the Demo section. **Decisions in passing:** (a) **ingested the new third sample into the *existing* two-story Oakhaven project**, not a fresh one — chosen (owner) to exercise + exhibit cross-story cascade matching, after a live read-only check of the graph showed the existing project was viable for it; (b) **showed the over-saturated graph honestly** rather than curating it down — the PoC deliberately over-extracts, curation lives at the human gate, and graph-quality/de-dup is the next milestone, so the Demo includes both the story-scoped and whole-project (hairball) views with an honest caption; (c) **trimmed the two graph PNGs** (pure-stdlib PNG crop) to drop the cut-off activity sidebar. *Considered & rejected:* a fresh isolated project (cleaner graph but demonstrates less — the cross-story feature is the headline); curating the graph to ~12 clean nodes (would mask the known over-extraction the owner wants exposed); fixing over-extraction first (out of Slice-B scope, it's the Graph-quality milestone). **Owner-caught accuracy fix:** the Demo framed merge-into-existing as cross-story-only; spec §3.3's on-accept re-match makes it intra-story too — reframed to the general mechanism. **Retro (owner-approved one skill edit):** added a `/review-pr` §2 bullet — *prose that describes a mechanism must be checked for completeness, not just non-falsity* (an over-narrowed-but-true description is a fidelity gap the claim-by-claim check passes). *Considered & rejected:* a new rule for "verify live state before recommending" (already covered by the Karpathy "don't assume" rule; demo capture is rare — simplicity-first). **Two notes recorded (act later, not now):** README "architecture vault is Obsidian-friendly" → Session-6 doc-hygiene task; a **PoC→next-phase preparation gate** → `docs/PLAN_LONG.md` + the handoff. **Checks:** docs-only (README/screenshots/sample/BACKLOG + the `/review-pr` skill edit) — code gates n/a; self-`/review-pr` ran (one nit + one owner-caught accuracy fix folded); CI docs-skip (`changes` + `secret-scan`) green.
- **2026-06-25 — Session 59 (Public-readiness Session 3, Slice A): README portfolio overhaul + an owner-surfaced spec §2.1/vault motivation amend (docs-only — README + spec + vault; no production code; PR #142 squash-merged `1a4ed87`).** Rewrote `README.md` portfolio-lead (architecture story first, quickstart after) and corrected its stale facts. Mid-review the **owner caught a factual overclaim**: the README (drawn from spec §2.1) framed Story Forge as "a genuine personal tool for an author writing *Wody Święte*" — but the sample stories are **LLM-generated**, there's no real manuscript, and personal authoring/research use is **aspirational** (the immediate driver is exploring the architecture + build process). Ran the **stop-and-amend** (owner chose "amend now, this conversation"): reframed **spec §2.1** (Wody Święte → the project's *test fixture*, App. B) and reconciled the vault **`PROJECT.md`** (business driver 1) + **`overview.md`** (Layer 2). **Decisions in passing:** (a) **opened Session 5 — "Security & CI posture writeup"** (owner-added at the resume scope review: the CVE + CI story lives only in internal records — §6.7/`SECURITY.md`/`WAIVERS.md`/skills — so a visitor-facing writeup earns a dedicated session; doc-hygiene shifts to 6); (b) README security kept **brief + link-forward** to Session 5, not the full CVE/CI narrative; (c) the spec amend **rode in the README PR** (same honesty theme, one curated squash) rather than a separate PR; (d) the vault *source* notes were **hand-corrected** (permitted by `architecture/AGENTS.md`) rather than routed to a sweep — a deliberate departure from Session 57's "route it" choice, justified by *no sweep currently planned* + owner's amend-now; the residual `proposals/*.md`/INDEX propagation is tracked for the next sweep (cross-cutting). *Considered & rejected:* a fuller security section in the README now (would pre-empt Session 5); a separate PR for the spec amend (over-ceremony for a tightly-coupled framing fix); leaving the spec as intended-persona (owner judged it a real overclaim); routing the whole vault fix to a heavyweight sweep 8 days after the last (disproportionate for a 2-line source-note correction). **Retro (owner-approved two doc edits):** (A) `architecture/AGENTS.md` now states *targeted source-note hand-edit vs route-to-a-sweep* — removes the recurring per-case judgment (2nd occurrence after S57); (B) the `meta-architect:review-architecture` skill gained a **§1b "foundational inputs the sweep can't verify"** — the Layer-1/2 persona/business inputs are owner-asserted, to be *flagged for re-confirmation* not machine-validated (names the blind spot that let this session's overclaim survive S58's full vault sweep). *Considered & rejected:* a periodic re-confirm *ritual* (ceremony for a low-frequency class the public-review lens already catches — B's flag-don't-validate note suffices); a rule for conversation-scope-stretch (owner-directed each time). **Checks:** docs-only (README/spec/vault/skill) — code gates n/a; self-`/review-pr` clean (load-bearing pass = the whole-repo §2 framing sweep); CI docs-skip (`changes` + `secret-scan`) green.
- **2026-06-25 — Session 58 (Public-readiness Session 2): architecture-vault re-sync + a retro fix to the sweep skill (vault/skill docs only — no production code; PR #140 squash-merged `842bd54`).** Ran `meta-architect:review-architecture` to bring the vault current with V1-complete reality + the §7-Step-3 PreNER amendment; report `reports/2026-06-25-architecture-review.md`, no blockers. **Retro decisions (owner):** (1) **amend the `review-architecture` skill** with a closing **§6b self-check** — bump `updated:` on every note edited (incl. proposal BUILT/resolved banners) + a §2-style grep for stale `Next:`/`proposed`/build-pending framing — *because the sweep itself leaked two freshness defects this session* (a stale proposal date; an un-cleared handoff-flagged INDEX "Next: M4"), caught only by the follow-on `/review-pr`; (2) **INDEX "Next steps" growth** — the run-log framing note (PR #140) **suffices**, no prune/cap rule added (simplicity); (3) **ADR-0002 ritual-wiring** — the 4th doc-freshness recurrence is strong evidence to wire `review-architecture` into the roll, but that's architectural and gets **its own session**, not a retro fold. *Considered & rejected:* date-bump-half-only (the stale-phrase grep earns its place too); a prune/cap rule for INDEX (framing note is enough); wiring the ritual now (scope creep into an ADR change). **Checks:** vault/skill docs only — code gates n/a; self-`/review-pr` clean post-fold; CI docs-skip (`changes` + `secret-scan`) green.
- **2026-06-25 — Unplanned interrupt: repo went PUBLIC + CI rework + light branch protection + stale-branch cleanup (infra/spec/governance; the `ci.yml`+spec change ships as a PR).** GitHub Actions stopped running because the **private** repo's free-minutes pool was exhausted. The repo was always *intended* public (CLAUDE.md/spec "public from day one… portfolio piece") and the current milestone is **Public readiness**, so the fix was to bring reality in line with intent: **flipped the repo to public** after a clean git-history pre-flight (no `.env`/secrets/keys ever committed) → **unlimited free standard-runner Actions**, unblocked. **(1) CI restructure (`ci.yml`):** a native `changes` job path-scopes the heavy jobs (service-container suites, frontend build, dep-age/OSV/Trivy scans) to **code-bearing** changes (anything outside `docs/**`, `architecture/**`, `*.md`); `detect-secrets` split into an **always-on `secret-scan`** job (every push/PR incl. docs); a **daily `schedule`** force-runs the scans so the CVE-treadmill is caught regardless of PR cadence; `concurrency` cancels superseded PR runs; uv/npm caching. **(2) §6.7 spec amendment** (stop-and-amend — the docs-skip touches the stated "scans on every push/PR" non-negotiable): added a **"CI scan cadence"** bullet, adjusted the OSV/Trivy cadence lines + the §7 CI list, reconciled `AGENTS.md` merge-flow + the `pin-image` skill. Framed **adapts-not-relaxes** (every code change still fully scanned + daily; treadmill coverage moves from per-PR-incidental to guaranteed-daily). **(3) Light branch protection** on `main` (block force-push/delete + require a PR; no required reviews/checks — owner still holds the merge button). **(4) Stale-branch cleanup** — deleted the 8 leftover remote branches (all mapped to MERGED PRs; the red-✗ ones were just old branch-tip status). *Considered & rejected:* top-up-and-stay-private (pays for what public gives free, and the repo's identity is public); whole-workflow docs-skip (owner chose to keep a fast secret-scan always-on); moving scans *off* every PR to save minutes (moot once public — minutes free, so we kept max per-code-PR posture). **Unplanned interrupt** ahead of the milestone's Session-1 PreNER amend (still the next planned unit); `/wrap-session` assigns the session number + Done line. **Checks:** the `ci.yml` PR runs full CI (it edits `.github/**` = code-bearing).
- **2026-06-25 — Session 57 (Public-readiness Session 1): PreNER spec↔reality stop-and-amend (spec/docs only — no production code; PR #138, `d0ee135`).** Resolved the owner-flagged drift (S54 smoke) that the spec described the spaCy `PreNERAgent` as an *active* pipeline stage while in code it is dormant (the live `/extract` is LLM-only). Ran the **stop-and-amend flow**: proposed exact edits → owner approval → amended the spec, then reconciled every home in one pass. **Decided in passing:** (a) the pipeline diagram actually lives in **§7**, not §6/§3 as the old handoff/cross-cutting loosely labelled — all new markers use the correct "§7 Step 3"; (b) **§6.4 left unchanged** (its "pre-NER assist" is a tier *use-case* list, not a claim the pipeline runs PreNER) — kept the amend surgical; (c) the **architecture vault** (PROJECT.md/overview frame PreNER as a live stage) is **not** hand-edited — writer-restricted to `meta-architect:*` (ADR 0002) — and is routed to **Session 2**'s `review-architecture` sweep (now named in that task). *Considered & rejected:* deleting the Step-3 box (it's the intended future design — marked DEFERRED instead); marking §6.4 too (over-editing an ambiguous, non-drift mention). **Checks:** docs/spec-only — code gates n/a; self-`/review-pr` clean (load-bearing pass was the §2 whole-repo reconciliation sweep); CI ran the docs-skip (`changes` + `secret-scan` only, heavy jobs skipped) green.
- **2026-06-25 — Session 55: the V1 → Public-readiness milestone ROLL (governance/planning only — no production code).** Mechanical roll of the DECIDED direction (owner, S54): all of V1 (M0–M4) is feature-complete, so M4 moves to the archive and a new **Public readiness** milestone opens. **(1) Archive move** — M4's task section + every Decided/Done entry + its resolved cross-cutting/Blocked items (and the lingering M2/M3 resolved leftovers + the two curation-note blockquotes) → `docs/PLAN_ARCHIVE.md`, byte-exact (verified by dated-bullet count); `PLAN_SHORT.md` dropped from ~230 KB to well under the §7 ~200 KB flag. **(2) Cross-cutting re-curated by hand** (§3) — 24 resolved cross-cutting + 16 resolved Blocked archived; 16 live cross-cutting + 1 live Blocked carried. **(3) New milestone opened** as *skeleton + first session* (owner's slicing-depth call): goal + 4-session outline, only Session 1 (the PreNER stop-and-amend) detailed. **(4) Drift caught + corrected:** the old handoff listed *two* spec↔reality stop-and-amends (PreNER + world-graph §3.6/§9); a grep of the spec showed the world-graph amend was **already done** (Session 49, PR #119 — the 4 residual mentions are intentional out-of-PoC deferral notes), so only PreNER remains — the milestone reflects one amend, not two. **(5) Two never-struck-but-done items resolved:** DM-Rel-5 (edge re-point on merge) and OQ-20 (relation-lifecycle state machine) sat as live `[ ]` items though M4 had completed them; **code/file-verified at this roll** (`domain/entity_merge.py`, `architecture/state-machines/relation-lifecycle.md`), then struck + archived rather than carried as limbo. *Considered & rejected:* full session-by-session slicing now (owner chose skeleton-first to avoid speculative planning); carrying DM-Rel-5/OQ-20 forward as flagged-but-live (an earlier draft did — the owner pushed to resolve, so they were verified and struck). **Checks:** docs-only (`docs/PLAN_SHORT.md` + `docs/PLAN_ARCHIVE.md`) — code gates n/a.
- _**Earlier decisions (Milestones 1–4)** — see **[`docs/PLAN_ARCHIVE.md`](PLAN_ARCHIVE.md)**._

### Done in previous sessions

- **2026-06-25 — Session 62 (Public-readiness Session 4, Slices B+C): the docstring sweep proved the in-code docs were already done, so the work became an agent-authored `docs/code/` reference layer (PR #148 merged green, `04263d7`).** Picked up item 4 Slice B — the planned docstring sweep — by running the new `/document-code` toolset. The honest result was the *opposite* of the plan's premise: a whole-codebase Explore survey (frontend `features/`, then every backend layer) found the in-code documentation already portfolio-grade — frontend `features/` 35/36 well-documented with **0 real gaps**, the backend ~64% richly documented — so the "sweep" was a near-no-op that filled the **one** genuine gap (`api/stories.py` `upload_story`, whose contract — file-type/size validation, four failure codes, the mint-vs-add-to-project branch, sandbox-before-write order — wasn't readable from the signature) and deliberately churned nothing else. That answered the owner's "do we have it?" for *in-code* docs: yes, written test-by-test as the code grew. The owner then clarified that the goal he meant was **standalone documentation in `docs/` that describes the code** for a reader or another agent — which *was* missing — and that the point is to **show the agentic system can produce it**. So Slice C built exactly that: a **`docs/code/` reference layer**, one narrative note per layer (backend `domain`/`agents`/`adapters`/`api`; frontend `features`/`data-layer`) plus an index, each describing module responsibilities and key classes/routes at module altitude and cross-linked into the code — authored by **one `general-purpose` agent per layer, each adopting the `code-scribe` doctrine** (the agentic showcase: all six independently refused to emit broken sibling links and flagged it, rather than guessing). 180 relative links, all verified; wired into `CODE_GUIDE.md` (the navigation layer) and the README Project map. Because a descriptive reference layer is exactly the "restate the code" the `code-scribe` doctrine had forbidden, ran a **stop-and-amend**: doctrine rule 6 now recognises a drift-disciplined *reference* doc (describe at altitude, never copy signatures), and `/document-code` gained a `reference` mode plus a `changed`-mode refresh of the notes. **Process changes (retro, focused on the doc system as the owner asked):** to make the system *remember* to keep these docs fresh, added a **`/review-pr` §2 lens** (a structural code change leaving a `docs/code` note stale gets flagged → run `/document-code changed`) **and** a **`/wrap-session` §3a backstop** (defense-in-depth, the owner's pick — mirroring how security waivers and architecture reports each got their own forcing function); and fixed the `/document-code` `reference` mode so parallel writer agents are handed their siblings' planned filenames up front (this run cost a six-edit cross-link cleanup without it). **Lesson:** a survey that *disproves* the planned work is a success, not a waste — it turned a busywork sweep into the deliverable that was actually missing; and a new doc layer needs a forcing function the day it lands, or it rots like every other un-triggered artifact. **Next: Public-readiness Session 5 (Security & CI posture writeup) or Session 6 (Doc hygiene) — owner's pick; item 4 is fully done.** **Checks:** doc surface + one docstring — code-bearing PR ran **full CI green**; backend gates green at wrap (ruff/format/mypy clean, 542 passed).
- **2026-06-25 — Session 61 (Public-readiness Session 4, Slice A): a reusable code-documentation toolset, proven on one slice (docs/tooling only; PR #146 merged green, `35a4f9f`).** The owner reframed milestone item 4 from "add docstrings" into "build the *system* that documents the code and keeps it honest" — at a portfolio "good enough" bar, with a hard steer to *prefer less-but-certain over speculative layers*. Built `.claude/skills/document-code/`: a `/document-code` **skill** + a `code-scribe` **doctrine** (document only what the code verifiably shows; prefer omission to a guess; touch only the doc surface; navigate-don't-restate; Karpathy-minimal), with a diff-driven `changed` **update mode** so the same system refreshes docs as code moves. The pivotal design call — made only after the owner pushed **twice** to check what we already had — was to **register no new agent**: the built-in `Explore` (read-only survey) and `general-purpose` (apply) subagents cover both phases, so the only genuinely-new piece is the doctrine *text* the executor adopts (the meta-architect pattern, minus the plugin ceremony). **Proved it on one slice** by running `/document-code guide` to author **`docs/CODE_GUIDE.md`** — a navigation-only newcomer guide (dependencies-first reading order, a request traced upload→graph against spec §7, a directory map, links to the always-current `AGENTS.md`/spec; it points, never restates, so it can't drift far) — linked from README's Project map, then **iterated the skill once** from what the run taught (cite filenames by checking them — a fabricated ADR link was caught only at the verify gate). Backend is already ~98% documented; the **docstring sweep** (frontend `features/*` components + a few backend gaps) is deliberately **deferred to the next conversation** — this session was the toolset + one proof. Two owner side-notes were handled: an **agentic-system architecture note** (real but distributed across spec §6.5/§7, the vault, ADRs, README, `AGENTS.md`, the 98% docstrings; no consolidated home — the vault's `components/` dir is empty) → recorded in `docs/BACKLOG.md` as not-urgent; and the toolset noted there as the **first lightweight stone** of the existing "code documentation generation" backlog item. Self-`/review-pr` (the mechanism-completeness lens added in S60 earned its keep) caught three guide should-fixes — a self-contradictory reading-order, a request-trace that conflated the automated cascade with the human-review stage and omitted embedding, and the stale BACKLOG home — all folded. **Process change (retro):** root `AGENTS.md` §1 "Simplicity first" gained **"Reuse before building — especially tooling"** (survey the built-in subagents / project skills / meta-architect plugin before minting a new agent/skill), earned by exactly the twice-prompted reuse redirect. **Lesson:** when the change *is* tooling, "reuse before building" needs stating explicitly — the instinct that's obvious for product code wasn't my default for agent design until the owner prompted it. **Next: Public-readiness Session 4 Slice B** — run `/document-code` over the frontend components + the backend gaps (or Session 5 security writeup / Session 6 doc-hygiene, owner's pick — they're not strictly ordered). **Checks:** docs/tooling-only — code CI gates don't apply; CI ran the docs-skip (`changes` + `secret-scan`) green; self-`/review-pr` (3 should-fixes folded).
- **2026-06-25 — Session 60 (Public-readiness Session 3, Slice B): README Demo screenshots — annotated end-to-end pipeline walk (docs-only; PR #144 merged green, `3bf057c`).** Completed milestone item 3: captured one real ingest→review→graph run over the Oakhaven samples and embedded six annotated screenshots in the README Demo (`docs/screenshots/`) — outline/chunking, the agent-activity panel (showing the routed `gpt-oss:120b-cloud` cloud-free call, latency, cost), the Stage-3 judge *declining* a bad merge with its reasoning, a *cross-story* merge proposal, and the story-scoped vs whole-project graph views. Added a third sample (`docs/samples/oakhaven-3.md`, "Chapter Seven: The Boarding") ingested into the existing two-story project so the run genuinely exercises cross-story cascade matching. Rewrote the Demo prose: three samples (was two), a screenshot-led walk, and an **honest graph-quality caption** — the PoC over-extracts on purpose, curation lives at the human gate, and precision/de-dup is the next milestone (the whole-project hairball is shown deliberately). Two graph PNGs were trimmed (pure-stdlib PNG crop) to drop the cut-off activity sidebar. The owner caught one over-narrowed claim — merge-into-existing was framed cross-story-only, but spec §3.3's on-accept re-match makes it intra-story too — fixed to the general mechanism. **Process:** surfaced + recorded two graph-quality notes in `docs/BACKLOG.md` during the run (entity-span granularity is case-dependent — "smuggler" is an alias, "desperate" a transient state; and the author's own text-knowledge is load-bearing for merges today, with iteration needed beyond one pass). **Retro** added a `/review-pr` §2 bullet — *prose describing a mechanism must be checked for completeness, not just non-falsity* — encoding exactly the over-narrowing the owner caught. **Next: Session 4 — code documentation** (per-directory `AGENTS.md` coverage + docstrings), per the milestone skeleton. **Checks:** docs-only — code CI gates don't apply; CI ran the docs-skip (`changes` + `secret-scan`) green; self-`/review-pr` run (nit + accuracy fix folded before merge).
- **2026-06-25 — Session 59 (Public-readiness Session 3, Slice A): README portfolio overhaul + an owner-surfaced spec/vault motivation amend (docs-only; PR #142 merged green, `1a4ed87`).** Rewrote `README.md` for an outside visitor as the headline portfolio piece — restructured from run-book to portfolio-lead (`What is this` → `Demo` → `Architecture` → `Quickstart` → `Development` → `Project map`), with a Mermaid ingest-pipeline diagram, the real M2–M4 agent inventory, the multi-model routing story (provider order, within-tier failover, fail-closed budget cap), the layered backend, and a brief secure-by-default paragraph that links forward to the new Session-5 security writeup. Stale facts were corrected (the Status line still said "M1 complete / entering M2" though V1 is feature-complete; the agent/routing/ADR pointers were thin). The Demo section was written to stand on its own on the bundled Oakhaven sample, so no `TODO` or broken image embed lands on `main` between this slice and the screenshot-capture slice (B). **The session's real lesson came from the owner's review:** the README — faithfully drawn from spec §2.1 — claimed Story Forge is "a genuine personal tool for an author writing *Wody Święte*", and the owner flagged that this is not true: the sample stories are LLM-generated for the PoC, there is no real manuscript, and personal authoring/research use is only a longer-term aim. This was a *foundational-input* overclaim, not as-built drift — it had propagated from the spec/interview into the README and the vault, and Session 58's vault re-sync (a freshness/honesty sweep just one session earlier) could not have caught it, because no internal consistency check can know the owner isn't the persona the spec assumes. The public-readiness "stranger reads it cold" lens is exactly what surfaced it. Ran the stop-and-amend (owner: "amend now"): spec §2.1 reframed (Wody Święte as the project's test fixture), vault `PROJECT.md`/`overview.md` reconciled, the README made honest. Also opened a 6th milestone session (Security & CI posture writeup) at the resume scope review, since the CVE/CI story lived only in internal records. **Retro** produced two vault-convention edits: a *hand-edit-vs-route-to-sweep* rule in `architecture/AGENTS.md`, and a new §1b *"foundational inputs the sweep can't verify"* in the `review-architecture` skill — the latter naming the structural reason Session 58's honesty sweep couldn't have caught the overclaim (a wrong owner-asserted premise reads as consistent everywhere). **Next: Public-readiness Session 3 Slice B** (screenshot capture) — or Session 4 (code docs), owner's pick. **Checks:** docs-only — code CI gates don't apply; CI ran the docs-skip green; self-`/review-pr` clean.
- **2026-06-25 — Session 58 (Public-readiness Session 2): the architecture-vault re-sync + a retro fix to the sweep skill (vault/skill docs only; PR #140 merged green, `842bd54`).** Ran `meta-architect:review-architecture` at the V1 → Public-readiness roll to bring the vault — part of what a stranger reads — current with reality. The report (`architecture/reports/2026-06-25-architecture-review.md`) found **no blockers** and two risks, both vault drift fixed on sight: the vault still framed the spaCy PreNER baseline as a *live pipeline stage* though spec §7 Step 3 was just amended to deferred/dormant (Session 57) — reconciled `PROJECT.md`/`overview.md` after **verifying against the code** that PreNER is genuinely dormant (the live `/extract` passes empty hints, the coordinator has no PreNER call); and `overview.md`'s as-built snapshot had stopped at M4.S3a though S3b/S3c/multi-story all shipped and V1 is feature-complete (the 4th recurrence of doc-freshness drift) — refreshed, with the world graph reframed as post-PoC. `invariants.md` was code-verified honest (INV-9's six writer-paths confirmed against `neo4j_repo.py`/`entity_edit.py`; the coordinator constructs no graph writer) and its lagging frontmatter date bumped; a BUILT banner was added to `m4-multi-story`; the forward "but what if" (auto-chunker silent content-loss; membership-derivation under delete) was homed as OQ-28 for the coming Graph-quality milestone. The self-`/review-pr` then caught two freshness defects the sweep *itself* had introduced — a proposal BUILT banner added without a date bump, and the handoff-flagged INDEX "Next: M4" line left un-cleared — both folded into the PR. **Process change (retro):** the `meta-architect:review-architecture` skill gained a **§6b self-check** (bump `updated:` on every edited note incl. banners; grep for stale `Next:`/`proposed`/build-pending framing — before writing the report), so a freshness-fixing sweep stops leaking freshness debt; the INDEX run-log got a one-line framing note instead of a prune rule; and the now-4th doc-freshness recurrence was recorded as evidence toward wiring the sweep into the roll ritual (ADR 0002) in a dedicated later session. **Lesson:** a ritual must hold *itself* to the standard it enforces on everything else — the sweep that fixes stale dates left one, and only the independent §2 grep caught it; the fix was to give the sweep that same grep as a closing gate. **Next: Public-readiness Session 3** — README overhaul + demo artifacts. **Checks:** vault/skill docs only — code CI gates don't apply; CI ran the docs-skip green.
- **2026-06-25 — Session 57 (Public-readiness Session 1): the PreNER spec↔reality stop-and-amend (spec/docs only; PR #138 merged green, `d0ee135`).** The spec had described the spaCy `PreNERAgent` as an active pre-LLM pipeline stage (§7 "Step 3: Pre-NER baseline"; the §7 Step-4 optimization note), but in code it is dormant — built yet wired into nothing, so the live extraction path is LLM-only. Surfaced in the Session-54 multi-story smoke and flagged by the owner as spec-vs-code drift, this session ran the stop-and-amend flow to make the spec honest: §7 Step 3 now reads *deferred/dormant for the PoC* (pointing at the `docs/BACKLOG.md` "spaCy-PreNER-without-the-LLM" eval and the `docs/PLAN_LONG.md` data flywheel that would eventually wire it), and the other homes — the §6.5 agent catalog, the §6.1 stack row, the §9 Milestone-2 deliverables, plus the `PLAN_LONG`/`BACKLOG` doc notes and the struck `PLAN_SHORT` cross-cutting item — all carry a matching "built, not wired" marker. The architecture vault still frames PreNER as a live stage but is writer-restricted (ADR 0002), so it was left for the next session's architecture sweep to reconcile rather than hand-edited. Self-review was clean (the load-bearing check was the whole-repo decision-reconciliation grep); CI exercised the new docs-skip cadence correctly (only the change-detector and secret-scan ran, the heavy suites skipped). **Lesson:** the handoff had loosely cited the drift as "§6 / §3" when the pipeline actually lives in **§7** — resume caught it by grepping rather than trusting the label, and the amend now records the correct section everywhere, fixing the imprecision at its source. **Next: Public-readiness Session 2** — the architecture-vault re-sync (`meta-architect:review-architecture`). **Checks:** docs/spec-only — code CI gates don't apply.
- **2026-06-25 — Session 56: unplanned interrupt — repo went PUBLIC + CI rework + light branch protection + stale-branch cleanup (PR #135, merged green; infra + a §6.7 spec amendment, no product code).** GitHub Actions stopped mid-session — the **private** repo's free-minutes pool was exhausted. Root cause: the repo was private though "public from day one" is baked into the spec/`AGENTS.md`; flipped it to **public** after a clean git-history pre-flight (no `.env`/secrets/keys ever committed) → unlimited free standard-runner Actions, unblocked. **CI restructure (`ci.yml`):** a native `changes` job (git-diff, no new action) path-scopes the heavy jobs (service-container suites, frontend build, dep-age/OSV/Trivy scans) to **code-bearing** changes (outside `docs/**`, `architecture/**`, `*.md`); `detect-secrets` split into an **always-on `secret-scan`**; a **daily `schedule`** force-runs the scans so the CVE-treadmill is caught regardless of PR cadence; `concurrency` cancels superseded PR runs; uv/npm caching. **§6.7 stop-and-amend** (the docs-skip touches the stated "scans on every push/PR" rule): added a "CI scan cadence" bullet, adjusted the OSV/Trivy cadence lines + the §7 CI list, reconciled `AGENTS.md` merge-flow + the `pin-image` skill — framed *adapts-not-relaxes* (every code change still fully scanned + a daily run). **Light branch protection** on `main` (block force-push/delete + require a PR; no required reviews/checks — owner keeps the merge button). **Stale-branch cleanup** — deleted 8 leftover remote branches (all merged PRs). **Verified the docs-skip empirically** on a throwaway scratch PR (#136, closed): a docs-only diff ran only `changes` + `secret-scan`; the 4 heavy jobs skipped. **Self-`/review-pr` clean** — the dangerous misclassification direction (a code file scored as docs → scans skipped on real code) is structurally impossible: the regex only matches `*.md`/`docs/**`/`architecture/**`. **Retro:** tightened `wrap-session` step 8 to say a docs PR now runs only the secret-scan (and branch protection requires the PR). **Lesson:** runtime-verify a CI gating change on a scratch branch rather than reasoning about the skipped path — cheap certainty. **Checks:** PR #135 merged green (full suite — it edits `.github/**` = code-bearing); main post-merge CI green. **Next: Public-readiness Session 1** — the PreNER spec↔reality stop-and-amend (unchanged; this was an interrupt).
- **2026-06-25 — Session 55: V1 → Public-readiness milestone roll (governance/planning only, no production code).** With all of V1 (M0–M4) feature-complete after the Session-54 multi-story smoke, rolled the plan to the next milestone. Archived Milestone 4 — its full task section, every Decided and Done entry, and its resolved cross-cutting/Blocked items (plus the M2/M3-era resolved leftovers and the old curation-note blockquotes) — into `docs/PLAN_ARCHIVE.md`, byte-exact (move, not delete; verified by dated-bullet count), taking `PLAN_SHORT.md` from ~230 KB to well under the §7 flag. Walked the cross-cutting and Blocked lists by hand (§3) — carried 16 live cross-cutting + 1 live Blocked, archived the rest (including two never-struck-but-done items, DM-Rel-5 and OQ-20, code/file-verified resolved at this roll). Opened the new **Public readiness** milestone as a skeleton with only the first session (the PreNER spec↔reality stop-and-amend) sliced in detail (owner's slicing-depth call). Corrected a handoff drift along the way: the world-graph §3.6/§9 stop-and-amend the old handoff paired with PreNER was already completed in Session 49 (PR #119), so the milestone now schedules one amend, not two. **Next: Public-readiness Session 1** — draft the PreNER spec edits for owner approval. **Checks:** docs-only; the running app was untouched; code CI gates don't apply.
- _**Earlier sessions (Milestones 1–4)** — see **[`docs/PLAN_ARCHIVE.md`](PLAN_ARCHIVE.md)**._

---

## How the two skills use this file

- **`/resume-session`** (session start) reads the **Session handoff** block, verifies the on-disk anchors it names, runs `git log`/`git status`, opens the spec sections listed, and confirms reality matches before work begins.
- **`/wrap-session`** (session end) runs the green-state checks (report-only), checks off completed tasks, strikes (does not delete) obsolete ones, appends a dated line to **Done in previous sessions**, refreshes **Blocked / questions** and **Decided**, and rewrites the **Session handoff** block to point at the next session. It then reminds you to commit on a feature branch (squash-merge hygiene).
- **When a spec change happened:** both skills stop and require `story-forge-poc-spec.md` + `PLAN_LONG.md` to be reconciled with this file before continuing.
